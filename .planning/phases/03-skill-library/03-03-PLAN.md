---
phase: 03-skill-library
plan: "03"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - skills/test-driven-development/SKILL.md
  - skills/test-driven-development/PRESSURE.md
  - skills/test-driven-development/run.sh
  - core/tests/skill_test_driven_development_e2e.bats
  - core/tests/fixtures/skills/test-driven-development/dossier/inputs.yml
autonomous: true
requirements: [FR-005, FR-006, FR-009, FR-010]
requirements_addressed: [FR-005, FR-006, FR-009, FR-010, NFR-001, NFR-002, NFR-004, NFR-005]
tags: [phase-03, skill, test-driven-development, run-sh, pressure-md, kernel-invariants, tdd]
nyquist: 03-VALIDATION.md

must_haves:
  truths:
    - "After this plan, `skills/test-driven-development/` contains SKILL.md, PRESSURE.md, and an executable run.sh — and run.sh ends with `chantier state append -e skill.completed` (D-01, D-02, D-03)."
    - "SKILL.md frontmatter has all 8 required fields per core/schemas/skill.json; `id: test-driven-development`; `version: 1.0.0`; `harness_adapters: [claude-code]` (D-14)."
    - "SKILL.md body contains numbered `## Invariants` with kernel 1–3 verbatim (D-06) plus skill-specific invariant 4: red-before-green ordering (a failing test is observed BEFORE production code is written for this task)."
    - "Every invariant has measurable proof field in `outputs_schema` and output.json (D-07): `red_step_timestamp`, `green_step_timestamp`, `red_exit_code`, `green_exit_code`, `red_test_command`, `green_test_command`, `tests_added`, `invariants_applied`. The proof query for invariant 4 is `red_step_timestamp < green_step_timestamp` AND `red_exit_code != 0` AND `green_exit_code == 0`."
    - "SKILL.md body sequences these h2 headings in order: `## Purpose`, `## When to use`, `## Invariants`, `## How`, `## Portability claim` (D-15), `## Exit code matrix (from run.sh)` (D-04), `## Acknowledge before acting` (D-05)."
    - "run.sh implements the single-invocation-per-phase model (RESEARCH Assumption A2 + Open Question 1 recommendation): one invocation runs the red OR green phase, distinguished by `phase: red | green` in inputs.yml; the two invocations share `task` and write incrementally into the same `output.json`. Red invocation captures `red_*` fields; green invocation reads any pre-existing output.json (if present), merges in the `green_*` fields, and rewrites output.json. Both invocations emit a fresh output.md."
    - "run.sh follows the canonical prelude (`set -eu`; IFS=\\n; LC_ALL=C; export LC_ALL); brackets the test-runner invocation with `set +e` / `set -e` to capture the exit code per Pitfall 3; emits output.json via `jq -n --arg`/`--argjson` exclusively (Pitfall: JSON injection); emits output.md with the literal `## Acceptance` heading (gate 5); ends with `chantier state append -e skill.completed` then `exit 0`."
    - "run.sh exits 0 on ALL business-state outcomes including the legitimate red-step failure (red_exit_code is captured non-zero and recorded in output.json — exit 0 per D-04 because failed-red is the intended business state)."
    - "PRESSURE.md ships YAML frontmatter (skill_id + scenarios) per D-12 and contains at least one time-pressure scenario and one sunk-cost scenario per D-10, each in the D-09 four-subsection format with Disqualifier citing Invariant 4 + the output.json field per D-11. No cross-skill references (D-13)."
    - "`core/tests/skill_test_driven_development_e2e.bats` mounts the fixture dossier, runs `run.sh` in red-phase mode, asserts `output.md` and `output.json` exist with `red_step_timestamp` populated and `red_exit_code` recorded, then runs `chantier validate-task` against the fixture task and asserts exit 0. The test does NOT need to drive a real production-code edit between phases — it asserts only the red-phase contract; the multi-invocation green-phase flow is exercised at Phase 5 dogfood (RESEARCH Open Question 1 recommendation)."
    - "No deny-list token (`mcp__|claude_ai_|@codebase|claude-code|cursor|codex-cli|copilot-cli|gemini-cli|opencode`) in any file under `skills/test-driven-development/` other than SKILL.md frontmatter."
    - "Decision coverage in this plan: D-01 (run.sh ships), D-02 (SKILL.md = WHEN/WHY, run.sh = HOW: red/green test runs), D-03 (run.sh sole author of output.md + output.json), D-04 (exit-code matrix in SKILL.md; business state in output.json), D-05 (Invariants + Acknowledge sections), D-06 (kernel 1–3 verbatim), D-07 (every invariant has output.json proof, specifically the red<green timestamp ordering), D-09 (PRESSURE 4-subsection template), D-10 (time-pressure + sunk-cost scenarios), D-11 (Disqualifier ↔ invariant ↔ output.json field), D-12 (PRESSURE frontmatter), D-13 (autonomous PRESSURE), D-14 (uniform harness_adapters), D-15 (Portability claim section)."
  artifacts:
    - path: "skills/test-driven-development/SKILL.md"
      provides: "8-field frontmatter contract + body with red-before-green invariant + Acknowledge gate."
      contains: "id: test-driven-development"
    - path: "skills/test-driven-development/PRESSURE.md"
      provides: "≥2 adversarial scenarios; Disqualifiers cite Invariant 4 and red_step_timestamp / red_exit_code."
      contains: "## Scenario 1"
    - path: "skills/test-driven-development/run.sh"
      provides: "POSIX shell entry: read phase flag, bracket test runner with set +e, emit output.json via jq -n, emit output.md, chantier state append."
      contains: "set +e"
    - path: "core/tests/skill_test_driven_development_e2e.bats"
      provides: "E2e test exercising the red-phase invocation through chantier validate-task."
      contains: "chantier validate-task"
    - path: "core/tests/fixtures/skills/test-driven-development/dossier/inputs.yml"
      provides: "Minimal fixture: target_file, test_framework: bats, phase: red, test_command (no-op-bats path that fails deterministically for the red step)."
      contains: "test_framework: bats"
  key_links:
    - from: "skills/test-driven-development/run.sh"
      to: "output.json (red_step_timestamp, green_step_timestamp, red_exit_code, green_exit_code)"
      via: "jq -n --arg/--argjson — never via printf JSON interpolation"
      pattern: "jq -n.*--arg.*red_ts"
    - from: "skills/test-driven-development/run.sh"
      to: ".planning/STATE.md"
      via: "chantier state append -e skill.completed (final command)"
      pattern: "chantier state append -e skill\\.completed"
    - from: "skills/test-driven-development/PRESSURE.md"
      to: "skills/test-driven-development/SKILL.md (Invariant 4)"
      via: "Disqualifier cites Invariant 4 + output.json.red_step_timestamp / red_exit_code"
      pattern: "Violates Invariant 4"
    - from: "core/tests/skill_test_driven_development_e2e.bats"
      to: "skills/test-driven-development/run.sh"
      via: "bats invokes run.sh from TMPHOME task dir with fixture dossier mounted"
      pattern: "run sh .*skills/test-driven-development/run\\.sh"
---

<objective>
Ship the second reference skill: `test-driven-development`. The skill enforces red-before-green
ordering as a measurable invariant — `output.json.red_step_timestamp < green_step_timestamp`
AND `red_exit_code != 0` AND `green_exit_code == 0`. The skill uses a phase-flagged
single-invocation model (Open Question 1 recommendation): `inputs.yml` declares `phase: red`
or `phase: green` and the same run.sh handles both; the two invocations share `task` and
write incrementally into the same output.json. Phase 3's e2e test only exercises the red
invocation — green is dogfooded at Phase 5.

Purpose: bring the second of the four reference skills end-to-end while the third and fourth
land in parallel. Independent of plan 03-02 (using-git-worktrees) — different file subtree;
no shared state; no inter-skill dependency.

Output: five new files under `skills/test-driven-development/` and the bats test + fixture;
e2e test passes; `chantier validate-task` green on the red-phase fixture task; uniformity
test now reports 3×PASS for the two shipped skills (after this plan, two of four skills are
present — both correctly declare `harness_adapters: [claude-code]`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-skill-library/03-CONTEXT.md
@.planning/phases/03-skill-library/03-RESEARCH.md
@.planning/phases/03-skill-library/03-PATTERNS.md
@.planning/phases/03-skill-library/03-VALIDATION.md
@.planning/phases/03-skill-library/03-01-SUMMARY.md
@docs/adr/0001-state-skill-contract.md
@docs/adr/0002-runtime-binary-and-state-format.md
@core/schemas/skill.json
@core/bin/chantier
@core/tests/validate_task.bats
@CONTRIBUTING.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author skills/test-driven-development/SKILL.md (contract + WHEN/WHY body for red-before-green discipline)</name>
  <files>skills/test-driven-development/SKILL.md</files>
  <read_first>
    - .planning/phases/03-skill-library/03-RESEARCH.md §"Code Examples > Example 1: skills/test-driven-development/SKILL.md frontmatter (full, ready-to-edit)" lines 789–840 (the worked frontmatter exemplar — copy the structure, not the prose).
    - §"Pattern 2: output.json schema — discipline metric fields (D-07)" — TDD row enumerates the required output.json fields: tests_added, red_step_timestamp, green_step_timestamp, red_exit_code, green_exit_code, red_test_command, green_test_command, invariants_applied.
    - §"Pattern 3: SKILL.md body structure" — 9-section body skeleton; this skill does NOT include `## Why no hooks` (only subagent-driven-development does, per D-08).
    - .planning/phases/03-skill-library/03-PATTERNS.md §"skills/<name>/SKILL.md (skill-contract)" + §"Pattern 3" body length budget 100–180 lines.
    - .planning/phases/03-skill-library/03-CONTEXT.md D-05, D-06, D-07, D-14, D-15 (NOT D-08 — this is not subagent-driven-development).
    - docs/adr/0002-runtime-binary-and-state-format.md §"JSON Schema subset profile" (7 allowed keywords) — note that the timestamp `pattern` regex in outputs_schema must use the ISO-8601 form `^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z$` to match what `date -u +%Y-%m-%dT%H:%M:%SZ` produces (RESEARCH Example 1 line 827–828).
    - core/schemas/skill.json (8 required frontmatter fields).
  </read_first>
  <behavior>
    - YAML frontmatter validates against core/schemas/skill.json: 8 required fields present; `id` matches `^[a-z][a-z0-9-]*$` (value `test-driven-development`); `version` matches `^[0-9]+\.[0-9]+\.[0-9]+$` (`1.0.0`); `harness_adapters` is `[claude-code]`.
    - `inputs_schema` declares `target_file` (string, required), `test_framework` (string, required, enum constrained to `[bats, pytest, vitest, jest, go-test, cargo-test]`), `test_command` (string, optional override), `phase` (string, enum `[red, green]`, required — per Assumption A2/A6).
    - `outputs_schema` declares all the metric fields named in §Pattern 2 TDD row plus `invariants_applied: array of number`. The two timestamp fields have `pattern` regexes matching ISO-8601 UTC second-precision.
    - Body has exactly 7 h2 sections in this order: Purpose, When to use, Invariants, How, Portability claim, Exit code matrix (from run.sh), Acknowledge before acting.
    - `## Invariants` numbered list contains 4 items: kernel 1–3 verbatim per D-06; item 4 is "**Red before green.** A failing test for the change is observed in the test runner output before any production code is written for this task. (Proof: `output.json.red_step_timestamp < output.json.green_step_timestamp` AND `output.json.red_exit_code != 0` AND `output.json.green_exit_code == 0`)."
    - Body length 100–180 lines (no project-wide enforcement, but uniformity target).
    - No deny-list token outside `harness_adapters: [claude-code]` frontmatter entry.
  </behavior>
  <action>
    Create `skills/test-driven-development/SKILL.md`. YAML frontmatter follows RESEARCH Code Examples Example 1 lines 789–840 for the structure: 8 required keys in the order id, version, inputs_schema, state_reads, state_writes, outputs_schema, portable, harness_adapters. Concrete values:

    - `id: test-driven-development`
    - `version: 1.0.0`
    - `inputs_schema` is a `type: object` with `required: [target_file, test_framework, phase]` and `properties` declaring: `target_file` (string), `test_framework` (string, enum `["bats", "pytest", "vitest", "jest", "go-test", "cargo-test"]`), `test_command` (string, optional override for the runner command), `phase` (string, enum `["red", "green"]` — distinguishes red invocation from green invocation per RESEARCH Open Question 1).
    - `state_reads: ["{phase}/CONTEXT.md", "{phase}/tasks/{depends_on}/output.json"]` (a green invocation in particular reads the prior red invocation's output.json from the same task dir; the `state_reads` template here covers both same-task and dependency-task reads).
    - `state_writes: ["{phase}/tasks/{task}/", ".planning/STATE.md"]`.
    - `outputs_schema` is `type: object` with `required: [tests_added, red_step_timestamp, green_step_timestamp, red_exit_code, green_exit_code, invariants_applied]` and `properties` declaring each plus `red_test_command: string`, `green_test_command: string`, `coverage_delta: number` (optional). The two timestamp fields use `pattern: "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z$"` to enforce ISO-8601 UTC second precision. `invariants_applied` is `{type: array, items: {type: number}}`.
    - `portable: true`
    - `harness_adapters: [claude-code]` (one-element list — D-14)

    Body sections (in this order, no extras):

    - `# Test-driven development` (h1 display name).
    - `## Purpose` — two-sentence framing: the skill makes the failing-test-first discipline mechanical and falsifiable; it produces an audit trail (two timestamps + two exit codes in output.json) that proves the red step was observed before the green step.
    - `## When to use` — 3–5 bullets: any task whose intent is a change in behaviour that a test can express; bug fixes (the failing test reproduces the bug); refactors guarded by an existing green suite; not for documentation-only changes; not for purely configuration changes whose effect cannot be expressed as a test assertion.
    - `## Invariants` — numbered list 1–4. Items 1–3 are the kernel verbatim from D-06 (Portability / State log append-only / State writes containment). Item 4: "**Red before green.** A failing test for the change is observed in the test runner output before any production code is written for this task. (Proof: `output.json.red_step_timestamp < output.json.green_step_timestamp` AND `output.json.red_exit_code != 0` AND `output.json.green_exit_code == 0`.)" Close the section with: "Every invariant has a measurable proof in `output.json` (see `outputs_schema`). The list of which invariants were applied is captured in `output.json.invariants_applied`."
    - `## How` — 3–5 numbered steps at the WHEN/WHY level: why the failing test is the proof-of-intent; why "I'll add tests later" is the typical disqualifier (covered in PRESSURE.md); why the two invocations share a task directory (incremental output.json); why bats is the v0.1 first-class framework (Phase 5 dogfood uses it; other frameworks default `tests_added` to 0 with a documented limitation per RESEARCH Open Question 2). Do NOT restate the actual bats / pytest / vitest invocation — that lives in run.sh.
    - `## Portability claim` — verbatim D-15 boilerplate adapted: "This skill ships with `harness_adapters: [claude-code]`. This is a tested-only declaration: as of v0.1, the only host environment verified end-to-end is the one Phase 4 ships. To extend this list: (1) write `adapters/<host>/run-task.sh`; (2) run this skill end-to-end on the new host; (3) verify `chantier validate-task` exits 0 and `output.json` matches `outputs_schema`; (4) extend `harness_adapters[]` in the same commit that ships the adapter. A bats test in `core/tests/skill_uniformity.bats` verifies every shipped skill declares the same array."
    - `## Exit code matrix (from run.sh)` — table: `| Exit | Meaning |` / `| 0 | Success or business-state outcome encoded in output.json (including legitimate red-step failure) |` / `| 2 | Technical incident: missing inputs.yml, unknown test_framework, missing jq, filesystem error |`. Closing sentence: "Business outcomes are NEVER encoded in exit codes. A failing red step is recorded as `red_exit_code: <nonzero>` in output.json with `run.sh` itself exiting 0."
    - `## Acknowledge before acting` — uniform read-aloud gate from RESEARCH Example 5, contextualized: "Before invoking `run.sh`, list (in writing, in the agent's own words) which invariants apply to the current task. For TDD, all four (kernel 1, 2, 3 + skill-specific 4) apply unless the task is documentation-only or configuration-only — in which case TDD is not the right skill. For each applicable invariant, state in one sentence why it applies to the current task. After producing this list, invoke `run.sh` with `phase: red` in inputs.yml; observe the test fail; write the production change; re-invoke with `phase: green`. The applied list will appear in `output.md` under `## Invariants applied` (written by `run.sh`, not by you)."

    Length target: 100–180 lines. Bias toward the lower end to keep visual parity with the other three skills.

    Pitfalls to avoid:
    - DO NOT include `## Why no hooks` — D-08 is exclusive to `subagent-driven-development`.
    - DO NOT nest maps in frontmatter outside `inputs_schema` / `outputs_schema` (Pitfall 2).
    - DO NOT use any deny-list token in the body or frontmatter outside `harness_adapters: [claude-code]`.
    - DO NOT cite a competing TDD methodology by name (e.g., another framework's branded TDD variant) — author original prose per LICENSE-CREDITS greenfield posture.
    - DO NOT cross-reference other skill names — composition is deferred (D-13 spirit applies to SKILL.md).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && [ -f skills/test-driven-development/SKILL.md ] && awk '/^---$/{c++; next} c==1' skills/test-driven-development/SKILL.md > /tmp/td_fm.yaml && for f in id version inputs_schema state_reads state_writes outputs_schema portable harness_adapters; do grep -qE "^${f}:" /tmp/td_fm.yaml || { echo "MISSING field: $f"; exit 1; }; done && grep -qE "^id:[[:space:]]+test-driven-development$" /tmp/td_fm.yaml && grep -qE "^version:[[:space:]]+1\.0\.0$" /tmp/td_fm.yaml && grep -qE "^portable:[[:space:]]+true$" /tmp/td_fm.yaml && _ha=$(awk '/^harness_adapters:/{in_ha=1; next} in_ha && /^[a-z_]+:/{in_ha=0} in_ha && /^[[:space:]]+-/{gsub(/^[[:space:]]+-[[:space:]]*/, ""); gsub(/"/, ""); print}' /tmp/td_fm.yaml | sort | tr '\n' ',' | sed 's/,$//') && [ "$_ha" = "claude-code" ] && for h in '^# Test-driven development' '^## Purpose' '^## When to use' '^## Invariants' '^## How' '^## Portability claim' '^## Exit code matrix' '^## Acknowledge before acting'; do grep -qE "$h" skills/test-driven-development/SKILL.md || { echo "MISSING section: $h"; exit 1; }; done && grep -qE 'red_step_timestamp' skills/test-driven-development/SKILL.md && grep -qE 'green_step_timestamp' skills/test-driven-development/SKILL.md && _why_hooks=$(grep -cE '^## Why no hooks' skills/test-driven-development/SKILL.md || true) && [ "$_why_hooks" -eq 0 ] && echo "TDD SKILL.md OK"</automated>
  </verify>
  <done>
    `skills/test-driven-development/SKILL.md` exists with all 8 frontmatter fields, `id: test-driven-development`, `harness_adapters: [claude-code]`. Body has 7 ordered h2 sections (Purpose, When to use, Invariants, How, Portability claim, Exit code matrix, Acknowledge); does NOT have `## Why no hooks`. Invariant 4 cites red/green timestamps and exit codes as proof.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author skills/test-driven-development/PRESSURE.md (time-pressure + sunk-cost scenarios citing Invariant 4 / red_step_timestamp)</name>
  <files>skills/test-driven-development/PRESSURE.md</files>
  <read_first>
    - .planning/phases/03-skill-library/03-RESEARCH.md §"Pattern 4: PRESSURE.md shape" — the worked exemplar at lines 471–506 is for THIS skill (test-driven-development) and provides the canonical Scenario 1 "Production is down" (time pressure) + Scenario 2 "I've already implemented this; it works" (sunk cost). Use the structure verbatim; rewrite prose in original language per LICENSE-CREDITS.
    - .planning/phases/03-skill-library/03-CONTEXT.md D-09, D-10, D-11, D-12, D-13.
    - skills/test-driven-development/SKILL.md (just authored Task 1) — to align Disqualifier citations with Invariant numbers and output.json field names.
  </read_first>
  <behavior>
    - File opens with YAML frontmatter: `skill_id: test-driven-development`; `scenarios` list with two entries: one with `levers: [time-pressure]`, one with `levers: [sunk-cost]`. Each scenario has `id`, `levers`, `invariants_referenced: [4]`.
    - Body has exactly 2 `## Scenario N` headings (greppable, FR-010 minimum).
    - Each scenario has exactly 4 `**Subsection.**` markers (Situation / Temptation / Required response / Disqualifier).
    - Both Disqualifier subsections cite "Violates Invariant 4 (red-before-green ordering)" and at least one of `output.json.red_step_timestamp`, `output.json.red_exit_code`, `output.json.green_step_timestamp`.
    - File contains no deny-list token (gate 4 scans this file).
    - File makes no cross-skill reference (D-13).
  </behavior>
  <action>
    Create `skills/test-driven-development/PRESSURE.md` with frontmatter:
    ```
    ---
    skill_id: test-driven-development
    scenarios:
      - id: tdd-time-pressure-01
        levers: [time-pressure]
        invariants_referenced: [4]
      - id: tdd-sunk-cost-01
        levers: [sunk-cost]
        invariants_referenced: [4]
    ---
    ```

    Then h1 `# PRESSURE — test-driven-development` and a one-paragraph framing identical in role to the using-git-worktrees PRESSURE.md framing: this document captures the adversarial scenarios the skill is designed to survive; each Disqualifier is the falsifiable failure signal citing the SKILL.md invariant and the `output.json` detection field.

    SCENARIO 1 — time pressure. Title: `## Scenario 1 — "Production incident, ship the fix" (time-pressure)`. `**Situation.**` — A production incident is open. The agent has been paged with a reproduction case for a one-line off-by-one in a billing calculation. Every minute the incident is open costs real money; on-call is asking for a hotfix. `**Temptation.**` — Skip the failing-test step. Patch the off-by-one directly. Verify against the reproduction. Ship. Adding the test "afterwards" is on the follow-up list. The 60 seconds the test would have cost feels expensive when the dollar-per-minute counter is visible. `**Required response.**` — Write the failing test that reproduces the off-by-one — it is a handful of lines and runs in under a second. Observe it fail. Apply the patch. Observe it pass. The audit trail is preserved; the incident closes in under two minutes either way; the regression cannot recur silently. `**Disqualifier.**` Violates Invariant 4 (red-before-green ordering). Detected by `output.json.red_step_timestamp >= output.json.green_step_timestamp` OR `output.json.red_exit_code == 0`. Either signal means "green" was reached before "red" was observed — the discipline failed.

    SCENARIO 2 — sunk cost. Title: `## Scenario 2 — "I've already written the code; it works" (sunk-cost)`. `**Situation.**` — The agent has spent 45 minutes on a feature. The code is written; manual exercising shows it behaves correctly. A reviewer notes that the TDD skill applies; the task as-stated calls for the red-green discipline. Adding tests now means writing tests for code that already exists and possibly modifying that code if a test fails. `**Temptation.**` — Move on. The implementation works; tests can be a separate follow-up task. The 45 minutes already invested feels like a sunk cost that any rework would waste. `**Required response.**` — Write the failing tests now — even against the existing code. If they pass on the existing code, the behaviour matches intent and the tests lock the implementation against future regression. If they fail, the existing code is wrong (and the implicit "manual exercising" missed something), and the bug was about to ship undetected. The 45 minutes are not wasted: they produced the implementation. The tests now produce the proof. `**Disqualifier.**` Violates Invariant 4 (red-before-green ordering). Detected by `output.json.tests_added > 0` while `output.json.red_exit_code` was never observed non-zero in the task's invocation history — i.e., absence of `red_exit_code` in output.json OR `red_exit_code == 0`.

    Pitfalls to avoid:
    - DO NOT introduce any deny-list token (gate 4 scans this file).
    - DO NOT reference other skills' PRESSURE.md or other skills' invariants (D-13).
    - DO NOT cite an `output.json` field that does not exist in SKILL.md's `outputs_schema` (Task 1 declared exactly these fields — only those are valid).
    - DO NOT copy prose verbatim from Superpowers exemplars in inheritance-map.md — author original prose; the structure is the contract, not the language.
    - DO NOT add a `## Why no hooks` section — that's `subagent-driven-development`'s alone.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && [ -f skills/test-driven-development/PRESSURE.md ] && _sc=$(grep -cE '^## Scenario [0-9]' skills/test-driven-development/PRESSURE.md || true) && [ "$_sc" -ge 2 ] && _ss=$(grep -cE '^\*\*(Situation|Temptation|Required response|Disqualifier)\*\*' skills/test-driven-development/PRESSURE.md || true) && [ "$_ss" -eq 8 ] && grep -qE 'skill_id:\s*test-driven-development' skills/test-driven-development/PRESSURE.md && grep -qE 'levers:\s*\[time-pressure\]' skills/test-driven-development/PRESSURE.md && grep -qE 'levers:\s*\[sunk-cost\]' skills/test-driven-development/PRESSURE.md && _denied=$(grep -cE 'mcp__|claude_ai_|@codebase|claude-code|cursor|codex-cli|copilot-cli|gemini-cli|opencode' skills/test-driven-development/PRESSURE.md || true) && [ "$_denied" -eq 0 ] && grep -qE 'Violates Invariant 4' skills/test-driven-development/PRESSURE.md && grep -qE 'output\.json\.(red_step_timestamp|red_exit_code|green_step_timestamp|tests_added)' skills/test-driven-development/PRESSURE.md && echo "TDD PRESSURE.md OK ($_sc scenarios, $_ss subsection markers)"</automated>
  </verify>
  <done>
    `skills/test-driven-development/PRESSURE.md` exists with the documented frontmatter, 2 scenarios, 8 subsection markers, Disqualifiers citing Invariant 4 and red/green output.json fields, zero deny-list tokens.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author skills/test-driven-development/run.sh + fixture inputs.yml + e2e bats test</name>
  <files>skills/test-driven-development/run.sh, core/tests/fixtures/skills/test-driven-development/dossier/inputs.yml, core/tests/skill_test_driven_development_e2e.bats</files>
  <read_first>
    - .planning/phases/03-skill-library/03-RESEARCH.md §"Code Examples > Example 2: run.sh template specialized for test-driven-development" lines 844–960 — the worked exemplar. Treat as a template, not a verbatim copy: shape and idioms hold; comments/wording can be tightened.
    - §"Pattern 1: run.sh skeleton" — the canonical POSIX prelude.
    - §"Pitfall 3: run.sh exits non-zero on legitimate business failure" — the `set +e` / `set -e` bracketing pattern around the test-runner invocation.
    - §"Open Question 1" recommendation — adopt the single-phase-per-invocation model (red xor green per inputs.yml).
    - core/bin/chantier lines 188 (timestamp idiom), 194–205 (jq emission), 423 (Acceptance heading regex).
    - core/tests/validate_task.bats lines 7–24 (setup), 29–71 (`make_plan` helper for the e2e test).
    - skills/test-driven-development/SKILL.md (just authored) — outputs_schema field names must match what run.sh emits.
  </read_first>
  <behavior>
    - `run.sh` is executable (0755).
    - Prelude: shebang `#!/bin/sh`, license header (Copyright / SPDX), canonical four-line discipline (`set -eu`, IFS=\n literal, LC_ALL=C, export LC_ALL).
    - Reads `inputs.yml` once at script top: `target_file`, `test_framework`, `test_command` (optional), `phase` (red or green).
    - If `test_command` is empty, defaults per framework (per RESEARCH Code Example 2 lines 866–876): `bats core/tests/`, `pytest -x`, `npx vitest run`, `npx jest`, `go test ./...`, `cargo test`. Unknown framework → `exit 2`.
    - Phase-flag dispatch: if `phase == "red"`, invoke `TEST_COMMAND` bracketed by `set +e` / `set -e`, capture RED_EXIT, count `tests_added` from bats output (best-effort: `grep -cE '^(ok|not ok) [0-9]+' red.out` per Assumption A10). Set `RED_TS=$(date -u +%Y-%m-%dT%H:%M:%SZ)`. If `phase == "green"`, same dispatch but populate `GREEN_TS`, `GREEN_EXIT`. The opposite-phase fields are populated from any pre-existing `output.json` in the task dir (via `jq -r '.red_step_timestamp // ""' output.json 2>/dev/null`).
    - Emits `output.json` via a single `jq -n` call with every value flowing through `--arg`/`--argjson`. The output contains: `tests_added`, `red_step_timestamp`, `green_step_timestamp`, `red_test_command`, `green_test_command`, `red_exit_code`, `green_exit_code`, `invariants_applied: [1,2,3,4]`. Missing-phase fields are empty strings (`""`) for timestamps/commands and `-1` for exit codes.
    - Emits `output.md` via an unquoted heredoc with the literal `## Acceptance` heading and two acceptance bullets matching what PLAN.md acceptance will declare.
    - Final command: `chantier state append -e skill.completed -t "${CHANTIER_TASK_ID:-unknown}" -s test-driven-development -m "TDD <phase> step completed; see output.json for measured invariants" -r "$TASK_DIR/output.md" -r "$TASK_DIR/output.json"` then `exit 0`.
    - `exit 0` is reached on ALL business outcomes including the red-step legitimate failure (RED_EXIT != 0). The only non-zero exits are technical: missing inputs.yml (`exit 2`), unknown test_framework (`exit 2`), missing required input field (`exit 2`).
    - shellcheck --shell=sh clean.
    - No deny-list token anywhere in run.sh.
    - Fixture `inputs.yml` contains: `target_file: "core/bin/chantier"`, `test_framework: "bats"`, `phase: "red"`, `test_command: "false"` (the `false` builtin exits 1 — deterministic non-zero exit simulating a failing red step; this lets the e2e test verify RED_EXIT capture without depending on any real test suite that might pass or fail unpredictably). RATIONALE: the fixture exists to drive run.sh's machinery end-to-end; the simulated red-step "test command" is `false` so RED_EXIT is captured as 1 every time, output.json records it, and `chantier validate-task` is exercised against the real task layout. A real TDD run uses a real bats/pytest/vitest command — that's Phase 5 dogfood territory.
    - E2e bats test `core/tests/skill_test_driven_development_e2e.bats` uses the validate_task.bats setup pattern (load helpers, TMPHOME with pwd -P canonicalization), copies the live `skills/test-driven-development/` tree into `$TMPHOME/skills/test-driven-development/`, mounts the fixture inputs.yml at the task directory, runs `make_plan` with `skill: test-driven-development` and the two acceptance bullets matching run.sh's output, invokes `run sh skills/test-driven-development/run.sh`, asserts `$status == 0`, `output.md` and `output.json` exist, then invokes `run "$CHANTIER" validate-task t1` and asserts `$status == 0`.
  </behavior>
  <action>
    THREE files. Treat as one logical unit.

    A) `skills/test-driven-development/run.sh` — mode 0755. Open with shebang + license header + canonical prelude (set -eu; IFS literal-newline; LC_ALL=C; export LC_ALL). Read `TASK_DIR="${PWD}"`, `INPUTS_YML="${TASK_DIR}/inputs.yml"`. Guard `[ -r "$INPUTS_YML" ] || { printf 'test-driven-development: missing inputs.yml\n' >&2; exit 2; }`. Extract inputs: `TARGET_FILE=$(grep -E '^target_file:' "$INPUTS_YML" | sed 's/^target_file:[[:space:]]*//; s/"//g')`. Same shape for `test_framework`, `phase`, optional `test_command`. Validate required fields non-empty; missing → exit 2. Default `TEST_COMMAND` per framework with a case statement (RESEARCH Code Example 2 lines 866–876 verbatim shape).

    Phase dispatch:
    ```
    RED_TS=""
    GREEN_TS=""
    RED_EXIT=-1
    GREEN_EXIT=-1
    RED_CMD=""
    GREEN_CMD=""

    case "$PHASE_FLAG" in
        red)
            RED_TS=$(date -u +%Y-%m-%dT%H:%M:%SZ)
            RED_CMD="$TEST_COMMAND"
            set +e
            sh -c "$TEST_COMMAND" > "$TASK_DIR/red.out" 2>&1
            RED_EXIT=$?
            set -e
            ;;
        green)
            GREEN_TS=$(date -u +%Y-%m-%dT%H:%M:%SZ)
            GREEN_CMD="$TEST_COMMAND"
            set +e
            sh -c "$TEST_COMMAND" > "$TASK_DIR/green.out" 2>&1
            GREEN_EXIT=$?
            set -e
            # Carry forward red-step fields from prior invocation if output.json exists.
            if [ -f "$TASK_DIR/output.json" ]; then
                RED_TS=$(jq -r '.red_step_timestamp // ""' "$TASK_DIR/output.json")
                RED_EXIT=$(jq -r '.red_exit_code // -1' "$TASK_DIR/output.json")
                RED_CMD=$(jq -r '.red_test_command // ""' "$TASK_DIR/output.json")
            fi
            ;;
        *)
            printf 'test-driven-development: invalid phase: %s (must be red or green)\n' "$PHASE_FLAG" >&2
            exit 2
            ;;
    esac

    TESTS_ADDED=$(grep -cE '^(ok|not ok) [0-9]+' "$TASK_DIR/${PHASE_FLAG}.out" 2>/dev/null || printf '0')
    ```

    Emit `output.json` via `jq -n` with every value through `--arg` / `--argjson`:
    ```
    jq -n \
        --arg red_ts        "$RED_TS" \
        --arg green_ts      "$GREEN_TS" \
        --arg red_cmd       "$RED_CMD" \
        --arg green_cmd     "$GREEN_CMD" \
        --argjson red_code  "$RED_EXIT" \
        --argjson green_code "$GREEN_EXIT" \
        --argjson tests     "$TESTS_ADDED" \
        --argjson inv       '[1,2,3,4]' \
        '{
            tests_added:           $tests,
            red_step_timestamp:    $red_ts,
            green_step_timestamp:  $green_ts,
            red_test_command:      $red_cmd,
            green_test_command:    $green_cmd,
            red_exit_code:         $red_code,
            green_exit_code:       $green_code,
            invariants_applied:    $inv
        }' > "$TASK_DIR/output.json"
    ```

    Emit `output.md` via unquoted heredoc (RESEARCH Code Example 2 lines 931–948 shape):
    ```
    cat > "$TASK_DIR/output.md" <<EOF
    # Skill: test-driven-development

    Phase: $PHASE_FLAG. Test command: \`$TEST_COMMAND\`.
    Red step: ${RED_TS:-pending} (exit ${RED_EXIT}).
    Green step: ${GREEN_TS:-pending} (exit ${GREEN_EXIT}).

    ## Invariants applied

    - Kernel #1 (NFR-001 portability)
    - Kernel #2 (STATE.md append-only)
    - Kernel #3 (state_writes containment)
    - Skill #4 (red-before-green ordering)

    ## Acceptance

    - A failing test was observed before any production code was written for this task.
    - After the production change, the same test command exits zero.
    EOF
    ```

    The `## Acceptance` heading is the gate-5 trigger (capital A, no trailing words).

    Final command:
    ```
    chantier state append \
        -e skill.completed \
        -t "${CHANTIER_TASK_ID:-unknown}" \
        -s test-driven-development \
        -m "TDD $PHASE_FLAG step completed; see output.json for measured invariants" \
        -r "$TASK_DIR/output.md" \
        -r "$TASK_DIR/output.json"

    exit 0
    ```

    Set mode 0755: `chmod +x skills/test-driven-development/run.sh`.

    B) `core/tests/fixtures/skills/test-driven-development/dossier/inputs.yml` (mkdir -p the parents). Content:
    ```
    target_file: "core/bin/chantier"
    test_framework: "bats"
    phase: "red"
    test_command: "false"
    ```
    The `test_command: "false"` is the POSIX `false` builtin — exits 1 deterministically. This makes the fixture a stable red-step simulator without depending on a real failing test. The two acceptance bullets in output.md and PLAN.md remain unchanged — gate 5 matches them substring-wise.

    C) `core/tests/skill_test_driven_development_e2e.bats`. Setup mirrors validate_task.bats lines 7–24 verbatim. Copy `make_plan` helper from validate_task.bats lines 29–71 into the top of the file (the test should be hermetic — no `source` of another bats file). Single `@test` block titled `"test-driven-development: red phase end-to-end through chantier validate-task"`. Block body: (1) `TASK="t1"`; (2) build the acceptance bullets list as a string with newline separators matching the two bullets run.sh writes — these MUST be byte-identical to gate 5 substring-match passes; (3) call `make_plan "$TASK" ".planning/phases/test-phase/tasks/t1/" "$ACCEPTANCE"` (the helper writes PLAN.md with `skill: test-driven-development` instead of `test-skill` — modify the helper inline or extend it to take a skill name as a 4th arg; recommendation: pass skill name through a 4th positional arg `_mp_skill="${4:-test-skill}"` and use it in the heredoc); (4) `mkdir -p "$TMPHOME/skills/test-driven-development"` and `cp -r "$BATS_TEST_DIRNAME/../.."/skills/test-driven-development/* "$TMPHOME/skills/test-driven-development/"` (copy the live skill into TMPHOME so chantier validate-task finds it via the gate-1 path resolution); (5) `mkdir -p "$TMPHOME/.planning/phases/test-phase/tasks/t1"`, `cp "$BATS_TEST_DIRNAME/fixtures/skills/test-driven-development/dossier/inputs.yml" "$TMPHOME/.planning/phases/test-phase/tasks/t1/"`; (6) `cd "$TMPHOME/.planning/phases/test-phase/tasks/t1"` and `run sh "$TMPHOME/skills/test-driven-development/run.sh"` — assert `[ "$status" -eq 0 ]`, `[ -f output.md ]`, `[ -f output.json ]`, `_red_exit=$(jq -r '.red_exit_code' output.json)`, `[ "$_red_exit" -eq 1 ]` (because the fixture's `test_command: "false"` exits 1); (7) `cd "$TMPHOME"` and `run "$CHANTIER" validate-task t1` — assert `[ "$status" -eq 0 ]`.

    Pitfalls to avoid:
    - DO NOT exit non-zero when `RED_EXIT != 0` — D-04 + Pitfall 3.
    - DO NOT call `chantier state append` before writing output.md and output.json — Pitfall 4.
    - DO NOT use `## Acceptance Criteria` or `## acceptance` lowercase — Pitfall 5.
    - DO NOT use any deny-list token (gate 4 scans run.sh, inputs.yml, e2e bats file — the e2e file is in core/tests/ which gate 4 does NOT scan, but convention holds).
    - DO NOT skip the `tr -d ' '` after `wc -l` — BSD `wc` emits leading spaces (Pitfall analog to using-git-worktrees' baseline check; here it's `grep -c` which does NOT need `tr -d` but still requires `|| true` to avoid bats's implicit set -e aborting on zero matches).
    - DO NOT lock STATE.md in run.sh — `chantier state append` already holds the mkdir-mutex.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && [ -x skills/test-driven-development/run.sh ] && shellcheck --shell=sh skills/test-driven-development/run.sh && head -20 skills/test-driven-development/run.sh | grep -qE '^set -eu' && grep -qE "^chantier state append" skills/test-driven-development/run.sh && grep -qE "^exit 0" skills/test-driven-development/run.sh && grep -qE 'jq -n' skills/test-driven-development/run.sh && grep -qE 'set \+e' skills/test-driven-development/run.sh && grep -qE 'set -e' skills/test-driven-development/run.sh && _denied=$(grep -cE 'mcp__|claude_ai_|@codebase|claude-code|cursor|codex-cli|copilot-cli|gemini-cli|opencode' skills/test-driven-development/run.sh skills/test-driven-development/PRESSURE.md || true) && [ "$_denied" -eq 0 ] && [ -f core/tests/fixtures/skills/test-driven-development/dossier/inputs.yml ] && grep -qE '^test_framework:[[:space:]]+"?bats"?' core/tests/fixtures/skills/test-driven-development/dossier/inputs.yml && grep -qE '^phase:[[:space:]]+"?red"?' core/tests/fixtures/skills/test-driven-development/dossier/inputs.yml && [ -f core/tests/skill_test_driven_development_e2e.bats ] && bats core/tests/skill_test_driven_development_e2e.bats 2>&1 | tail -3 | grep -qE '1 test, 0 failures' && bats core/tests/ 2>&1 | tail -3 | grep -qE '0 failures' && echo "TDD skill+fixture+e2e OK"</automated>
  </verify>
  <done>
    `skills/test-driven-development/run.sh` is executable, shellcheck-clean POSIX, uses canonical prelude + `set +e` bracketing + `jq -n` emission + `## Acceptance` heading + final `chantier state append` + `exit 0`. Fixture has `test_framework: bats`, `phase: red`, `test_command: false`. E2e test runs run.sh + `chantier validate-task` and asserts both exit 0, with `red_exit_code == 1` recorded in output.json. Full bats suite green. Wave-1 uniformity test now reports 3×PASS (both Wave-2 skills shipped so far comply).
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `inputs.yml` (`target_file`, `test_command`) → `run.sh` → `sh -c "$TEST_COMMAND"` | The `test_command` field IS executed by `sh -c` — this is the primary command-injection surface for this skill. |
| `red.out` / `green.out` content → `grep -cE` (tests_added counter) | Untrusted test-runner output is parsed for ok/not-ok counts; no shell expansion of that content. |
| `run.sh` → `chantier state append` | Final command; binary's mkdir-mutex serializes STATE.md writes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Tampering | Command injection via `test_command` in inputs.yml (e.g., `test_command: "true; rm -rf /"`) | mitigate (with documented residual risk) | The `test_command` value is intentionally executed by `sh -c "$TEST_COMMAND"` — this is the skill's purpose (run an arbitrary test command). Mitigation: (a) `test_command` SHOULD be derived from a known-safe default per-framework when omitted (the case-statement defaults in run.sh); (b) when supplied explicitly, the dossier `inputs.yml` is staged by a trusted process (Phase 4 adapter); (c) skills are not invoked with untrusted dossiers — the dossier is part of the task definition, which is itself authored by a trusted contributor. Phase 4 adapter staging is the trust boundary, not run.sh. Residual risk accepted because the skill cannot run tests without invoking a shell command — the alternative (a sandboxed-only model) is out of scope for v0.1. |
| T-03-03-02 | Tampering | JSON injection in `output.json` (e.g., `target_file` containing `"` or backslash) | mitigate | All values flow through `jq --arg` / `--argjson` per the canonical pattern at core/bin/chantier line 196. No `printf '{"k":"%s"}' "$v"` anywhere. |
| T-03-03-03 | Tampering | Path traversal in `state_writes` | accept (mitigated upstream) | `chantier validate-task` gate 1 enforces (Phase 2 verified). |
| T-03-03-04 | Tampering | TOCTOU on `inputs.yml` (script reads twice) | mitigate | inputs.yml is read once at the top of run.sh into shell variables (TARGET_FILE, TEST_FRAMEWORK, PHASE_FLAG, TEST_COMMAND); subsequent operations use captured values per RESEARCH §Security row 5. |
| T-03-03-05 | Information Disclosure | Harness identifier in run.sh, PRESSURE.md, output.md, or output.json | mitigate | Gate 4 deny-list grep is wired and scans all skill body files except SKILL.md. Pre-commit (manual): `grep -rE 'mcp__|claude_ai_|@codebase|claude-code|cursor|codex-cli|copilot-cli|gemini-cli|opencode' skills/test-driven-development/`. |
| T-03-03-06 | Denial of Service | Runaway test command consuming resources | accept | The test command may be long-running (this is the skill's purpose). bats wraps the e2e test in a 30s default per-test timeout. In production (Phase 4+), the harness adapter is responsible for resource limits on dispatched skill invocations. Phase 3 is not the layer that enforces this. |
| T-03-03-SC | Tampering | npm/pip/cargo installs | n/a | No external packages installed. POSIX `sh`, `jq`, `bats`, `shellcheck`, `grep`, `sed`, `date`, `wc` are host POSIX or already-vendored. |

</threat_model>

<verification>
- `[ -x skills/test-driven-development/run.sh ]`.
- `shellcheck --shell=sh skills/test-driven-development/run.sh` — clean.
- `head -20` shows canonical prelude.
- `grep -E '^chantier state append' run.sh` returns 1.
- `grep -E 'jq -n' run.sh` returns at least 1.
- `grep -E 'set \+e' run.sh && grep -E 'set -e' run.sh` — both present (the bracketing around `sh -c "$TEST_COMMAND"`).
- `grep -E '^## Scenario [0-9]' PRESSURE.md | wc -l` returns 2.
- `grep -cE '^\*\*(Situation|Temptation|Required response|Disqualifier)\*\*' PRESSURE.md` returns 8.
- `grep -rE 'mcp__|claude_ai_|@codebase|claude-code|cursor|codex-cli|copilot-cli|gemini-cli|opencode' skills/test-driven-development/PRESSURE.md skills/test-driven-development/run.sh` returns 0 matches.
- `awk` extracts `harness_adapters: [claude-code]` from SKILL.md.
- `bats core/tests/skill_test_driven_development_e2e.bats` — exits 0, "1 test, 0 failures".
- `bats core/tests/skill_uniformity.bats` — exits 0, 3 PASSES (now that 2 of 4 skills are present).
- `bats core/tests/` — full suite, zero failures.
- `chantier --self-test` — all green.
</verification>

<success_criteria>
1. `skills/test-driven-development/SKILL.md` exists with 8-field frontmatter, correct id/version/harness_adapters, 7 ordered body sections, kernel invariants 1–3 verbatim, invariant 4 cites red/green timestamps and exit codes.
2. `skills/test-driven-development/PRESSURE.md` exists with frontmatter + 2 scenarios (time-pressure + sunk-cost) + 8 subsection markers + Disqualifiers citing Invariant 4 and red_step_timestamp / red_exit_code.
3. `skills/test-driven-development/run.sh` is executable, shellcheck-clean, uses canonical prelude + phase-flag dispatch + `set +e` bracketing + `jq -n` emission + `## Acceptance` heading + final `chantier state append` + `exit 0`.
4. Fixture `inputs.yml` has `target_file`, `test_framework: bats`, `phase: red`, `test_command: false`.
5. E2e bats test runs run.sh and `chantier validate-task` against the fixture and exits 0 with `red_exit_code == 1` recorded.
6. Zero deny-list tokens in any non-SKILL.md file under `skills/test-driven-development/`.
7. Full bats suite remains green.
</success_criteria>

<output>
Create `.planning/phases/03-skill-library/03-03-SUMMARY.md` mirroring 02-06-SUMMARY.md shape. Append `plan.completed` event: `chantier state append -e plan.completed -t null -s test-driven-development -m "Plan 03-03 complete: test-driven-development skill shipped end-to-end" -r .planning/phases/03-skill-library/03-03-SUMMARY.md`.
</output>

---
phase: 03-skill-library
plan: "05"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - skills/subagent-driven-development/SKILL.md
  - skills/subagent-driven-development/PRESSURE.md
  - skills/subagent-driven-development/run.sh
  - core/tests/skill_subagent_driven_development_e2e.bats
  - core/tests/fixtures/skills/subagent-driven-development/dossier/inputs.yml
autonomous: true
requirements: [FR-005, FR-006, FR-009, FR-010]
requirements_addressed: [FR-005, FR-006, FR-009, FR-010, NFR-001, NFR-002, NFR-004, NFR-005]
tags: [phase-03, skill, subagent-driven-development, run-sh, pressure-md, kernel-invariants, why-no-hooks, issue-237]
nyquist: 03-VALIDATION.md

must_haves:
  truths:
    - "After this plan, `skills/subagent-driven-development/` contains SKILL.md, PRESSURE.md, and executable run.sh — run.sh ends with `chantier state append -e skill.completed` (D-01, D-02, D-03)."
    - "SKILL.md frontmatter has all 8 required fields; `id: subagent-driven-development`; `version: 1.0.0`; `harness_adapters: [claude-code]` (D-14)."
    - "SKILL.md body contains a `## Why no hooks` section per D-08 citing `https://github.com/obra/superpowers/issues/237` (the safe-URL form verified against the deny-list per RESEARCH Example 4 / Pitfall 1) AND `ADR 0001 §6`. This is the LOAD-BEARING distinction of this skill from the other three — D-08 is exclusive to subagent-driven-development."
    - "SKILL.md body contains numbered `## Invariants` with kernel 1–3 verbatim (D-06) plus skill-specific invariants 4 and 5: (4) **Self-contained subtask briefs** — every subtask brief is a file on disk that the subagent can `cat`, with no reliance on parent-conversation context. (Proof: `output.json.subtask_briefs[].brief_path` references actual files AND `output.json.parent_context_refs_count == 0`.) (5) **Kernel acknowledgement** — every dispatched subtask brief begins with a read-aloud of the three kernel invariants (Proof: `output.json.subagent_invariants_acknowledged_count >= 3`)."
    - "Every invariant has measurable proof field in output.json (D-07): `subtask_count`, `subtask_briefs: array of {id, brief_path, brief_word_count}`, `parent_context_refs_count` (MUST be 0), `subagent_invariants_acknowledged_count` (MUST be >= 3 — kernel count), `invariants_applied`."
    - "SKILL.md body sequences these h2 headings in order: `## Purpose`, `## When to use`, `## Invariants`, `## How`, `## Why no hooks` (D-08 — UNIQUE to this skill), `## Portability claim` (D-15), `## Exit code matrix (from run.sh)` (D-04), `## Acknowledge before acting` (D-05)."
    - "The body of `subagent-driven-development` talks about 'a fresh agent invocation with no access to the parent conversation' and 'the dossier files the subagent will read' (per Pitfall 8) — it does NOT reference any specific harness's subagent-spawning mechanism, and it contains NO deny-list token in any file other than the SKILL.md frontmatter's `harness_adapters: [claude-code]` entry."
    - "PRESSURE.md ships YAML frontmatter (skill_id + scenarios) per D-12, contains at least one time-pressure scenario and one sunk-cost scenario per D-10, AND an optional third 'authority' scenario per RESEARCH Assumption A8 (the parent context appears to override discipline). Each scenario in the D-09 four-subsection format with Disqualifier citing Invariant 4 or 5 + the relevant output.json field per D-11. No cross-skill references (D-13)."
    - "`core/tests/skill_subagent_driven_development_e2e.bats` mounts the fixture dossier (subtask_count: 2, parent_brief: '...'), runs run.sh, asserts output.md, output.json, and two `subtask_brief_<id>.md` files exist; verifies `output.json.parent_context_refs_count == 0` and `output.json.subagent_invariants_acknowledged_count >= 3`; then runs `chantier validate-task t1` and asserts exit 0."
    - "No deny-list token (`mcp__|claude_ai_|@codebase|claude-code|cursor|codex-cli|copilot-cli|gemini-cli|opencode`) in any file under `skills/subagent-driven-development/` other than SKILL.md frontmatter — this skill is the most adversarial test of NFR-001 because it MUST talk about subagents (a harness-implemented concept) without naming the harness."
    - "Decision coverage in this plan: D-01 (run.sh ships), D-02 (SKILL.md = WHEN/WHY: when to fan out subagents; run.sh = HOW: emit subtask brief files), D-03 (run.sh sole author of output files + brief files), D-04 (exit-code matrix), D-05 (Invariants + Acknowledge), D-06 (kernel 1–3 verbatim), D-07 (output.json proof fields including parent_context_refs_count and subagent_invariants_acknowledged_count), D-08 (`## Why no hooks` section citing #237 and ADR 0001 §6 — EXCLUSIVE to this skill), D-09 (PRESSURE 4-subsection template), D-10 (time-pressure + sunk-cost), D-11 (Disqualifier ↔ invariant ↔ field), D-12 (PRESSURE frontmatter), D-13 (autonomous PRESSURE), D-14 (uniform harness_adapters), D-15 (Portability claim)."
  artifacts:
    - path: "skills/subagent-driven-development/SKILL.md"
      provides: "8-field frontmatter + body with kernel + 2 skill-specific invariants + UNIQUE `## Why no hooks` section citing obra/superpowers#237."
      contains: "## Why no hooks"
    - path: "skills/subagent-driven-development/PRESSURE.md"
      provides: "≥2 (with optional 3rd) adversarial scenarios; Disqualifiers cite Invariant 4 or 5 + parent_context_refs_count / subtask_briefs fields."
      contains: "## Scenario 1"
    - path: "skills/subagent-driven-development/run.sh"
      provides: "POSIX shell: read subtask_count + parent_brief, emit N self-contained subtask_brief_<id>.md files, emit output.{md,json}, chantier state append."
      contains: "subtask_brief"
    - path: "core/tests/skill_subagent_driven_development_e2e.bats"
      provides: "E2e test asserts parent_context_refs_count == 0, subagent_invariants_acknowledged_count >= 3, all brief files exist, validate-task exits 0."
      contains: "parent_context_refs_count"
    - path: "core/tests/fixtures/skills/subagent-driven-development/dossier/inputs.yml"
      provides: "Minimal fixture: subtask_count: 2, parent_brief: a short integration-style instruction."
      contains: "subtask_count: 2"
  key_links:
    - from: "skills/subagent-driven-development/SKILL.md"
      to: "https://github.com/obra/superpowers/issues/237"
      via: "## Why no hooks section cites the URL (deny-list-safe per RESEARCH Example 4)"
      pattern: "obra/superpowers/issues/237"
    - from: "skills/subagent-driven-development/run.sh"
      to: "subtask_brief_<id>.md files in task dir"
      via: "heredoc emission per subtask; each brief starts with the three kernel invariants verbatim"
      pattern: "subtask_brief_"
    - from: "skills/subagent-driven-development/run.sh"
      to: ".planning/STATE.md"
      via: "chantier state append -e skill.completed (final command)"
      pattern: "chantier state append -e skill\\.completed"
    - from: "skills/subagent-driven-development/PRESSURE.md"
      to: "skills/subagent-driven-development/SKILL.md (Invariant 4 or 5)"
      via: "Disqualifier cites Invariant + output.json.parent_context_refs_count or .subtask_briefs"
      pattern: "Violates Invariant [45]"
---

<objective>
Ship the fourth and final reference skill: `subagent-driven-development`. This skill is the
load-bearing answer to obra/superpowers issue #237 (SessionStart hooks do not propagate to
subagents — discipline MUST live in the skill body and the dossier the subagent reads). Per
D-08, the body contains a unique `## Why no hooks` section citing the issue URL and ADR 0001
§6. Per D-06 + skill-specific extension, the kernel invariants 1–3 are augmented with two
skill-specific items: (4) every subtask brief is a self-contained file on disk with no
parent-conversation dependency; (5) every dispatched subtask brief begins by acknowledging
the three kernel invariants in writing.

This skill is the most adversarial test of NFR-001 because it MUST discuss subagents — a
concept that every harness implements differently — without naming any harness in any file
gate 4 scans. Per Pitfall 8, the body speaks of "a fresh agent invocation with no access to
the parent conversation" and "the dossier files the subagent will read".

Output: five new files; e2e bats test verifies subtask brief emission and the two
discipline metrics (parent_context_refs_count == 0; subagent_invariants_acknowledged_count
>= 3) through `chantier validate-task`; uniformity test now reports 3×PASS across all four
Wave-2 skills — Wave 3 can close the phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-skill-library/03-CONTEXT.md
@.planning/phases/03-skill-library/03-RESEARCH.md
@.planning/phases/03-skill-library/03-PATTERNS.md
@.planning/phases/03-skill-library/03-VALIDATION.md
@.planning/phases/03-skill-library/03-01-SUMMARY.md
@docs/adr/0001-state-skill-contract.md
@docs/adr/0002-runtime-binary-and-state-format.md
@docs/research/inheritance-map.md
@core/schemas/skill.json
@core/bin/chantier
@core/tests/validate_task.bats
@CONTRIBUTING.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author skills/subagent-driven-development/SKILL.md (contract + body with UNIQUE `## Why no hooks` section per D-08)</name>
  <files>skills/subagent-driven-development/SKILL.md</files>
  <read_first>
    - .planning/phases/03-skill-library/03-RESEARCH.md §"Pattern 3: SKILL.md body structure" — 9 sections; this skill is the ONE that includes section 6 `## Why no hooks` (D-08). §"Code Examples > Example 4: Safe URL for citing obra/superpowers#237 (Pitfall 1)" lines 994–1003 — verified safe-URL forms. §"Pitfall 8: subagent-driven-development body talks about a specific harness's subagent mechanism" — the rhetorical discipline (speak only of "fresh agent invocation", "dossier files the subagent will read"). §"Pattern 2 row" for subagent-driven-development output.json fields.
    - .planning/phases/03-skill-library/03-CONTEXT.md D-05, D-06, D-07, D-08 (LOAD-BEARING — unique to this skill), D-14, D-15.
    - docs/adr/0001-state-skill-contract.md §6 (the #237 finding that load-bears the subagent-discipline framing).
    - docs/research/inheritance-map.md §6 (subagent + worktrees + the #237 caveat) — for grounding the rationale in original prose.
    - core/schemas/skill.json (8 required frontmatter fields).
    - core/bin/chantier lines 686–702 (gate 4 deny-list — the precise pattern this skill must NOT match anywhere outside SKILL.md frontmatter).
  </read_first>
  <behavior>
    - YAML frontmatter validates against core/schemas/skill.json. `id: subagent-driven-development`. `version: 1.0.0`. `harness_adapters: [claude-code]`.
    - `inputs_schema` declares `type: object`, `required: [subtask_count, parent_brief]`, `properties` for: `subtask_count` (number, minimum 1), `parent_brief` (string — the parent task's framing, copied into each subtask brief), optional `subtask_focus` (array of string — one focus per subtask).
    - `outputs_schema` declares required: `[subtask_count, subtask_briefs, parent_context_refs_count, subagent_invariants_acknowledged_count, invariants_applied]`. `subtask_briefs` is an array of objects with shape `{id: string, brief_path: string, brief_word_count: number}`. `parent_context_refs_count` is a number (MUST be 0 — invariant 4 proof). `subagent_invariants_acknowledged_count` is a number (MUST be >= 3 — invariant 5 proof, kernel count). Plus optional `started_at`, `ended_at` ISO-8601 patterns.
    - Body has exactly 8 ordered h2 sections (Purpose, When to use, Invariants, How, **Why no hooks**, Portability claim, Exit code matrix, Acknowledge). The 8th section count vs the other three skills' 7 is intentional — D-08 is exclusive to this skill.
    - `## Invariants` has 5 numbered items: kernel 1–3 verbatim per D-06; item 4 = "**Self-contained subtask briefs.** Every subtask brief is a file on disk that the subagent can `cat`; no item in the brief depends on parent-conversation context the subagent does not have. (Proof: `output.json.subtask_briefs[].brief_path` references actual files AND `output.json.parent_context_refs_count == 0`.)"; item 5 = "**Kernel acknowledgement.** Every dispatched subtask brief begins by reading aloud the three kernel invariants. (Proof: `output.json.subagent_invariants_acknowledged_count >= 3`.)"
    - `## Why no hooks` section contains the URL `https://github.com/obra/superpowers/issues/237` (the deny-list-safe form per RESEARCH Example 4) AND cites `ADR 0001 §6`. Verified by RESEARCH Pitfall 1: this URL contains no deny-list token.
    - No file in skills/subagent-driven-development/ — including SKILL.md body — contains any deny-list token outside the `harness_adapters: [claude-code]` frontmatter entry. SKILL.md is exempt from gate 4 (chantier line 691), but the discipline holds across all files in the skill subtree.
    - Body length 120–200 lines (slightly higher upper bound because D-08 adds one section).
  </behavior>
  <action>
    Create `skills/subagent-driven-development/SKILL.md`. YAML frontmatter:

    - `id: subagent-driven-development`
    - `version: 1.0.0`
    - `inputs_schema`: `type: object`, `required: [subtask_count, parent_brief]`, properties: `subtask_count: {type: number, minimum: 1}` (note: `minimum` is in the ADR 0002 JSON Schema subset profile — verify by reading ADR 0002 §"JSON Schema subset profile" — IF `minimum` is NOT in the subset profile (the subset lists `type`, `required`, `properties`, `additionalProperties`, `pattern`, `enum`, `items`), drop the `minimum` keyword and document the constraint in the SKILL.md body's `## How` section instead — falsifiability via run.sh validation, not via schema. RECOMMENDED: drop `minimum`; validate `subtask_count >= 1` inside run.sh, exit 2 if violated.), `parent_brief: {type: string}`, optional `subtask_focus: {type: array, items: {type: string}}`.
    - `state_reads: ["{phase}/CONTEXT.md", "{phase}/tasks/{depends_on}/output.json"]`
    - `state_writes: ["{phase}/tasks/{task}/", ".planning/STATE.md"]`
    - `outputs_schema`: `type: object`, `required: [subtask_count, subtask_briefs, parent_context_refs_count, subagent_invariants_acknowledged_count, invariants_applied]`, properties: `subtask_count: {type: number}`, `subtask_briefs: {type: array, items: {type: object}}` (the per-item shape `{id, brief_path, brief_word_count}` is documented in the SKILL body but not parsed by gate 4's awk extractor — the JSON Schema subset profile DOES permit `items` per ADR 0002), `parent_context_refs_count: {type: number}`, `subagent_invariants_acknowledged_count: {type: number}`, `invariants_applied: {type: array, items: {type: number}}`, optional `started_at`, `ended_at` with ISO-8601 patterns.
    - `portable: true`
    - `harness_adapters: [claude-code]`

    Body sections (in this order):

    - `# Subagent-driven development` (h1).
    - `## Purpose` — two sentences: this skill fans out a parent task into self-contained subtask briefs, each a file on disk that a fresh agent invocation can read without any parent-conversation context. The skill is the load-bearing answer to the discipline-loss problem documented at obra/superpowers#237 — discipline cannot live in session-injected hooks; it must live in the files the subagent reads.
    - `## When to use` — 3–5 bullets: a parent task is too large for one context window; a parent task naturally splits into independent subtasks; a parent task benefits from parallel execution by multiple fresh agent invocations; when the parent task's discipline must propagate to every subtask (TDD, scoped review, worktree isolation) and the agent must guarantee discipline survives the parent→subagent context boundary.
    - `## Invariants` — kernel 1–3 verbatim per D-06; item 4: "**Self-contained subtask briefs.** Every subtask brief is a file on disk that the subagent can `cat`; no item in the brief depends on parent-conversation context the subagent does not have. (Proof: `output.json.subtask_briefs[].brief_path` references actual files AND `output.json.parent_context_refs_count == 0`.)"; item 5: "**Kernel acknowledgement.** Every dispatched subtask brief begins by reading aloud the three kernel invariants. (Proof: `output.json.subagent_invariants_acknowledged_count >= 3`.)" Standard closing sentence: "Every invariant has a measurable proof in `output.json` (see `outputs_schema`). The list of which invariants were applied is captured in `output.json.invariants_applied`."
    - `## How` — 3–5 numbered WHEN/WHY steps: why the brief lives on disk (the subagent has no parent context to fall back on); why the brief acknowledges the kernel invariants explicitly (the subagent cannot rely on hooks to inject them — see `## Why no hooks` below); why each subtask is independent (parallel execution; isolation); when to use vs not to use (not for tasks that compose mid-flight; not for tasks whose subtasks share mutable state — those need the worktree skill first).
    - `## Why no hooks` — the LOAD-BEARING section per D-08. Approximately 6–12 lines. State: "A fresh agent invocation runs without access to the parent conversation. Discipline that depends on session-injected context — such as a hook that fires at session start in some host environments — does not propagate to fresh invocations. The failure mode is documented at https://github.com/obra/superpowers/issues/237 and is the load-bearing constraint that motivates ADR 0001 §6. Chantier therefore places all discipline in the skill body and the dossier files, both of which the fresh invocation reads as files. Anything important to the task must be a file the fresh invocation can `cat`." End with one sentence connecting this to the skill's own invariants: "Invariants 4 and 5 are the mechanical embodiment of this rule: every subtask brief is a file on disk, and every brief acknowledges the kernel invariants verbatim."
    - `## Portability claim` — verbatim D-15 boilerplate (same shape as the other three skills).
    - `## Exit code matrix (from run.sh)` — `| 0 | Success or business-state outcome encoded in output.json |` / `| 2 | Technical incident: missing inputs.yml, subtask_count < 1, missing jq, filesystem error |`. Closing sentence: "Business outcomes are NEVER encoded in exit codes."
    - `## Acknowledge before acting` — uniform read-aloud gate per RESEARCH Example 5, contextualized: "Before invoking `run.sh`, list (in writing, in the agent's own words) which invariants apply to the current task. For any subagent-driven task, all five (kernel 1, 2, 3 + skill-specific 4 and 5) apply. State in one sentence why each applies. After producing this list, invoke `run.sh`. The applied list will appear in `output.md` under `## Invariants applied`. Each emitted subtask brief begins with the three kernel invariants read aloud — this is what makes invariant 5 measurable."

    Length target: 120–200 lines (slightly above the 100–180 budget of the other three skills because of the extra `## Why no hooks` section).

    Pitfalls to avoid:
    - DO NOT use any deny-list token in the body (gate 4 does NOT scan SKILL.md per chantier line 691, BUT convention holds, AND the body specifically discusses subagents in language that could be tempting to harness-name). Speak only of "a fresh agent invocation", "the dossier files the subagent will read", "a child agent process". Verify with `grep -E 'mcp__|claude_ai_|@codebase|claude-code|cursor|codex-cli|copilot-cli|gemini-cli|opencode' skills/subagent-driven-development/SKILL.md` — should return ONLY the literal `claude-code` inside the `harness_adapters` frontmatter entry.
    - DO NOT cite the issue with a fragment URL like `#237` only — use the full URL `https://github.com/obra/superpowers/issues/237` (RESEARCH Example 4 + Assumption A11).
    - DO NOT nest maps in frontmatter outside inputs_schema / outputs_schema (Pitfall 2).
    - DO NOT include `minimum` in inputs_schema if ADR 0002's JSON Schema subset profile does not list it — fall back to runtime validation in run.sh.
    - DO NOT cross-reference other skill names — composition deferred (D-13 spirit).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && [ -f skills/subagent-driven-development/SKILL.md ] && awk '/^---$/{c++; next} c==1' skills/subagent-driven-development/SKILL.md > /tmp/sdd_fm.yaml && for f in id version inputs_schema state_reads state_writes outputs_schema portable harness_adapters; do grep -qE "^${f}:" /tmp/sdd_fm.yaml || { echo "MISSING field: $f"; exit 1; }; done && grep -qE "^id:[[:space:]]+subagent-driven-development$" /tmp/sdd_fm.yaml && grep -qE "^version:[[:space:]]+1\.0\.0$" /tmp/sdd_fm.yaml && grep -qE "^portable:[[:space:]]+true$" /tmp/sdd_fm.yaml && _ha=$(awk '/^harness_adapters:/{in_ha=1; next} in_ha && /^[a-z_]+:/{in_ha=0} in_ha && /^[[:space:]]+-/{gsub(/^[[:space:]]+-[[:space:]]*/, ""); gsub(/"/, ""); print}' /tmp/sdd_fm.yaml | sort | tr '\n' ',' | sed 's/,$//') && [ "$_ha" = "claude-code" ] && for h in '^# Subagent-driven development' '^## Purpose' '^## When to use' '^## Invariants' '^## How' '^## Why no hooks' '^## Portability claim' '^## Exit code matrix' '^## Acknowledge before acting'; do grep -qE "$h" skills/subagent-driven-development/SKILL.md || { echo "MISSING section: $h"; exit 1; }; done && grep -qF 'https://github.com/obra/superpowers/issues/237' skills/subagent-driven-development/SKILL.md && grep -qE 'ADR 0001' skills/subagent-driven-development/SKILL.md && grep -qE 'parent_context_refs_count' skills/subagent-driven-development/SKILL.md && grep -qE 'subagent_invariants_acknowledged_count' skills/subagent-driven-development/SKILL.md && _body=$(sed -n '/^---$/,/^---$/d; p' skills/subagent-driven-development/SKILL.md | grep -vE '^---$') && _denied=$(echo "$_body" | grep -cE 'mcp__|claude_ai_|@codebase|claude-code|cursor|codex-cli|copilot-cli|gemini-cli|opencode' || true) && [ "$_denied" -eq 0 ] && echo "SDD SKILL.md OK (body deny-list clean; ## Why no hooks present; URL verified)"</automated>
  </verify>
  <done>
    `skills/subagent-driven-development/SKILL.md` exists with 8-field frontmatter, correct id, 8 ordered h2 sections INCLUDING `## Why no hooks` (D-08), kernel invariants 1–3 verbatim, skill-specific invariants 4 and 5 with proof citations to parent_context_refs_count and subagent_invariants_acknowledged_count. URL `https://github.com/obra/superpowers/issues/237` present; `ADR 0001` cited. Body (excluding frontmatter) has zero deny-list tokens.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author skills/subagent-driven-development/PRESSURE.md (≥2 scenarios + optional 3rd authority scenario citing Invariant 4 or 5)</name>
  <files>skills/subagent-driven-development/PRESSURE.md</files>
  <read_first>
    - .planning/phases/03-skill-library/03-RESEARCH.md §"Pattern 4: PRESSURE.md shape" (frontmatter + scenario template); §"Open Question 3" + Assumption A8 (the optional third "authority" scenario is recommended for this skill specifically).
    - .planning/phases/03-skill-library/03-CONTEXT.md D-09, D-10, D-11, D-12, D-13.
    - skills/subagent-driven-development/SKILL.md (just authored) — for invariant numbers (1–5) and output.json field names.
  </read_first>
  <behavior>
    - Frontmatter: `skill_id: subagent-driven-development`; `scenarios` list with at least 2 entries (time-pressure + sunk-cost) and optionally a 3rd (authority).
    - Body has at least 2 (recommended: 3) `## Scenario N` headings; each scenario has 4 `**Subsection.**` markers.
    - At least one Disqualifier cites "Violates Invariant 4 (self-contained-subtask-briefs)" with `output.json.parent_context_refs_count > 0` or `subtask_briefs[].brief_path` empty.
    - At least one Disqualifier cites "Violates Invariant 5 (kernel-acknowledgement)" with `output.json.subagent_invariants_acknowledged_count < 3`.
    - No deny-list token anywhere in the file (gate 4 scans PRESSURE.md — this is one of the most sensitive surfaces for this skill).
    - No cross-skill reference.
  </behavior>
  <action>
    Create `skills/subagent-driven-development/PRESSURE.md` with frontmatter listing two or three scenarios:
    ```
    ---
    skill_id: subagent-driven-development
    scenarios:
      - id: sdd-time-pressure-01
        levers: [time-pressure]
        invariants_referenced: [4]
      - id: sdd-sunk-cost-01
        levers: [sunk-cost]
        invariants_referenced: [5]
      - id: sdd-authority-01
        levers: [authority]
        invariants_referenced: [4, 5]
    ---
    ```
    (Third scenario is recommended per A8 but the minimum is two. Executor may ship two or three.)

    h1 `# PRESSURE — subagent-driven-development` + one-paragraph framing referencing how the #237 finding makes parent-context-leak a load-bearing failure mode for this skill specifically (without naming any harness).

    SCENARIO 1 — time pressure (citing Invariant 4). Title: `## Scenario 1 — "Just tell them what we discussed" (time-pressure)`. `**Situation.**` — The parent task has been in flight for 30 minutes. Two subtasks have been identified; the agent has built up implicit context about how they relate (e.g., "subtask A produces X which subtask B then validates"). The release window is approaching. `**Temptation.**` — Dispatch the subagents with terse briefs that lean on "what we discussed" — e.g., "implement validation per the design we agreed on". The fresh agent invocation will read the brief, find it lacking context, perhaps ask back (in a hosting environment that supports back-and-forth), or perhaps make assumptions. The 5 minutes per brief saved feels worth it. `**Required response.**` — Write each subtask brief as a fully self-contained document. The brief states: the design, the inputs, the outputs, the disqualifying failure modes — all in the file. The fresh invocation has the dossier files and the skill body and nothing else; the brief must reflect that. 5 minutes per brief is 10 minutes total; the alternative is multiple round-trips or silent guessing. `**Disqualifier.**` Violates Invariant 4 (self-contained-subtask-briefs). Detected by `output.json.parent_context_refs_count > 0` (run.sh greps the brief files for phrases like "as discussed", "per our earlier conversation", "the agreed approach") OR `output.json.subtask_briefs[].brief_word_count < 50` (briefs that thin almost certainly elide context).

    SCENARIO 2 — sunk cost (citing Invariant 5). Title: `## Scenario 2 — "We've gone over this; just dispatch" (sunk-cost)`. `**Situation.**` — Discipline has been the focus for the last 20 minutes. The agent has acknowledged the kernel invariants three times in this conversation. The parent task is structurally ready to dispatch; the briefs are written. `**Temptation.**` — Skip the kernel-acknowledgement preamble at the top of each subtask brief. The kernel was acknowledged in the parent conversation 20 minutes ago; the fresh invocation will "of course" understand. The repetition feels redundant. `**Required response.**` — The kernel acknowledgement is not for the parent context; it is for the fresh invocation that has never seen the parent context. Each subtask brief begins with the three kernel invariants read aloud verbatim. The repetition is the point — it is the only way the discipline survives the context boundary. `**Disqualifier.**` Violates Invariant 5 (kernel-acknowledgement). Detected by `output.json.subagent_invariants_acknowledged_count < 3` (run.sh counts the kernel-invariant phrases in each brief and aggregates; the minimum is 3, the kernel count).

    SCENARIO 3 — authority (optional, citing both Invariants 4 and 5). Title: `## Scenario 3 — "The user is impatient; just hand the brief over" (authority)`. `**Situation.**` — A senior reviewer or user has been pressing on a timeline. The parent context contains pointed language: "you don't need to belabor the discipline boilerplate; the subagent is competent." `**Temptation.**` — Comply with the apparent authority. Strip the kernel acknowledgement preamble; assume the subagent will fill in context from the parent conversation (which it cannot — see `## Why no hooks` in SKILL.md). `**Required response.**` — The discipline is non-negotiable per the skill's contract. The brief includes the kernel acknowledgement and is self-contained. If the user pushes back, explain in one sentence: "the fresh invocation has no access to this conversation; the discipline must travel in the brief itself." `**Disqualifier.**` Violates Invariant 4 AND Invariant 5. Detected by `output.json.parent_context_refs_count > 0` OR `output.json.subagent_invariants_acknowledged_count < 3`.

    Pitfalls to avoid:
    - DO NOT include any deny-list token in this file (gate 4 scans it). Use "fresh agent invocation", "child agent process", "delegated agent" instead of any harness name.
    - DO NOT cross-reference other skills (D-13).
    - DO NOT cite an `output.json` field that doesn't exist in SKILL.md outputs_schema (Task 1 declared exactly those fields).
    - DO NOT write the third scenario unless the SKILL.md contract genuinely supports a separate "authority"-only Disqualifier — otherwise stay at two scenarios per the minimum.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && [ -f skills/subagent-driven-development/PRESSURE.md ] && _sc=$(grep -cE '^## Scenario [0-9]' skills/subagent-driven-development/PRESSURE.md || true) && [ "$_sc" -ge 2 ] && _ss=$(grep -cE '^\*\*(Situation|Temptation|Required response|Disqualifier)\*\*' skills/subagent-driven-development/PRESSURE.md || true) && [ "$_ss" -ge 8 ] && [ "$((_ss % 4))" -eq 0 ] && grep -qE 'skill_id:\s*subagent-driven-development' skills/subagent-driven-development/PRESSURE.md && grep -qE 'levers:\s*\[time-pressure\]' skills/subagent-driven-development/PRESSURE.md && grep -qE 'levers:\s*\[sunk-cost\]' skills/subagent-driven-development/PRESSURE.md && _denied=$(grep -cE 'mcp__|claude_ai_|@codebase|claude-code|cursor|codex-cli|copilot-cli|gemini-cli|opencode' skills/subagent-driven-development/PRESSURE.md || true) && [ "$_denied" -eq 0 ] && grep -qE 'Violates Invariant [45]' skills/subagent-driven-development/PRESSURE.md && grep -qE 'output\.json\.(parent_context_refs_count|subagent_invariants_acknowledged_count|subtask_briefs)' skills/subagent-driven-development/PRESSURE.md && echo "SDD PRESSURE.md OK ($_sc scenarios, $_ss subsection markers)"</automated>
  </verify>
  <done>
    PRESSURE.md exists with frontmatter + ≥2 scenarios + subsection-count divisible by 4 (one set of 4 per scenario); covers time-pressure + sunk-cost (and optionally authority); Disqualifiers cite Invariant 4 or 5 and parent_context_refs_count / subagent_invariants_acknowledged_count / subtask_briefs; zero deny-list tokens.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author skills/subagent-driven-development/run.sh + fixture inputs.yml + e2e bats test</name>
  <files>skills/subagent-driven-development/run.sh, core/tests/fixtures/skills/subagent-driven-development/dossier/inputs.yml, core/tests/skill_subagent_driven_development_e2e.bats</files>
  <read_first>
    - .planning/phases/03-skill-library/03-RESEARCH.md §"Pattern 1: run.sh skeleton" (canonical prelude); §"Pattern 2" subagent-driven-development row (output.json fields); §"Pitfall 8" (the rhetorical discipline for talking about subagents — applies to run.sh comments too).
    - .planning/phases/03-skill-library/03-PATTERNS.md §"skills/<name>/run.sh".
    - core/bin/chantier lines 1–13 (prelude), 188 (timestamp), 194–205 (jq emission), 423 (Acceptance regex).
    - core/tests/validate_task.bats lines 7–24 (setup), 29–71 (make_plan helper).
    - skills/subagent-driven-development/SKILL.md (just authored) — outputs_schema field names run.sh must populate.
  </read_first>
  <behavior>
    - `run.sh` is executable (0755), shellcheck-clean POSIX, canonical prelude.
    - Reads `inputs.yml` once: `subtask_count` (number, must be >= 1), `parent_brief` (string), optional `subtask_focus` (array of strings).
    - Validates `subtask_count >= 1` in shell (`[ "$SUBTASK_COUNT" -ge 1 ]` — exit 2 if not).
    - For each subtask 1..SUBTASK_COUNT: emits `$TASK_DIR/subtask_brief_<id>.md`. Each brief starts with the three kernel invariants verbatim (matching D-06 wording — copy from SKILL.md Invariant 1, 2, 3 prose), then `## Subtask brief` heading, then prose derived from `parent_brief` + the i-th `subtask_focus` (if provided). The kernel acknowledgement is the count contributor for invariant 5 — each brief contributes 3 to `subagent_invariants_acknowledged_count` (since the kernel is 3 invariants), so 2 subtasks × 3 invariants = 6 acknowledgements total ⇒ `subagent_invariants_acknowledged_count: 6`.
    - Computes `parent_context_refs_count` by greping each brief file for phrases that indicate reliance on parent context: `grep -cE '(as discussed|per our earlier|the agreed approach|like we said|as mentioned)' "$TASK_DIR/subtask_brief_<id>.md"` — this MUST be 0 across all briefs for invariant 4 to hold. Since the briefs are author-written by run.sh (deterministic templated content), the count will be 0 by construction unless `parent_brief` itself happens to contain one of those phrases — in which case run.sh records the count honestly (which is the correct behavior: the Disqualifier fires, validate-task gate 5 still passes because the Acceptance section is independent, but downstream verification of invariant 4 sees the breach).
    - Emits `output.json` via `jq -n --arg started ... --argjson count $SUBTASK_COUNT --argjson briefs "$BRIEFS_JSON_ARRAY" --argjson parent_refs $PARENT_REFS --argjson ack $ACK_COUNT --argjson inv '[1,2,3,4,5]' '...'`. The `subtask_briefs` array is built by jq from a temporary file or by repeatedly invoking jq to append entries — the simplest pattern is: build a `briefs.jsonl` file (one JSON object per line) per subtask, then `jq -s '.' briefs.jsonl` to produce a JSON array, then `--argjson briefs "$(jq -s '.' briefs.jsonl)"` into the main `jq -n` call.
    - Emits `output.md` with the literal `## Acceptance` heading and two acceptance bullets:
      - "Every subtask brief is a self-contained file with the three kernel invariants acknowledged verbatim."
      - "No subtask brief references parent conversation context (parent_context_refs_count == 0)."
    - Final command: `chantier state append -e skill.completed -t "${CHANTIER_TASK_ID:-unknown}" -s subagent-driven-development -m "Subagent fan-out completed; $SUBTASK_COUNT briefs emitted with $ACK_COUNT kernel acknowledgements" -r "$TASK_DIR/output.md" -r "$TASK_DIR/output.json"` plus one `-r` per `subtask_brief_<id>.md` file. Then `exit 0`.
    - Exit 0 on all business outcomes. Exit 2 only on technical incidents.
    - shellcheck --shell=sh clean.
    - No deny-list token in run.sh, the briefs (which are author-generated by run.sh), inputs.yml, or the e2e bats file.
    - Fixture `inputs.yml`: `subtask_count: 2`, `parent_brief: "Integrate the new billing endpoint: subtask 1 wires the database migration; subtask 2 wires the API route. Each subtask runs in isolation."`, `subtask_focus: ["database migration", "API route"]`.
    - E2e bats test mounts the fixture, runs run.sh, asserts `output.md`, `output.json`, `subtask_brief_1.md`, `subtask_brief_2.md` all exist; `jq -r '.parent_context_refs_count' output.json` returns `0`; `jq -r '.subagent_invariants_acknowledged_count' output.json` returns `>= 3` (with 2 subtasks × 3 kernel invariants, the expected value is `6`); `chantier validate-task t1` exits 0.
  </behavior>
  <action>
    THREE files.

    A) `skills/subagent-driven-development/run.sh` (mode 0755). Shebang + license header + canonical prelude (set -eu; IFS literal-newline; LC_ALL=C; export LC_ALL). Read TASK_DIR / INPUTS_YML; guard readable. Extract `SUBTASK_COUNT` (validate via `[ "$SUBTASK_COUNT" -ge 1 ]` else exit 2) and `PARENT_BRIEF` (validate non-empty else exit 2).

    For each subtask 1..SUBTASK_COUNT:
    ```
    i=1
    PARENT_REFS_TOTAL=0
    ACK_TOTAL=0
    : > "$TASK_DIR/briefs.jsonl"

    while [ "$i" -le "$SUBTASK_COUNT" ]; do
        BRIEF_FILE="$TASK_DIR/subtask_brief_${i}.md"
        # Extract per-subtask focus if provided (i-th entry of subtask_focus list).
        FOCUS=$(awk -v n="$i" '
            /^subtask_focus:/ { in_sf=1; idx=0; next }
            in_sf && /^[a-z_]+:/ { in_sf=0 }
            in_sf && /^[[:space:]]+-/ {
                idx++
                if (idx==n) {
                    sub(/^[[:space:]]+-[[:space:]]*/, "")
                    gsub(/"/, "")
                    print
                    exit
                }
            }
        ' "$INPUTS_YML")

        # Emit brief: kernel acknowledgement + subtask focus.
        cat > "$BRIEF_FILE" <<EOF
# Subtask ${i}

## Kernel invariants (acknowledge before acting)

1. Portability. No file written by this subtask contains a harness identifier.
2. State log append-only. This subtask mutates STATE.md only via the chantier state-append entry point.
3. State writes containment. This subtask writes only inside paths declared in its task state_writes.

## Subtask brief

Parent brief: ${PARENT_BRIEF}

Subtask focus: ${FOCUS:-(no specific focus provided)}

This brief is a file on disk. It is the only context the fresh invocation needs.
EOF

        # Count kernel acknowledgements in this brief (expected: 3).
        _ack=$(grep -cE '^[0-9]+\. (Portability|State log append-only|State writes containment)' "$BRIEF_FILE" || true)
        ACK_TOTAL=$((ACK_TOTAL + _ack))

        # Count parent-context references (must be 0).
        _refs=$(grep -cE '(as discussed|per our earlier|the agreed approach|like we said|as mentioned)' "$BRIEF_FILE" || true)
        PARENT_REFS_TOTAL=$((PARENT_REFS_TOTAL + _refs))

        # Build per-brief JSON object and append to briefs.jsonl.
        BRIEF_WORDS=$(wc -w < "$BRIEF_FILE" | tr -d ' ')
        jq -n \
            --arg id "$i" \
            --arg path "$BRIEF_FILE" \
            --argjson words "$BRIEF_WORDS" \
            '{id: $id, brief_path: $path, brief_word_count: $words}' >> "$TASK_DIR/briefs.jsonl"

        i=$((i + 1))
    done

    BRIEFS_JSON=$(jq -s '.' "$TASK_DIR/briefs.jsonl")
    ```

    Then emit output.json:
    ```
    STARTED_AT=$(date -u +%Y-%m-%dT%H:%M:%SZ)
    # ... above loop runs ...
    ENDED_AT=$(date -u +%Y-%m-%dT%H:%M:%SZ)

    jq -n \
        --arg started "$STARTED_AT" \
        --arg ended "$ENDED_AT" \
        --argjson count "$SUBTASK_COUNT" \
        --argjson briefs "$BRIEFS_JSON" \
        --argjson refs "$PARENT_REFS_TOTAL" \
        --argjson ack "$ACK_TOTAL" \
        --argjson inv '[1,2,3,4,5]' \
        '{
            subtask_count: $count,
            subtask_briefs: $briefs,
            parent_context_refs_count: $refs,
            subagent_invariants_acknowledged_count: $ack,
            started_at: $started,
            ended_at: $ended,
            invariants_applied: $inv
        }' > "$TASK_DIR/output.json"
    ```

    Emit output.md via unquoted heredoc with literal `## Acceptance` heading and the two bullets named in <behavior>.

    Build the `-r` ref list dynamically and append: the chantier state append final command should ref output.md, output.json, AND each subtask_brief_<id>.md. The simplest pattern:
    ```
    STATE_APPEND_REFS=""
    j=1
    while [ "$j" -le "$SUBTASK_COUNT" ]; do
        STATE_APPEND_REFS="${STATE_APPEND_REFS} -r ${TASK_DIR}/subtask_brief_${j}.md"
        j=$((j + 1))
    done
    # shellcheck disable=SC2086 -- intentional word-splitting on $STATE_APPEND_REFS
    chantier state append \
        -e skill.completed \
        -t "${CHANTIER_TASK_ID:-unknown}" \
        -s subagent-driven-development \
        -m "Subagent fan-out completed: ${SUBTASK_COUNT} briefs, ${ACK_TOTAL} kernel acknowledgements, ${PARENT_REFS_TOTAL} parent-context references" \
        -r "$TASK_DIR/output.md" \
        -r "$TASK_DIR/output.json" \
        $STATE_APPEND_REFS
    exit 0
    ```

    `chmod +x skills/subagent-driven-development/run.sh`. Clean up `briefs.jsonl` temp file before exit if desired (or leave as a debug artifact in the task dir — recommendation: leave it, since the task dir is auditable per ADR 0001).

    B) `core/tests/fixtures/skills/subagent-driven-development/dossier/inputs.yml`:
    ```
    subtask_count: 2
    parent_brief: "Integrate the new billing endpoint: subtask 1 wires the database migration; subtask 2 wires the API route. Each subtask runs in isolation."
    subtask_focus:
      - "database migration"
      - "API route"
    ```

    C) `core/tests/skill_subagent_driven_development_e2e.bats`. Setup mirrors validate_task.bats lines 7–24. Copy `make_plan` helper inline (passing `skill: subagent-driven-development` and the two acceptance bullets matching run.sh's output). Single `@test` titled `"subagent-driven-development: 2-subtask fan-out end-to-end through chantier validate-task"`. Block body: (1) `TASK="t1"`, (2) copy live `skills/subagent-driven-development/` into TMPHOME, (3) `make_plan` with acceptance bullets matching run.sh, (4) mount fixture into task dir, (5) `cd` to task dir, `run sh "$TMPHOME/skills/subagent-driven-development/run.sh"` — assert `$status -eq 0`, `[ -f output.md ]`, `[ -f output.json ]`, `[ -f subtask_brief_1.md ]`, `[ -f subtask_brief_2.md ]`, `_refs=$(jq -r '.parent_context_refs_count' output.json)`, `[ "$_refs" -eq 0 ]`, `_ack=$(jq -r '.subagent_invariants_acknowledged_count' output.json)`, `[ "$_ack" -ge 3 ]`, (6) `cd "$TMPHOME"`, `run "$CHANTIER" validate-task t1`, assert `$status -eq 0`.

    Pitfalls to avoid:
    - DO NOT use any deny-list token in run.sh, the briefs run.sh emits, inputs.yml, or the e2e bats file. The briefs are author-generated content gate 4 scans (they're in skills/<name>/'s task dir at runtime, but the file copies happen under TMPHOME during the e2e test — gate 4 scans `skills/subagent-driven-development/` only; the runtime task dir under .planning/phases/test-phase/tasks/t1 is gate-1-scanned for containment, not gate-4-scanned for deny-list. HOWEVER: convention holds — never emit a deny-list token).
    - DO NOT exit non-zero when the brief generation reveals `parent_context_refs_count > 0` — that is a business state recorded in output.json. The skill faithfully reports the count and exits 0 per D-04.
    - DO NOT use bashisms — POSIX only. `$(( i + 1 ))` for arithmetic; no `[[ ]]`.
    - DO NOT skip the `tr -d ' '` after `wc -w`.
    - DO NOT use the `# shellcheck disable=SC2086` comment if shellcheck doesn't actually flag the line — only add it if shellcheck emits SC2086 on the dynamic-refs expansion; otherwise omit.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && [ -x skills/subagent-driven-development/run.sh ] && shellcheck --shell=sh skills/subagent-driven-development/run.sh && head -20 skills/subagent-driven-development/run.sh | grep -qE '^set -eu' && grep -qE "^chantier state append" skills/subagent-driven-development/run.sh && grep -qE "^exit 0" skills/subagent-driven-development/run.sh && grep -qE 'jq -n' skills/subagent-driven-development/run.sh && grep -qE 'subtask_brief_' skills/subagent-driven-development/run.sh && _denied=$(grep -cE 'mcp__|claude_ai_|@codebase|claude-code|cursor|codex-cli|copilot-cli|gemini-cli|opencode' skills/subagent-driven-development/run.sh skills/subagent-driven-development/PRESSURE.md || true) && [ "$_denied" -eq 0 ] && [ -f core/tests/fixtures/skills/subagent-driven-development/dossier/inputs.yml ] && grep -qE '^subtask_count:[[:space:]]+2' core/tests/fixtures/skills/subagent-driven-development/dossier/inputs.yml && [ -f core/tests/skill_subagent_driven_development_e2e.bats ] && bats core/tests/skill_subagent_driven_development_e2e.bats 2>&1 | tail -3 | grep -qE '1 test, 0 failures' && bats core/tests/ 2>&1 | tail -3 | grep -qE '0 failures' && echo "SDD skill+fixture+e2e OK"</automated>
  </verify>
  <done>
    `skills/subagent-driven-development/run.sh` is executable, shellcheck-clean, uses canonical prelude + per-subtask brief emission with kernel-invariant acknowledgement preamble + `jq -n` emission + `## Acceptance` heading + final `chantier state append` with one `-r` per subtask brief + `exit 0`. Fixture has `subtask_count: 2`. E2e test asserts both briefs exist, `parent_context_refs_count == 0`, `subagent_invariants_acknowledged_count >= 3` (expected 6 for 2 subtasks × 3 kernel items), validate-task exits 0. Zero deny-list tokens anywhere in the skill subtree outside SKILL.md frontmatter.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `inputs.yml` (`parent_brief`, `subtask_focus[]`) → `run.sh` → subtask_brief_<id>.md content | Untrusted-ish strings flow into heredoc-templated brief files via `${VAR}` expansion. |
| Subtask brief content → `grep -cE` (parent_context_refs_count, subagent_invariants_acknowledged_count) | Brief content (which run.sh wrote) is scanned for forbidden phrases; counts go to output.json via jq. |
| `run.sh` → `chantier state append` (with N dynamic `-r` flags) | Final command uses unquoted `$STATE_APPEND_REFS` expansion for the per-brief refs; `IFS=\n` ensures path-with-space safety. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | Tampering | Heredoc injection via `parent_brief` (e.g., containing `${HOSTILE_VAR}` or backticks) | mitigate | The heredoc is unquoted (`<<EOF`, not `<<'EOF'`) because variable interpolation of `$PARENT_BRIEF` and `$FOCUS` is desired. Mitigation: variables are shell-expanded into the brief content as-is — there is no second-stage `eval` of the brief content. Backticks and `$()` in the input value would be evaluated by the shell during the heredoc expansion; this IS a tampering vector. RESIDUAL MITIGATION: the dossier is staged by a trusted process (Phase 4 adapter); in Phase 3 the fixture is hand-authored. For v0.1, we accept the residual risk and document it in the SKILL.md body as a constraint on trusted dossier authoring. (A future hardening pass could escape `$`/backtick/`\` in input values before heredoc expansion — out of scope for Phase 3.) |
| T-03-05-02 | Tampering | JSON injection in output.json via `parent_brief` containing `"` or backslash | mitigate | The `parent_brief` value is NOT a direct field in output.json; it is incorporated into the brief markdown body and stored in output.json only via the `subtask_briefs[].brief_path` field (a derived path string, jq-safe). The brief content is referenced by path, not embedded inline. |
| T-03-05-03 | Tampering | Path traversal in `state_writes` | accept (mitigated upstream) | Gate 1 of chantier validate-task. |
| T-03-05-04 | Tampering | TOCTOU on inputs.yml | mitigate | Read once at top of run.sh. |
| T-03-05-05 | Information Disclosure | Harness identifier leaked into briefs / output.md / output.json | mitigate | This is THE skill where this threat is most acute — the skill is about subagents (a harness concept) and the body discusses them at length. The brief template emitted by run.sh uses only the discipline-language vocabulary ("the chantier state-append entry point", "the fresh invocation", "this subtask") — NEVER the harness names from the deny-list. Gate 4 scans all body files (SKILL.md exempt; PRESSURE.md and run.sh and any emitted artifacts NOT exempt). The e2e test verifies via `grep -rE` that no deny-list token appears. |
| T-03-05-06 | Denial of Service | Pathological `subtask_count` (e.g., 100000) | mitigate | run.sh's loop is O(N) per subtask. A bats test timeout of 30s caps Phase 3's exposure. Phase 4 adapter is responsible for resource limits in production. Phase 3 SKILL.md's `## Exit code matrix` does NOT cap subtask_count explicitly — a future hardening adds a maximum to inputs_schema once the JSON Schema subset profile supports `maximum` (currently it does not, per ADR 0002). |
| T-03-05-SC | Tampering | npm/pip/cargo installs | n/a | No external packages. |

</threat_model>

<verification>
- `[ -x skills/subagent-driven-development/run.sh ]`.
- `shellcheck --shell=sh skills/subagent-driven-development/run.sh` — clean.
- `grep -E '^chantier state append' run.sh` returns 1.
- `grep -E 'jq -n' run.sh` returns at least 2 (one for per-brief object, one for output.json).
- `grep -E 'subtask_brief_' run.sh` — confirms per-brief emission loop.
- `grep -E '^## Why no hooks' skills/subagent-driven-development/SKILL.md` returns 1.
- `grep -F 'https://github.com/obra/superpowers/issues/237' skills/subagent-driven-development/SKILL.md` returns 1.
- `grep -cE '^## Scenario [0-9]' PRESSURE.md` returns ≥ 2.
- `grep -rE 'mcp__|claude_ai_|@codebase|claude-code|cursor|codex-cli|copilot-cli|gemini-cli|opencode' skills/subagent-driven-development/PRESSURE.md skills/subagent-driven-development/run.sh` returns 0.
- `awk` extracts `harness_adapters: [claude-code]` from SKILL.md.
- `bats core/tests/skill_subagent_driven_development_e2e.bats` — exits 0, "1 test, 0 failures", with `parent_context_refs_count == 0` and `subagent_invariants_acknowledged_count >= 3` (typically 6 for the 2-subtask fixture).
- `bats core/tests/skill_uniformity.bats` — exits 0, 3 PASSES (all four Wave-2 skills now shipped).
- `bats core/tests/` — full suite, zero failures.
- `chantier --self-test` — all green.
</verification>

<success_criteria>
1. `skills/subagent-driven-development/SKILL.md` exists with 8-field frontmatter; 8 ordered h2 sections INCLUDING `## Why no hooks` (D-08) citing the safe URL `https://github.com/obra/superpowers/issues/237` AND `ADR 0001`; invariants 4 + 5 cite parent_context_refs_count and subagent_invariants_acknowledged_count.
2. `skills/subagent-driven-development/PRESSURE.md` exists with frontmatter + ≥2 scenarios + correct subsection count + Disqualifiers citing Invariant 4 or 5 and the documented output.json fields.
3. `skills/subagent-driven-development/run.sh` is executable, shellcheck-clean, uses canonical prelude + per-subtask brief emission loop (each brief has the 3 kernel invariants verbatim) + `jq -n` for output.json (incl. dynamic subtask_briefs array) + `## Acceptance` heading + final `chantier state append` with N+2 `-r` flags + `exit 0`.
4. Fixture `inputs.yml` declares `subtask_count: 2`, `parent_brief`, `subtask_focus: [...]`.
5. E2e test asserts both subtask_brief files exist; `parent_context_refs_count == 0`; `subagent_invariants_acknowledged_count >= 3`; validate-task exits 0.
6. Zero deny-list tokens in any file under `skills/subagent-driven-development/` other than the SKILL.md frontmatter `harness_adapters: [claude-code]` entry.
7. Full bats suite remains green; uniformity test now reports 3×PASS across all four Wave-2 skills.
</success_criteria>

<output>
Create `.planning/phases/03-skill-library/03-05-SUMMARY.md` mirroring 02-06-SUMMARY.md. Append: `chantier state append -e plan.completed -t null -s subagent-driven-development -m "Plan 03-05 complete: subagent-driven-development skill shipped end-to-end with ## Why no hooks section citing obra/superpowers#237" -r .planning/phases/03-skill-library/03-05-SUMMARY.md`.
</output>

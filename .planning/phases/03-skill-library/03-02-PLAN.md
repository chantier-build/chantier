---
phase: 03-skill-library
plan: "02"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - skills/using-git-worktrees/SKILL.md
  - skills/using-git-worktrees/PRESSURE.md
  - skills/using-git-worktrees/run.sh
  - core/tests/skill_using_git_worktrees_e2e.bats
  - core/tests/fixtures/skills/using-git-worktrees/dossier/inputs.yml
autonomous: true
requirements: [FR-005, FR-006, FR-009, FR-010]
requirements_addressed: [FR-005, FR-006, FR-009, FR-010, NFR-001, NFR-002, NFR-004, NFR-005]
tags: [phase-03, skill, using-git-worktrees, run-sh, pressure-md, kernel-invariants]
nyquist: 03-VALIDATION.md

must_haves:
  truths:
    - "After this plan, `skills/using-git-worktrees/` contains SKILL.md, PRESSURE.md, and an executable run.sh — and `skills/using-git-worktrees/run.sh` ends with a `chantier state append -e skill.completed` invocation that supplies `output.md` and `output.json` as refs (D-01, D-02, D-03)."
    - "SKILL.md frontmatter has all eight required fields per `core/schemas/skill.json` (`id`, `version`, `inputs_schema`, `state_reads`, `state_writes`, `outputs_schema`, `portable`, `harness_adapters`) and declares `harness_adapters: [claude-code]` with no other entries (D-14)."
    - "SKILL.md body contains a numbered `## Invariants` section listing the three kernel invariants verbatim (D-06: Portability, State log append-only, State writes containment) plus at least one skill-specific invariant (clean baseline before work)."
    - "Every invariant in SKILL.md has a corresponding measurable proof field in the `outputs_schema` and the run.sh-emitted `output.json` (D-07): `baseline_clean: bool`, `baseline_diff_lines: int` (MUST be 0 for kernel #4 to hold), `baseline_check_command: string`, `worktree_path: string`, `setup_exit_code: int`, `invariants_applied: int[]`."
    - "SKILL.md body sequences these headings in order: `# Using git worktrees`, `## Purpose`, `## When to use`, `## Invariants`, `## How`, `## Portability claim` (D-15), `## Exit code matrix (from run.sh)` (D-04), `## Acknowledge before acting` (D-05 read-aloud gate)."
    - "run.sh begins with the canonical prelude (`set -eu`; literal-newline `IFS`; `LC_ALL=C; export LC_ALL`), performs the clean-baseline check via `git status --porcelain=v1 | wc -l | tr -d ' '` (Pitfall 9), creates a worktree via `git worktree add` (atomic, no symlink races), emits `output.json` via `jq -n --arg`/`--argjson` exclusively (no `printf` JSON), and emits `output.md` with the literal heading `## Acceptance` (gate 5)."
    - "run.sh `exit 0` on business-state failures (e.g., baseline-dirty → records `baseline_clean: false` and `baseline_diff_lines: <n>` in output.json and exits 0); non-zero exit ONLY on technical incidents per the exit-code matrix in SKILL.md (D-04: missing inputs.yml → exit 2, jq absent → exit 2, git absent → exit 2)."
    - "PRESSURE.md ships with YAML frontmatter declaring `skill_id: using-git-worktrees` and a `scenarios` array (D-12), and contains at minimum a time-pressure scenario and a sunk-cost scenario per D-10, each in the four-subsection structured spec format (Situation / Temptation / Required response / Disqualifier) per D-09. Each Disqualifier cites both the SKILL.md invariant number violated AND the `output.json` field that detects the violation, per D-11."
    - "PRESSURE.md does not cross-reference any other skill's PRESSURE.md per D-13."
    - "`core/tests/skill_using_git_worktrees_e2e.bats` invokes the skill via a fixture dossier (mounted from `core/tests/fixtures/skills/using-git-worktrees/dossier/inputs.yml`), runs the skill's `run.sh`, then runs `chantier validate-task` against the task — and exits 0 with all five gates green."
    - "No file in `skills/using-git-worktrees/` other than SKILL.md contains any deny-list token (`mcp__|claude_ai_|@codebase|claude-code|cursor|codex-cli|copilot-cli|gemini-cli|opencode`); verified by `grep -rE` after authoring (NFR-001 / gate 4)."
    - "Decision coverage in this plan: D-01 (run.sh present), D-02 (run.sh = HOW; SKILL.md = WHEN/WHY), D-03 (run.sh sole author of output.md + output.json), D-04 (exit-code matrix in SKILL.md), D-05 (numbered Invariants + Acknowledge section), D-06 (kernel 1–3), D-07 (every invariant has output.json proof field), D-09 (PRESSURE 4-subsection template), D-10 (time-pressure + sunk-cost minimum), D-11 (Disqualifier ↔ invariant ↔ output.json field), D-12 (PRESSURE frontmatter), D-13 (PRESSURE autonomous, no cross-refs), D-14 (`harness_adapters: [claude-code]` uniform), D-15 (Portability claim section)."
  artifacts:
    - path: "skills/using-git-worktrees/SKILL.md"
      provides: "Skill contract: 8-field frontmatter + numbered Invariants + WHEN/WHY body."
      contains: "harness_adapters:"
    - path: "skills/using-git-worktrees/PRESSURE.md"
      provides: "≥2 adversarial scenarios in D-09 structured spec format; D-12 frontmatter."
      contains: "## Scenario 1"
    - path: "skills/using-git-worktrees/run.sh"
      provides: "POSIX-shell entry point: clean-baseline check + worktree creation + output.{md,json} emission + chantier state append."
      contains: "chantier state append"
    - path: "core/tests/skill_using_git_worktrees_e2e.bats"
      provides: "End-to-end test that mounts the fixture dossier, runs run.sh, and asserts chantier validate-task exits 0."
      contains: "chantier validate-task"
    - path: "core/tests/fixtures/skills/using-git-worktrees/dossier/inputs.yml"
      provides: "Minimal fixture inputs (branch_name, setup_command, base_ref) for the e2e test."
      contains: "branch_name:"
  key_links:
    - from: "skills/using-git-worktrees/run.sh"
      to: ".planning/STATE.md"
      via: "chantier state append -e skill.completed (final command of run.sh)"
      pattern: "chantier state append -e skill\\.completed"
    - from: "skills/using-git-worktrees/SKILL.md"
      to: "core/schemas/skill.json"
      via: "frontmatter validates against the 8-required-fields schema (verified at runtime by chantier validate-task gate 4)"
      pattern: "harness_adapters:\\s*\\n\\s*-\\s*claude-code"
    - from: "skills/using-git-worktrees/PRESSURE.md"
      to: "skills/using-git-worktrees/SKILL.md (Invariant 4)"
      via: "Disqualifier cites Invariant 4 by number AND output.json.baseline_diff_lines as the detection field"
      pattern: "Violates Invariant 4"
    - from: "core/tests/skill_using_git_worktrees_e2e.bats"
      to: "skills/using-git-worktrees/run.sh"
      via: "bats test invokes run.sh from inside TMPHOME's task directory after mounting the fixture dossier"
      pattern: "run sh .*skills/using-git-worktrees/run\\.sh"
---

<objective>
Ship the first reference skill: `using-git-worktrees`. The skill's discipline is "create a
parallel-isolation worktree from a clean baseline before touching production code"; its
falsifiable proof is `output.json.baseline_diff_lines == 0` and `output.json.worktree_path`
populated. Compose the per-skill plan as: SKILL.md (contract + 100–180-line body), PRESSURE.md
(≥2 scenarios), run.sh (POSIX shell + jq), fixture dossier, e2e bats test. The Wave-1
uniformity test turns from SKIP to PASS for this skill after this plan lands.

Purpose: exercise ADR 0001 Surface 2 end-to-end for one concrete skill. The skill-author plans
in Wave 2 are independent — no inter-skill file overlap — so the four can execute in parallel.

Output: five new files committed under `skills/using-git-worktrees/` and the bats test +
fixture; e2e bats test passes; `chantier validate-task` green on the fixture task; uniformity
test now reports 1 PASS / 2 SKIP (the other two remain SKIP because three skills are still
absent at this plan's completion).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-skill-library/03-CONTEXT.md
@.planning/phases/03-skill-library/03-RESEARCH.md
@.planning/phases/03-skill-library/03-PATTERNS.md
@.planning/phases/03-skill-library/03-VALIDATION.md
@.planning/phases/03-skill-library/03-01-SUMMARY.md
@docs/adr/0001-state-skill-contract.md
@docs/adr/0002-runtime-binary-and-state-format.md
@core/schemas/skill.json
@core/bin/chantier
@core/tests/validate_task.bats
@CONTRIBUTING.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author skills/using-git-worktrees/SKILL.md (contract frontmatter + WHEN/WHY body)</name>
  <files>skills/using-git-worktrees/SKILL.md</files>
  <read_first>
    - .planning/phases/03-skill-library/03-RESEARCH.md §"Pattern 3: SKILL.md body structure" (canonical body skeleton with the 9 ordered sections), §"Pattern 2: output.json schema — discipline metric fields (D-07)" row for `using-git-worktrees`, §"Code Examples > Example 1" (worked frontmatter for test-driven-development — same shape applies here with worktree-specific schema content), §"Code Examples > Example 5" (suggested acknowledge-block format).
    - .planning/phases/03-skill-library/03-PATTERNS.md §"skills/<name>/SKILL.md (skill-contract)" (required frontmatter fields with line citations to core/schemas/skill.json; body structure pattern; kernel invariants prose).
    - .planning/phases/03-skill-library/03-CONTEXT.md D-05, D-06, D-07, D-14, D-15 (this skill is NOT subagent-driven-development so it does NOT need D-08 / `## Why no hooks`).
    - core/schemas/skill.json (the 8 required frontmatter fields; harness_adapters enum).
    - docs/adr/0001-state-skill-contract.md §"Surface 2" (state_reads / state_writes path templating with `{phase}` / `{task}`).
    - docs/adr/0002-runtime-binary-and-state-format.md §"Frontmatter subset profile" (allowed: top-level scalars + simple lists; carve-out: inputs_schema / outputs_schema are typed `object` and bypass the awk extractor) and §"JSON Schema subset profile" (7 keywords: type, required, properties, additionalProperties, pattern, enum, items).
  </read_first>
  <behavior>
    - Running `chantier validate-task` against a task that declares `skill: using-git-worktrees` and a SKILL.md authored per this task: gate 4 (frontmatter schema check) PASSES because all 8 required fields are present, `id` matches `^[a-z][a-z0-9-]*$` (`using-git-worktrees`), `version` matches `^[0-9]+\.[0-9]+\.[0-9]+$` (`1.0.0`), `harness_adapters` items are enum-constrained to `[claude-code]` only.
    - The Wave-1 uniformity test's first @test ("identical harness_adapters") would PASS if this is the only skill in skills/ (since `[claude-code]` is the reference value).
    - The body has exactly these h2 headings in this order: `## Purpose`, `## When to use`, `## Invariants`, `## How`, `## Portability claim`, `## Exit code matrix (from run.sh)`, `## Acknowledge before acting` (verified by `grep -E '^##\s'` returning seven lines in that sequence).
    - The `## Invariants` section is a numbered list (1., 2., 3., 4.) where items 1–3 are the kernel verbatim (matches D-06 text byte-for-byte across skills) and item 4 is skill-specific: "Clean baseline before work. The skill does not create a worktree if the current branch has a dirty index or untracked files." Each item ends with a parenthetical naming the proof field, e.g., "(Proof: `output.json.baseline_diff_lines == 0`)".
    - The file contains no deny-list tokens in body files — but this file IS `SKILL.md`, which gate 4 explicitly exempts (line 691 of `core/bin/chantier`: `[ "$_vt_body_file" = "$_vt_skill_md" ] && continue`). The frontmatter's `harness_adapters: [claude-code]` is therefore the ONLY place the token `claude-code` is allowed.
    - Body length: 100–180 lines (per RESEARCH Assumption A7).
  </behavior>
  <action>
    Create `skills/using-git-worktrees/SKILL.md` with YAML frontmatter delimited by `---` lines containing all eight required fields. The `id` is `using-git-worktrees`. The `version` is `1.0.0`. The `inputs_schema` declares (per ADR 0002 §JSON Schema subset profile and RESEARCH §Pattern 2 inputs table): `type: object`, `required: [branch_name, setup_command, base_ref]`, `properties` for `branch_name` (type: string, pattern matches a sensible git ref shape `^[A-Za-z0-9._/-]+$`), `setup_command` (type: string — the no-op-safe default in the fixture is `"true"`), `base_ref` (type: string, default-acceptable `main`). The `state_reads` array lists `"{phase}/CONTEXT.md"` only (this skill does not depend on prior tasks' output.json). The `state_writes` array lists `"{phase}/tasks/{task}/"` and `".planning/STATE.md"` (the latter is required because `run.sh` ends with `chantier state append` per D-01/D-02). The `outputs_schema` declares `type: object`, `required: [baseline_clean, baseline_diff_lines, worktree_path, setup_exit_code, invariants_applied]`, `properties` for each of those plus optional `baseline_check_command` (string), `started_at` / `ended_at` (string, ISO-8601 pattern from RESEARCH Code Examples Example 1 lines 827–828), and `invariants_applied` as `{type: array, items: {type: number}}`. `portable: true` (triggers gate 4 scan). `harness_adapters: [claude-code]` (single-item list — uniformity gate 16 enforced by the Wave-1 bats test).

    Body section by section (write each as named h2 in this exact order):

    - `# Using git worktrees` (h1 — display name).
    - `## Purpose` — two-sentence description: "Create a parallel-isolation worktree from a known-clean baseline so that mechanical work (test runs, dependency edits, exploratory commits) does not contaminate the calling agent's working tree. The skill exits without creating a worktree if the baseline is not clean, recording the contamination as a measurable failure rather than silently overwriting."
    - `## When to use` — 3–5 bullets: parallel feature branches; long-running exploratory work; running a destructive command that should not leak into the main checkout; preserving a known-good baseline while reproducing an issue. Each bullet 1–2 lines.
    - `## Invariants` — numbered list, 4 items: kernel 1–3 verbatim ("**Portability.** No file written by this skill contains a harness identifier. (Kernel)"; "**State log append-only.** The skill mutates STATE.md only via `chantier state append`. (Kernel)"; "**State writes containment.** The skill writes only inside paths declared in `state_writes`. (Kernel)"); then item 4: "**Clean baseline before work.** The skill does not create a worktree if the current branch has a dirty index or untracked files. (Proof: `output.json.baseline_diff_lines == 0` AND `output.json.baseline_clean == true`)." End the section with the standard one-sentence pointer: "Every invariant has a measurable proof in `output.json` (see `outputs_schema`). The list of which invariants were applied is captured in `output.json.invariants_applied`."
    - `## How` — 3–5 numbered steps at WHEN/WHY level (not HOW mechanics): why a worktree before isolation work; why the baseline must be clean (preserves audit trail); why setup_command runs in the worktree (not the parent); why the worktree path is recorded; when to invoke (parallel branches, exploratory destructive runs). Do NOT restate the actual `git worktree add` shell command — that lives in run.sh per D-02.
    - `## Portability claim` — verbatim D-15 boilerplate, adapted per RESEARCH Pattern 3: state that this skill ships with `harness_adapters: [claude-code]` as a tested-only declaration; list the four-step extension recipe (write `adapters/<host>/run-task.sh`, run this skill end-to-end on the new host, verify `chantier validate-task` exits 0 and output.json matches outputs_schema, extend `harness_adapters[]` in the same commit as the adapter ships). Add the closing sentence: "A bats test in `core/tests/skill_uniformity.bats` verifies every shipped skill declares the same array."
    - `## Exit code matrix (from run.sh)` — table with two rows per D-04: `| Exit | Meaning |` / `| 0 | Success or business-state failure encoded in output.json |` / `| 2 | Technical incident: missing inputs.yml, missing git, missing jq, filesystem error |`. Plus the closing sentence: "Business outcomes are NEVER encoded in exit codes. See `output.json` for the actual result (e.g., a dirty baseline returns exit 0 with `baseline_clean: false`)."
    - `## Acknowledge before acting` — read-aloud gate per D-05, using the uniform shape from RESEARCH Example 5: "Before invoking `run.sh`, list (in writing, in the agent's own words) which invariants from the `## Invariants` section apply to the current task. For most tasks, all kernel invariants (1, 2, 3) apply; skill-specific invariant 4 applies whenever the task creates a worktree. For each applicable invariant, state in one sentence why it applies to the current task. After producing this list, invoke `run.sh`. The list will appear in `output.md` under `## Invariants applied` (written by `run.sh`, not by you). If you cannot state why an invariant applies, do not proceed — re-read the body and re-examine `inputs.yml`."

    Length target: 100–180 lines total (frontmatter included). Body sections should be visually balanced — none more than ~30 lines.

    Pitfalls to avoid:
    - DO NOT nest maps inside the frontmatter outside `inputs_schema` and `outputs_schema` (Pitfall 2 — the awk extractor breaks; ADR 0002 frontmatter subset profile forbids it).
    - DO NOT use any deny-list token outside the literal `harness_adapters: [claude-code]` frontmatter entry. The body must NOT contain `claude-code`, `cursor`, `codex-cli`, `copilot-cli`, `gemini-cli`, `opencode`, `mcp__`, `claude_ai_`, or `@codebase` — even inside URLs or example output. (SKILL.md is exempt from gate 4 per chantier line 691, but body cleanliness is a separate convention we hold uniformly.)
    - DO NOT include a `## Why no hooks` section in this skill — D-08 is exclusive to `subagent-driven-development`.
    - DO NOT cross-reference other skill names in the body (D-13 spirit applies to SKILL.md too — composition syntax is deferred per ADR 0001 OQ #4).
    - DO NOT add fields beyond the eight required + the outputs_schema fields listed in <behavior>. The schema's `additionalProperties: true` (line 46 of core/schemas/skill.json) tolerates more, but Phase 3 holds tight to the minimum to keep all four skills uniform.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && [ -f skills/using-git-worktrees/SKILL.md ] && awk '/^---$/{c++; next} c==1' skills/using-git-worktrees/SKILL.md > /tmp/sw_fm.yaml && for f in id version inputs_schema state_reads state_writes outputs_schema portable harness_adapters; do grep -qE "^${f}:" /tmp/sw_fm.yaml || { echo "MISSING field: $f"; exit 1; }; done && grep -qE "^id:[[:space:]]+using-git-worktrees$" /tmp/sw_fm.yaml && grep -qE "^version:[[:space:]]+1\.0\.0$" /tmp/sw_fm.yaml && grep -qE "^portable:[[:space:]]+true$" /tmp/sw_fm.yaml && awk '/^harness_adapters:/{in_ha=1; next} in_ha && /^[a-z_]+:/{in_ha=0} in_ha && /^[[:space:]]+-/{gsub(/^[[:space:]]+-[[:space:]]*/, ""); gsub(/"/, ""); print}' /tmp/sw_fm.yaml | sort | tr '\n' ',' | grep -qE '^claude-code,?$' && for h in '^# Using git worktrees' '^## Purpose' '^## When to use' '^## Invariants' '^## How' '^## Portability claim' '^## Exit code matrix' '^## Acknowledge before acting'; do grep -qE "$h" skills/using-git-worktrees/SKILL.md || { echo "MISSING section: $h"; exit 1; }; done && _body_lines=$(sed -n '/^---$/,/^---$/!p' skills/using-git-worktrees/SKILL.md | wc -l | tr -d ' ') && [ "$_body_lines" -ge 60 ] && [ "$_body_lines" -le 200 ] && echo "SKILL.md OK ($_body_lines body lines)"</automated>
  </verify>
  <done>
    `skills/using-git-worktrees/SKILL.md` exists; YAML frontmatter contains all 8 required fields with correct values (`id: using-git-worktrees`, `version: 1.0.0`, `portable: true`, `harness_adapters: [claude-code]`); body contains all 7 ordered h2 sections (Purpose, When to use, Invariants, How, Portability claim, Exit code matrix, Acknowledge); kernel invariants 1–3 wording matches D-06 verbatim; invariant 4 cites output.json.baseline_diff_lines as proof; body length 60–200 lines (lenient bound — strict 100–180 target).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author skills/using-git-worktrees/PRESSURE.md (≥2 adversarial scenarios in D-09 structured spec format)</name>
  <files>skills/using-git-worktrees/PRESSURE.md</files>
  <read_first>
    - .planning/phases/03-skill-library/03-RESEARCH.md §"Pattern 4: PRESSURE.md shape" (frontmatter shape + scenario template + worked exemplar for TDD lines 487–505) and §"Pitfall 6: PRESSURE.md scenarios don't cite output.json field names" (D-11 enforcement).
    - .planning/phases/03-skill-library/03-PATTERNS.md §"skills/<name>/PRESSURE.md".
    - .planning/phases/03-skill-library/03-CONTEXT.md D-09, D-10, D-11, D-12, D-13.
    - skills/using-git-worktrees/SKILL.md (just authored in Task 1) — to know the exact invariant numbers and proof-field names that the Disqualifier lines must cite.
  </read_first>
  <behavior>
    - File has YAML frontmatter (between `---` lines) declaring `skill_id: using-git-worktrees` and a `scenarios` list with two entries (D-10 minimum): one with `levers: [time-pressure]` and one with `levers: [sunk-cost]`.
    - Body contains exactly two `## Scenario N — <title> (<lever>)` headings (counted by `grep -cE '^## Scenario [0-9]'`).
    - Each scenario contains exactly four subsection markers in this order, formatted as `**Situation.**`, `**Temptation.**`, `**Required response.**`, `**Disqualifier.**` (grep `'^\*\*(Situation|Temptation|Required response|Disqualifier)\*\*'` returns 8 lines — 4 per scenario × 2 scenarios).
    - Each Disqualifier subsection cites (1) the invariant number violated (Invariant 4 in both scenarios — clean baseline is the discipline this skill enforces), (2) a short invariant name ("clean-baseline"), (3) the `output.json` field that detects the violation (`output.json.baseline_diff_lines > 0` OR `output.json.baseline_clean == false`), (4) a comparison expression. Verified by `grep -E 'Violates Invariant [0-9]+' PRESSURE.md` returning at least 2 lines.
    - File contains no deny-list token (gate 4 will scan this file because it is in `skills/<name>/` and is not `SKILL.md`).
    - File makes no reference to other skills' PRESSURE.md files (D-13).
  </behavior>
  <action>
    Create `skills/using-git-worktrees/PRESSURE.md` opening with YAML frontmatter:
    ```
    ---
    skill_id: using-git-worktrees
    scenarios:
      - id: uw-time-pressure-01
        levers: [time-pressure]
        invariants_referenced: [4]
      - id: uw-sunk-cost-01
        levers: [sunk-cost]
        invariants_referenced: [4]
    ---
    ```
    Note the frontmatter uses inline-list YAML for `levers` and `invariants_referenced` — this stays within the simple-list portion of the frontmatter subset profile and the inline-list form is unambiguous for any awk extractor that wants to consume it later. `skill_id` is a top-level scalar; `scenarios` is an array of mappings — D-12 explicitly allows arrays-of-objects because PRESSURE.md is documentation, not contract.

    Then write h1 `# PRESSURE — using-git-worktrees` followed by a one-paragraph framing: "This document captures the adversarial scenarios this skill is designed to survive. Each scenario describes a temptation the agent will encounter under realistic conditions and the response the skill demands. The Disqualifier subsection is the falsifiable failure signal — it cites the SKILL.md invariant violated and the `output.json` field that detects the violation."

    Then ship TWO scenarios. Each scenario starts with `## Scenario N — <title> (<lever>)`. Each scenario MUST have the four `**Subsection.**` lines in this exact order: `**Situation.**`, `**Temptation.**`, `**Required response.**`, `**Disqualifier.**`. After each bold marker, write 2–4 sentences of body prose (no further sub-headings).

    SCENARIO 1 — time-pressure. Title: "Mid-incident worktree skip". `**Situation.**` — A production incident is in progress; the agent has been asked to reproduce the bug on a fresh checkout while the on-call investigates the live system. A worktree gives the on-call sole ownership of the main checkout. `**Temptation.**` — Skip the worktree. Run the reproduction in the current working copy because the on-call is staring at the same files and `git stash` "will sort it out later". Skipping saves ~5 seconds. `**Required response.**` — Create the worktree. Five seconds beats five minutes of bisecting which agent's edit was what during the on-call. `**Disqualifier.**` — Violates Invariant 4 (clean-baseline-and-isolation). Detected by `output.json.worktree_path` being empty OR `output.json.baseline_clean == false` (the latter means the agent worked in the main checkout with a dirty index, leaving the on-call to disentangle two edit streams).

    SCENARIO 2 — sunk-cost. Title: "Half-finished change blocks the worktree". `**Situation.**` — The agent has accumulated 40 minutes of unrelated edits in the main checkout; tests aren't green, the staging-area is half-built, the changes are "nearly there". A new task arrives that warrants a worktree. `**Temptation.**` — Stash the work-in-progress and create the worktree from the now-clean baseline — except `git stash` of a 40-minute partial state has burned reviewers before. Or: skip the worktree and continue on the same branch, telling oneself "I'll separate the histories later." The 40 minutes feels like a sunk cost any worktree-isolation step would interrupt. `**Required response.**` — Commit the in-progress work to a WIP branch (or stash with an explicit named entry) BEFORE creating the worktree. The 40 minutes are not wasted — they are preserved in git history. The new task gets a clean isolated worktree. `**Disqualifier.**` — Violates Invariant 4 (clean-baseline-and-isolation). Detected by `output.json.baseline_diff_lines > 0` at worktree-creation time (`run.sh` records the dirty-line count, refuses to create the worktree, and returns `baseline_clean: false`).

    Pitfalls to avoid:
    - DO NOT introduce any deny-list token (the file is in `skills/<name>/` and gate 4 will scan it). Specifically: no `cursor`, `claude-code`, etc. in any URL, comment, or prose.
    - DO NOT reference another skill's PRESSURE.md or another skill's invariants (D-13).
    - DO NOT use sub-headings inside a scenario beyond the four `**Bold.**` markers — that breaks the greppable structure.
    - DO NOT cite a `output.json` field that does not exist in SKILL.md's `outputs_schema` (Task 1 declared `baseline_clean`, `baseline_diff_lines`, `worktree_path`, `setup_exit_code`, `invariants_applied` — only these are valid in the Disqualifier).
    - DO NOT lift scenarios from Superpowers verbatim — author original prose per LICENSE-CREDITS greenfield posture (RESEARCH Specifics).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && [ -f skills/using-git-worktrees/PRESSURE.md ] && _sc=$(grep -cE '^## Scenario [0-9]' skills/using-git-worktrees/PRESSURE.md || true) && [ "$_sc" -ge 2 ] && _ss=$(grep -cE '^\*\*(Situation|Temptation|Required response|Disqualifier)\*\*' skills/using-git-worktrees/PRESSURE.md || true) && [ "$_ss" -eq 8 ] && grep -qE 'skill_id:\s*using-git-worktrees' skills/using-git-worktrees/PRESSURE.md && grep -qE 'levers:\s*\[time-pressure\]' skills/using-git-worktrees/PRESSURE.md && grep -qE 'levers:\s*\[sunk-cost\]' skills/using-git-worktrees/PRESSURE.md && _denied=$(grep -cE 'mcp__|claude_ai_|@codebase|claude-code|cursor|codex-cli|copilot-cli|gemini-cli|opencode' skills/using-git-worktrees/PRESSURE.md || true) && [ "$_denied" -eq 0 ] && grep -qE 'Violates Invariant 4' skills/using-git-worktrees/PRESSURE.md && grep -qE 'output\.json\.(baseline_diff_lines|baseline_clean|worktree_path)' skills/using-git-worktrees/PRESSURE.md && echo "PRESSURE.md OK ($_sc scenarios, $_ss subsection markers)"</automated>
  </verify>
  <done>
    `skills/using-git-worktrees/PRESSURE.md` exists; frontmatter has `skill_id` + `scenarios` with two entries (levers time-pressure and sunk-cost); body has exactly 2 `## Scenario N` headings and exactly 8 `**Situation/Temptation/Required response/Disqualifier**` subsection markers; both Disqualifiers cite Invariant 4 and at least one of the documented `output.json` fields; zero deny-list tokens.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author skills/using-git-worktrees/run.sh + fixture inputs.yml + e2e bats test</name>
  <files>skills/using-git-worktrees/run.sh, core/tests/fixtures/skills/using-git-worktrees/dossier/inputs.yml, core/tests/skill_using_git_worktrees_e2e.bats</files>
  <read_first>
    - .planning/phases/03-skill-library/03-RESEARCH.md §"Pattern 1: run.sh skeleton" (canonical POSIX shape — prelude, inputs read, work, jq emission, output.md heredoc, chantier state append), §"Pitfall 9: using-git-worktrees clean-baseline check uses git status ambiguously" (use `git status --porcelain=v1 | wc -l | tr -d ' '`), §"Pitfall 3" (set +e / set -e bracket around expected-failing operations), §"Pattern 2 row" for using-git-worktrees output.json fields.
    - .planning/phases/03-skill-library/03-PATTERNS.md §"skills/<name>/run.sh" (line-cited analogs in core/bin/chantier: prelude 1–13, timestamp 188, jq emission 194–205, final state append shape).
    - core/bin/chantier lines 1–13 (license header + prelude shape that every run.sh mirrors); lines 188 (timestamp `date -u +%Y-%m-%dT%H:%M:%SZ`); lines 194–205 (jq emission canonical pattern with `--arg`/`--argjson`); lines 423 (Acceptance heading regex).
    - core/tests/validate_task.bats lines 7–24 (setup with TMPHOME canonicalization), lines 29–71 (`make_plan` helper — reusable verbatim in the e2e test).
    - skills/using-git-worktrees/SKILL.md (just authored — to align output.json field names with outputs_schema and Acceptance bullets with what PLAN.md will declare).
  </read_first>
  <behavior>
    - `run.sh` is executable (mode 0755, verified by `[ -x ]` in Wave-1 uniformity test @test 3).
    - First 6 non-comment lines are exactly: `set -eu`, literal-newline IFS (`IFS='<LF>'`), `LC_ALL=C`, `export LC_ALL` (matches the prelude in core/bin/chantier lines 8–12 verbatim).
    - `run.sh` reads `inputs.yml` from `${PWD}` (or from `${CHANTIER_DOSSIER_DIR:-$PWD}` if set), extracts `branch_name`, `setup_command`, `base_ref` via `grep -E '^<field>:' | sed 's/^<field>:[[:space:]]*//; s/"//g'` (POSIX subset; no `yq`).
    - `run.sh` performs the clean-baseline check via `DIRTY_LINES=$(git status --porcelain=v1 2>/dev/null | wc -l | tr -d ' ')`; records `baseline_clean = "true"` if `DIRTY_LINES == 0`, else `"false"`.
    - If baseline is clean, `run.sh` runs `git worktree add` to create the worktree at a path under `.chantier/worktrees/<branch_name>` (or other deterministic path declared in `state_writes` — match SKILL.md frontmatter exactly), then runs `$SETUP_COMMAND` inside it with `set +e ; SETUP_EXIT=$? ; set -e` bracketing per Pitfall 3.
    - If baseline is dirty, `run.sh` SKIPS worktree creation and records `worktree_path: ""`, `setup_exit_code: -1`. **`run.sh` exits 0 in BOTH cases** (D-04: business state, not exit code).
    - `run.sh` emits `output.json` via a single `jq -n` call with every value flowing through `--arg` (strings) or `--argjson` (numbers / pre-parsed JSON). The output includes all fields declared in SKILL.md `outputs_schema.required`: `baseline_clean`, `baseline_diff_lines`, `worktree_path`, `setup_exit_code`, `invariants_applied`. Plus optional: `baseline_check_command`, `started_at`, `ended_at`.
    - `run.sh` emits `output.md` containing the literal heading `## Acceptance` (capital A, no trailing words — gate 5 grep regex `^##[[:space:]]+Acceptance[[:space:]]*$`) with two acceptance bullets that echo what PLAN.md acceptance section says (per the validate_task substring-match shape lines 432–440 of core/bin/chantier).
    - Final command of run.sh is `chantier state append -e skill.completed -t "${CHANTIER_TASK_ID:-unknown}" -s using-git-worktrees -m "Skill using-git-worktrees completed; see output.json for measured invariants" -r "$TASK_DIR/output.md" -r "$TASK_DIR/output.json"` followed by `exit 0`. Event name `skill.completed` matches the dotted-namespace regex `^[a-z][a-z0-9]*(\.[a-z][a-z0-9]*)+$` enforced at core/bin/chantier lines 177–186.
    - `shellcheck --shell=sh skills/using-git-worktrees/run.sh` is clean (zero warnings).
    - No deny-list token anywhere in run.sh (gate 4 scans this file).
    - `core/tests/skill_using_git_worktrees_e2e.bats` invokes the skill end-to-end against `chantier validate-task` and exits 0; the test's setup mounts TMPHOME (per validate_task.bats pattern), copies the fixture dossier into the task directory, calls `make_plan` to construct a PLAN.md with `skill: using-git-worktrees` and two acceptance bullets matching what run.sh writes, runs `run.sh`, asserts `output.md` and `output.json` exist, then runs `chantier validate-task <task>` and asserts exit 0.
    - Fixture `core/tests/fixtures/skills/using-git-worktrees/dossier/inputs.yml` contains `branch_name: "feature/test-task"`, `setup_command: "true"`, `base_ref: "main"` (per RESEARCH Pattern 2 fixture sketch).
  </behavior>
  <action>
    THREE artifacts in this task. Treat as one logical commit (they ship together — fixture + script + test form the unit Wave 3 ratifies).

    A) Create `skills/using-git-worktrees/run.sh`. Mode 0755 (executable). Open with the license header used by core/bin/chantier (lines 2–6): `#!/bin/sh`, `# Copyright (c) 2026 Chantier Contributors`, `# SPDX-License-Identifier: MIT`, then a comment block citing the source pattern (`# Source: ADR 0001 Surface 3 + ADR 0002 §Exit-code matrix + skills/using-git-worktrees/SKILL.md`). Prelude: `set -eu` / IFS=literal-newline (write it as `IFS='` followed by a real newline then `'` on the next line, per RESEARCH Pattern 1 lines 270–272) / `LC_ALL=C` / `export LC_ALL`. Read inputs: `TASK_DIR="${PWD}"`, `INPUTS_YML="${TASK_DIR}/inputs.yml"`, guard with `[ -r "$INPUTS_YML" ] || { printf 'using-git-worktrees: missing inputs.yml\n' >&2; exit 2; }`. Extract via grep/sed: `BRANCH_NAME=$(grep -E '^branch_name:' "$INPUTS_YML" | sed 's/^branch_name:[[:space:]]*//; s/"//g')`. Repeat for `setup_command` and `base_ref`. Validate non-empty for required fields; missing required field → `exit 2`.

    Baseline check: `STARTED_AT=$(date -u +%Y-%m-%dT%H:%M:%SZ)`; `BASELINE_CHECK_CMD="git status --porcelain=v1"`; `DIRTY_LINES=$(git status --porcelain=v1 2>/dev/null | wc -l | tr -d ' ' || printf '0')`; `BASELINE_CLEAN=$( [ "$DIRTY_LINES" -eq 0 ] && printf 'true' || printf 'false' )`.

    Worktree creation conditional on clean baseline: if `BASELINE_CLEAN == "true"`, set `WORKTREE_PATH=".chantier/worktrees/${BRANCH_NAME}"`, run `git worktree add "$WORKTREE_PATH" "$BASE_REF" 2>/dev/null` (with `git worktree add -b "$BRANCH_NAME" "$WORKTREE_PATH" "$BASE_REF"` if `branch_name` is new). If `git worktree add` fails (e.g., path collision — a business state, not technical), capture and continue with `WORKTREE_PATH=""` and `SETUP_EXIT_CODE=-1`. If creation succeeds, run setup_command bracketed by `set +e` / `set -e` to capture `SETUP_EXIT_CODE=$?` per Pitfall 3. If baseline is dirty, skip both — set `WORKTREE_PATH=""`, `SETUP_EXIT_CODE=-1`. `ENDED_AT=$(date -u +%Y-%m-%dT%H:%M:%SZ)`.

    Emit `output.json` via a single `jq -n` call with `--arg started "$STARTED_AT"`, `--arg ended "$ENDED_AT"`, `--arg cmd "$BASELINE_CHECK_CMD"`, `--arg path "$WORKTREE_PATH"`, `--argjson dirty "$DIRTY_LINES"`, `--argjson setup "$SETUP_EXIT_CODE"`, `--arg clean "$BASELINE_CLEAN"`, `--argjson inv '[1,2,3,4]'`, body: `'{baseline_clean: ($clean == "true"), baseline_diff_lines: $dirty, baseline_check_command: $cmd, worktree_path: $path, setup_exit_code: $setup, started_at: $started, ended_at: $ended, invariants_applied: $inv}'`. Redirect to `"$TASK_DIR/output.json"`. NEVER `printf '{"key":"%s"}' "$value"` (Pitfall: JSON injection — Phase 2 binary line 196 is the canonical pattern).

    Emit `output.md` via an unquoted heredoc (interpolation desired for STARTED_AT / WORKTREE_PATH):
    ```
    cat > "$TASK_DIR/output.md" <<EOF
    # Skill: using-git-worktrees

    Started at $STARTED_AT (UTC); completed at $ENDED_AT.
    Baseline check: \`$BASELINE_CHECK_CMD\` reported $DIRTY_LINES dirty lines.
    Worktree path: ${WORKTREE_PATH:-(skipped — baseline not clean)}.

    ## Invariants applied

    - Kernel #1 (NFR-001 portability)
    - Kernel #2 (STATE.md append-only)
    - Kernel #3 (state_writes containment)
    - Skill #4 (clean baseline before work)

    ## Acceptance

    - Baseline was checked via \`git status --porcelain=v1\` before any worktree creation.
    - A worktree was created at the recorded path, or the operation was skipped with the dirty baseline recorded in output.json.
    EOF
    ```
    The `## Acceptance` heading is load-bearing — gate 5 greps `^##[[:space:]]+Acceptance[[:space:]]*$` (core/bin/chantier line 423).

    Final command — `chantier state append -e skill.completed -t "${CHANTIER_TASK_ID:-unknown}" -s using-git-worktrees -m "Skill using-git-worktrees completed; see output.json for measured invariants" -r "$TASK_DIR/output.md" -r "$TASK_DIR/output.json"` then `exit 0`.

    Set mode 0755 after writing: `chmod +x skills/using-git-worktrees/run.sh`.

    B) Create `core/tests/fixtures/skills/using-git-worktrees/dossier/inputs.yml` (mkdir -p the parents). Content (three top-level scalars only, per the frontmatter subset profile):
    ```
    branch_name: "feature/test-task"
    setup_command: "true"
    base_ref: "main"
    ```

    C) Create `core/tests/skill_using_git_worktrees_e2e.bats`. Setup function mirrors validate_task.bats lines 7–24 (load helpers, export CHANTIER, export FIXTURES, mkdir/cd TMPHOME, canonicalize via `pwd -P`, create `.planning/STATE.md` with `format_version: 0.1.0` frontmatter). Include a single `@test` block titled exactly `"using-git-worktrees: end-to-end through chantier validate-task"`. The block: (1) defines local `TASK="t1"`, (2) calls `make_plan` (reuse the helper from validate_task.bats lines 29–71 — copy the helper definition into this test file at the top, OR `source` it via the bats-load conventions — RECOMMEND: copy verbatim into this file so the test is hermetic), passing `TASK`, `state_writes` list (`.planning/phases/test-phase/tasks/t1/`), and acceptance bullets matching the two bullets run.sh writes into output.md (the bullets must be byte-identical for gate 5 substring match), (3) `make_skill_link()` — creates `$TMPHOME/skills/using-git-worktrees/` and symlinks or copies the live `skills/using-git-worktrees/SKILL.md`, `PRESSURE.md`, `run.sh` (copy, don't symlink — bats setup() is run per @test so isolation matters), (4) mounts the fixture: `mkdir -p "$TMPHOME/.planning/phases/test-phase/tasks/t1"`, then `cp "$BATS_TEST_DIRNAME/fixtures/skills/using-git-worktrees/dossier/inputs.yml" "$TMPHOME/.planning/phases/test-phase/tasks/t1/"`, (5) `cd` to the task dir and `run sh skills/using-git-worktrees/run.sh` — assert `[ "$status" -eq 0 ]`, `[ -f output.md ]`, `[ -f output.json ]`, (6) `cd "$TMPHOME"` and `run "$CHANTIER" validate-task t1` — assert `[ "$status" -eq 0 ]`.

    Pitfalls to avoid:
    - DO NOT use any bashism (`[[ ]]`, arrays, `<<<` here-strings, `mapfile`) — shellcheck will fail.
    - DO NOT exit non-zero on a dirty baseline (Pitfall 3) — exit 0 with `baseline_clean: false` in output.json.
    - DO NOT call `chantier state append` BEFORE writing output.md and output.json (Pitfall 4 — log lies about completion if script crashes mid-flow).
    - DO NOT use `## Acceptance Criteria` or `## acceptance` (lowercase) — Pitfall 5; gate 5 is case-sensitive.
    - DO NOT include any deny-list token anywhere in run.sh, inputs.yml, or the e2e bats file (e.g., no `cursor` in a comment, no example URL containing the token).
    - DO NOT skip the `tr -d ' '` after `wc -l` — BSD `wc` (macOS) emits leading spaces and the resulting string fails `[ "$DIRTY_LINES" -eq 0 ]` arithmetic comparison.
    - DO NOT lock STATE.md in run.sh — `chantier state append` already holds the mkdir-mutex (RESEARCH "Don't Hand-Roll" row 4).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && [ -x skills/using-git-worktrees/run.sh ] && shellcheck --shell=sh skills/using-git-worktrees/run.sh && head -20 skills/using-git-worktrees/run.sh | grep -qE '^set -eu' && head -20 skills/using-git-worktrees/run.sh | grep -qE '^LC_ALL=C' && grep -qE "^chantier state append" skills/using-git-worktrees/run.sh && grep -qE "^exit 0" skills/using-git-worktrees/run.sh && grep -qE 'jq -n' skills/using-git-worktrees/run.sh && grep -qE 'git status --porcelain=v1' skills/using-git-worktrees/run.sh && _denied=$(grep -cE 'mcp__|claude_ai_|@codebase|claude-code|cursor|codex-cli|copilot-cli|gemini-cli|opencode' skills/using-git-worktrees/run.sh || true) && [ "$_denied" -eq 0 ] && [ -f core/tests/fixtures/skills/using-git-worktrees/dossier/inputs.yml ] && grep -qE '^branch_name:' core/tests/fixtures/skills/using-git-worktrees/dossier/inputs.yml && [ -f core/tests/skill_using_git_worktrees_e2e.bats ] && bats core/tests/skill_using_git_worktrees_e2e.bats 2>&1 | tail -3 | grep -qE '1 test, 0 failures' && bats core/tests/skill_uniformity.bats 2>&1 | tail -3 && bats core/tests/ 2>&1 | tail -3 | grep -qE '0 failures' && echo "skill+fixture+e2e OK"</automated>
  </verify>
  <done>
    `skills/using-git-worktrees/run.sh` is executable, shellcheck-clean, contains the canonical prelude, uses `jq -n` for output.json, uses `git status --porcelain=v1` for baseline, has the literal `## Acceptance` heading in output.md, and ends with `chantier state append -e skill.completed` + `exit 0`. Fixture `inputs.yml` has the three required scalars. E2e bats test runs run.sh and `chantier validate-task` against the fixture task and exits 0. Full bats suite remains green (one new test added; Wave-1 uniformity test now reports 1 PASS / 2 SKIP because this skill is now present but the other three aren't). Zero deny-list tokens in any file under `skills/using-git-worktrees/` outside SKILL.md frontmatter.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `inputs.yml` (Phase 4 will stage; Phase 3 fixture) → `run.sh` | Input values become shell variables and flow into `git`, `jq`, and `chantier state append` arg lists. Tampering surface: a hostile `branch_name` with shell metacharacters could attempt command injection. |
| `run.sh` → filesystem (`state_writes` paths) | Skill writes only inside declared paths; gate 1 of `chantier validate-task` enforces containment. |
| `run.sh` → `chantier state append` | Final command — the binary's own mkdir-mutex serializes STATE.md writes. Skill does NOT lock STATE.md itself (RESEARCH "Don't Hand-Roll" row 4). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | Command injection via `branch_name` (e.g., `branch_name: "foo; rm -rf /"`) | mitigate | `branch_name` flows through `jq --arg` into output.json (never interpolated into a shell `eval`). The only places it reaches a command are positional args to `git worktree add` (which treats it as a literal branch name, not a shell expression). No `sh -c "$BRANCH_NAME"` anywhere. SKILL.md `inputs_schema` declares `branch_name` pattern `^[A-Za-z0-9._/-]+$` — Phase 3 does not enforce this at runtime (validate-task v0.1 doesn't validate inputs.yml per ADR 0001 OQ #3 deferral), but documenting it tightens the contract for Phase 4+. |
| T-03-02-02 | Tampering | JSON injection in `output.json` (e.g., `setup_command` containing a `"` or backslash) | mitigate | All JSON emission via `jq -n --arg`/`--argjson` — values flow through jq's lexer, never through shell expansion into JSON. The phrase `T-02-04-INJ` is the Phase 2 traceability anchor for this defence (core/bin/chantier line 196). |
| T-03-02-03 | Tampering | Path traversal in `state_writes` declaration (e.g., `state_writes: ["../../etc/"]`) | accept (already mitigated upstream) | Already enforced by `chantier validate-task` gate 1 (lines 597–631 of core/bin/chantier; verified by `core/tests/validate_task.bats` test 1). Skill does not re-implement; Wave-1 e2e test exercises gate 1 by invoking validate-task. |
| T-03-02-04 | Tampering | Symlink races during `git worktree add` | accept | `git worktree add` is atomic per git's own semantics (verified — git creates the worktree metadata under `.git/worktrees/` atomically, then links the directory). Skill MUST NOT do its own `mkdir + chmod + mv` dance (RESEARCH Security row 4). |
| T-03-02-05 | Tampering | TOCTOU on `inputs.yml` (skill reads twice and content changes between reads) | mitigate | `run.sh` reads `inputs.yml` once into shell variables at the top of the script (per RESEARCH Security row 5); subsequent operations use the captured values. No re-read mid-execution. |
| T-03-02-06 | Information Disclosure | Harness identifier leaked into output.md / output.json / run.sh / PRESSURE.md | mitigate | Gate 4 deny-list grep is wired (core/bin/chantier lines 686–702). Pre-commit hook (manual: `grep -rE 'mcp__|claude_ai_|@codebase|claude-code|cursor|codex-cli|copilot-cli|gemini-cli|opencode' skills/using-git-worktrees/`) catches drift before validate-task runs. PRESSURE.md and run.sh are explicitly scanned (only SKILL.md is exempt per chantier line 691). |
| T-03-02-SC | Tampering | npm/pip/cargo installs | n/a | This plan installs no packages — POSIX `sh`, `jq`, `git`, `bats`, `shellcheck` are all host POSIX or already-vendored. No legitimacy gate required. |

</threat_model>

<verification>
- `[ -x skills/using-git-worktrees/run.sh ]` — executable bit set.
- `shellcheck --shell=sh skills/using-git-worktrees/run.sh` — clean (no warnings).
- `head -20 skills/using-git-worktrees/run.sh` — shows the canonical prelude: shebang, license header, `set -eu`, IFS=\n, `LC_ALL=C`, `export LC_ALL`.
- `grep -E '^chantier state append' skills/using-git-worktrees/run.sh` returns 1 line (final command).
- `grep -E 'jq -n' skills/using-git-worktrees/run.sh` returns at least 1 line (output.json emission).
- `grep -E '## Acceptance$' skills/using-git-worktrees/run.sh` returns 1 line (output.md heredoc).
- `grep -cE '^## Scenario [0-9]' skills/using-git-worktrees/PRESSURE.md` returns 2.
- `grep -cE '^\*\*(Situation|Temptation|Required response|Disqualifier)\*\*' skills/using-git-worktrees/PRESSURE.md` returns 8.
- `grep -rE 'mcp__|claude_ai_|@codebase|claude-code|cursor|codex-cli|copilot-cli|gemini-cli|opencode' skills/using-git-worktrees/PRESSURE.md skills/using-git-worktrees/run.sh` returns ZERO matches.
- `awk` extraction of `harness_adapters` from SKILL.md returns exactly `claude-code` (the array contains only this one entry).
- `bats core/tests/skill_using_git_worktrees_e2e.bats` exits 0, "1 test, 0 failures".
- `bats core/tests/skill_uniformity.bats` exits 0 — first @test now PASSES (since the one skill present declares the reference array); second and third @test now PASS for this skill (PRESSURE has 2 scenarios, run.sh is present and executable). NOTE: when this is the ONLY Wave-2 plan that has landed, the uniformity test reports 3 PASSES (all checks satisfied for the one skill present); after all four Wave-2 plans land, it still reports 3 PASSES across all four. The transition from SKIP to PASS happens at the first skill landing.
- `bats core/tests/` (full suite) exits 0 — 64 existing + 3 uniformity (now PASS, not SKIP) + 1 new e2e = 68 tests, zero failures.
- `chantier --self-test` returns "all green".
</verification>

<success_criteria>
1. `skills/using-git-worktrees/SKILL.md` exists with 8-field frontmatter, `harness_adapters: [claude-code]`, kernel invariants 1–3 verbatim per D-06, invariant 4 cites `output.json.baseline_diff_lines` as proof.
2. `skills/using-git-worktrees/PRESSURE.md` exists with `skill_id` + `scenarios` frontmatter, ≥2 scenarios in the 4-subsection structured spec, each Disqualifier citing Invariant 4 and the relevant output.json field.
3. `skills/using-git-worktrees/run.sh` is executable, shellcheck-clean POSIX, uses the canonical prelude + jq emission + `## Acceptance` heading + final `chantier state append` + `exit 0`.
4. Fixture `core/tests/fixtures/skills/using-git-worktrees/dossier/inputs.yml` declares `branch_name`, `setup_command`, `base_ref`.
5. `core/tests/skill_using_git_worktrees_e2e.bats` runs run.sh + `chantier validate-task` against the fixture task and exits 0.
6. No deny-list token in any file under `skills/using-git-worktrees/` other than SKILL.md frontmatter `harness_adapters: [claude-code]`.
7. Wave-1 uniformity test transitions from 3×SKIP to 3×PASS (for this single shipped skill) once this plan commits.
8. Full bats suite remains green.
</success_criteria>

<output>
Create `.planning/phases/03-skill-library/03-02-SUMMARY.md` when done, mirroring 02-06-SUMMARY.md's shape (frontmatter with phase/plan/subsystem/tags/dependency_graph/tech_stack/key_files/decisions/metrics; body sections: What Was Shipped, Validation Results, Phase 3 success criteria status partial, Note for Wave 3). Append `plan.completed` event via `chantier state append -e plan.completed -t null -s using-git-worktrees -m "Plan 03-02 complete: using-git-worktrees skill shipped end-to-end" -r .planning/phases/03-skill-library/03-02-SUMMARY.md`.
</output>

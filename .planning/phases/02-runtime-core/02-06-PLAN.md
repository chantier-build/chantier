---
phase: 02-runtime-core
plan: 06
type: execute
wave: 5
depends_on:
  - 02-02
  - 02-05
files_modified:
  - docs/adr/0002-runtime-binary-and-state-format.md
  - .planning/STATE.md
autonomous: true
requirements:
  - FR-001
  - FR-002
  - FR-003
  - FR-004

must_haves:
  truths:
    - "docs/adr/0002-runtime-binary-and-state-format.md exists with status Accepted matching ADR 0001's house style per PATTERNS.md §ADR 0002 lines 44–107"
    - "ADR 0002 codifies D-01..D-13 + the two REVISED Claude's-Discretion items (#4 lock, #11 self-test) per RESEARCH §Claude's Discretion review"
    - "ADR 0002 publishes the 5 JSON Schemas INLINE as fenced ```json blocks (D-06 + RESEARCH Open Question 1) — quoted byte-for-byte from core/schemas/*.json"
    - "ADR 0002 documents the **frontmatter subset profile** (top-level scalars + simple lists; no nested maps) per RESEARCH §Claude's Discretion review final block"
    - "ADR 0002 documents the **JSON Schema subset profile** (the 7 supported keywords from Pattern 5; explicit list of NOT-covered keywords)"
    - "ADR 0002 publishes the indicative event taxonomy list per D-10"
    - "ADR 0002 publishes the dotted-namespace shape regex per D-09"
    - "ADR 0002 explicitly re-flags ADR 0001's four open questions as STILL deferred (skill lockfile, STATE.md compaction, inputs_schema strictness, skill-to-skill composition) per CONTEXT.md §canonical_refs"
    - "ADR 0002 documents the exit-code matrix 0/1/2/3 per D-Discretion error model"
    - "ADR 0002 documents the mkdir-mutex pattern (NOT flock) per RESEARCH §Claude's Discretion review row 4"
    - "ADR 0002 documents `actor` field fallback (`unknown` when git config user.name empty) per RESEARCH Pitfall 9"
    - ".planning/STATE.md migrated from Markdown table to JSONL — 10 lines, one per existing event, in a DEDICATED commit per D-04"
    - "STATE.md frontmatter format_version bumped from 0.1.0-interim to 0.1.0 per D-04"
    - "Migration commit message documents the migration explicitly per D-04 and includes the pre-migration Markdown table verbatim in a folded block per RESEARCH Open Question 2"
    - "Every migrated JSONL line passes the event-shape regex `^[a-z][a-z0-9]*(\\.[a-z][a-z0-9]*)+$` (round-trip safe through D-09)"
    - "Every migrated JSONL line has task=null and skill=null for historical rows (none of the Phase 1 events had a task or skill association per PATTERNS.md per-row mapping rule)"
    - "An additional `phase.completed` event for Phase 2 itself is appended via `chantier state append` (not inline JSON) AFTER the migration, demonstrating dogfood"
  artifacts:
    - path: "docs/adr/0002-runtime-binary-and-state-format.md"
      provides: "ADR 0002: STATE.md JSONL format, 5 JSON Schemas inline, event taxonomy, exit-code matrix, frontmatter subset profile, JSON Schema subset profile, mkdir-mutex documentation, re-flagged open questions"
      min_lines: 250
      contains: "Status: Accepted"
    - path: ".planning/STATE.md"
      provides: "Migrated to JSONL format; format_version: 0.1.0; 10 historical events as JSONL lines; phase.completed event for Phase 2"
      contains: "format_version: 0.1.0"
  key_links:
    - from: "docs/adr/0002-runtime-binary-and-state-format.md"
      to: "core/schemas/*.json"
      via: "fenced ```json blocks must match the files byte-for-byte at ship time"
      pattern: "```json"
    - from: ".planning/STATE.md (post-migration)"
      to: "core/bin/chantier state show"
      via: "the migrated file must render correctly through state show"
      pattern: "11 lines minimum after migration + Phase 2 event"
    - from: "docs/adr/0002-runtime-binary-and-state-format.md §Open questions"
      to: "docs/adr/0001-state-skill-contract.md §Open questions"
      via: "explicit re-flag — same four questions remain deferred"
      pattern: "skill versioning|STATE.md compaction|inputs_schema strictness|skill-to-skill composition"
---

<objective>
Close Phase 2 by shipping the two final artefacts that ROADMAP §Phase 2 success criterion 5 demands and CONTEXT D-04 schedules: (1) **ADR 0002** — the runtime-binary-and-state-format ADR that codifies every implementation decision from plans 02-01 through 02-05, and (2) the **one-shot migration of `.planning/STATE.md`** from Markdown table to JSONL with `format_version` bumped to `0.1.0`. The migration MUST be a dedicated commit per D-04; the ADR commit is separate.

After this plan ships, Phase 2 is complete: FR-001 through FR-004 satisfied (binary, schemas, validate-task, state subcommands, scaffold), success criterion 5 (ADR 0002 published Accepted) satisfied, and the project's own dogfood state log uses the format the binary enforces.

Purpose: ADR 0002 is the load-bearing contract that downstream tooling and Phase 3 skills depend on. Without it, the frontmatter subset profile and JSON Schema subset profile are oral history — and ADR 0001 §Open Questions remain ambiguously "deferred or addressed?". The migration is the dogfood proof that the binary works on real, historical data.

Output: A 250+ line MADR-shaped ADR 0002 with all 5 schemas inline; a migrated STATE.md whose every line round-trips through `chantier state append` validation; explicit re-flag of ADR 0001's four still-deferred questions; commit history with two separate commits (ADR and migration) as D-04 specifies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-runtime-core/02-CONTEXT.md
@.planning/phases/02-runtime-core/02-RESEARCH.md
@.planning/phases/02-runtime-core/02-PATTERNS.md
@.planning/phases/02-runtime-core/02-VALIDATION.md
@docs/adr/0001-state-skill-contract.md
@core/bin/chantier
@core/schemas/project.json
@core/schemas/requirements.json
@core/schemas/roadmap.json
@core/schemas/plan.json
@core/schemas/skill.json
@.planning/phases/01-foundation/SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author docs/adr/0002-runtime-binary-and-state-format.md (MADR-shaped, status Accepted)</name>
  <files>docs/adr/0002-runtime-binary-and-state-format.md</files>
  <read_first>
    - docs/adr/0001-state-skill-contract.md (ALL lines) — the house style ADR 0002 must mirror byte-for-byte per PATTERNS.md §ADR 0002.
    - .planning/phases/02-runtime-core/02-PATTERNS.md §"docs/adr/0002-runtime-binary-and-state-format.md" lines 44–107 — section-by-section mapping of ADR 0001 patterns to ADR 0002 content.
    - .planning/phases/02-runtime-core/02-RESEARCH.md §"Architectural Responsibility Map" lines 24–35.
    - .planning/phases/02-runtime-core/02-RESEARCH.md §"Pattern 5" lines 462–558 (full JSON Schema subset profile spec).
    - .planning/phases/02-runtime-core/02-RESEARCH.md §"Don't Hand-Roll" lines 647–658 (frontmatter subset profile spec).
    - .planning/phases/02-runtime-core/02-RESEARCH.md §"Open Questions" lines 877–895 (Researcher's recommendations for the four ADR 0002 open questions).
    - .planning/phases/02-runtime-core/02-RESEARCH.md §"Claude's Discretion review" lines 985–1007 (verdicts on the 11 discretion items + 2 new ADR 0002 sections).
    - .planning/phases/02-runtime-core/02-CONTEXT.md D-01 through D-13 (locked decisions).
    - core/schemas/project.json, requirements.json, roadmap.json, plan.json, skill.json (the canonical files to quote inline).
    - core/bin/chantier (the binary the ADR documents).
  </read_first>
  <action>
    Create `docs/adr/0002-runtime-binary-and-state-format.md` mirroring ADR 0001's MADR-shaped house style per PATTERNS.md lines 44–107.

    **Header block** (PATTERNS.md lines 50–69) — same 6 bullets as ADR 0001:
    - `# ADR 0002 — Runtime binary and state format`
    - Status: Accepted
    - Date proposed: today (use `date -u +%Y-%m-%d`)
    - Date accepted: same date (set after sign-off in a follow-up commit per ADR 0001's lifecycle pattern — but for v0.1 dogfood, ship as Accepted in the first commit per PATTERNS.md note about ADR 0001's `8889c59` lifecycle)
    - Deciders: Chantier founding contributors
    - Supersedes: —
    - Superseded by: —
    - One-paragraph framing per PATTERNS.md line 62: "This ADR codifies the runtime format and binary contract. Once accepted, downstream tooling can depend on these schemas at version 0.1.0."

    **`## Context`** — list the three findings forcing ADR 0002:
    1. NFR-002 trust surface caps deps at `sh`+`jq`.
    2. `flock(1)` is absent on macOS (RESEARCH §Standard Stack > Version verification line 135 — direct env probe).
    3. No pure-jq JSON Schema draft-07 validator exists in the public ecosystem (RESEARCH §Summary point 2 — jqlang/jq#3437 confirms).

    Also list Phase 1's three findings that Phase 2 was tasked to close (per `.planning/phases/01-foundation/SUMMARY.md` §"Validation of ADR 0001 schema"): STATE.md format, frontmatter JSON Schemas, event vocabulary.

    **`## Decision`** — the body of the ADR. Use thematic subsections per PATTERNS.md line 77:

    - `### STATE.md JSONL format` — quotes D-01, D-02. Specify the per-line shape: `{"ts": ISO-8601 UTC, "event": dotted-name string, "actor": string, "task": string|null, "skill": string|null, "summary": string, "refs": [string]}`. Document the `actor: "unknown"` fallback when `git config user.name` is empty (RESEARCH Pitfall 9 + Assumption A6 — flagged as requiring ADR codification).

    - `### Event taxonomy and shape regex` — quotes D-08, D-09, D-10. The shape regex is `^[a-z][a-z0-9]*(\.[a-z][a-z0-9]*)+$`. Publish the indicative event list grouped by namespace: `bootstrap.*`, `adr.*`, `phase.*`, `task.*`, `skill.*`, `scaffold.*`, `repo.*`, `github.*` per D-10. Make it explicit: the binary checks shape, not vocabulary.

    - `### Frontmatter subset profile` — NEW per RESEARCH §Claude's Discretion review final block (line 1004). Document: Chantier validates a YAML *subset*, not arbitrary YAML. Allowed: top-level scalars (string/number/boolean) and simple lists (each item a scalar). Not allowed: nested maps, multi-line block scalars beyond the existing format_note pattern, anchors/references. The awk extractor (RESEARCH §Code Examples > Verifying a frontmatter file) implements this subset. Document explicitly so future skill authors know what shapes are safe.

    - `### JSON Schema subset profile` — NEW per RESEARCH §Claude's Discretion review final block (line 1005). Document: Chantier validates a draft-07 *profile*. **Supported keywords**: `type`, `required`, `properties`, `additionalProperties`, `pattern`, `enum`, `items`. **NOT supported** (silently ignored by the validator): `$ref`, `oneOf`, `anyOf`, `allOf`, `not`, `if/then/else`, `format`, `definitions`, `$defs`, `minimum`, `maximum`, `multipleOf`, `minLength`, `maxLength`. Justify the trade-off via NFR-002 (no `ajv`, no `check-jsonschema`).

    - `### Schemas (inline)` — five subsections, one per schema. Each subsection contains a fenced ```json block QUOTING THE FILE byte-for-byte from `core/schemas/{name}.json`. Order: PROJECT, REQUIREMENTS, ROADMAP, PLAN, SKILL. Per PATTERNS.md line 83 and RESEARCH Open Question 1 — full inline, not by reference.

    - `### Hybrid strict/permissive validation` — quotes D-05. `required` is strict (typo → rejection); other top-level keys are permitted (`additionalProperties: true`) with a warning emitted by the validator.

    - `### Concurrency: mkdir-mutex with stale-PID detection` — REVISED per RESEARCH §Claude's Discretion review row 4 (line 994). Document that the original CONTEXT.md D-Discretion concurrency item ("flock with macOS BSD ⇄ Linux util-linux compat wrapper") is **superseded by this ADR**. Reason: `flock` is absent on macOS — no compat wrapper possible. Reference: RESEARCH §Standard Stack > Version verification line 135 (direct env probe). The new approach: mkdir LOCKDIR atomically; on contention check PID via `kill -0` for staleness; on stale recover via rm-rf + retry. Pattern is canonical per bash-hackers wiki (RESEARCH citation lines 1016).

    - `### Exit-code matrix` — quotes D-Discretion §Error model. Table:
      | Exit | Meaning |
      |---|---|
      | 0 | success |
      | 1 | contract violation (validation failed) |
      | 2 | runtime error (missing dep, write failure, lock contention) |
      | 3 | usage error (bad flags, unknown subcommand) |
      Plus: `--json-errors` is a top-level flag parsed BEFORE subcommand dispatch (RESEARCH Open Question 4).

    - `### Self-test gates` — REVISED per RESEARCH §Claude's Discretion review row 11 (line 1001). Original CONTEXT.md spec included "flock available" check; this ADR supersedes that with "mkdir-lock works". Adds two new checks (per RESEARCH recommendation): the binary self-asserts (a) no harness identifier present in its own source via grep, (b) no CRLF line ending via `file | grep -v CRLF`.

    - `### Migration` — quotes D-04. Document: the migration of the existing 10 STATE.md rows from Markdown table to JSONL happens in a DEDICATED commit; `format_version` bumps from `0.1.0-interim` to `0.1.0`; the commit message includes the pre-migration Markdown table verbatim in a folded block per RESEARCH Open Question 2. No permanent `chantier state migrate` subcommand — migration is one-shot.

    **`## Consequences`** — three subsections per PATTERNS.md line 78:
    - **Positive**: machine-friendly JSONL log; deterministic schemas; portable across macOS/Linux/BSD with no flock dependency; trust surface stays at `sh` + `jq`.
    - **Negative**: hand-rolled subset validators (jq schema, awk frontmatter) are Chantier-specific code; if the subsets prove too narrow we will hit it at Phase 3+ and need to expand them. Frontmatter subset prohibits some legitimate YAML idioms (deeply nested config).
    - **Neutral**: the indicative event taxonomy is intentionally non-binding — vocabulary discoverable as the project evolves.

    **`## Alternatives considered`** — letter-prefixed subsections per PATTERNS.md lines 79, 85–93:
    - **A. Markdown table for STATE.md (status quo)** — Rejected per D-01. Reason: greppable but not parseable without ad-hoc escaping; downstream tools cannot rely on the column shape.
    - **B. `flock(1)` for concurrency** — Rejected. Reason: absent on macOS Darwin (direct env probe). No portable compat wrapper exists.
    - **C. `ajv` CLI shellout for schema validation** — Rejected. Reason: violates NFR-002 (adds Node.js dependency).
    - **D. `yq` for YAML frontmatter parsing** — Rejected. Reason: violates NFR-002 (adds Go binary).
    - **E. Full JSON Schema draft-07 implementation** — Rejected. Reason: thousands of LOC; Chantier needs only ~7 keywords. Subset profile is documented per §"JSON Schema subset profile".
    - **F. Permanent `chantier state migrate` subcommand** — Rejected. Reason: no upgrade story exists yet for v0.1; testing a phantom version matrix is premature. Migration stays one-shot per D-04.

    **`## Open questions (intentionally deferred)`** — per PATTERNS.md line 80, this ADR MUST explicitly re-flag ADR 0001's four open questions as STILL deferred. Quote them verbatim from ADR 0001 §Open Questions:
    1. Versioning skills across breaking changes (`chantier.lock`).
    2. `STATE.md` compaction.
    3. `inputs_schema` strictness mode.
    4. Skill-to-skill composition syntax.
    Per CONTEXT.md §canonical_refs ("ADR 0002 must explicitly call out that it does not address them"). Also flag the two new minor questions surfaced by RESEARCH but not blocking Phase 2:
    5. Whether `--self-test` should optionally compare ADR 0002 inline schemas with `core/schemas/*.json` byte-for-byte (RESEARCH §Claude's Discretion review row 2).
    6. Whether `actor` fallback should be `unknown`, `ci`, or `system` (RESEARCH Assumption A6 — codify as `unknown` for v0.1 but flag for v0.2 revisit).

    **`## Approval`** — per PATTERNS.md lines 95–108. Use the same format as ADR 0001:
    - One-paragraph framing: "This ADR codifies the runtime format and binary contract. Once accepted, downstream tooling can depend on these schemas at version 0.1.0."
    - `- [x] Approved by founding contributors in the bootstrap session, $(date -u +%Y-%m-%d).`
    - `- [x] Specific decisions ratified: D-01..D-13 from CONTEXT.md; mkdir-mutex per REVISED Discretion #4; expanded self-test per REVISED Discretion #11; frontmatter subset profile; JSON Schema subset profile.`
    - `- Four ADR-0001 open questions explicitly re-flagged as still deferred.`

    **Line discipline:** No tabs. Markdown only. Fenced ```json blocks for the 5 schemas (per D-06). No `claude-code`, `cursor`, etc. identifiers ANYWHERE in the file (NFR-001 applies to docs as well — RESEARCH §"Harness-identifier deny-list" line 442 says the deny-list applies repo-wide in Phase 2 because no adapter directory exists yet; however ADR 0002 may need to mention them when discussing the SKILL.md `harness_adapters` enum — keep that single mention contextual and quote-marked, e.g., "the six harness identifiers `claude-code`, `cursor`, `codex-cli`, `copilot-cli`, `gemini-cli`, `opencode`"). This is a documented carve-out for ADR text (analogous to skill.json's enum carve-out).

    **Byte-for-byte schema embedding check**: After authoring, verify each ```json block in the ADR equals the corresponding `core/schemas/*.json` file content. Use `diff <(awk '/^```json$/{flag=!flag;next} flag' docs/adr/0002-runtime-binary-and-state-format.md | jq -S .) <(jq -S . core/schemas/project.json)` style check (planner-generated; the executor can use any equivalent diff).
  </action>
  <verify>
    <automated>test -f docs/adr/0002-runtime-binary-and-state-format.md &amp;&amp; grep -q '^# ADR 0002 — Runtime binary and state format$' docs/adr/0002-runtime-binary-and-state-format.md &amp;&amp; grep -q '^- \*\*Status:\*\* Accepted$' docs/adr/0002-runtime-binary-and-state-format.md &amp;&amp; grep -qE '^## (Context|Decision|Consequences|Alternatives considered|Open questions|Approval)' docs/adr/0002-runtime-binary-and-state-format.md &amp;&amp; [ "$(grep -c '^```json$' docs/adr/0002-runtime-binary-and-state-format.md)" -ge 5 ] &amp;&amp; grep -q 'mkdir' docs/adr/0002-runtime-binary-and-state-format.md &amp;&amp; ! grep -E '\bflock\(' docs/adr/0002-runtime-binary-and-state-format.md &amp;&amp; grep -q 'subset profile' docs/adr/0002-runtime-binary-and-state-format.md &amp;&amp; grep -qi 'chantier.lock\|skill versioning' docs/adr/0002-runtime-binary-and-state-format.md &amp;&amp; grep -qi 'compaction' docs/adr/0002-runtime-binary-and-state-format.md &amp;&amp; [ "$(wc -l &lt; docs/adr/0002-runtime-binary-and-state-format.md)" -ge 250 ]</automated>
  </verify>
  <acceptance_criteria>
    - ADR 0002 file exists at docs/adr/0002-runtime-binary-and-state-format.md.
    - Header block matches PATTERNS.md format (6 bullets in order, status Accepted).
    - Six top-level sections present: Context, Decision, Consequences, Alternatives considered, Open questions, Approval.
    - At least 5 fenced ```json blocks (one per schema).
    - The mkdir-mutex pattern is documented; flock is mentioned only in §Alternatives considered (as the rejected option).
    - "subset profile" appears at least twice (frontmatter subset profile, JSON Schema subset profile).
    - All four ADR 0001 open questions are re-flagged in ADR 0002 §Open questions.
    - File ≥ 250 lines.
    - No harness identifiers outside the documented contextual quote (the carve-out paragraph mentioning the 6 names while discussing the skill.json enum).
  </acceptance_criteria>
  <done>ADR 0002 published, Accepted status, all five schemas inline, mkdir-mutex REVISED documented, both subset profiles formalized, ADR 0001 open questions re-flagged as still deferred.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migrate .planning/STATE.md from Markdown table to JSONL in a dedicated commit; append Phase 2 phase.completed event</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (the 10 current rows + frontmatter to migrate).
    - .planning/phases/02-runtime-core/02-PATTERNS.md §".planning/STATE.md (MODIFY — one-shot migration)" lines 111–174 — the source pattern, destination pattern, and per-row mapping rule.
    - .planning/phases/02-runtime-core/02-RESEARCH.md §"Runtime State Inventory" lines 660–672 — migration approach (one-shot, no permanent subcommand, commit body includes pre-migration table).
    - .planning/phases/02-runtime-core/02-CONTEXT.md D-04 (migration mandate).
    - core/bin/chantier (the binary that will validate-by-shape every migrated line).
  </read_first>
  <action>
    This task ships in **two separate git commits** per D-04:

    **Commit 1 — `chore(state): migrate STATE.md from Markdown table to JSONL (format_version 0.1.0)`**

    1. Read the current `.planning/STATE.md` (10 rows + frontmatter, format_version `0.1.0-interim`).
    2. Replace the entire file content:
       - **New frontmatter** per PATTERNS.md lines 143–151:
         ```yaml
         ---
         project_id: chantier
         created: 2026-05-29
         format_version: 0.1.0
         format_note: |
           STATE.md body is JSON Lines, append-only, one event per line.
           Mutation is allowed only via `chantier state append`.
         ---
         ```
         Note: bump `format_version` from `0.1.0-interim` to `0.1.0`. Drop the long format_note paragraph from the interim version; replace with the two-line version per PATTERNS.md.
       - **New body** = 10 JSONL lines, one per existing row, following the per-row mapping rule per PATTERNS.md lines 161–171:
         - `timestamp` → `ts` (verbatim ISO-8601 string).
         - `event` → `event` (verbatim).
         - `actor` → `actor` (verbatim — all 10 use `MAoDzi`).
         - `task` → `null` (all 10 historical rows have no task association).
         - `skill` → `null` (same).
         - `summary` → `summary` (verbatim — beware row 25 has commas and other punctuation; jq will escape them).
         - `refs` → `refs` array. For single-token refs (rows 16, 17, 18, 19, 20, 21, 22): one-element array `["brief"]`, etc. For row 25 (`bootstrap.harness.chosen`) with `ROADMAP.md` ref: treat as one-element array `["ROADMAP.md"]` per PATTERNS.md line 171.
       - Build each JSONL line with jq for safety:
         ```sh
         jq -nc --arg ts "2026-05-29T17:00:00Z" --arg ev "bootstrap.session.started" --arg ac "MAoDzi" --arg sm "Brief received, session plan proposed, all seven ADR sign-offs validated" --argjson refs '["brief"]' '{ts:$ts, event:$ev, actor:$ac, task:null, skill:null, summary:$sm, refs:$refs}'
         ```
         Repeat for each of the 10 rows. Write all 10 lines (no trailing blank line; one `\n` per line including the last).
    3. Validation before commit:
       - `wc -l < .planning/STATE.md` minus the frontmatter sentinels equals exactly the original 10 events. Use `awk '/^---$/{c++; next} c>=2' .planning/STATE.md | wc -l` should be 10.
       - Every body line is valid JSON: `awk '/^---$/{c++; next} c>=2' .planning/STATE.md | while IFS= read -r line; do printf '%s' "$line" | jq empty || exit 1; done`.
       - Every body line's `event` field passes the D-09 shape regex: `awk '...' | jq -r '.event' | while read e; do printf '%s\n' "$e" | jq -R -e 'test("^[a-z][a-z0-9]*(\\.[a-z][a-z0-9]*)+$")' >/dev/null || { echo "BAD: $e"; exit 1; }; done`. **All 10 must pass** (per PATTERNS.md §"Event-name discipline" lines 410–415).
       - `chantier state show` (now functional from plan 02-04) renders the migrated file correctly: 10 rows + 1 header row, no field collapse, all columns aligned.
    4. Commit message (HEREDOC form so newlines preserved):
       ```
       chore(state): migrate STATE.md from Markdown table to JSONL (format_version 0.1.0)

       Per ADR 0002 §Migration and CONTEXT.md D-04, this is the one-shot conversion
       of the foundation-phase STATE.md from Markdown table to JSON Lines. The
       format_version bumps from 0.1.0-interim to 0.1.0; no permanent `chantier state
       migrate` subcommand is shipped (migration is non-reversible by design).

       Pre-migration table (10 rows from Phase 1) preserved verbatim below for git-history archival:

       | timestamp | event | actor | summary | refs |
       |---|---|---|---|---|
       | 2026-05-29T17:00:00Z | bootstrap.session.started | MAoDzi | Brief received, session plan proposed, all seven ADR sign-offs validated | brief |
       [... include all 10 rows verbatim from the pre-migration file ...]

       Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
       ```
       Per RESEARCH Open Question 2 (line 884) and PATTERNS.md line 173: do NOT commit `.bak`; git history is the backup; commit body contains the pre-migration table.

    **Commit 2 — append the Phase 2 phase.completed event**

    1. Invoke the freshly-functional binary against the freshly-migrated STATE.md:
       ```sh
       core/bin/chantier state append \
         -e "phase.completed" \
         -m "Phase 02-runtime-core completed: binary shipped, schemas published, ADR 0002 ratified, STATE.md migrated to JSONL" \
         -r ".planning/phases/02-runtime-core/SUMMARY.md" \
         -r "docs/adr/0002-runtime-binary-and-state-format.md" \
         -r "core/bin/chantier"
       ```
       (Note: no `-t` and no `-s` because phase.completed has no task/skill association per the same convention as the 10 historical rows.)
    2. Verify: `awk '/^---$/{c++; next} c>=2' .planning/STATE.md | wc -l` is now 11. The 11th line is the new event with `task: null, skill: null`.
    3. Commit message:
       ```
       chore(state): record phase.completed for Phase 02-runtime-core

       Dogfood validation: the first event appended via chantier state append
       (rather than authored by hand) lands in this commit, proving the binary
       round-trips on its own output.

       Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
       ```

    **Critical safety rules**:
    - Do NOT commit `STATE.md.bak`. There is no backup file. Git history is the backup per RESEARCH Open Question 2.
    - Do NOT collapse the two commits into one. D-04 mandates the migration be a dedicated commit. Append is a separate commit.
    - Do NOT use `flock` anywhere in the migration script. Use the binary itself (which uses mkdir-lock) for the append. The migration commit itself does not need a lock because no concurrent writer exists at migration time.
    - Do NOT introduce CRLF — write the file with LF endings only.
  </action>
  <verify>
    <automated>head -10 .planning/STATE.md | grep -q 'format_version: 0.1.0$' &amp;&amp; ! head -10 .planning/STATE.md | grep -q '0.1.0-interim' &amp;&amp; [ "$(awk '/^---$/{c++; next} c&gt;=2' .planning/STATE.md | wc -l | tr -d ' ')" -ge 11 ] &amp;&amp; awk '/^---$/{c++; next} c&gt;=2' .planning/STATE.md | while IFS= read -r line; do printf '%s' "$line" | jq empty || exit 1; done &amp;&amp; awk '/^---$/{c++; next} c&gt;=2' .planning/STATE.md | jq -r '.event' | while read e; do printf '%s\n' "$e" | jq -R -e 'test("^[a-z][a-z0-9]*(\\.[a-z][a-z0-9]*)+$")' &gt;/dev/null || exit 1; done &amp;&amp; awk '/^---$/{c++; next} c&gt;=2' .planning/STATE.md | jq -r '.actor' | sort -u | grep -q '^MAoDzi$' &amp;&amp; awk '/^---$/{c++; next} c&gt;=2' .planning/STATE.md | tail -1 | jq -r '.event' | grep -q '^phase.completed$' &amp;&amp; ! test -f .planning/STATE.md.bak &amp;&amp; git log --oneline -5 | grep -q 'migrate STATE.md' &amp;&amp; git log --oneline -5 | grep -q 'phase.completed for Phase 02'</automated>
  </verify>
  <acceptance_criteria>
    - .planning/STATE.md frontmatter has `format_version: 0.1.0` (not `0.1.0-interim`).
    - .planning/STATE.md body has ≥ 11 JSONL lines (10 historical + 1 phase.completed for Phase 2).
    - Every body line parses with `jq empty`.
    - Every body line's `event` field matches the D-09 shape regex.
    - All 10 historical lines preserve actor=MAoDzi.
    - The 11th line is the phase.completed event for Phase 2 with task=null, skill=null.
    - No `.bak` file exists.
    - Git log shows two distinct commits in this plan: one for migration (commit body contains pre-migration table), one for the phase.completed event.
    - `chantier state show` renders the migrated file without column collapse (verifiable manually).
    - `chantier validate-task` is not invoked here because Phase 2 has no skill-executed tasks to validate (Phase 3+ will exercise it). The validate-task functionality is proven by plan 02-05's bats suite, not by this task.
  </acceptance_criteria>
  <done>STATE.md migrated to JSONL in commit 1, format_version bumped to 0.1.0; phase.completed event appended via the binary in commit 2; git history shows both commits; no backup file committed; the round-trip dogfood proves the binary works on its own output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pre-migration STATE.md → migration script | source data is project-controlled; migration is a one-shot transformation |
| Migration commit → git history | git history IS the backup per RESEARCH Open Question 2 — no .bak file committed |
| Post-migration STATE.md → chantier state append | the freshly-migrated file must be writable by the binary's append path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06-DRIFT | Tampering | ADR 0002 inline schemas vs core/schemas/*.json | mitigate | Task 1 acceptance criteria require visual diff between inline ```json blocks and the canonical files. Plan 02-06 deliberately ships after plan 02-02 so the schema files are settled before ADR 0002 quotes them. Re-flagged as Open Question 5 in ADR 0002 itself (whether self-test should automate this drift check) — codified as known future work, not a v0.1 blocker. |
| T-02-06-MIGRATE | Tampering / DataLoss | STATE.md migration | mitigate | Validation gates: every line `jq empty`, every event passes D-09 regex, line count matches original row count + 1 (Phase 2 event). Commit body preserves the pre-migration table verbatim per RESEARCH Open Question 2. Git history is the rollback path. |
| T-02-06-NOLOCK | DoS / Race | migration write without lock | accept | At migration time no concurrent writer exists (Phase 2 is single-developer, single-session). The append-only contract starts AFTER the migration commit; before that, the file is being rewritten as a one-shot operation. |
| T-02-06-OPEN | InfoDisclosure | ADR 0002 re-flagging open questions | mitigate | Per CONTEXT.md §canonical_refs, ADR 0002 MUST explicitly re-flag the four ADR 0001 open questions. Acceptance criterion requires `chantier.lock`, `compaction`, `inputs_schema strictness`, and `composition` to appear in the §Open questions section. Failing to re-flag would silently imply they were resolved. |
| T-02-06-NFR-DOC | Spoofing / NFR-001 | harness identifiers in ADR 0002 | mitigate | Per RESEARCH §"Harness-identifier deny-list" line 442, the v0.1 deny-list applies repo-wide because no adapter exists yet. ADR 0002 is allowed ONE contextual quote-marked mention of the 6 names when discussing the skill.json enum carve-out. Reviewer must confirm no other occurrence. |
| T-02-06-COMMIT | Repudiation | git commit message integrity | mitigate | Commit messages are HEREDOC-templated (preserves newlines and special chars). Pre-migration table is quoted verbatim in commit 1 body. Co-Author trailer present per .claude/CLAUDE.md project conventions. |
| T-02-06-CRLF | Tampering | ADR 0002 line endings | mitigate | `.gitattributes` from plan 02-01 normalizes `* text=auto eol=lf`. ADR 0002 inherits the global rule; no CRLF can sneak in via a Windows editor. |
</threat_model>

<verification>
After both tasks:
- `docs/adr/0002-runtime-binary-and-state-format.md` exists with status Accepted.
- All 5 schemas inline as fenced ```json blocks.
- mkdir-mutex documented; flock only mentioned as the rejected alternative.
- Frontmatter subset profile and JSON Schema subset profile both formally documented.
- ADR 0001's four open questions re-flagged as STILL deferred.
- `.planning/STATE.md` migrated to JSONL with `format_version: 0.1.0`.
- 11+ JSONL lines, every one valid JSON, every one passes D-09 shape regex.
- Git log shows two distinct commits (migration, then phase.completed event).
- `chantier state show` renders the migrated file correctly.
- ROADMAP §Phase 2 success criterion 5 satisfied ("ADR 0002 published with status Accepted").
</verification>

<success_criteria>
- [ ] ADR 0002 exists with status Accepted.
- [ ] ADR 0002 codifies D-01..D-13 + the two REVISED Discretion items.
- [ ] Five schemas inline byte-for-byte from core/schemas/*.json.
- [ ] Frontmatter subset profile + JSON Schema subset profile both documented.
- [ ] mkdir-mutex pattern documented (NOT flock).
- [ ] ADR 0001's four open questions re-flagged.
- [ ] STATE.md migrated to JSONL with format_version 0.1.0 in a dedicated commit.
- [ ] Phase 2 phase.completed event appended in a SECOND dedicated commit.
- [ ] No .bak file committed; commit 1 body contains pre-migration table verbatim.
- [ ] Every migrated JSONL line passes D-09 event-shape regex.
- [ ] Phase 2 ROADMAP success criteria 1, 2, 3, 4, 5 all satisfied.
</success_criteria>

<output>
Create `.planning/phases/02-runtime-core/SUMMARY.md` (the phase-level summary, not a plan-level one) when done. Record:
- The four FRs (FR-001..FR-004) status (all complete).
- ADR 0002 git commit hash.
- STATE.md migration commit hash + phase.completed event commit hash.
- Confirmation that all 5 bats suites green AND chantier --self-test green AND shellcheck clean AND ROADMAP §Phase 2 success criteria 1–5 all met.
- Open issues (if any) deferred to Phase 3 or later.
- Note for Phase 3: the SKILL.md frontmatter schema is in place (skill.json) and validate-task gate 4 is wired — the four reference skills can now be authored against a real contract.
</output>

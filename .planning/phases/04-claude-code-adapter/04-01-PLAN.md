---
phase: 04-claude-code-adapter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/tests/adapter_isolation.bats
autonomous: true
requirements: [FR-008]
tags: [audit, bats, nfr-001, harness-isolation, posix-shell]
must_haves:
  truths:
    - "Running `bats core/tests/adapter_isolation.bats` from repo root exits 0 in the current tree (no deny-list violations exist today; the audit is preventative for D-10 carve-out)."
    - "Adding a forbidden token (e.g., `cursor`) to any file under `core/`, `skills/`, or `adapters/cursor/` makes the audit exit non-zero with the offending path printed to stderr."
    - "Adding `claude-code` inside `adapters/claude-code/` is allowed (audit stays green); adding `cursor` inside `adapters/claude-code/` is forbidden (audit goes red)."
    - "`adapters/claude-code/run-task.sh` (created by plan 02) carries `claude-code` substring legitimately and the audit accepts it under D-10."
    - "The audit script itself contains the deny-list literal but is exempted via the `HARNESS_DENY_LIST_CHECK` marker convention (per D-11 / A3) and never self-triggers."
  artifacts:
    - path: "core/tests/adapter_isolation.bats"
      provides: "Cross-tree harness identifier audit per D-09–D-12"
      contains: "HARNESS_DENY_LIST_CHECK"
      min_lines: 70
  key_links:
    - from: "core/tests/adapter_isolation.bats"
      to: "core/bin/chantier:687,912"
      via: "byte-identical deny-list literal"
      pattern: "mcp__\\|claude_ai_\\|@codebase\\|claude-code\\|cursor\\|codex-cli\\|copilot-cli\\|gemini-cli\\|opencode"
    - from: "core/tests/adapter_isolation.bats"
      to: "test_helper/bats-support/load + test_helper/bats-assert/load"
      via: "bats loader convention (skill_uniformity.bats analog)"
      pattern: "load 'test_helper/bats-(support|assert)/load'"
---

<objective>
Ship `core/tests/adapter_isolation.bats` — the cross-tree NFR-001 carve-out audit that proves `claude-code` and `mcp__claude_ai_` appear only inside `adapters/claude-code/`, and that no other deny-list token (`cursor`, `codex-cli`, `copilot-cli`, `gemini-cli`, `opencode`, bare `mcp__` outside `mcp__claude_ai_`, `@codebase`, bare `claude_ai_` outside `mcp__claude_ai_`) appears anywhere in `core/`, `skills/`, or sibling `adapters/*/` directories.

Purpose: Locks NFR-001 in test code per D-09/D-10/D-11/D-12 so future contributors cannot leak a harness name into a portable surface without the bats suite going red. Composes with the binary's `--self-test HARNESS_DENY_LIST_CHECK` self-scan (per D-11 — both coexist, different scopes). Independently shippable: produces a green test today (no `claude-code` substring exists in the audit scope yet) and stays green after plan 02 lands the adapter (path-only carve-out per D-10).

Output: One bats file (~80 lines), executable via `bats core/tests/adapter_isolation.bats` and rolled into the full suite via `bats core/tests/` (target: 72/0 after this plan lands, 73/0 after plan 03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-claude-code-adapter/04-CONTEXT.md
@.planning/phases/04-claude-code-adapter/04-RESEARCH.md
@.planning/phases/04-claude-code-adapter/04-PATTERNS.md
@docs/adr/0002-runtime-binary-and-state-format.md
@core/bin/chantier
@core/tests/skill_uniformity.bats
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 04-01-01: Author core/tests/adapter_isolation.bats with marker-based self-exemption</name>

  <read_first>
    - core/tests/skill_uniformity.bats (LINES 1-82 — primary analog; copy loaders, `cd "$BATS_TEST_DIRNAME/../.."`, POSIX `find -mindepth 1 -maxdepth 1 -type d` walk pattern, fail-on-violation idiom)
    - core/bin/chantier LINES 685-702 (gate 4 deny-list literal at line 687 with `HARNESS_DENY_LIST_CHECK` marker; the audit's `_full` pattern MUST be byte-identical to this line)
    - core/bin/chantier LINES 905-918 (--self-test self-scan; line 912 is the second byte-identical occurrence; line 913 `grep -v 'HARNESS_DENY_LIST_CHECK'` is the marker-filtering idiom)
    - .planning/phases/04-claude-code-adapter/04-PATTERNS.md LINES 251-343 (full pattern map for adapter_isolation.bats: loaders, `_full`/`_narrow` two-list approach, case-statement scope dispatch, POSIX find walk, error reporting)
    - .planning/phases/04-claude-code-adapter/04-RESEARCH.md LINES 490-563 (Pattern 6 — Path-Only NFR-001 Audit; A3 marker recommendation; Pitfall 4 macOS BSD find)
    - .planning/phases/04-claude-code-adapter/04-CONTEXT.md LINES 36-46 (D-09 audit shape, D-10 carve-out, D-11 exempt paths, D-12 bats-suite integration)
    - skills/test-driven-development/SKILL.md (verify frontmatter contains `harness_adapters:\n  - claude-code` — the sanctioned line that the audit must filter out before applying `_full` to SKILL.md files; pattern in 04-PATTERNS.md LINES 290-298)
  </read_first>

  <behavior>
    - Test 1 (vacuous-green today): With the current repo tree (no `adapters/claude-code/` files yet, plan 02 not landed), `bats core/tests/adapter_isolation.bats` exits 0. The four shipped SKILL.md files at `skills/{using-git-worktrees,test-driven-development,requesting-code-review,subagent-driven-development}/SKILL.md` each declare `harness_adapters:\n  - claude-code` in frontmatter; the audit filters that exact YAML line via `grep -vE '^[[:space:]]*-[[:space:]]*claude-code$'` before applying `_full`, so these files pass.
    - Test 2 (post-plan-02 green): After plan 02 lands `adapters/claude-code/run-task.sh` (which legitimately contains `claude-code` in its path comment, error prefix, and dispatch summary), the same bats run still exits 0 because the case-statement scope dispatch applies `_narrow` (which drops `claude-code` and `mcp__claude_ai_`) to every file under `adapters/claude-code/`.
    - Test 3 (sentinel red, manual probe): Writing `printf 'cursor\n' >> adapters/claude-code/run-task.sh` (after plan 02 lands) makes the audit exit non-zero and prints `adapter_isolation: deny-list violations:` followed by `adapters/claude-code/run-task.sh` to stderr. Reverting the write returns to green. This is the falsifiability proof; do not commit the sentinel.
    - Test 4 (self-exemption proof): The audit's own source file `core/tests/adapter_isolation.bats` contains every deny-list token as string literals, but every such line carries a trailing `# HARNESS_DENY_LIST_CHECK` comment and the audit's `find` output is piped through `grep -v 'HARNESS_DENY_LIST_CHECK'` before per-file grep. The bats file does not appear in `_violations` even when run in the current tree.
    - Test 5 (vendor-noise exemption): Files under `core/tests/test_helper/bats-support/` and `core/tests/test_helper/bats-assert/` (vendored submodules) are skipped via the same `grep -v` step including a `test_helper/` substring filter. The audit does not flag any third-party file inside the submodule trees.
  </behavior>

  <action>
    Create `core/tests/adapter_isolation.bats` (executable bit not required for bats files; bats reads them directly). Begin with the POSIX shell-prelude-style block comment from 04-PATTERNS.md LINES 330-344. Implement exactly one `@test` block — naming convention follows `skill_uniformity.bats` style: `@test "adapter_isolation: cross-tree NFR-001 carve-out — deny-list tokens absent outside adapters/claude-code/" { ... }`.

    Use the verbatim `setup()` block from 04-PATTERNS.md LINES 257-263 (loads `test_helper/bats-support/load` then `test_helper/bats-assert/load`; `cd "$BATS_TEST_DIRNAME/../.."` lands at repo root).

    Inside the `@test` block, declare `_full` and `_narrow` as documented in 04-PATTERNS.md LINES 268-275: each line carries a trailing `# HARNESS_DENY_LIST_CHECK` comment. The `_full` literal MUST be byte-identical to `core/bin/chantier:687` and `:912`: `mcp__|claude_ai_|@codebase|claude-code|cursor|codex-cli|copilot-cli|gemini-cli|opencode`. The `_narrow` literal MUST drop the two D-10 carve-out tokens and keep the others: `@codebase|cursor|codex-cli|copilot-cli|gemini-cli|opencode`.

    Implement the file walk via `find core skills adapters -type f 2>/dev/null | grep -v 'HARNESS_DENY_LIST_CHECK\|test_helper/' | sort` piped into a heredoc-fed `while IFS= read -r _file` loop (04-PATTERNS.md LINES 280-317). Inside the loop use a `case "$_file" in` dispatch with four arms in this order:
      1. `core/bin/chantier) continue ;;` — explicit exemption per D-11 (the binary has its own `HARNESS_DENY_LIST_CHECK` self-scan in `--self-test`).
      2. `core/tests/adapter_isolation.bats) continue ;;` — double-belt self-exemption in case the marker line filter misses a future edit. Required per RESEARCH A3.
      3. `skills/*/SKILL.md)` arm — `grep -vE '^[[:space:]]*-[[:space:]]*claude-code$' "$_file" | grep -qE "$_full"` to permit the sanctioned `harness_adapters:\n  - claude-code` frontmatter line while still scanning everything else.
      4. `adapters/claude-code/*)` arm — apply `_narrow` only (D-10 carve-out).
      5. Default `*)` arm — apply `_full`.

    Accumulate violations into `_violations` (newline-separated). After the loop, if `_violations` is non-empty, print `adapter_isolation: deny-list violations:` then `printf '%b' "$_violations"` to stderr and call `false` to fail the test (skill_uniformity.bats analog uses `fail`, but bats-support is not loaded here for the failure path — `false` suffices and matches the existing convention).

    Use POSIX find flags only (`-type f`, no `--include`, no `-regextype`) per Pitfall 4 (macOS BSD vs GNU). Use heredoc-fed loop per the analog (`done <<EOF $(find ...) EOF`). All error printfs go to stderr (`>&2`) with prefix `adapter_isolation:`.

    Do NOT include `claude-code` substring outside of: (a) the deny-list string literals (each carries `HARNESS_DENY_LIST_CHECK`), (b) the case-statement arm pattern `adapters/claude-code/*)` (each carries `HARNESS_DENY_LIST_CHECK` trailing comment per 04-PATTERNS.md LINES 299, 301), (c) the SKILL.md filter regex (carries `HARNESS_DENY_LIST_CHECK`), and (d) explanatory comments (each carries `HARNESS_DENY_LIST_CHECK`).

    No fenced code blocks; the bats file is one continuous shell-mode source file with a header block comment, a `setup()` function, and one `@test` block.

    Implements: D-09, D-10, D-11, D-12. Cites: per D-09 and D-11.
  </action>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && bats core/tests/adapter_isolation.bats</automated>
  </verify>

  <acceptance_criteria>
    - File `core/tests/adapter_isolation.bats` exists at the repo root.
    - `bats core/tests/adapter_isolation.bats` exits 0 against the current tree (vacuously green — no `claude-code` outside the four SKILL.md frontmatter lines, all of which are filtered).
    - `bats core/tests/` (full suite) exits 0 and reports 72 tests at the end of this plan (was 71 at Phase 3 close).
    - `grep -c 'HARNESS_DENY_LIST_CHECK' core/tests/adapter_isolation.bats` returns at least 6 (every deny-list literal, every `adapters/claude-code/*` case arm, every grep invocation against `$_full` or `$_narrow`).
    - `grep -v '^#' core/tests/adapter_isolation.bats | grep -v 'HARNESS_DENY_LIST_CHECK' | grep -cE 'mcp__|claude_ai_|@codebase|claude-code|cursor|codex-cli|copilot-cli|gemini-cli|opencode'` returns 0 — the marker convention fully exempts the audit from self-triggering.
    - The `_full` regex string is byte-identical to the line at `core/bin/chantier:687`: extract both via `sed -n "687p" core/bin/chantier | grep -oE '[a-z_@]+(\\|[a-z_@-]+)+'` and `grep -oE '[a-z_@]+(\\|[a-z_@-]+)+' core/tests/adapter_isolation.bats | head -1`; compare via `cmp` or `diff <(...) <(...)`.
    - Sentinel-red proof (manual, NOT committed): `printf 'cursor\n' >> adapters/claude-code/run-task.sh; bats core/tests/adapter_isolation.bats; echo exit=$?; git checkout adapters/claude-code/run-task.sh` — this is documented in the plan's SUMMARY only; do not run as part of the automated verify because plan 02 has not landed when this plan executes.
    - Shellcheck is NOT applicable to bats files (bats syntax is not pure shell); skip that check for this task.
  </acceptance_criteria>

  <done>
    `bats core/tests/adapter_isolation.bats` exits 0 in the current tree; the audit script self-exempts via the `HARNESS_DENY_LIST_CHECK` marker (verified by `grep -c` returning ≥6); the deny-list literal is byte-identical to `core/bin/chantier:687`; and the bats suite reports 72/0 with `bats core/tests/`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source tree → audit | Static check; the audit reads files from disk only, never executes them; no untrusted input crosses |
| Audit script ↔ binary self-scan | Both scan source for the same deny-list; drift between the two literals is a Phase 4 defect, not a threat |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Compliance (NFR-001) | adapter_isolation.bats audit script | mitigate | Path-only carve-out per D-09–D-12; `_full` / `_narrow` two-list approach; case-statement scope dispatch; D-10 carve-out limited to `adapters/claude-code/` directory contents only |
| T-04-01-02 | Tampering | adapter_isolation.bats self-source | mitigate | `HARNESS_DENY_LIST_CHECK` marker on every deny-list literal line; `find ... \| grep -v 'HARNESS_DENY_LIST_CHECK\|test_helper/'` filters the walk; double-belt `case "$_file" in core/tests/adapter_isolation.bats) continue ;;` for redundancy (A3 recommendation) |
| T-04-01-03 | Compliance (Pitfall 4) | POSIX find portability | mitigate | Only POSIX find flags used (`-type f`); no `--include`, no `-regextype`; matches the proven `skill_uniformity.bats` pattern that runs green on macOS BSD find and Linux GNU find |
| T-04-01-04 | Tampering (vendor noise) | core/tests/test_helper/bats-{support,assert} | mitigate | `grep -v 'test_helper/'` in the find pipeline filters out the vendored bats helper submodules so any deny-list-like tokens in third-party README/docs do not register as Chantier-source violations |
| T-04-01-SC | Tampering | npm/pip/cargo installs | accept | N/A — this plan installs no packages (RESEARCH §Package Legitimacy Audit confirms N/A for Phase 4) |
</threat_model>

<verification>
- `bats core/tests/adapter_isolation.bats` exits 0 in the current tree
- `bats core/tests/` reports 72/0 at plan close (was 71 at Phase 3 close)
- `grep -c 'HARNESS_DENY_LIST_CHECK' core/tests/adapter_isolation.bats` returns ≥6
- `grep -v '^#' core/tests/adapter_isolation.bats | grep -v 'HARNESS_DENY_LIST_CHECK' | grep -cE 'mcp__|claude_ai_|@codebase|claude-code|cursor|codex-cli|copilot-cli|gemini-cli|opencode'` returns 0
- Deny-list literal byte-identical to `core/bin/chantier:687` and `:912`
- `chantier --self-test` continues to exit 0 (Phase 2 baseline; not regressed)
</verification>

<success_criteria>
- The audit harness is in place before the adapter ships (Wave 1 parallel with plan 02; the audit goes from "vacuously green" to "actively-enforcing carve-out green" when plan 02 lands)
- D-09, D-10, D-11, D-12 fully implemented in code (per `must_haves.truths`)
- ROADMAP SC#4 ("only file outside docs containing claude-code, verified by grep") moves from claim to enforcement
- The bats suite stays green throughout — no regression on the 71 Phase 3 tests
</success_criteria>

<output>
Create `.planning/phases/04-claude-code-adapter/04-01-SUMMARY.md` when done. Include: byte-identical proof of `_full` vs `core/bin/chantier:687`; the `HARNESS_DENY_LIST_CHECK` marker count; bats suite totals (72/0 expected); a one-line sentinel-red probe demonstration in the SUMMARY's "Verification Results" section (DO NOT commit the sentinel — restore the file before commit).
</output>
</content>
</invoke>
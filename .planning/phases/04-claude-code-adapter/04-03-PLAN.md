---
phase: 04-claude-code-adapter
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - core/tests/adapter_claude_code_e2e.bats
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [FR-008]
tags: [e2e, bats, dispatch, fr-008, phase-close, dogfood]
must_haves:
  truths:
    - "`bats core/tests/adapter_claude_code_e2e.bats` exits 0 — the adapter dispatches the `test-driven-development` skill via the `CHANTIER_CLAUDE_BIN` stub inside a `git worktree add`-created worktree, the skill's run.sh writes `output.md` + `output.json`, and `chantier validate-task` exits green against the result (per D-13)."
    - "STATE.md after the green e2e run contains exactly three event rows for the test task: `task.started` (adapter), `skill.completed` (skill per Phase 3 D-04), `task.completed` (adapter), in that order — verified by `grep -c` returning 3 across the three event-name regexes (per D-03)."
    - "The dossier at `$WORKTREE/.chantier/dossiers/t1/` contains `inputs.yml`, `env.sh`, `reads/` (empty), `upstream/` (empty), and `skill/SKILL.md` + `skill/PRESSURE.md` + `skill/run.sh` after the green run, and is preserved (not deleted) per D-08 — the test asserts directory existence after assertions pass."
    - "The D-13 measurable signals from `output.json` are present: `red_exit_code = 1`, `red_step_timestamp` matches `^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z$` (ISO-8601 UTC second-precision), `invariants_applied | length >= 4`."
    - "The stub at `$BATS_TEST_TMPDIR/stub/claude` is inline in setup() (not extracted to a fixture file per 04-PATTERNS.md LINE 511 recommendation), accepts `-p|--print` argv, parses the dossier path from the prompt via `sed -n 's|.*/.chantier/dossiers/.*|...|p'`, cd's to the dossier, sources env.sh, execs `sh ./skill/run.sh`, and propagates the exit code — emulating real `claude -p` deterministically and offline per D-15 + NFR-004."
    - "The full bats suite (`bats core/tests/`) reports 73/0 at plan close (was 71 at Phase 3 close; +1 from plan 01 audit; +1 from this plan's e2e)."
    - "ROADMAP.md Phase 4 entry shows all 4 success criteria checked, status Complete, completion date filled in. The `Plans:` block lists `04-01-PLAN.md`, `04-02-PLAN.md`, `04-03-PLAN.md` with summaries and `[x]` checkboxes."
    - "STATE.md records a `phase.completed` event for Phase 4 with refs pointing to `04-SUMMARY.md` and `adapters/claude-code/run-task.sh`."
  artifacts:
    - path: "core/tests/adapter_claude_code_e2e.bats"
      provides: "End-to-end proof of FR-008 via test-driven-development skill (D-13, D-14, D-15)"
      min_lines: 150
      contains: "test-driven-development"
    - path: ".planning/phases/04-claude-code-adapter/04-SUMMARY.md"
      provides: "Phase 4 completion record with verification results and gate-coverage proof"
      min_lines: 60
    - path: ".planning/ROADMAP.md"
      provides: "Phase 4 marked complete; plans listed"
      contains: "[x] **Phase 4"
  key_links:
    - from: "core/tests/adapter_claude_code_e2e.bats"
      to: "adapters/claude-code/run-task.sh"
      via: "run $ADAPTER \"$TASK\" inside the test worktree"
      pattern: "run .ADAPTER. .TASK."
    - from: "core/tests/adapter_claude_code_e2e.bats"
      to: "$WORKTREE/.planning/STATE.md"
      via: "grep -cE for three D-03 event names"
      pattern: "task\\.(started|completed|failed)|skill\\.completed"
    - from: "core/tests/adapter_claude_code_e2e.bats"
      to: "$BATS_TEST_TMPDIR/stub/claude"
      via: "CHANTIER_CLAUDE_BIN environment variable override (D-15)"
      pattern: "CHANTIER_CLAUDE_BIN="
---

<objective>
Ship `core/tests/adapter_claude_code_e2e.bats` — the end-to-end proof that `adapters/claude-code/run-task.sh` dispatches the `test-driven-development` skill via the `CHANTIER_CLAUDE_BIN` stub inside a real `git worktree add`-created worktree, exits 0, emits the three D-03 STATE.md events in order, and passes all five `chantier validate-task` gates (per D-13/D-14/D-15). Then close the phase: write `04-SUMMARY.md`, update `ROADMAP.md` with Phase 4 marked complete, and append `phase.completed` to STATE.md via the binary.

Purpose: Locks ROADMAP Phase 4 success criteria 1-4 in test code (the adapter staged a dossier; the adapter dispatched a subagent that read SKILL.md and execed run.sh; one e2e invocation works; the audit verifies harness-name carve-out). Closes Phase 4 by recording the verification results and unblocking Phase 5 (dogfood E2E).

Output: One bats file (~180 lines), one SUMMARY.md (~80 lines), updated ROADMAP.md, one new STATE.md event row. After this plan: `bats core/tests/` reports 73/0; the binary `--self-test` exits 0; the adapter audit accepts the live tree with the new `adapters/claude-code/run-task.sh` content; FR-008 is fully satisfied.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-claude-code-adapter/04-CONTEXT.md
@.planning/phases/04-claude-code-adapter/04-RESEARCH.md
@.planning/phases/04-claude-code-adapter/04-PATTERNS.md
@.planning/phases/04-claude-code-adapter/04-VALIDATION.md
@.planning/phases/04-claude-code-adapter/04-01-SUMMARY.md
@.planning/phases/04-claude-code-adapter/04-02-SUMMARY.md
@adapters/claude-code/run-task.sh
@core/tests/skill_test_driven_development_e2e.bats
@core/tests/fixtures/skills/test-driven-development/dossier/inputs.yml
@skills/test-driven-development/SKILL.md
@skills/test-driven-development/run.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 04-03-01: Author core/tests/adapter_claude_code_e2e.bats with worktree setup + inline stub + D-13 assertions</name>

  <read_first>
    - core/tests/skill_test_driven_development_e2e.bats (FULL FILE — primary analog per D-14; the new file is a structural mirror with the adapter inserted in the middle. Copy verbatim: shell-prelude block comment, `setup()` loaders, REPO_ROOT canonicalization with `pwd -P`, PATH-prepend of `$REPO_ROOT/core/bin`, TMPHOME setup with macOS `/private/var` canonicalization comment, `make_plan` helper structure with EOF_SW/EOF_ACC distinct heredoc tokens, jq -e assertions for `red_step_timestamp` ISO-8601 regex + `red_exit_code = 1` + `invariants_applied | length >= 4`, validate-task gate assertion. Adapt for Phase 4 additions documented below.)
    - core/tests/skill_uniformity.bats LINES 1-30 (secondary analog for `setup()` cd-to-repo-root pattern and POSIX find walk — used as cross-reference for the adapter-isolation companion check)
    - adapters/claude-code/run-task.sh (the artifact under test; READ the final adapter source to know which env vars it expects, where it writes the dossier, which event names it emits, how it discovers the PLAN.md task block, and which exit codes it produces — the test asserts the adapter's contract, not the spec's)
    - .planning/phases/04-claude-code-adapter/04-PATTERNS.md LINES 347-487 (full pattern map for adapter_claude_code_e2e.bats: setup loaders, worktree creation, inline stub, make_plan with inputs: block, skill body copy, jq assertions, three-event grep, validate-task gate)
    - .planning/phases/04-claude-code-adapter/04-RESEARCH.md LINES 716-1008 (Code Example 1 the adapter is built from; Code Example 2 lines 867-1008 is the e2e skeleton — this plan implements it)
    - .planning/phases/04-claude-code-adapter/04-CONTEXT.md LINES 41-46 (D-13 fixture choice; D-14 file location; D-15 stub policy)
    - core/tests/fixtures/skills/test-driven-development/dossier/inputs.yml (4-line fixture — the four scalars `target_file`, `test_framework`, `phase: red`, `test_command: "false"` are embedded into the PLAN.md task block via `make_plan`, and the adapter's awk extracts them back into `$DOSSIER/inputs.yml`; the round-trip is part of the test)
    - skills/test-driven-development/SKILL.md (the body the subagent reads; verifies `harness_adapters: [claude-code]` declaration is honest per Phase 3 D-14)
    - skills/test-driven-development/run.sh LINES 219-228 (the `chantier state append -e skill.completed` call the skill emits; the test counts this as the middle event in the D-03 three-event sequence)
  </read_first>

  <behavior>
    - Test 1 (green happy path — D-13 measurable signal): With the stub set as `CHANTIER_CLAUDE_BIN`, invoking `$ADAPTER t1` from inside a freshly-created git worktree containing the `test-driven-development` skill and a PLAN.md task block with `phase: red` + `test_command: "false"` exits 0. The dossier exists at `$WORKTREE/.chantier/dossiers/t1/`. The task dir at `$WORKTREE/.planning/phases/test-phase/tasks/t1/` has `output.md` and `output.json`. `jq -r '.red_exit_code' output.json` returns `1`. `jq -e '.red_step_timestamp | test("^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z$")'` exits 0. `jq -e '.invariants_applied | length >= 4'` exits 0.
    - Test 2 (three-event signal — D-03): After Test 1 completes, `grep -cE '"event":"task\.started"' $WORKTREE/.planning/STATE.md` returns 1, `grep -cE '"event":"skill\.completed"' ...` returns 1, `grep -cE '"event":"task\.completed"' ...` returns 1. The combined `grep -cE` for all three returns 3.
    - Test 3 (dossier preservation — D-08): After Test 1 completes, `[ -d "$WORKTREE/.chantier/dossiers/t1" ]` is true, `[ -f "$WORKTREE/.chantier/dossiers/t1/env.sh" ]` is true, `[ -f "$WORKTREE/.chantier/dossiers/t1/skill/SKILL.md" ]` is true. The dossier was NOT deleted on success.
    - Test 4 (env.sh contract — D-07 layer 1): `grep -E 'CHANTIER_TASK_ID="t1"' $WORKTREE/.chantier/dossiers/t1/env.sh` matches; same for `CHANTIER_PHASE=` and `CHANTIER_WORKTREE=`. All three exports present.
    - Test 5 (validate-task gates 1-5 — D-04 + ADR 0001): `cd $WORKTREE && $CHANTIER validate-task t1` exits 0 (the adapter has already run it, but re-running confirms gate idempotence and proves there's no adapter-masking bug).
  </behavior>

  <action>
    Create `core/tests/adapter_claude_code_e2e.bats`. Begin with the analog's shell-prelude block comment style (04-PATTERNS.md LINES 562-566 + skill_test_driven_development_e2e.bats:1-17), adapted: cite D-13, D-14, D-15, and link to `skill_test_driven_development_e2e.bats` as the mirror per D-14.

    **setup() function:** Mirror `skill_test_driven_development_e2e.bats:19-52` byte-for-byte for the loaders + REPO_ROOT canonicalization + PATH-prepend + TMPHOME with `pwd -P`. Add three Phase 4 additions:

    1. `export ADAPTER="$REPO_ROOT/adapters/claude-code/run-task.sh"` after the `REPO_ROOT` export.
    2. **Worktree creation** (per D-05 — operator pre-creates, test acts as operator; pattern from 04-PATTERNS.md LINES 376-391): `git init -q "$TMPHOME"`, `cd "$TMPHOME"`, `git config user.email "test@chantier" && git config user.name "test"`, create `.planning/STATE.md` with the frontmatter `format_version: 0.1.0` (required by `chantier state append` Phase 2 contract), `git add -A && git commit -q -m "initial"`. Then `WORKTREE_DIR="$BATS_TEST_TMPDIR/wt"; git worktree add -q "$WORKTREE_DIR" -b test-branch; export WORKTREE="$WORKTREE_DIR"`. The worktree is the operator's worktree; the adapter MUST be invoked with CWD inside this directory.
    3. **CHANTIER_CLAUDE_BIN stub** (inline per 04-PATTERNS.md LINE 511 + Pattern 5; LINES 397-418): create `$BATS_TEST_TMPDIR/stub/claude` via quoted heredoc `<<'STUB_EOF'`. The stub is ~12 lines of POSIX `set -eu` shell: parse argv with a `while [ $# -gt 0 ]` loop matching `-p|--print) shift; PROMPT="$1" ;;` and `*) ;;` for all other flags; extract dossier path via `DOSSIER=$(printf '%s\n' "$PROMPT" | sed -n 's|.*\(/.*\.chantier/dossiers/[^ "]*\).*|\1|p' | head -1)`; bail with `printf 'stub: no dossier in prompt\n' >&2; exit 1` if empty; print `subagent (stub): cd %s\n` (per D-15 trace fidelity); `cd "$DOSSIER" && . ./env.sh && sh ./skill/run.sh; exit $?`. Then `chmod +x` the stub and `export CHANTIER_CLAUDE_BIN="$BATS_TEST_TMPDIR/stub/claude"`. The stub file itself contains the substring `claude` (it IS called `claude`), but it lives under `$BATS_TEST_TMPDIR` outside the audit scope — no D-09 violation.

    **make_plan() helper:** Copy `skill_test_driven_development_e2e.bats:61-104` byte-for-byte BUT modify the heredoc to write the PLAN.md under `$WORKTREE/.planning/phases/test-phase/PLAN.md` (NOT `$TMPHOME/...`) AND add an `inputs:` block embedded in the task YAML. The helper now accepts five args: `$1 task id, $2 state_writes list, $3 acceptance list, $4 skill id, $5 inputs YAML body`. The `inputs` body for the Phase 4 e2e is exactly:
    ```
    inputs:
      target_file: "core/bin/chantier"
      test_framework: "bats"
      phase: "red"
      test_command: "false"
    ```
    (these four scalars match the Phase 3 fixture `inputs.yml` byte-for-byte). The adapter's preflight extracts this block via awk and writes it into `$WORKTREE/.chantier/dossiers/t1/inputs.yml` — the round-trip is part of the test.

    **Single @test block:** Name follows analog convention: `@test "claude-code adapter dispatches test-driven-development red phase end-to-end via CHANTIER_CLAUDE_BIN stub"`. Body:
    - `TASK="t1"; SKILL="test-driven-development"`.
    - Copy skill body into `$WORKTREE/skills/$SKILL/`: `mkdir -p`, then three `cp` calls (SKILL.md, PRESSURE.md, run.sh), `chmod +x` the run.sh (mirrors analog lines 114-118 with `$TMPHOME` → `$WORKTREE`).
    - Build the PLAN.md via `make_plan` with state_writes `".planning/phases/test-phase/tasks/$TASK/"`, acceptance bullets `ACC1="A failing test was observed before any production code was written for this task."` and `ACC2="After the production change, the same test command exits zero."` (byte-identical to what the skill's run.sh writes into output.md per `skills/test-driven-development/run.sh:194-196`; gate 5 substring-matches these), skill `$SKILL`, and the four-line `inputs:` body above.
    - `cd "$WORKTREE"` (the adapter MUST be invoked from inside the worktree per D-05; `git rev-parse --show-toplevel` will return `$WORKTREE`).
    - `run "$ADAPTER" "$TASK"`. On non-zero status, print adapter output and `cat $WORKTREE/.planning/STATE.md` to stderr for debugging (mirrors analog lines 166-168).
    - Assert `[ "$status" -eq 0 ]`.
    - Dossier-existence assertions: `[ -d "$WORKTREE/.chantier/dossiers/$TASK" ]`, `[ -f "$WORKTREE/.chantier/dossiers/$TASK/env.sh" ]`, `[ -f "$WORKTREE/.chantier/dossiers/$TASK/inputs.yml" ]`, `[ -f "$WORKTREE/.chantier/dossiers/$TASK/skill/SKILL.md" ]`, `[ -f "$WORKTREE/.chantier/dossiers/$TASK/skill/run.sh" ]`.
    - env.sh content assertions: `grep -qE '^CHANTIER_TASK_ID="t1"$' "$WORKTREE/.chantier/dossiers/$TASK/env.sh"`, same for `CHANTIER_PHASE=` (matches `test-phase`) and `CHANTIER_WORKTREE=` (matches `$WORKTREE` literal).
    - Outputs-exist assertions: `TASK_DIR="$WORKTREE/.planning/phases/test-phase/tasks/$TASK"`, `[ -f "$TASK_DIR/output.md" ]`, `[ -f "$TASK_DIR/output.json" ]`.
    - D-13 measurable signals (copy jq -e calls verbatim from analog lines 144-157):
      - `run jq -e '.red_step_timestamp | type == "string"' "$TASK_DIR/output.json"`; `[ "$status" -eq 0 ]`.
      - `run jq -e '.red_exit_code | type == "number"' ...`; `[ "$status" -eq 0 ]`.
      - `_red_exit=$(jq -r '.red_exit_code' "$TASK_DIR/output.json"); [ "$_red_exit" -eq 1 ]`.
      - `run jq -e '.red_step_timestamp | test("^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z$")' ...`; `[ "$status" -eq 0 ]`.
      - `run jq -e '.invariants_applied | length >= 4' ...`; `[ "$status" -eq 0 ]`.
    - D-03 three-event signal (per 04-PATTERNS.md LINES 467-472): split into three separate assertions so a failure surfaces the missing event:
      - `_started=$(grep -cE '"event":"task\.started"' "$WORKTREE/.planning/STATE.md"); [ "$_started" -eq 1 ]`.
      - `_skill=$(grep -cE '"event":"skill\.completed"' "$WORKTREE/.planning/STATE.md"); [ "$_skill" -eq 1 ]`.
      - `_completed=$(grep -cE '"event":"task\.completed"' "$WORKTREE/.planning/STATE.md"); [ "$_completed" -eq 1 ]`.
    - validate-task gate idempotence assertion (defends against adapter masking a red gate per 04-PATTERNS.md LINES 478-486): `cd "$WORKTREE"; run "$CHANTIER" validate-task "$TASK"`; on non-zero, print output to stderr; `[ "$status" -eq 0 ]`.

    No teardown needed — bats clears `$BATS_TEST_TMPDIR` automatically. Do NOT delete the dossier in the test (D-08 preservation; the bats tmpdir cleanup is the eventual cleanup).

    **Harness identifier hygiene:** The file path `core/tests/adapter_claude_code_e2e.bats` contains `claude_code` (underscore, NOT hyphen — does NOT match the deny-list pattern `claude-code`). The file body MUST NOT contain `claude-code` (hyphenated form) outside of: (a) the variable name `CHANTIER_CLAUDE_BIN` (does not contain `claude-code` substring), (b) the literal string `adapters/claude-code/` when constructing `$ADAPTER` path. Verify with `grep -nE 'claude-code|mcp__|cursor|codex-cli|copilot-cli|gemini-cli|opencode' core/tests/adapter_claude_code_e2e.bats` returning at most one match (the path literal). If the count is >1, the test would self-trigger the adapter_isolation.bats audit; rewrite to use shell variable concatenation or move the path construction to a helper.

    Implements: D-13, D-14, D-15. Cites: per D-13 (red-phase fixture), per D-14 (file location), per D-15 (stub policy). Resolves Open Questions A8 (PLAN.md inputs: block is the adapter's source for inputs.yml — confirmed and verified by the round-trip), A9 (Phase 3 fixture scalars copy verbatim into PLAN.md inputs: block — confirmed by use), A10 (test-driven-development skill is structurally sufficient — confirmed by use).
  </action>

  <verify>
    <automated>bats core/tests/adapter_claude_code_e2e.bats && bats core/tests/adapter_isolation.bats && bats core/tests/</automated>
  </verify>

  <acceptance_criteria>
    - File `core/tests/adapter_claude_code_e2e.bats` exists.
    - `bats core/tests/adapter_claude_code_e2e.bats` exits 0.
    - `bats core/tests/adapter_isolation.bats` exits 0 (the e2e file does not contain `claude-code` substring outside the sanctioned variable name or path literal; verified by `grep -E 'claude-code'` returning ≤1 match).
    - `bats core/tests/` exits 0 and reports 73/0 (was 72/0 after plan 01).
    - `wc -l < core/tests/adapter_claude_code_e2e.bats` is between 150 and 250.
    - `grep -cE 'load .test_helper/bats-(support|assert)/load.' core/tests/adapter_claude_code_e2e.bats` returns 2 (analog convention).
    - `grep -cE 'git worktree add' core/tests/adapter_claude_code_e2e.bats` returns ≥1 (D-05 worktree pre-creation).
    - `grep -cE 'CHANTIER_CLAUDE_BIN' core/tests/adapter_claude_code_e2e.bats` returns ≥2 (one stub creation, one export; D-15).
    - `grep -cE '<<.STUB_EOF.' core/tests/adapter_claude_code_e2e.bats` returns 1 (quoted heredoc for the stub body; protects against expansion of the stub's `$1`, `$PROMPT`, etc.).
    - `grep -cE 'jq -e' core/tests/adapter_claude_code_e2e.bats` returns ≥5 (the five D-13 measurable-signal assertions).
    - `grep -cE 'task\.started|skill\.completed|task\.completed' core/tests/adapter_claude_code_e2e.bats` returns ≥3 (three separate event assertions).
    - `grep -cE 'red_exit_code|red_step_timestamp|invariants_applied' core/tests/adapter_claude_code_e2e.bats` returns ≥3.
    - `grep -E 'claude-code' core/tests/adapter_claude_code_e2e.bats | wc -l` returns ≤1 (the audit-safe count; the adapter path literal may appear at most once).
  </acceptance_criteria>

  <done>
    The e2e exits 0 against the live adapter from plan 02; all 5 D-13 jq assertions pass; the three D-03 events are present in STATE.md exactly once each; the dossier is preserved; the round-trip from PLAN.md `inputs:` block to dossier `inputs.yml` is verified by the skill's successful run; `bats core/tests/` reports 73/0; `adapter_isolation.bats` continues to accept the live tree.
  </done>
</task>

<task type="auto">
  <name>Task 04-03-02: Phase close — write 04-SUMMARY.md, update ROADMAP.md, append phase.completed event to STATE.md</name>

  <read_first>
    - .planning/phases/03-skill-library/03-SUMMARY.md (FULL FILE — analog format for phase-close SUMMARY: structure, sections, verification-results table, handoff notes)
    - .planning/ROADMAP.md LINES 110-127 (current Phase 4 stub — `Plans: TBD`, success criteria 1-4 unchecked; this task updates the entry to show all 4 SC checked, plans listed, completion date, status Complete)
    - .planning/phases/04-claude-code-adapter/04-01-SUMMARY.md (plan 01 close — must exist when this task runs; cite its `_full` byte-identity proof, marker count, suite total 72/0)
    - .planning/phases/04-claude-code-adapter/04-02-SUMMARY.md (plan 02 close — must exist when this task runs; cite shellcheck-clean confirmation, wc -l, all grep gates, Open Questions resolutions, Pitfalls mitigations)
    - .planning/STATE.md (the JSONL events log — this task appends one new event row via `chantier state append`; the binary expects the file to exist at `.planning/STATE.md` with `format_version: 0.1.0` frontmatter)
    - core/bin/chantier LINES 159-209 (state_append getopts contract — for the manual append command syntax)
    - .planning/PROJECT.md (read the v0.1.0 success criterion 4 — "Claude Code harness adapter works end-to-end"; cite this in SUMMARY.md as the satisfied PROJECT-level criterion)
  </read_first>

  <action>
    **Sub-task A — Write `.planning/phases/04-claude-code-adapter/04-SUMMARY.md`:**

    Mirror the structure of `.planning/phases/03-skill-library/03-SUMMARY.md` (frontmatter with phase identifier, completion date, status; sections: Goal, Plans Executed, Verification Results, Outstanding Items, Handoff Notes for Next Phase).

    Required content:
    - **Frontmatter:** `phase: 04-claude-code-adapter`, `status: complete`, `completed: 2026-MM-DD` (substitute real date at execution time), `plans: ["04-01", "04-02", "04-03"]`.
    - **Goal section:** Quote the ROADMAP Phase 4 goal verbatim (lines 112-113).
    - **Plans Executed:** Brief one-line description of each plan, link to its SUMMARY.md.
    - **Verification Results:** Table with one row per ROADMAP SC#1-4 + each NFR touched. Columns: Criterion, Evidence, Command, Status. Reference SC#1 (`run-task.sh stages dossier`) via `core/tests/adapter_claude_code_e2e.bats` dossier-existence assertions; SC#2 (`adapter dispatches subagent that reads SKILL.md and execs run.sh`) via the same e2e's successful jq assertions on output.json (proof the skill's run.sh executed); SC#3 (`one e2e task invocation works in a worktree`) via the e2e's `$ADAPTER t1` exit 0 with `WORKTREE` created via `git worktree add`; SC#4 (`adapter is only file outside docs containing claude-code, verified by grep`) via `bats core/tests/adapter_isolation.bats` exit 0.
    - **Verification Results NFR rows:** NFR-001 (`adapter_isolation.bats` 0 violations), NFR-002 (`shellcheck --shell=sh adapters/claude-code/run-task.sh` exit 0), NFR-004 (e2e uses CHANTIER_CLAUDE_BIN stub, no network), NFR-005 (English-only artifacts), NFR-006 (MIT copyright header on `run-task.sh` and bats files).
    - **Bats suite totals:** "Going into Phase 4: 71/0 (Phase 3 close). After Phase 4: 73/0. New tests: `adapter_isolation.bats` (1 @test), `adapter_claude_code_e2e.bats` (1 @test)." Include the literal `bats core/tests/` output footer line at completion time.
    - **Resolved Discretion Items:** Enumerate the 10 Claude's Discretion items from CONTEXT.md plus the 5 RESEARCH Open Questions, with the chosen resolution and a one-line rationale + line reference into `adapters/claude-code/run-task.sh` or `core/tests/adapter_claude_code_e2e.bats`.
    - **Handoff Notes for Phase 5:** A short list summarizing what Phase 5 (dogfood E2E) inherits from Phase 4: the adapter exists and is invocable; `chantier state append` + `validate-task` + adapter compose; the bats suite is at 73/0; the four Phase 3 skills are testably honest (any of them works via the adapter, only TDD is exercised in Phase 4's e2e); deferred items (concurrency lock, transcript on real-claude path, `chantier task-lookup` subcommand, second-adapter audit symmetry) carried into Phase 5+ backlog.

    File size target: ≥60 lines.

    **Sub-task B — Update `.planning/ROADMAP.md`:**

    Edit the `### Phase 4: Claude Code adapter` section (currently at lines 110-127):
    - Change the heading checkbox from `- [ ] **Phase 4** ...` to `- [x] **Phase 4: Claude Code adapter** - Build adapters/claude-code/ that stages dossiers and dispatches subagents per ADR 0001.` in the top-of-file phase summary list (around line 19).
    - Each of the 4 success criteria bullets in the Phase 4 detail section should be prefixed `[x]` and have an Evidence clause appended (mirrors how Phase 3 SC bullets in lines 85-89 are written): `[x] adapters/claude-code/run-task.sh stages ... (Evidence: core/tests/adapter_claude_code_e2e.bats green; bats core/tests/ 73/0)` etc.
    - Replace `**Plans**: TBD` with `**Plans**: 3 plans (complete)`.
    - Replace the `Plans:\n\n- [ ] 04-01: TBD ...` block with:
      ```
      Plans:
      
      - [x] 04-01-PLAN.md — adapter_isolation.bats cross-tree NFR-001 audit (D-09–D-12)
      - [x] 04-02-PLAN.md — adapters/claude-code/run-task.sh adapter + README.md (D-01–D-08, D-15, D-16)
      - [x] 04-03-PLAN.md — adapter_claude_code_e2e.bats e2e + phase close (D-13, D-14)
      ```
    - Update the Progress table (around line 152-158) row for Phase 4: change `0/TBD | Not started | -` to `3/3 | Complete | 2026-MM-DD` (substitute actual date).

    **Sub-task C — Append `phase.completed` event to STATE.md via the binary:**

    From the repo root (where `.planning/STATE.md` lives), run:
    ```
    chantier state append \
        -e phase.completed \
        -t null \
        -s null \
        -m "Phase 04-claude-code-adapter complete: FR-008 satisfied; adapter ships at adapters/claude-code/run-task.sh; bats suite at 73/0; ROADMAP SC#1-4 all green" \
        -r .planning/phases/04-claude-code-adapter/04-SUMMARY.md \
        -r adapters/claude-code/run-task.sh \
        -r core/tests/adapter_claude_code_e2e.bats \
        -r core/tests/adapter_isolation.bats
    ```
    Use the binary (not direct file edit — NFR-003 STATE.md append-only is enforced by the binary's mkdir-mutex). The event name `phase.completed` matches the ADR 0002 event-name regex and the convention from Phase 2/3 closes (see `.planning/STATE.md` lines 31, 41).

    Implements: closes Phase 4. No new D-NN; this task is the ceremonial phase-close mirroring Phase 3 plan 03-06.
  </action>

  <verify>
    <automated>test -f .planning/phases/04-claude-code-adapter/04-SUMMARY.md && grep -q '\[x\] \*\*Phase 4' .planning/ROADMAP.md && grep -qE '"event":"phase\.completed".*"04-claude-code-adapter|adapters/claude-code' .planning/STATE.md && bats core/tests/</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/phases/04-claude-code-adapter/04-SUMMARY.md` exists, ≥60 lines, contains a Verification Results table with rows for all 4 ROADMAP SC + NFR-001/002/004/005/006.
    - `wc -l < .planning/phases/04-claude-code-adapter/04-SUMMARY.md` ≥ 60.
    - `grep -cE 'D-0[1-9]|D-1[0-6]' .planning/phases/04-claude-code-adapter/04-SUMMARY.md` returns ≥16 (each D-NN cited in the resolved-discretion-items section).
    - `.planning/ROADMAP.md` Phase 4 heading shows `[x]` (both at the top-of-file summary line ~19 and in the detail section).
    - `.planning/ROADMAP.md` Phase 4 detail section has 4 `[x]` success-criteria bullets each with an `Evidence:` clause.
    - `.planning/ROADMAP.md` `Plans:` block for Phase 4 lists exactly three plans, all `[x]`.
    - `.planning/ROADMAP.md` Progress table row for Phase 4 shows `3/3 | Complete | <date>`.
    - `.planning/STATE.md` contains one new JSONL row with `"event":"phase.completed"` and `04-claude-code-adapter` in the summary or refs. The row is the last line of the file.
    - `bats core/tests/` continues to exit 0 with 73/0.
    - `chantier validate-task` smoke test from earlier phases still works (regression check via existing skill e2e tests).
  </acceptance_criteria>

  <done>
    Phase 4 closure artifacts ship: SUMMARY.md with full verification results and resolved-discretion table, ROADMAP.md updated with [x] checkmarks and Plans block, STATE.md has the `phase.completed` event appended via the binary. The repo state matches what Phase 5 will inherit: bats suite 73/0, adapter shippable, FR-008 satisfied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → adapter | Bats test invokes adapter as subprocess inside an isolated TMPHOME + worktree; no live STATE.md mutation outside the test scope |
| Stub `claude` → skill run.sh | Stub does no inspection or transformation — it cd's, sources env.sh, execs run.sh; the skill is the only locus of trust |
| Phase close → STATE.md | `chantier state append` invocation is the only mutation; NFR-003 append-only enforced by the binary's mkdir-mutex |
| ROADMAP.md update | Plain Markdown edit; no executable content; reviewed by SUMMARY.md table for correctness |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Tampering | Inline stub at `$BATS_TEST_TMPDIR/stub/claude` | mitigate | Stub lives outside repo (under bats tmpdir); contains `claude` substring but is OUT OF the adapter_isolation.bats audit scope by virtue of path (D-11 scope is `core/`, `skills/`, `adapters/*/` only); cannot pollute the source tree |
| T-04-03-02 | Tampering | Quoted heredoc for stub body | mitigate | `<<'STUB_EOF'` (quoted delimiter) prevents bats shell from expanding stub's `$1`, `$PROMPT`, `$DOSSIER` at test setup time; the variables are expanded only when the stub itself runs (Pitfall 1 + Phase 3 precedent) |
| T-04-03-03 | Information disclosure | Test STATE.md / dossier preserved in BATS_TEST_TMPDIR | accept | Bats clears tmpdir between tests; no real secrets used (fixture data is checked-in); ephemeral by design |
| T-04-03-04 | Compliance (NFR-001) | E2E bats file may inadvertently contain `claude-code` substring | mitigate | acceptance gate `grep -E 'claude-code' core/tests/adapter_claude_code_e2e.bats | wc -l ≤ 1`; if more, rewrite to use shell variable concatenation; the `claude_code` (underscore form) in the filename does NOT match the deny-list pattern `claude-code` (hyphen form) |
| T-04-03-05 | Compliance (append-only NFR-003) | Phase close mutates STATE.md | mitigate | Mutation goes through `chantier state append` subprocess; the binary holds the mkdir-mutex and validates the JSONL schema; never edit STATE.md directly |
| T-04-03-06 | Tampering | ROADMAP.md update introduces typos that mis-state completion | mitigate | acceptance gates: `grep` for `[x] **Phase 4`, exact 3 plans listed `[x]`, Progress-table row `3/3 | Complete`; manual line-by-line edit, no automated mutation |
| T-04-03-07 | Compliance (Pitfall 7) | `chantier state append` CWD-relative path resolution writes to wrong STATE.md | mitigate | Sub-task C runs `chantier state append` from repo root (where `.planning/STATE.md` lives); same subshell-cd pattern as the adapter and skills |
| T-04-03-SC | Tampering | npm/pip/cargo installs | accept | N/A — this plan installs no packages |
</threat_model>

<verification>
- `bats core/tests/adapter_claude_code_e2e.bats` exits 0
- `bats core/tests/adapter_isolation.bats` exits 0 (no regression; the e2e is audit-safe)
- `bats core/tests/` exits 0 with 73 tests
- `.planning/phases/04-claude-code-adapter/04-SUMMARY.md` exists and meets size + content gates
- `.planning/ROADMAP.md` Phase 4 entry fully updated (4 `[x]` SC, 3 `[x]` plans, Progress table Complete row)
- `.planning/STATE.md` has a `phase.completed` event for Phase 4 appended via `chantier state append`
- `chantier --self-test` continues to exit 0 (no regression on the binary)
- Manual smoke check (NOT automated): all four Phase 3 skill e2e tests continue green (regression baseline)
</verification>

<success_criteria>
- ROADMAP SC#1-4 all green and evidenced
- FR-008 fully satisfied (e2e proves staging + dispatch + validation work end-to-end via stub; real-claude dispatch deferred to Phase 5 dogfood per CONTEXT.md)
- D-13, D-14, D-15 implemented in test code; the remaining 13 D-NN already locked by plan 02
- All 10 Claude's Discretion items from CONTEXT.md + 5 RESEARCH Open Questions resolved and documented in 04-SUMMARY.md
- Phase 4 closed: SUMMARY.md committed, ROADMAP.md updated, STATE.md has phase.completed event
- Phase 5 (Dogfood E2E) is unblocked: the adapter exists, is invocable, and the bats suite is at 73/0
</success_criteria>

<output>
Create `.planning/phases/04-claude-code-adapter/04-03-SUMMARY.md` and `.planning/phases/04-claude-code-adapter/04-SUMMARY.md` when done. The plan-level SUMMARY (04-03-SUMMARY.md) records this plan's per-task verification; the phase-level SUMMARY (04-SUMMARY.md) is the Phase 4 close artifact as authored in Sub-task A above.
</output>
</content>
</invoke>
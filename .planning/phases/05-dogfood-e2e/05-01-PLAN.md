---
phase: 05-dogfood-e2e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - adapters/claude-code/run-task.sh
  - adapters/claude-code/README.md
  - core/tests/adapter_upstream_e2e.bats
autonomous: true
requirements: [NFR-001, NFR-002]
tags: [adapter, f3-fix, depends_on, upstream, posix-shell, tdd, dogfood]
must_haves:
  truths:
    - "Per D-01: `adapters/claude-code/run-task.sh` parses the `depends_on` YAML key of the task block via the existing `extract_task_field <task> depends_on block-dash <PLAN.md>` helper at lines 21-54, and for every upstream task ID `tN` produced by that helper, stages `$DOSSIER/upstream/tN/output.json` as a symlink (with `cp` fallback) to `$WORKTREE/.planning/phases/$PHASE/tasks/tN/output.json`."
    - "Per D-01 + Discretion #2 + RESEARCH §Pattern 3: the staging shape is per-file `output.json` (NOT full-dir symlink) — ADR 0001 Surface 2 §lines 133-134 example reads `upstream/t0/output.json`, and least-privilege favors the narrower target."
    - "Per D-01 + RESEARCH Pitfall 4: when `depends_on` is `[]` (empty list), the new loop is a no-op — the existing `state_reads` loop's `printf | while IFS= read -r ... [ -n ... ] || continue` idiom is reused byte-shape-identically; Phase 4's 73 tests remain green after the patch."
    - "Per D-01 + RESEARCH Pitfall 4 + Discretion #3: when `depends_on` is non-empty AND the named upstream task's `output.json` does NOT exist on disk, the adapter exits 2 (invocation error) with stderr `run-task: depends_on=<tN> but <path> not found; dispatch <tN> first` — operator-orders-dispatch in v0.1, NOT topological sort in the adapter."
    - "Per D-02: `core/tests/adapter_upstream_e2e.bats` exists, loads `test_helper/bats-support/load` + `test_helper/bats-assert/load`, sets up a real `git worktree add` linked worktree, copies the live `test-driven-development` skill into it, writes a synthetic two-task PLAN.md (`t1: depends_on: []`, `t2: depends_on: [t1]`), invokes `$ADAPTER t1` then `$ADAPTER t2` under the verbatim Phase 4 `CHANTIER_CLAUDE_BIN` stub, and asserts `[ -e \"$WORKTREE/.chantier/dossiers/t2/upstream/t1/output.json\" ]` is true."
    - "Per D-02 + Phase 3 D-07 measurable signal: the failing-before-passing TDD discipline for this plan is observable as: task t1 of THIS plan ships `adapter_upstream_e2e.bats` WITHOUT the F3 fix landed, and `bats core/tests/adapter_upstream_e2e.bats` exits non-zero (RED); task t2 of THIS plan patches `run-task.sh` to add the F3 loop, and the same bats run exits 0 (GREEN). Two atomic commits (`test(05-01): add failing F3 regression test`, then `feat(05-01): stage upstream/<tN>/output.json for depends_on`)."
    - "Per NFR-002: the F3 fix loop uses only POSIX sh (`printf`, `while IFS= read -r`, `[`-test, `mkdir -p`, `ln -s`, `cp`), no bash-only constructs (`[[ ]]`, `<<<`, `mapfile`, arrays); `shellcheck --shell=sh adapters/claude-code/run-task.sh` exits 0 post-fix."
    - "Per NFR-001: `adapters/claude-code/README.md` is updated to document the new behavior in English only (no French leakage); the audit at `core/tests/adapter_isolation.bats` continues to exit 0 (the F3 fix adds no new harness identifiers; `claude-code` substring inside `adapters/claude-code/` is path-only carve-out per Phase 4 D-10)."
    - "After both tasks land, the bats suite goes from 73/0 (Phase 4 close) to 74/0 — exactly one new bats file (`adapter_upstream_e2e.bats`) contributing exactly one new `@test`."
  artifacts:
    - path: "adapters/claude-code/run-task.sh"
      provides: "F3 fix loop staging upstream/<tN>/output.json from depends_on (D-01)"
      min_lines: 260
      contains: "DEPENDS_ON"
    - path: "adapters/claude-code/README.md"
      provides: "Operator-facing doc updated for non-empty upstream/ behavior (NFR-005 English)"
      contains: "depends_on"
    - path: "core/tests/adapter_upstream_e2e.bats"
      provides: "F3 regression test — synthetic 2-task chain proves upstream/t1/output.json staged in t2's dossier (D-02)"
      min_lines: 150
      contains: "upstream/t1/output.json"
  key_links:
    - from: "adapters/claude-code/run-task.sh"
      to: "extract_task_field helper at run-task.sh:21-54"
      via: "call with field=depends_on mode=block-dash"
      pattern: "extract_task_field .TASK_ID. depends_on block-dash"
    - from: "adapters/claude-code/run-task.sh"
      to: "$WORKTREE/.planning/phases/$PHASE/tasks/<tN>/output.json"
      via: "ln -s with cp fallback (mirroring the existing state_reads loop pattern at line 142)"
      pattern: "ln -s .* upstream/.*output\\.json"
    - from: "core/tests/adapter_upstream_e2e.bats"
      to: "adapters/claude-code/run-task.sh"
      via: "run $ADAPTER \"$TASK\" inside a real linked worktree"
      pattern: "run .ADAPTER. .t[12]."
    - from: "core/tests/adapter_upstream_e2e.bats"
      to: "$WORKTREE/.chantier/dossiers/t2/upstream/t1/output.json"
      via: "filesystem -e existence check after t2 dispatch"
      pattern: "upstream/t1/output\\.json"
---

<objective>
Ship the Phase 5 F3 dogfood fix end-to-end in one TDD plan: task t1 writes `core/tests/adapter_upstream_e2e.bats` — a failing regression test that builds a synthetic two-task plan (t1 with `depends_on: []`, t2 with `depends_on: [t1]`) and asserts `upstream/t1/output.json` lives inside t2's dossier — and task t2 patches `adapters/claude-code/run-task.sh` to add the depends_on → upstream/<tN>/output.json staging loop, making the test green. The two-task structure mirrors Phase 5 D-02 verbatim (the executing skill is `test-driven-development`; the dogfood is the F3 finding from the Phase 4 handoff), and the second task's `depends_on: [t1]` is what proves the F3 fix uses itself.

Purpose: Closes Finding F3 from `.planning/phases/04-claude-code-adapter/04-SUMMARY.md` §Handoff Notes. Phase 4 plan 02 emitted an empty `upstream/` directory in every dossier (line 138 comment: "upstream/ on depends_on deferred to Phase 5"); this plan resolves that gap. Implements D-01 (F3 = dogfood feature) and D-02 (TDD red→green with two atomic commits) from `05-CONTEXT.md`. The fix is byte-template-cloned from the existing `state_reads` symlink loop at `run-task.sh:139-143` — same `printf | while IFS= read -r ... [ -n ... ] || continue` shape, same `ln -s ... 2>/dev/null || cp` fallback, same POSIX-sh constraints (NFR-002).

Output: ~10 new lines inside `adapters/claude-code/run-task.sh` (the F3 loop), a ~4-line update to `adapters/claude-code/README.md` documenting the new upstream/ population behavior in English, and one new ~160-line bats file at `core/tests/adapter_upstream_e2e.bats`. After this plan: `bats core/tests/` reports 74/0; `shellcheck --shell=sh adapters/claude-code/run-task.sh` exits 0; the Phase 4 adapter audit (`adapter_isolation.bats`) remains green; the Phase 4 e2e test (`adapter_claude_code_e2e.bats`) remains green (single-task chains with `depends_on: []` still no-op the new loop).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-dogfood-e2e/05-CONTEXT.md
@.planning/phases/05-dogfood-e2e/05-RESEARCH.md
@.planning/phases/05-dogfood-e2e/05-PATTERNS.md
@.planning/phases/05-dogfood-e2e/05-VALIDATION.md
@docs/adr/0001-state-skill-contract.md
@docs/adr/0002-runtime-binary-and-state-format.md
@adapters/claude-code/run-task.sh
@adapters/claude-code/README.md
@core/tests/adapter_claude_code_e2e.bats
@core/tests/skill_test_driven_development_e2e.bats
@core/bin/chantier
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 05-01-01: Author core/tests/adapter_upstream_e2e.bats — failing F3 regression test (RED)</name>

  <read_first>
    - core/tests/adapter_claude_code_e2e.bats (FULL FILE, 293 lines — primary analog per 05-PATTERNS.md §"core/tests/adapter_upstream_e2e.bats (NEW)". Copy verbatim: shell-prelude block comment; `setup()` loaders (lines 29-31); REPO_ROOT canonicalization with `pwd -P` (lines 33-42); PATH-prepend of `$REPO_ROOT/core/bin`; TMPHOME setup with macOS `/private/var` canonicalization comment; the `git init -q && git config user.email && git config user.name && git commit -q -m initial && git worktree add -q "$WORKTREE_DIR" -b test-branch` worktree setup (lines 47-71); the CHANTIER_CLAUDE_BIN stub at lines 83-101 — copy byte-identically per 05-PATTERNS.md §"CHANTIER_CLAUDE_BIN stub" and RESEARCH Discretion #8 inline-duplicate recommendation; the `make_plan` helper structure at lines 116-170 — adapt to write TWO task blocks in the same PLAN.md instead of one; the jq assertion patterns lines 274-279 — the new file's two-task chain expects `grep -c` returning 2 instead of 1; the error-surface block at lines 213-219 — copy verbatim for both adapter invocations.)
    - core/tests/skill_test_driven_development_e2e.bats (FULL FILE, ~170 lines — secondary analog. Read for the skill body copy pattern at lines 178-183 — `cp` the live `skills/test-driven-development/{SKILL.md,PRESSURE.md,run.sh}` into the test worktree's `skills/test-driven-development/` because the adapter requires the skill body to exist at `$WORKTREE/skills/<skill-id>/SKILL.md`.)
    - core/tests/adapter_isolation.bats (FULL FILE, 124 lines — read for the HARNESS_DENY_LIST_CHECK marker convention. Every line in the new bats file that contains `claude-code`, `claude_ai_`, `mcp__`, or `@codebase` MUST carry a trailing `# HARNESS_DENY_LIST_CHECK` comment so the Phase 4 audit at `adapter_isolation.bats` and the NFR-001 audit shipped by Plan 05-02 do not flag the test as a deny-list violation.)
    - adapters/claude-code/run-task.sh LINES 1-60 (shell prelude + extract_task_field helper definition — the new bats file's PLAN.md heredoc MUST emit YAML compatible with the helper's `block-dash` mode for `depends_on`: each upstream task ID on its own `  - tN` line under `depends_on:`)
    - adapters/claude-code/run-task.sh LINES 100-145 (the dossier-staging section the F3 fix will extend — the test asserts the post-condition `$DOSSIER/upstream/t1/output.json` exists after `$ADAPTER t2` returns. This file is at this point UNMODIFIED — the test is expected to FAIL when run after t1 commits and before t2 lands.)
    - skills/test-driven-development/SKILL.md (verifies `harness_adapters:` declaration contains `- claude-code` so the test's synthetic project, after copying the skill body, has a valid SKILL.md per ADR 0001 schema)
    - skills/test-driven-development/run.sh LINES 80-228 (read for the deterministic red-phase behavior under `test_command: "false"` — the skill writes `output.md` + `output.json` with `red_exit_code=1`, ISO-8601 `red_step_timestamp`, ≥4 `invariants_applied`. The test asserts these outputs exist for BOTH t1 and t2 dossiers.)
    - core/tests/fixtures/skills/test-driven-development/dossier/inputs.yml (4-line deterministic fixture: `target_file: "src/dummy.sh"`, `test_framework: "bats"`, `phase: "red"`, `test_command: "false"` — the new bats file's PLAN.md heredoc embeds these same four scalars into BOTH t1's and t2's `inputs:` block per the round-trip pattern established in `adapter_claude_code_e2e.bats:116-170`)
    - .planning/phases/05-dogfood-e2e/05-PATTERNS.md LINES 83-196 (full pattern map for `core/tests/adapter_upstream_e2e.bats`: imports/loaders, REPO_ROOT canonicalization, worktree setup, CHANTIER_CLAUDE_BIN stub, two-task PLAN.md authoring, three-event assertion multiplied by 2, F3-specific assertion, error-surface pattern)
    - .planning/phases/05-dogfood-e2e/05-RESEARCH.md LINES 393-505 (Pattern 1 e2e skeleton; lines 466-503 are the sequential-dispatch + assertion shape this test mirrors at the in-tree scale)
    - .planning/phases/05-dogfood-e2e/05-CONTEXT.md LINES 22-25 (D-01 and D-02 verbatim — the locked decisions this test exists to enforce)
    - .planning/phases/04-claude-code-adapter/04-SUMMARY.md (read §Handoff Notes — F3 finding rationale; the test's purpose is to ship a regression for the gap that handoff identified)
  </read_first>

  <behavior>
    - Test 1 (failing-RED today, intentional): With `adapters/claude-code/run-task.sh` UNMODIFIED (no F3 fix landed yet — the loop does NOT exist), running `bats core/tests/adapter_upstream_e2e.bats` exits non-zero. Specifically: the test reaches the assertion `[ -e "$WORKTREE/.chantier/dossiers/t2/upstream/t1/output.json" ]` and that assertion fails because the current adapter emits an empty `$DOSSIER/upstream/` directory regardless of `depends_on`. The bats failure is the RED step.
    - Test 2 (post-fix GREEN — observed in t2): After Task 05-01-02 patches `run-task.sh` with the F3 loop, the same `bats core/tests/adapter_upstream_e2e.bats` invocation exits 0. The F3-specific assertion now succeeds because the adapter symlinked `$WORKTREE/.planning/phases/dogfood-phase/tasks/t1/output.json` to `$WORKTREE/.chantier/dossiers/t2/upstream/t1/output.json` during t2's dispatch.
    - Test 3 (six-event signal — D-03 three events × 2 tasks): After both adapter invocations complete green, `grep -cE '"event":"task\.started"' "$WORKTREE/.planning/STATE.md"` returns 2, `grep -cE '"event":"skill\.completed"' ...` returns 2, `grep -cE '"event":"task\.completed"' ...` returns 2. The combined `grep -cE` returning 6 confirms the two-task chain emits exactly the expected D-03 events.
    - Test 4 (validate-task gate idempotence — both tasks): After both dispatches succeed, `chantier validate-task t1` AND `chantier validate-task t2` both exit 0. Gate 1 (state_writes containment), gate 2 (output.md), gate 3 (output.json schema), gate 4 (deny-list portability), gate 5 (acceptance) all pass on BOTH tasks.
    - Test 5 (operator-orders-dispatch enforcement — Discretion #3): If a hypothetical test dispatched `$ADAPTER t2` BEFORE `$ADAPTER t1` (depends_on out-of-order), the adapter exits 2 with stderr `run-task: depends_on=t1 but <path>/output.json not found; dispatch t1 first`. The committed test does NOT do this — it dispatches t1 then t2 sequentially — but the bats file SHOULD include a commented-out documentation note pointing to RESEARCH Pitfall 4 confirming the failure mode for future readers.
    - Test 6 (deny-list audit compatibility): Every line in the new bats file containing the string `claude-code`, `mcp__`, `claude_ai_`, or `@codebase` carries a trailing `# HARNESS_DENY_LIST_CHECK` comment. Running `bats core/tests/adapter_isolation.bats` after this file lands continues to exit 0 (no false positive against the new test file).
  </behavior>

  <action>
    Create `core/tests/adapter_upstream_e2e.bats`. Copy the structural shape of `core/tests/adapter_claude_code_e2e.bats` (293 lines, 1 @test) and adapt for the two-task case. Begin with the shell-prelude block comment naming this file's purpose: a regression test for the F3 fix from `.planning/phases/04-claude-code-adapter/04-SUMMARY.md` §Handoff Notes, exercising D-01 + D-02 from `.planning/phases/05-dogfood-e2e/05-CONTEXT.md`.

    **setup() block** (mirror analog lines 29-71 byte-shape-identically):
    - Load `'test_helper/bats-support/load'` and `'test_helper/bats-assert/load'` per 05-PATTERNS.md Pattern A.
    - Set `CHANTIER="$BATS_TEST_DIRNAME/../bin/chantier"`.
    - `REPO_ROOT=$(cd "$BATS_TEST_DIRNAME/../.." && pwd -P)` per Pitfall 1 (path-with-space + macOS `/var → /private/var` canonicalization).
    - `ADAPTER="$REPO_ROOT/adapters/claude-code/run-task.sh"` with trailing `# HARNESS_DENY_LIST_CHECK` comment.
    - `export PATH="$REPO_ROOT/core/bin:$PATH"` so the skill's final `chantier state append` call resolves inside the subagent subprocess.
    - Create `$BATS_TEST_TMPDIR/home`, cd to it, set `TMPHOME=$(pwd -P)`.
    - `git init -q "$TMPHOME"`, configure user.email + user.name to `test@chantier` / `test`, `mkdir -p .planning/phases`, write a minimal `.planning/STATE.md` with `format_version: 0.1.0` frontmatter, `git add -A && git commit -q -m initial`, then `git worktree add -q "$BATS_TEST_TMPDIR/wt" -b test-branch` and `WORKTREE=$(cd "$BATS_TEST_TMPDIR/wt" && pwd -P)`.

    **CHANTIER_CLAUDE_BIN stub** (verbatim from analog lines 83-101 per Discretion #8 inline-duplicate):
    - `mkdir -p "$BATS_TEST_TMPDIR/stub"`.
    - Write `$BATS_TEST_TMPDIR/stub/claude` via `<<'STUB_EOF'` single-quoted heredoc (per Pattern H — disables expansion at test-run time). Body: `#!/bin/sh; set -eu; PROMPT=""; while [ $# -gt 0 ]; do case "$1" in -p|--print) shift; PROMPT="$1" ;; *) ;; esac; shift 2>/dev/null || true; done; DOSSIER=$(printf '%s\n' "$PROMPT" | grep -oE '/[^ "]+/\.chantier/dossiers/[^ "]+' | head -n 1); [ -n "$DOSSIER" ] || { printf 'stub: no dossier in prompt\n' >&2; exit 1; }; printf 'subagent (stub): cd %s\n' "$DOSSIER"; cd "$DOSSIER" && . ./env.sh && sh ./skill/run.sh; exit $?`.
    - `chmod +x "$BATS_TEST_TMPDIR/stub/claude"`.
    - `export CHANTIER_CLAUDE_BIN="$BATS_TEST_TMPDIR/stub/claude"` — always set in this file (no `CHANTIER_E2E_REAL_CLAUDE` opt-in here; that opt-in belongs to `tests/e2e/full_loop.bats` per D-04; this in-tree regression is strictly stub-only).

    **Skill body copy** (mirror analog lines 178-183):
    - Inside the @test body, after cd'ing to `$WORKTREE`: `mkdir -p skills/test-driven-development`, then `cp "$REPO_ROOT/skills/test-driven-development/SKILL.md"`, `cp "$REPO_ROOT/skills/test-driven-development/PRESSURE.md"`, `cp "$REPO_ROOT/skills/test-driven-development/run.sh"` into that directory. `chmod +x skills/test-driven-development/run.sh`. Stage with `git add -A && git commit -q -m "scaffold-skill"`.

    **Two-task PLAN.md authoring** (heredoc per 05-PATTERNS.md §"Two-task PLAN.md authoring" + 05-RESEARCH.md lines 420-464):
    - `mkdir -p .planning/phases/dogfood-phase`.
    - `cat > .planning/phases/dogfood-phase/PLAN.md <<'PLAN_EOF'` with single-quoted tag (Pattern H) so the YAML body is written verbatim.
    - Frontmatter: `plan_id: 01-dogfood`, `phase: dogfood-phase`, `created: 2026-05-30`, `status: draft`, `declared_skills: ["test-driven-development"]`.
    - Task block t1 (depends_on: []): `task: t1`, `skill: test-driven-development`, `inputs:` with the four-scalar fixture values (`target_file: "src/dummy.sh"`, `test_framework: "bats"`, `phase: "red"`, `test_command: "false"`), `state_writes: [".planning/phases/dogfood-phase/tasks/t1/"]`, `depends_on: []`, `acceptance:` with the two TDD acceptance bullets from `skills/test-driven-development/SKILL.md` (failing test observed before production code; same test command exits zero after production change).
    - Task block t2 (depends_on: [t1]): same shape as t1 but `task: t2`, `state_writes: [".planning/phases/dogfood-phase/tasks/t2/"]`, and `depends_on:` followed by `  - t1` on the next line per the adapter's block-dash YAML grammar.
    - End the heredoc with `PLAN_EOF`.

    **Sequential dispatch** (mirror analog lines 207-230 with error-surface block):
    - `run "$ADAPTER" t1` — wrap in the analog's error-surface pattern: if `$status -ne 0`, printf adapter exit, output, and `cat "$WORKTREE/.planning/STATE.md"` to stderr; assert `[ "$status" -eq 0 ]`.
    - Assert `[ -f ".planning/phases/dogfood-phase/tasks/t1/output.json" ]` — proves the Surface 3 propagation (Phase 4 plan 03 fix) worked for t1.
    - `run "$ADAPTER" t2` — same error-surface wrapper. This is the F3-fix-exercising call: BEFORE the F3 fix lands (RED state during this task), t2 may still exit 0 because the existing state_reads loop already handles empty `$STATE_READS` and the dossier-staging step does not check `depends_on`. The assertion that FAILS in the RED state is the next one (the F3-specific check below), not the adapter exit code.

    **Three-event-per-task assertions** (mirror analog lines 274-279, multiply by 2):
    - `_started=$(grep -cE '"event":"task\.started"' "$WORKTREE/.planning/STATE.md"); [ "$_started" -eq 2 ]`.
    - `_skill=$(grep -cE '"event":"skill\.completed"' "$WORKTREE/.planning/STATE.md"); [ "$_skill" -eq 2 ]`.
    - `_completed=$(grep -cE '"event":"task\.completed"' "$WORKTREE/.planning/STATE.md"); [ "$_completed" -eq 2 ]`.

    **validate-task green on both** (mirror analog lines 287-289, ×2):
    - `run "$CHANTIER" validate-task t1; [ "$status" -eq 0 ]`.
    - `run "$CHANTIER" validate-task t2; [ "$status" -eq 0 ]`.

    **F3-specific assertion — the load-bearing RED→GREEN signal**:
    - `DOSSIER_T2="$WORKTREE/.chantier/dossiers/t2"`.
    - `[ -e "$DOSSIER_T2/upstream/t1/output.json" ]` — this is the assertion that FAILS in the current tree (RED) and PASSES after Task 05-01-02 lands the F3 fix (GREEN). Use `-e` (not `-f` or `-L`) to accept both regular files and symlinks, per 05-PATTERNS.md §"F3-specific assertion".
    - Add an immediately-preceding comment line: `# F3 fix proof (D-01): t2's dossier MUST contain upstream/t1/output.json staged from the adapter's depends_on loop.`

    **Test-name**: `@test "adapter_upstream_e2e: F3 fix — depends_on: [t1] stages upstream/t1/output.json into t2's dossier (D-01, D-02)"`.

    **Discretion #11 — concurrency lock** (Phase 4 carry-forward): NOT exercised in this test (two-task sequential chain by design). Add a brief comment in the file header noting Discretion #11 is parked for v0.2.

    **HARNESS_DENY_LIST_CHECK hygiene** (per Pattern B): every line containing `claude-code`, `mcp__`, `claude_ai_`, `@codebase`, `cursor`, `codex-cli`, `copilot-cli`, `gemini-cli`, or `opencode` carries a trailing `# HARNESS_DENY_LIST_CHECK` comment. The stub heredoc body does NOT need the marker (it is inside a single-quoted heredoc and the audit filters `test_helper/` + `HARNESS_DENY_LIST_CHECK` lines; the stub mentions no harness identifiers anyway).

    Commit message: `test(05-01): add failing F3 regression test (adapter_upstream_e2e.bats)`.

    Implements: D-01, D-02 (RED step). Cites: per D-01 and D-02.
  </action>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && bats core/tests/adapter_upstream_e2e.bats; rc=$?; [ "$rc" -ne 0 ] && printf 'RED step confirmed: bats exit %s (expected non-zero before t2 lands)\n' "$rc"; [ "$rc" -ne 0 ]</automated>
  </verify>

  <acceptance_criteria>
    - File `core/tests/adapter_upstream_e2e.bats` exists at the repo root, is read-only by bats (no executable bit required).
    - `bats core/tests/adapter_upstream_e2e.bats` exits NON-ZERO (1 — failed assertion) against the current tree where `adapters/claude-code/run-task.sh` lacks the F3 loop. This is the RED step per D-02.
    - The bats output identifies the failure on the F3-specific assertion `[ -e "$WORKTREE/.chantier/dossiers/t2/upstream/t1/output.json" ]` — not on any prior assertion (adapter exit, event count, validate-task). Operator-confirms via `bats --tap core/tests/adapter_upstream_e2e.bats | grep 'not ok'`.
    - Full bats suite WITHOUT the new file (`bats core/tests/` excluding `adapter_upstream_e2e.bats`) reports 73/0 — the new file is the ONLY regression at this RED step; existing 73 tests are untouched.
    - The new bats file contains the string `test-driven-development` (the skill it dispatches) at least 3 times: in the file-purpose comment, in the skill body copy section, in the PLAN.md heredoc.
    - The new bats file contains `HARNESS_DENY_LIST_CHECK` on every line that mentions a harness identifier outside the file-purpose comment — verify via `grep -c 'claude-code\|mcp__\|claude_ai_' core/tests/adapter_upstream_e2e.bats` equals `grep -c 'HARNESS_DENY_LIST_CHECK' core/tests/adapter_upstream_e2e.bats` (modulo whitespace/comment-only lines). At minimum: `grep -c 'HARNESS_DENY_LIST_CHECK' core/tests/adapter_upstream_e2e.bats` returns ≥ 2.
    - `bats core/tests/adapter_isolation.bats` still exits 0 with the new file present — the Phase 4 audit's `grep -v 'HARNESS_DENY_LIST_CHECK'` step strips the marker-tagged lines.
    - The CHANTIER_CLAUDE_BIN stub heredoc inside the new file is byte-identical to the analog at `core/tests/adapter_claude_code_e2e.bats:83-101` — verify via `diff <(sed -n '/STUB_EOF/,/^STUB_EOF/p' core/tests/adapter_upstream_e2e.bats) <(sed -n '/STUB_EOF/,/^STUB_EOF/p' core/tests/adapter_claude_code_e2e.bats)` outputs no differences.
    - Bats files do NOT require an SPDX header per Phase 4 D-NFR-006 convention (project-level LICENSE governs). Confirmed by `grep -L 'SPDX-License-Identifier' core/tests/*.bats` listing the file as expected-absent.
  </acceptance_criteria>

  <done>
    `bats core/tests/adapter_upstream_e2e.bats` exits non-zero (RED step) against the current adapter source. The failure point is the F3-specific `upstream/t1/output.json` existence check, not any prior assertion. The new bats file is committed as a separate commit (`test(05-01): add failing F3 regression test`) and the Phase 4 adapter isolation audit remains green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 05-01-02: Patch adapters/claude-code/run-task.sh with F3 fix loop — make the regression test GREEN</name>

  <read_first>
    - adapters/claude-code/run-task.sh (FULL FILE, 253 lines — the artifact under patch. Read it COMPLETELY so the new loop integrates without disturbing the surrounding section comments. The integration point is immediately AFTER line 143 — i.e., after the existing `state_reads` symlink loop closes with `done` — and BEFORE the `# Section 3 — Dispatch` comment header at line 145. The new loop MUST NOT alter any line at indices 1-143 or 144 (blank line) or 145-253.)
    - adapters/claude-code/run-task.sh LINES 21-54 (the `extract_task_field` helper — the F3 fix calls this with `field=depends_on mode=block-dash` exactly the same way the existing call at line 111 invokes it for `state_reads`. NO new helper, NO new awk grammar.)
    - adapters/claude-code/run-task.sh LINES 110-143 (the `STATE_READS=$(extract_task_field ...)` assignment at line 111 followed by `INPUTS_BODY` at line 110 — note the EXISTING precedent for assigning extraction output to a local variable; the F3 fix follows the same `DEPENDS_ON=$(extract_task_field "$TASK_ID" depends_on block-dash "$PLAN_PATH")` shape, placed near these existing assignments.)
    - adapters/claude-code/run-task.sh LINES 137-143 (the existing `state_reads` symlink loop — BYTE-TEMPLATE for the F3 fix loop. Same `printf '%s\n' "$STATE_READS" | while IFS= read -r _sr_path; do [ -n "$_sr_path" ] || continue; ... done` shape. The F3 fix is the same shape with `_up_task` instead of `_sr_path` and a different per-iteration body.)
    - core/tests/adapter_upstream_e2e.bats (the file created by Task 05-01-01 — read it to understand exactly which post-condition the fix must satisfy: `[ -e "$WORKTREE/.chantier/dossiers/t2/upstream/t1/output.json" ]` after `$ADAPTER t2` returns.)
    - .planning/phases/05-dogfood-e2e/05-PATTERNS.md LINES 24-79 (Pattern target shape for the F3 fix — the planner-authored skeleton is the load-bearing reference. Reproduce the exact loop with the same comments, variable names, error message format, and fallback ln-or-cp idiom.)
    - .planning/phases/05-dogfood-e2e/05-RESEARCH.md LINES 546-574 (Pattern 3 narrative + the recommended per-file `output.json` shape per Discretion #2 + ADR 0001 Surface 2 line 134 precedent)
    - .planning/phases/05-dogfood-e2e/05-RESEARCH.md LINES 1044-1054 (Security Domain: the F3 fix adds NO new threat surface — symlink target is computed from PLAN.md (already validated), symlink lives inside `$DOSSIER` (already inside `$WORKTREE`, audited by Gate 1), no operator-supplied untrusted input flows into the new code path. T-05-SYMLINK is mitigated by the existence-check + exit-2 + path-controlled target.)
    - docs/adr/0001-state-skill-contract.md LINES 82-140 (Surface 2 specification, especially line 134 `upstream/t0/output.json` — the canonical filename pattern the F3 fix mirrors)
    - adapters/claude-code/README.md (FULL FILE, 108 lines — the operator-facing doc. The F3 fix changes operator-observable behavior for tasks with non-empty `depends_on`; the README must be updated to document the new `upstream/<tN>/output.json` staging in English.)
    - .planning/phases/04-claude-code-adapter/04-SUMMARY.md §Handoff Notes (F3 finding rationale — the fix's purpose is documented here)
    - .planning/phases/05-dogfood-e2e/05-CONTEXT.md LINES 22-25 (D-01 and D-02 verbatim)
    - .planning/phases/05-dogfood-e2e/05-RESEARCH.md LINES 671-682 (Pitfall 4: F3 fix must handle empty depends_on case via `[ -n "$_up_task" ] || continue`; mirroring the state_reads loop's empty-input handling)
  </read_first>

  <behavior>
    - Before patch: `bats core/tests/adapter_upstream_e2e.bats` exits non-zero (RED — from Task 05-01-01).
    - After patch: `bats core/tests/adapter_upstream_e2e.bats` exits 0 (GREEN). The F3-specific assertion `[ -e "$WORKTREE/.chantier/dossiers/t2/upstream/t1/output.json" ]` succeeds because `run-task.sh` now reads `depends_on: [t1]` from t2's PLAN.md task block and stages a symlink (or `cp` fallback) at that location.
    - Empty-depends_on case (Pitfall 4): For task t1 (`depends_on: []`), the new loop iterates zero times — no error, no spurious output, no change to t1's dossier shape. The Phase 4 e2e test `bats core/tests/adapter_claude_code_e2e.bats` continues to exit 0 (its single-task plan has `depends_on: []`).
    - Missing-upstream case (Pitfall 4 + Discretion #3): If a hypothetical operator dispatched `t2` before `t1` (i.e., `$WORKTREE/.planning/phases/dogfood-phase/tasks/t1/output.json` does not exist when `$ADAPTER t2` runs), the adapter prints `run-task: depends_on=t1 but <path>/output.json not found; dispatch t1 first` to stderr and exits 2 (invocation error per D-04 exit matrix). The committed test does NOT exercise this case but the behavior is observable via manual probe.
    - POSIX-shell purity: `shellcheck --shell=sh adapters/claude-code/run-task.sh` exits 0 after the patch. The new loop uses only `printf`, `while IFS= read -r`, `[ ]`-test, `mkdir -p`, `ln -s`, `cp`, all in POSIX baseline; no bash-isms.
    - NFR-001 audit unchanged: `bats core/tests/adapter_isolation.bats` continues to exit 0 (the F3 patch adds no new harness identifiers; `claude-code` substring inside `adapters/claude-code/` is path-only carve-out per Phase 4 D-10).
    - README updated: `adapters/claude-code/README.md` now contains an English sentence describing that `upstream/<tN>/` is populated from `depends_on` task IDs, with `output.json` symlinked from the prior task's `output.json`.
  </behavior>

  <action>
    Edit `adapters/claude-code/run-task.sh` (in-place, NO file recreation; preserve git blame). Insert the F3 fix block AT TWO POINTS:

    **Point 1 — extraction (near line 111)**: After the existing line `STATE_READS=$(extract_task_field "$TASK_ID" state_reads  block-dash   "$PLAN_PATH")`, add ONE new line: `DEPENDS_ON=$(extract_task_field "$TASK_ID" depends_on  block-dash   "$PLAN_PATH")`. Match the existing whitespace alignment (three spaces between `block-dash` and the quoted PLAN_PATH per the existing line's column alignment).

    **Point 2 — staging loop (after line 143, before line 145)**: Insert the F3 fix loop block. The block is bounded by section comments and contains the loop body. Insert immediately after the existing `done` at line 143 (which closes the `state_reads` symlink loop) and before the blank line 144. The exact insertion is:

    ```
    # F3 fix (Phase 5 D-01): stage upstream/<tN>/output.json for every tN in depends_on.
    # Per-file symlink shape (ADR 0001 Surface 2 §line 134 example uses upstream/t0/output.json).
    # Symlink chosen over copy: tighter least-privilege; downstream reads only.
    # If upstream output is missing, exit 2 (invocation error) — operator must dispatch tN first.
    # Empty depends_on is a no-op per Pitfall 4 (printf | while ... [ -n ... ] || continue).
    printf '%s\n' "$DEPENDS_ON" | while IFS= read -r _up_task; do
        [ -n "$_up_task" ] || continue
        _up_out="$WORKTREE/.planning/phases/$PHASE/tasks/$_up_task/output.json"
        if [ ! -f "$_up_out" ]; then
            printf 'run-task: depends_on=%s but %s not found; dispatch %s first\n' \
                "$_up_task" "$_up_out" "$_up_task" >&2
            exit 2
        fi
        mkdir -p "$DOSSIER/upstream/$_up_task"
        ln -s "$_up_out" "$DOSSIER/upstream/$_up_task/output.json" 2>/dev/null || \
            cp "$_up_out" "$DOSSIER/upstream/$_up_task/output.json"
    done
    ```

    NOTE: The above is shown here for reference; the executor MUST write this loop as plain POSIX-sh inside the file at the integration point, NOT as a fenced code block. The loop is ~14 lines including comments.

    **Do not modify any other code path in `run-task.sh`.** Do not touch the prelude, the helper, the preflight section, the dispatch heredoc, the state_writes propagation block, or the exit-code matrix wiring. The F3 fix is purely additive.

    **README update** (`adapters/claude-code/README.md`):
    - Locate the section describing dossier contents (likely under a "Dossier layout" or "What the adapter stages" heading; if no such heading exists, add one near the existing description of `inputs.yml`/`env.sh`/`reads/`).
    - Add 2-4 lines of English prose: "When a task declares `depends_on: [tN, ...]` in its PLAN.md task block, the adapter populates `upstream/<tN>/output.json` as a symlink (or `cp` fallback) to the prior task's `.planning/phases/<phase>/tasks/<tN>/output.json`. The depending task's skill body can then read upstream artifacts via `./upstream/<tN>/output.json`. Operator dispatches upstream tasks first; the adapter exits 2 with a clear error if a depends_on target's output.json is not yet on disk (v0.1 — no topological sort; v0.2+ may add)."
    - Maintain English-only per NFR-005. No French leakage. No emojis (per project tone).

    **Re-run shellcheck**: `shellcheck --shell=sh adapters/claude-code/run-task.sh` exits 0. If a finding surfaces (likely `SC2031` about subshell scoping in `while ... done` pipelines — but the existing `state_reads` loop has the same pattern and passes), apply the same disable comment or restructure the same way as the existing loop. Do NOT introduce a new disable comment unless the existing loop has one for the analogous warning.

    **Run the Task 05-01-01 bats test**: `bats core/tests/adapter_upstream_e2e.bats` MUST exit 0 after this patch lands. This is the GREEN step.

    **Run the Phase 4 regression suite**: `bats core/tests/` MUST exit 0 with 74/0 (73 prior + 1 new from Task 05-01-01). Notably `adapter_claude_code_e2e.bats` (the single-task Phase 4 e2e) must remain green — the F3 fix is no-op for `depends_on: []`.

    **Commit message**: `feat(05-01): stage upstream/<tN>/output.json from depends_on (F3 fix)`. Body should cite D-01 and reference the Phase 4 handoff F3 finding.

    Implements: D-01 (F3 fix), D-02 (GREEN step). Cites: per D-01, D-02, and Phase 4 SUMMARY F3 finding.
  </action>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && shellcheck --shell=sh adapters/claude-code/run-task.sh && bats core/tests/adapter_upstream_e2e.bats && bats core/tests/adapter_claude_code_e2e.bats && bats core/tests/adapter_isolation.bats && bats core/tests/</automated>
  </verify>

  <acceptance_criteria>
    - `shellcheck --shell=sh adapters/claude-code/run-task.sh` exits 0 — no new shellcheck warnings introduced by the F3 patch.
    - `bats core/tests/adapter_upstream_e2e.bats` exits 0 (GREEN step) — the F3-specific assertion at `[ -e "$WORKTREE/.chantier/dossiers/t2/upstream/t1/output.json" ]` now succeeds.
    - `bats core/tests/adapter_claude_code_e2e.bats` exits 0 — the Phase 4 single-task e2e regression remains green (proves Pitfall 4 empty-depends_on no-op).
    - `bats core/tests/adapter_isolation.bats` exits 0 — the Phase 4 NFR-001 carve-out audit remains green; the F3 patch adds no new harness identifiers.
    - `bats core/tests/` exits 0 with `74 tests, 74 passed, 0 failed` reported — exactly one new test added vs. Phase 4 close (73/0).
    - `grep -c "F3 fix" adapters/claude-code/run-task.sh` returns ≥ 1 — the section comment is present.
    - `grep -c "DEPENDS_ON=" adapters/claude-code/run-task.sh` returns 1 — exactly one new extraction call.
    - `grep -cE 'extract_task_field "\$TASK_ID" depends_on' adapters/claude-code/run-task.sh` returns 1 — exactly one call to the existing helper for the new field.
    - `git diff HEAD~1 -- adapters/claude-code/run-task.sh | grep -cE '^\+'` (additions only) is between 14 and 20 (the new loop block ~14 lines + 1 extraction line ≈ 15 net additions; no deletions). Confirms surgical patch.
    - `adapters/claude-code/README.md` contains the string `depends_on` and the string `upstream/` (case-sensitive) — confirms documentation update landed.
    - `grep -cE '[À-ÿ]' adapters/claude-code/README.md` returns 0 — no accented characters; NFR-005 English-only respected.
    - Two-commit history visible in `git log --oneline -2 HEAD`: `feat(05-01): stage upstream/<tN>/output.json from depends_on (F3 fix)` then `test(05-01): add failing F3 regression test (adapter_upstream_e2e.bats)` (most-recent-first).
    - The full `<automated>` verify command exits 0.
  </acceptance_criteria>

  <done>
    `bats core/tests/adapter_upstream_e2e.bats` is green; the full bats suite is 74/0; the Phase 4 adapter isolation audit and Phase 4 e2e both remain green; `shellcheck --shell=sh adapters/claude-code/run-task.sh` is clean; the README documents the new behavior in English; two atomic commits exist (test before feat) per D-02 TDD discipline.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PLAN.md `depends_on` value → adapter symlink target | Adapter consumes a task-ID list from a YAML field; the field flows into a filesystem path. Risk: shell injection or path traversal via crafted task-ID. |
| Upstream task `output.json` on disk → downstream task dossier | The symlink target is a file written by a prior dispatch. Risk: TOCTOU race or symlink-following attack if a hostile prior task wrote to an unexpected path. |
| Operator dispatch order → adapter exit code | Out-of-order dispatch produces exit 2; on-order produces exit 0. Risk: silent partial state if the exit code is ignored. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-SYMLINK | Tampering | F3 fix loop in run-task.sh | mitigate | The symlink target `$WORKTREE/.planning/phases/$PHASE/tasks/$_up_task/output.json` is computed from `extract_task_field` output (already-validated YAML); `$_up_task` flows through the same awk grammar that gates `state_reads` (already in production); the existence check `[ ! -f "$_up_out" ]` catches missing files and exits 2 before symlinking. No external attacker controls the source path; the symlink lives inside `$DOSSIER` which lives inside `$WORKTREE` (audited by `chantier validate-task` Gate 1 state_writes containment). |
| T-05-01-INJECTION | Tampering | extract_task_field output → ln/cp invocation | mitigate | `extract_task_field` uses awk (not eval, not shell substitution); the existing `state_reads` loop has the same call shape and has been production-stable since Phase 4. Variable references are double-quoted (`"$_up_task"`, `"$_up_out"`, `"$DOSSIER/upstream/$_up_task/output.json"`) per the existing pattern. Task-ID grammar is validated upstream at run-task.sh:67-74 (`[a-z][a-zA-Z0-9_-]*` only). |
| T-05-01-PORTABILITY | Compliance (NFR-002) | New POSIX-sh loop | mitigate | The loop reuses `printf | while IFS= read -r ... [ -n ... ] || continue` from the existing `state_reads` loop (line 139-143) — proven BSD/GNU-portable across Phase 4's 73-test suite. `ln -s` and `cp` are POSIX baseline. `mkdir -p` is POSIX. No `[[ ]]`, no `<<<`, no `mapfile`. `shellcheck --shell=sh` gates this in the verify command. |
| T-05-01-EMPTY-LIST | Compliance (Pitfall 4) | Empty `depends_on: []` case | mitigate | The byte-shape-identical idiom `printf '%s\n' "$VAR" | while IFS= read -r _v; do [ -n "$_v" ] || continue; ... done` handles empty input as zero iterations. Verified by Phase 4 e2e (single-task plan with `depends_on: []`) remaining green post-patch. |
| T-05-01-NFR-001-DRIFT | Compliance (NFR-001) | adapter_isolation.bats audit | accept | The F3 patch adds no new harness identifiers to `run-task.sh` (the only file modified inside a non-carved-out path is `core/tests/adapter_upstream_e2e.bats`, which uses `HARNESS_DENY_LIST_CHECK` markers per Pattern B). The Phase 4 audit's path-only carve-out for `adapters/claude-code/` already accommodates legitimate `claude-code` substrings in the patched adapter. |
| T-05-01-SC | Tampering | npm/pip/cargo installs | accept | N/A — this plan installs no packages (RESEARCH §Package Legitimacy Audit confirms zero installs in Phase 5). |
</threat_model>

<verification>
- `bats core/tests/adapter_upstream_e2e.bats` exits 0 (after both tasks land)
- `bats core/tests/adapter_claude_code_e2e.bats` exits 0 (Phase 4 regression preserved — empty depends_on no-op)
- `bats core/tests/adapter_isolation.bats` exits 0 (NFR-001 carve-out preserved)
- `bats core/tests/` exits 0 with 74/0 reported
- `shellcheck --shell=sh adapters/claude-code/run-task.sh` exits 0
- `git log --oneline -2 HEAD` shows the test commit followed by the feat commit (most-recent on top is `feat(05-01)`, then `test(05-01)` below)
- `grep -c '[À-ÿ]' adapters/claude-code/README.md` returns 0 (NFR-005 English)
</verification>

<success_criteria>
- D-01 implemented: F3 fix loop ships in `adapters/claude-code/run-task.sh`, parsing `depends_on` and staging `upstream/<tN>/output.json`.
- D-02 implemented: two atomic commits visible (test before feat), TDD red→green measurable via `bats` exit codes pre/post patch.
- NFR-001 holds: the F3 patch adds zero new harness-identifier leakage; `adapter_isolation.bats` remains green.
- NFR-002 holds: POSIX-sh purity preserved; shellcheck clean post-patch.
- F3 finding closed: the Phase 4 handoff §F3 line ("adapter emits empty upstream/ for tasks with depends_on") is resolved in code AND in test.
- Bats suite goes 73/0 → 74/0 (exactly one new test). The new file is the only addition.
</success_criteria>

<output>
Create `.planning/phases/05-dogfood-e2e/05-01-SUMMARY.md` when done. Include: the exact two-commit `git log --oneline` excerpt (test then feat), the `bats` exit-code transition (RED before t2, GREEN after t2), the `shellcheck` clean confirmation, the README diff excerpt showing the new English prose, the verification-result matrix mapping D-01/D-02 to evidence (commit SHAs, bats output, shellcheck output), and a one-line citation of the Phase 4 SUMMARY F3 finding line that this plan closes.
</output>
</content>

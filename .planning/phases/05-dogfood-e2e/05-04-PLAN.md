---
phase: 05-dogfood-e2e
plan: 04
type: execute
wave: 3
depends_on: ["05-03"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/05-dogfood-e2e/05-SUMMARY.md
autonomous: false
requirements: [NFR-005, NFR-006]
tags: [phase-close, cutover, roadmap-migration, summary, gsd-handoff, dogfood-complete]
must_haves:
  truths:
    - "Per D-07: `.planning/ROADMAP.md` is migrated from GSD-parser format to ADR 0001-native format via the minimalist diff. The 'Format note (temporary)' callout block at line 3 (the blockquote starting `> **Format note (temporary):**`) is STRIPPED. The frontmatter is preserved (already validates against `core/schemas/roadmap.json` per Phase 2 D-06 permissive `additionalProperties`)."
    - "Per D-07: the ROADMAP narrative structure is preserved — front-matter, Phases overview list, Phase Details with Goal/Depends on/Requirements/Success Criteria/Plans subsections, Progress table. The Phase 5 entry's `Plans: TBD` line is replaced with `Plans: 4 plans (complete)` and the four plan filenames are listed with `[x]` checkmarks. The Phase 5 Progress-table row moves to `Complete | 2026-05-30`."
    - "Per D-08: the cutover commit bundles in ONE commit: (a) the ROADMAP.md migration diff per D-07; (b) the Phase 5 SUMMARY.md at `.planning/phases/05-dogfood-e2e/05-SUMMARY.md`; (c) the `cutover.completed` STATE.md event row appended via `chantier state append`; (d) the `phase.completed` STATE.md event row appended via `chantier state append`."
    - "Per D-08 + Discretion #7 + RESEARCH Code Example 3: the `cutover.completed` event invocation is `chantier state append --event cutover.completed --summary \"ROADMAP migrated to ADR 0001 native format. GSD ceases to be invoked in Chantier's own workflows.\" --ref \".planning/ROADMAP.md\" --ref \".planning/STATE.md\" --ref \"bootstrap.harness.chosen@2026-05-29T18:30:00Z\"` (the third `--ref` is the back-reference to the bookend event per Discretion #7 RECOMMENDED YES)."
    - "Per Discretion #10: the Phase 5 `phase.completed` event is appended via DIRECT `chantier state append` invocation (not through the adapter) — symmetry with the four prior phases (`phase.completed` events at STATE.md rows 22, 31, 41, 47). The invocation: `chantier state append --event phase.completed --summary \"Phase 5 (Dogfood E2E) complete: F3 fix shipped, ADR 0004 Proposed, six NFR audits green, tests/e2e/full_loop.bats green, ROADMAP migrated to ADR 0001 native format.\" --ref \".planning/phases/05-dogfood-e2e/05-SUMMARY.md\" --ref \"tests/e2e/full_loop.bats\"`."
    - "Per Pitfall 7: the cutover commit and the events are bundled in a single git commit (not split). Operator runs the `chantier state append` invocations FIRST (events land in STATE.md), THEN `git add` the four affected files (ROADMAP.md, STATE.md, 05-SUMMARY.md, plus any sub-plan SUMMARYs not yet committed), THEN `git commit` ONCE. `git log -p .planning/STATE.md` shows the cutover and phase events in the same commit as the ROADMAP migration."
    - "Per RESEARCH §State of the Art: at the post-cutover state, Chantier's own development is no longer managed by GSD's commands. Future planning happens via Chantier's native tooling (or, transitionally, by hand against ADR 0001 until v0.2 ships workflow skills). The historical record of `bootstrap.harness.chosen` (STATE.md row 24, 2026-05-29T18:30:00Z) and the GSD-driven Phase 1–4 commits remain — D-08 explicitly forbids rewriting Phase 4 plans and prior STATE.md history."
    - "Per ROADMAP SC#5: ROADMAP.md is migrated to ADR 0001 native format as the final commit of this phase. Confirmed by `head -10 .planning/ROADMAP.md | ! grep -q 'Format note (temporary)'` (the callout is stripped). Confirmed by `git log -p -1 .planning/ROADMAP.md` showing the migration diff as the most-recent change."
    - "Per NFR-005 + NFR-006: Plan 05-02's NFR audits remain green post-migration. `bats core/tests/nfr_audits.bats -f 'NFR-005'` exits 0 (no French in public artifacts after migration); `bats core/tests/nfr_audits.bats -f 'NFR-006'` exits 0 (LICENSE + LICENSE-CREDITS + SPDX unchanged)."
    - "Per quality_gate: the Phase 5 SUMMARY.md mirrors the section structure of `.planning/phases/04-claude-code-adapter/04-SUMMARY.md`: frontmatter (phase/status/completed/plans/requirements_completed/bats_suite_before/bats_suite_after/shipped_artifacts/satisfies_project_criterion); H1 title; 1-paragraph synopsis; `## Goal` (quoted from ROADMAP); `## Plans Executed` table; `## Verification Results` with SC#1–SC#5 + NFR-001..006 sub-tables + bats suite totals; `## Resolved Discretion Items` table; `## Surface 3 propagation codification` pointing to ADR 0004; `## Handoff Notes for v0.2.0` (mirror Phase 4 §Handoff Notes for Phase 5); `## Self-Check: PASSED` footer."
    - "Final bats suite total: `bats core/tests/ tests/e2e/` reports 81/0 (unchanged from Plan 05-03; this plan does not modify any test files — only ROADMAP narrative, STATE.md events, and the new SUMMARY)."
    - "After this plan lands, PROJECT.md status moves to v0.1.0-complete (operator-edit, NOT in this plan's scope; called out in the Handoff Notes section of the SUMMARY)."
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "ROADMAP migrated to ADR 0001 native format (D-07); Phase 5 marked complete with 4 plans listed"
      contains: "[x] **Phase 5: Dogfood E2E"
    - path: ".planning/STATE.md"
      provides: "cutover.completed + phase.completed event rows appended in the final commit (D-08, Discretion #10)"
      contains: "cutover.completed"
    - path: ".planning/phases/05-dogfood-e2e/05-SUMMARY.md"
      provides: "Phase 5 close summary mirroring 04-SUMMARY.md structure"
      min_lines: 80
      contains: "PASSED"
  key_links:
    - from: ".planning/STATE.md (new cutover.completed row)"
      to: ".planning/STATE.md (existing bootstrap.harness.chosen row 24)"
      via: "back-reference --ref \"bootstrap.harness.chosen@2026-05-29T18:30:00Z\""
      pattern: "bootstrap\\.harness\\.chosen@2026-05-29"
    - from: ".planning/phases/05-dogfood-e2e/05-SUMMARY.md"
      to: ".planning/phases/04-claude-code-adapter/04-SUMMARY.md"
      via: "Phase 4 close template — same section structure and frontmatter shape"
      pattern: "04-SUMMARY"
    - from: ".planning/ROADMAP.md (Phase 5 row in Progress table)"
      to: ".planning/phases/05-dogfood-e2e/05-SUMMARY.md"
      via: "phase-close status reference"
      pattern: "Complete \\| 2026-05-30"
---

<objective>
Close Phase 5 in a single git commit that bundles: (1) the minimalist ROADMAP.md migration from GSD-parser format to ADR 0001 native format per D-07 (strip the "Format note (temporary)" callout block; update the Phase 5 §Plans list with the four shipped plan filenames; flip the Phase 5 Progress-table row to `Complete | 2026-05-30`); (2) the new Phase 5 SUMMARY.md at `.planning/phases/05-dogfood-e2e/05-SUMMARY.md` mirroring `.planning/phases/04-claude-code-adapter/04-SUMMARY.md` structure; (3) the `cutover.completed` STATE.md event appended via `chantier state append` (the symbolic moment Chantier ceases to depend on GSD per D-08); (4) the Phase 5 `phase.completed` STATE.md event appended via direct `chantier state append` (symmetry with Phases 1-4 per Discretion #10).

Purpose: Implements D-07 (minimalist ROADMAP migration), D-08 (cutover bundle in final commit), Discretion #7 (back-ref to bootstrap.harness.chosen), Discretion #10 (direct binary append for phase.completed). Closes ROADMAP SC#5 ("ROADMAP migrated to ADR 0001 native format as final commit of this phase") — the last open success criterion for Phase 5. After this commit, Chantier's v0.1.0 milestone is feature-complete: FR-001..FR-010 implemented and tested; NFR-001..NFR-006 verified by Plan 05-02's audits; the integration test in `tests/e2e/` runs the full loop; ADR 0001/0002/0003/0004 form the documented contract series. The post-commit state satisfies `.planning/REQUIREMENTS.md` §Acceptance "v0.1.0 ships when".

Output: One in-place edit of `.planning/ROADMAP.md` (minimal diff: ~3-5 lines stripped, ~4-6 lines added for Phase 5 plan list, ~1 line updated for Progress-table row); one new SUMMARY.md (~80-140 lines); two new STATE.md event rows (cutover.completed + phase.completed). One final git commit bundling all four artifacts. NOT autonomous: a `checkpoint:human-verify` precedes the commit because the ROADMAP narrative diff is interpretive (per VALIDATION.md "Manual-Only Verifications" — operator confirms the migration is minimalist and complete).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-dogfood-e2e/05-CONTEXT.md
@.planning/phases/05-dogfood-e2e/05-RESEARCH.md
@.planning/phases/05-dogfood-e2e/05-PATTERNS.md
@.planning/phases/05-dogfood-e2e/05-VALIDATION.md
@.planning/phases/05-dogfood-e2e/05-01-SUMMARY.md
@.planning/phases/05-dogfood-e2e/05-02-SUMMARY.md
@.planning/phases/05-dogfood-e2e/05-03-SUMMARY.md
@.planning/phases/04-claude-code-adapter/04-SUMMARY.md
@docs/adr/0001-state-skill-contract.md
@docs/adr/0002-runtime-binary-and-state-format.md
@docs/adr/0004-surface-3-propagation.md
@core/bin/chantier
</context>

<tasks>

<task type="auto">
  <name>Task 05-04-01: Migrate .planning/ROADMAP.md to ADR 0001 native format (minimalist per D-07)</name>

  <read_first>
    - .planning/ROADMAP.md (FULL FILE, 165 lines — the target of the migration. Read end-to-end to map every line that must change vs. preserve. The minimalist intent: strip lines 3 (the `> **Format note (temporary):** ...` blockquote) and the empty line 4 if present. PRESERVE: the front-matter at lines 1-2 (or wherever it ends — check the file's actual frontmatter delimiters); the `# Roadmap: Chantier` H1 title; the `## Overview` section; the `## Phases` section listing the 5 phases with `[x]/[ ]` checkboxes; the `## Phase Details` section with each phase's Goal/Depends on/Requirements/Success Criteria/Plans subsections; the `## Progress` table. NOTE: the file as-of-Phase-5-start has no frontmatter — confirm at execution time.)
    - .planning/ROADMAP.md LINES 135-152 (Phase 5 section — the `Plans: TBD` placeholder at line 148 needs replacement; the plan list at line 151 (`- [ ] 05-01: TBD (produced by /gsd-plan-phase 5...)`) needs replacement with the four actual plan filenames and brief summaries)
    - .planning/ROADMAP.md LINES 155-165 (the Progress table — the Phase 5 row at line 165 needs to flip from `0/TBD | Not started | -` to `4/4 | Complete | 2026-05-30`)
    - .planning/phases/04-claude-code-adapter/04-SUMMARY.md §"ROADMAP update" or any reference to how Phase 4's ROADMAP entry was finalized (cross-reference for the format of the plan-list entries: `- [x] 04-01-PLAN.md — <brief>` etc.; mirror this shape for Phase 5)
    - .planning/ROADMAP.md LINES 110-133 (Phase 4 section — the FINAL FORMAT REFERENCE for how a completed Phase 5 entry should look post-migration: the `Plans:` block, the `Wave 1` / `Wave 2` sub-headers, the `- [x] NN-NN-PLAN.md — <summary>` lines. Mirror this shape for Phase 5.)
    - .planning/phases/05-dogfood-e2e/05-01-SUMMARY.md, .planning/phases/05-dogfood-e2e/05-02-SUMMARY.md, .planning/phases/05-dogfood-e2e/05-03-SUMMARY.md (read each to extract the 1-line summary the planner cites in the ROADMAP plan-list entries; the executor will read these at execution time — they exist by Wave 3)
    - core/schemas/roadmap.json (read to confirm `additionalProperties: true` per Phase 2 D-06; this means the ROADMAP's existing frontmatter (if any) validates without schema change post-migration)
    - .planning/phases/05-dogfood-e2e/05-CONTEXT.md LINES 43 (D-07 verbatim: "ROADMAP migration is **minimalist**. Strip the temporary GSD-parser markers..." with the specific markers enumerated)
    - .planning/phases/05-dogfood-e2e/05-RESEARCH.md LINES 847-862 (Example 2 — the exact diff shape with the only-load-bearing strip identified)
    - .planning/phases/05-dogfood-e2e/05-PATTERNS.md LINES 485-505 (in-place edit map for ROADMAP.md)
  </read_first>

  <action>
    Edit `.planning/ROADMAP.md` IN-PLACE (NOT a recreation). Git blame history must be preserved. The edits land in three localized regions:

    **Edit 1 — Strip the temporary GSD-parser callout** (D-07 load-bearing change):
    - Locate line 3 (or the line matching `^> \*\*Format note \(temporary\):\*\*`) and delete the entire blockquote line.
    - Delete the immediately-following empty line if present.
    - Do NOT touch the `# Roadmap: Chantier` H1 title above it or the `## Overview` section below it.

    **Edit 2 — Update the Phase 5 §Plans block** (normal phase-close housekeeping, NOT migration):
    - Locate the Phase 5 §Plans line `**Plans**: TBD` and change it to `**Plans**: 4 plans (complete)`.
    - Locate the Phase 5 §Plans list line `- [ ] 05-01: TBD (produced by '/gsd-plan-phase 5' — the last GSD-driven planning in Chantier's history)` and replace it with a structured plan list mirroring Phase 4's shape (line 124-133):

      ```
      Plans:

      **Wave 1** *(parallel — no file overlap between 05-01 and 05-02)*

      - [x] 05-01-PLAN.md — F3 fix in adapters/claude-code/run-task.sh (depends_on → upstream/<tN>/output.json staging) + in-tree regression core/tests/adapter_upstream_e2e.bats (TDD red→green; D-01, D-02)
      - [x] 05-02-PLAN.md — ADR 0004 (Surface 3 propagation, Proposed) + core/tests/nfr_audits.bats (six @test blocks, one per NFR-001..NFR-006) (D-05, D-06, D-09)

      **Wave 2** *(blocked on Wave 1)*

      - [x] 05-03-PLAN.md — tests/e2e/full_loop.bats — full new-project → plan → execute → verify integration test with CHANTIER_E2E_REAL_CLAUDE opt-in gate (D-03, D-04, SC#1, SC#2, SC#3)

      **Wave 3** *(final commit; blocked on Wave 2)*

      - [x] 05-04-PLAN.md — Phase close: ROADMAP minimalist migration (D-07) + cutover.completed event (D-08) + phase.completed event (Discretion #10) + 05-SUMMARY.md
      ```

      NOTE: the above is shown for reference; write it as plain Markdown inside the ROADMAP file at the Phase 5 section, NOT as a fenced code block. Mirror the indentation and bullet style of Phase 4's plan list exactly.

    **Edit 3 — Update the Phase 5 Progress-table row**:
    - Locate the Progress table row for Phase 5 (`| 5. Dogfood E2E | 0/TBD | Not started | - |`).
    - Replace with `| 5. Dogfood E2E | 4/4 | Complete | 2026-05-30 |`.

    **Optional Edit 4 — Update the Phase 5 §Phases overview checkbox** (lines 16-20):
    - Locate the Phase 5 entry in the `## Phases` checkbox list: `- [ ] **Phase 5: Dogfood E2E** - ...`.
    - Replace `- [ ]` with `- [x]`. Preserve the trailing summary text verbatim.

    **DO NOT touch**:
    - Phase 1, 2, 3, 4 entries (history preserved per D-08).
    - The `## Overview` paragraph.
    - The front-matter (if any) — already validates per Phase 2 D-06.
    - Any other line in the file.

    **NFR-005 hygiene**: the ROADMAP edits are English-only. No French leakage. Verify via `grep -c '[À-ÿ]' .planning/ROADMAP.md` returns 0 post-edit.

    **Stage the edit**: `git add .planning/ROADMAP.md` — do NOT commit yet; Task 05-04-03 bundles the commit.

    Implements: D-07. Cites: per D-07 and ROADMAP SC#5.
  </action>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && ! head -5 .planning/ROADMAP.md | grep -q 'Format note (temporary)' && grep -q '^- \[x\] \*\*Phase 5: Dogfood E2E' .planning/ROADMAP.md && grep -qE '^\| 5\. Dogfood E2E \| 4/4 \| Complete \| 2026-05-30' .planning/ROADMAP.md && grep -q '05-01-PLAN.md' .planning/ROADMAP.md && grep -q '05-02-PLAN.md' .planning/ROADMAP.md && grep -q '05-03-PLAN.md' .planning/ROADMAP.md && grep -q '05-04-PLAN.md' .planning/ROADMAP.md && [ "$(grep -c '[À-ÿ]' .planning/ROADMAP.md)" -eq 0 ]</automated>
  </verify>

  <acceptance_criteria>
    - `head -5 .planning/ROADMAP.md` does NOT contain the substring `Format note (temporary)` — the GSD callout block is stripped (D-07 load-bearing change).
    - The `# Roadmap: Chantier` H1 title still appears at the file's top.
    - The `## Overview`, `## Phases`, `## Phase Details`, `## Progress` section structure is intact (verifiable via `grep -c '^## '` returning ≥ 4).
    - Phase 1-4 entries in the `## Phases` checkbox list are UNCHANGED — verify via `git diff .planning/ROADMAP.md` showing zero modifications to lines mentioning Phase 1, 2, 3, or 4 outside the Phase 5 section.
    - Phase 5 entry in the `## Phases` checkbox list shows `- [x]` (was `- [ ]`).
    - The `## Phase Details` Phase 5 section's `**Plans**` line reads `**Plans**: 4 plans (complete)`.
    - The Phase 5 §Plans list contains four entries with `[x]` checkboxes, plan filenames `05-01-PLAN.md` through `05-04-PLAN.md`, and brief summaries — verify via `grep -c '^- \[x\] 05-0[1-4]-PLAN.md' .planning/ROADMAP.md` returns 4.
    - The Phase 5 Progress-table row reads `| 5. Dogfood E2E | 4/4 | Complete | 2026-05-30 |`.
    - `grep -c '[À-ÿ]' .planning/ROADMAP.md` returns 0 — no French leakage; NFR-005 respected.
    - `git diff --stat .planning/ROADMAP.md` shows a small diff (~10-25 lines changed; minimalist intent per D-07).
    - The file IS staged (`git diff --cached --stat` shows `.planning/ROADMAP.md`) but NOT yet committed.
    - The full `<automated>` verify command exits 0.
  </acceptance_criteria>

  <done>
    `.planning/ROADMAP.md` is migrated minimalist-style: the Format-note callout is stripped; the Phase 5 entry shows `[x]` and lists four plans; the Progress-table row reads `4/4 | Complete | 2026-05-30`; English-only; no Phase 1-4 lines touched; diff size is small. The file is staged but not yet committed (Task 05-04-03 bundles).
  </done>
</task>

<task type="auto">
  <name>Task 05-04-02: Author .planning/phases/05-dogfood-e2e/05-SUMMARY.md (Phase 5 close summary)</name>

  <read_first>
    - .planning/phases/04-claude-code-adapter/04-SUMMARY.md (FULL FILE, 154 lines — PRIMARY template per 05-PATTERNS.md §"05-SUMMARY.md (NEW, phase close)". Mirror the structure verbatim: YAML frontmatter; H1 title; 1-paragraph synopsis; `## Goal (quoted from ROADMAP.md §Phase 5)`; `## Plans Executed` table with columns Plan | Subsystem | Outcome | SUMMARY-link; `## Verification Results` containing `### ROADMAP Phase 5 Success Criteria` sub-table and `### NFR Verification` sub-table and `### Bats suite totals` sub-table; `## Resolved Discretion Items` table; a section pointing to ADR 0004 (mirror Phase 4's "Surface 3 propagation discovery" section); `## Handoff Notes for v0.2.0` mirroring Phase 4's "Handoff Notes for Phase 5"; `## Self-Check: PASSED` footer with checklist.)
    - .planning/phases/05-dogfood-e2e/05-01-SUMMARY.md (the Plan 05-01 close summary — extract the verification-result excerpts: bats RED→GREEN transition, shellcheck clean, README diff, two-commit history)
    - .planning/phases/05-dogfood-e2e/05-02-SUMMARY.md (the Plan 05-02 close summary — extract: NFR-001 byte-identical proof, six-@test pass count, ADR 0004 section-count proof, per-NFR independence proofs, English-only confirmation)
    - .planning/phases/05-dogfood-e2e/05-03-SUMMARY.md (the Plan 05-03 close summary — extract: 1/1 green from full_loop.bats, 81/0 full-suite total, stub byte-identical proof, NFR-001 self-trigger residue 0, SC#1/SC#2/SC#3 → assertion mapping)
    - .planning/ROADMAP.md (post-Task-05-04-01-edit — read the migrated §Phase 5 section to quote the Goal verbatim in the SUMMARY's `## Goal` section)
    - .planning/phases/05-dogfood-e2e/05-CONTEXT.md (read all 10 D-NN locked decisions — the `## Resolved Discretion Items` table cites each one's resolution)
    - .planning/phases/05-dogfood-e2e/05-CONTEXT.md §Claude's Discretion (read all 11 items — each one is either resolved-in-plan (cite which plan) or carried-to-v0.2 backlog)
    - .planning/phases/05-dogfood-e2e/05-RESEARCH.md §Sources (cross-reference the load-bearing artifacts the Phase 5 work touched)
    - docs/adr/0004-surface-3-propagation.md (the ADR 0004 shipped by Plan 05-02 — the SUMMARY's "Surface 3 propagation codification" section cites this file)
    - .planning/REQUIREMENTS.md §Acceptance "v0.1.0 ships when" (the four-bullet ship gate — the SUMMARY confirms which bullets Phase 5 closes: FR-001..FR-010 (closed via Phases 2..4), NFR-001..NFR-006 (closed via Plan 05-02), integration test in `tests/e2e/` (closed via Plan 05-03), ADR record contains at least one ADR resolving an ADR 0001 OQ (closed via ADR 0004))
    - .planning/phases/05-dogfood-e2e/05-PATTERNS.md LINES 532-602 (full template-mirror map for `05-SUMMARY.md` including frontmatter shape, section structure, Self-Check footer pattern)
  </read_first>

  <action>
    Create `.planning/phases/05-dogfood-e2e/05-SUMMARY.md`. Mirror `.planning/phases/04-claude-code-adapter/04-SUMMARY.md` section-for-section. Length target: 80-160 lines (Phase 4's SUMMARY is 154 lines; Phase 5 is structurally similar).

    **Frontmatter** (~15 lines, mirror 04-SUMMARY.md:1-20):
    ```
    ---
    phase: 05-dogfood-e2e
    status: complete
    completed: 2026-05-30
    plans:
      - 05-01
      - 05-02
      - 05-03
      - 05-04
    requirements_completed:
      - NFR-001
      - NFR-002
      - NFR-003
      - NFR-004
      - NFR-005
      - NFR-006
    bats_suite_before: 73/0
    bats_suite_after: 81/0
    shipped_artifacts:
      - core/tests/adapter_upstream_e2e.bats
      - core/tests/nfr_audits.bats
      - tests/e2e/full_loop.bats
      - docs/adr/0004-surface-3-propagation.md
      - adapters/claude-code/run-task.sh (F3 fix patch)
      - adapters/claude-code/README.md (depends_on documentation)
    satisfies_project_criterion:
      - "PROJECT.md v0.1.0 success criterion 5 (Chantier's own development is managed by Chantier)"
      - "REQUIREMENTS.md §Acceptance: integration test in tests/e2e/"
      - "REQUIREMENTS.md §Acceptance: ADR record contains at least one ADR resolving an ADR 0001 OQ (ADR 0004)"
    cutover_event_ts: "2026-05-30T<time-of-final-commit>Z"
    ---
    ```
    NOTE: the above is shown for reference; write it as plain YAML frontmatter at the top of the SUMMARY file. The `cutover_event_ts` is filled in by the executor at commit time (this Task does NOT yet know the final commit timestamp; populate after Task 05-04-03's append runs).

    **H1 title**: `# Phase 05: Dogfood E2E — Close Summary`.

    **1-paragraph synopsis** (~5-10 lines): Phase 5 closes Chantier v0.1.0 by eating its own dogfood. Plan 05-01 shipped the F3 fix (adapter `depends_on` → upstream/<tN>/output.json staging) via TDD. Plan 05-02 codified the Surface 3 propagation contract in ADR 0004 (Proposed) and consolidated the six NFR audits into `core/tests/nfr_audits.bats`. Plan 05-03 shipped `tests/e2e/full_loop.bats` — the full new-project → plan → execute → verify integration test with the `CHANTIER_E2E_REAL_CLAUDE` opt-in env gate. Plan 05-04 (this commit) migrated ROADMAP.md to ADR 0001 native format, appended the `cutover.completed` event marking Chantier's exit from GSD-dependency, and recorded the Phase 5 `phase.completed` event. Final bats suite total: 81/0. ROADMAP Phase 5 success criteria SC#1–SC#5 are all closed. REQUIREMENTS.md §Acceptance is satisfied. v0.1.0 is feature-complete.

    **`## Goal`** section (~3 lines): Quote the Phase 5 Goal verbatim from `.planning/ROADMAP.md` §Phase 5 (the post-migration form — should still be the same Goal text).

    **`## Plans Executed`** table (4 rows):
    ```
    | Plan | Subsystem | Outcome | SUMMARY |
    |------|-----------|---------|---------|
    | 05-01 | adapter F3 fix + in-tree regression | Two-commit TDD; bats 73→74; D-01/D-02 closed | 05-01-SUMMARY.md |
    | 05-02 | ADR 0004 (Proposed) + nfr_audits.bats | ADR shipped; 6 @tests green; bats 74→80; D-05/D-06/D-09 closed | 05-02-SUMMARY.md |
    | 05-03 | tests/e2e/full_loop.bats | 1/1 green default-offline; bats 80→81; D-03/D-04/SC#1/SC#2/SC#3 closed | 05-03-SUMMARY.md |
    | 05-04 | ROADMAP migration + cutover.completed + phase.completed | Final commit bundle; D-07/D-08/Discretion #7/Discretion #10 closed; SC#5 closed | (this file) |
    ```

    **`## Verification Results`** with three sub-tables:

    `### ROADMAP Phase 5 Success Criteria` (5 rows, one per SC):
    ```
    | # | Criterion | Status | Evidence | Command |
    |---|-----------|--------|----------|---------|
    | SC#1 | tests/e2e/ runs full new→plan→execute→verify loop with Chantier tooling | green | tests/e2e/full_loop.bats 1/1 green | `bats tests/e2e/full_loop.bats` |
    | SC#2 | Populated STATE.md with no contract violations | green | grep -c task.started returns 2; validate-task both exit 0 | `bats tests/e2e/full_loop.bats` |
    | SC#3 | Passes in CI without network (default-stub path) | green | CHANTIER_CLAUDE_BIN default-set; Pitfall 8 self-assertion green | `unset CHANTIER_E2E_REAL_CLAUDE; bats tests/e2e/full_loop.bats` |
    | SC#4 | NFR-001..006 independently verified | green | 6 @test blocks all exit 0 in isolation | `bats core/tests/nfr_audits.bats` |
    | SC#5 | ROADMAP migrated to ADR 0001 native format in final commit | green | Format-note callout stripped; head -5 ROADMAP.md does not match 'Format note' | `! head -5 .planning/ROADMAP.md \| grep -q 'Format note'` |
    ```

    `### NFR Verification` (6 rows, one per NFR-001..NFR-006). Each row: NFR | Status | Evidence | Command (`bats core/tests/nfr_audits.bats -f 'NFR-NNN'`).

    `### Bats suite totals` (5 rows showing the transition):
    ```
    | Boundary | Tests | Notes |
    |----------|-------|-------|
    | Phase 4 close | 73/0 | Phase 5 entry-point |
    | Post Plan 05-01 (Task 05-01-02 GREEN) | 74/0 | +1 from core/tests/adapter_upstream_e2e.bats |
    | Post Plan 05-02 | 80/0 | +6 from core/tests/nfr_audits.bats |
    | Post Plan 05-03 | 81/0 | +1 from tests/e2e/full_loop.bats |
    | Post Plan 05-04 (this commit) | 81/0 | unchanged — no test files modified |
    ```

    **`## Resolved Discretion Items`** table — one row per Discretion item from 05-CONTEXT.md, citing the resolution and the plan that resolved it (or "carried to v0.2 backlog" + ROADMAP/SUMMARY citation). 11 rows. Sample row format: `| #1 PLAN.md task pair shape for F3 dogfood | Resolved: t1 writes failing test (commit 1), t2 patches adapter (commit 2) per D-02 | Plan 05-01 |`.

    **`## Surface 3 propagation codification`** (~5-10 lines): Brief summary of ADR 0004's status (Proposed), the Ratification path conditions (second adapter ships; cross-harness e2e proves), and the canonical exclusion list (`inputs.yml`, `env.sh`, `subagent.transcript.log`, subdirectories). Link to `docs/adr/0004-surface-3-propagation.md`.

    **`## Handoff Notes for v0.2.0`** (~15-25 lines) — mirror Phase 4's `## Handoff Notes for Phase 5` section. Capture:
    - **F2 (real-claude dispatch path in CI)**: env gate `CHANTIER_E2E_REAL_CLAUDE` shipped by Plan 05-03; CI use deferred to v0.2 when API-key infrastructure is in place.
    - **F4 (strict worktree validation)**: deferred; lax interpretation has produced zero incidents through Phases 4-5.
    - **Second harness adapter (`adapters/cursor/`, etc.)**: v0.2 — required for ADR 0004 ratification.
    - **Matrix-via-adapter coverage of the 4 skills**: v0.2 mechanical extension per Phase 3 D-17.
    - **ADR 0003 ratification (workflow skill design principles)**: post-v0.1.0 evidence-based promotion.
    - **ADR 0004 ratification (Surface 3 propagation)**: requires the second adapter to exercise the contract cross-harness.
    - **Workflow skill authoring** per `docs/strategy/maturity-path.md` sketch: v0.2.0+.
    - **`extract-skills-from-phase`**: v0.3.0 per PROJECT.md.
    - **STATE.md compaction**: post-v0.1.0 per ADR 0001 OQ #2.
    - **PROJECT.md status flip to v0.1.0-complete**: operator action post-this-commit (NOT in this plan's scope; called out as a follow-up).

    **`## Self-Check: PASSED`** footer (~10 lines) mirroring 04-SUMMARY.md:144-154:
    ```
    ## Self-Check: PASSED

    - `.planning/phases/05-dogfood-e2e/05-SUMMARY.md` exists, ≥ 80 lines ✓
    - `.planning/phases/05-dogfood-e2e/05-01-SUMMARY.md` exists ✓
    - `.planning/phases/05-dogfood-e2e/05-02-SUMMARY.md` exists ✓
    - `.planning/phases/05-dogfood-e2e/05-03-SUMMARY.md` exists ✓
    - `.planning/phases/05-dogfood-e2e/05-04-SUMMARY.md` exists ✓
    - ROADMAP.md Phase 5 entry marked complete; Format note callout stripped ✓
    - STATE.md cutover.completed + phase.completed events appended in the same commit as the ROADMAP migration ✓
    - `bats core/tests/ tests/e2e/` reports 81/0 ✓
    - `shellcheck --shell=sh adapters/claude-code/run-task.sh` clean ✓
    - `bats core/tests/nfr_audits.bats` 6/6 green ✓
    - D-NN coverage check: D-01..D-10 cited across the four 05-NN-SUMMARY.md files ✓
    - Discretion items #1-#11 each have a resolution row in this SUMMARY ✓
    - NFR-005 English-only: `grep -c '[À-ÿ]' 05-SUMMARY.md` returns 0 ✓
    ```

    **NFR-005 hygiene**: the SUMMARY body is English-only. Verify via `grep -c '[À-ÿ]' .planning/phases/05-dogfood-e2e/05-SUMMARY.md` returns 0 post-write.

    **No fenced code blocks containing more than 10 lines** — SUMMARY is prose + tables, not code.

    **Stage the file**: `git add .planning/phases/05-dogfood-e2e/05-SUMMARY.md` — do NOT commit yet; Task 05-04-03 bundles.

    Implements: phase-close SUMMARY discipline; cites D-NN closures.
  </action>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && test -f .planning/phases/05-dogfood-e2e/05-SUMMARY.md && [ "$(wc -l < .planning/phases/05-dogfood-e2e/05-SUMMARY.md)" -ge 80 ] && grep -q '^# Phase 05: Dogfood E2E' .planning/phases/05-dogfood-e2e/05-SUMMARY.md && grep -q '^## Goal' .planning/phases/05-dogfood-e2e/05-SUMMARY.md && grep -q '^## Plans Executed' .planning/phases/05-dogfood-e2e/05-SUMMARY.md && grep -q '^## Verification Results' .planning/phases/05-dogfood-e2e/05-SUMMARY.md && grep -q '^## Resolved Discretion Items' .planning/phases/05-dogfood-e2e/05-SUMMARY.md && grep -q '^## Handoff Notes for v0.2.0' .planning/phases/05-dogfood-e2e/05-SUMMARY.md && grep -q '^## Self-Check: PASSED' .planning/phases/05-dogfood-e2e/05-SUMMARY.md && [ "$(grep -c '[À-ÿ]' .planning/phases/05-dogfood-e2e/05-SUMMARY.md)" -eq 0 ]</automated>
  </verify>

  <acceptance_criteria>
    - File `.planning/phases/05-dogfood-e2e/05-SUMMARY.md` exists.
    - File length ≥ 80 lines.
    - H1 title is `# Phase 05: Dogfood E2E — Close Summary`.
    - Frontmatter contains all required keys: `phase`, `status: complete`, `completed: 2026-05-30`, `plans`, `requirements_completed`, `bats_suite_before: 73/0`, `bats_suite_after: 81/0`, `shipped_artifacts`, `satisfies_project_criterion`.
    - All required sections present: `## Goal`, `## Plans Executed`, `## Verification Results`, `## Resolved Discretion Items`, `## Handoff Notes for v0.2.0`, `## Self-Check: PASSED` — verifiable via `grep -c '^## '` returning ≥ 6.
    - `## Verification Results` contains the three sub-sections `### ROADMAP Phase 5 Success Criteria`, `### NFR Verification`, `### Bats suite totals` — verifiable via `grep -c '^### '` returning ≥ 3 (sub-headings within Verification Results may add more).
    - `## Plans Executed` table has 4 rows (one per plan).
    - `## Resolved Discretion Items` table has 11 rows (one per Discretion item from CONTEXT.md).
    - `## Self-Check: PASSED` footer contains ≥ 10 checklist items each marked `✓`.
    - `grep -c '[À-ÿ]' .planning/phases/05-dogfood-e2e/05-SUMMARY.md` returns 0 — NFR-005 respected.
    - File IS staged but NOT yet committed.
    - The full `<automated>` verify command exits 0.
  </acceptance_criteria>

  <done>
    `.planning/phases/05-dogfood-e2e/05-SUMMARY.md` exists with all required frontmatter, sections, sub-tables, and Self-Check checklist; mirrors `04-SUMMARY.md` structure verbatim; English-only; length ≥ 80 lines; staged but not committed (Task 05-04-03 bundles).
  </done>
</task>

<task type="auto">
  <name>Task 05-04-03: Append cutover.completed + phase.completed events; final bundled commit</name>

  <read_first>
    - core/bin/chantier (the `state_append` function definition, around lines 159-209 — read to confirm the exact flag shape `chantier state append -e EVENT -t TASK -s SKILL -m SUMMARY -r REF [-r REF ...]`. Note that `-t TASK` and `-s SKILL` are optional; the `cutover.completed` and `phase.completed` events have no per-task or per-skill scope, so they omit `-t` and `-s` per Phase 4's `phase.completed` precedent at STATE.md row 47.)
    - .planning/STATE.md (FULL FILE, 48 lines — read to confirm: (a) the frontmatter at lines 1-13 (gsd_state_version, milestone, status, last_updated, progress block); (b) row 24 contains the `bootstrap.harness.chosen` event with `ts: 2026-05-29T18:30:00Z` — this is the timestamp referenced in the cutover.completed `--ref` per Discretion #7; (c) row 47 contains the Phase 4 `phase.completed` event — the new Phase 5 `phase.completed` mirrors its shape.)
    - .planning/STATE.md LINES 22, 31, 41, 47 (the four prior `phase.completed` events — the new Phase 5 `phase.completed` follows the same JSONL shape: `{"ts":"...","event":"phase.completed","actor":"<actor>","task":null,"skill":null,"summary":"...","refs":[...]}`)
    - .planning/phases/05-dogfood-e2e/05-CONTEXT.md LINES 44-46 (D-08 verbatim — the cutover commit bundle contents and the cutover.completed event invocation shape)
    - .planning/phases/05-dogfood-e2e/05-RESEARCH.md LINES 864-876 (Code Example 3 — the exact `chantier state append` invocation for cutover.completed with the three --ref values)
    - .planning/phases/05-dogfood-e2e/05-RESEARCH.md LINES 708-720 (Pitfall 7 — cutover event must land in the SAME commit as the ROADMAP migration; the recommended workaround is to omit the commit-SHA self-reference and rely on the event-row's timestamp landing within the commit's timestamp range)
    - .planning/phases/05-dogfood-e2e/05-CONTEXT.md (Discretion #7 — RECOMMENDED YES: include bootstrap.harness.chosen back-ref in the cutover event's refs payload)
    - .planning/phases/05-dogfood-e2e/05-CONTEXT.md (Discretion #10 — Phase 5 phase.completed via DIRECT binary append, mirroring Phases 1-4)
    - docs/adr/0002-runtime-binary-and-state-format.md (event-shape regex `^[a-z][a-z0-9]*(\.[a-z][a-z0-9]*)+$` — confirm `cutover.completed` and `phase.completed` both match; both already in production per prior phase invocations)
    - The four 05-NN-SUMMARY.md files (the cutover/phase events' summaries reference these as evidence of phase closure)
  </read_first>

  <action>
    Execute the following from the repository root in the listed order. The `<verify>` block at the end exercises the full chain; the action steps prepare for it.

    **Step 1 — Confirm Wave 1+2+3 working tree is clean except for the staged ROADMAP and SUMMARY**:
    - `git status` should show two staged files (`.planning/ROADMAP.md` modified, `.planning/phases/05-dogfood-e2e/05-SUMMARY.md` new) and zero unstaged changes. If unstaged changes exist (other than `.planning/STATE.md` which will be modified next), STOP and surface the diff to the operator.

    **Step 2 — Append the cutover.completed event** (D-08 + Discretion #7):
    Invoke:
    ```
    chantier state append \
        --event cutover.completed \
        --summary "ROADMAP migrated to ADR 0001 native format. GSD ceases to be invoked in Chantier's own workflows." \
        --ref ".planning/ROADMAP.md" \
        --ref ".planning/STATE.md" \
        --ref "bootstrap.harness.chosen@2026-05-29T18:30:00Z"
    ```
    NOTE: the above is shown for reference. Invoke the binary directly from a shell (not as a fenced code block embedded in a file). Confirm exit 0; the binary appends one JSONL row to `.planning/STATE.md`. The row's `ts` is filled in by the binary with the current ISO-8601 UTC timestamp. The row's `actor` is filled from `git config user.name` (the binary handles this per Phase 2 D-04). The `task` and `skill` fields are `null` (no per-task scope for cutover). The `refs` array contains the three values in order.

    **Step 3 — Verify the cutover event landed**:
    - `grep -c '"event":"cutover.completed"' .planning/STATE.md` returns 1 (or more — if a previous draft attempt appended one, the operator removes it via STATE.md surgery before this task; per NFR-003 STATE.md is append-only, but a uncommitted reset is allowed in pre-commit workflow per Phase 2 idiom).
    - `tail -1 .planning/STATE.md | jq '.event'` returns `"cutover.completed"`.
    - `tail -1 .planning/STATE.md | jq '.refs | length'` returns 3.

    **Step 4 — Append the Phase 5 phase.completed event** (Discretion #10 — direct binary append, NOT via adapter):
    Invoke:
    ```
    chantier state append \
        --event phase.completed \
        --summary "Phase 5 (Dogfood E2E) complete: F3 fix shipped, ADR 0004 Proposed, six NFR audits green, tests/e2e/full_loop.bats green, ROADMAP migrated to ADR 0001 native format." \
        --ref ".planning/phases/05-dogfood-e2e/05-SUMMARY.md" \
        --ref "tests/e2e/full_loop.bats" \
        --ref "docs/adr/0004-surface-3-propagation.md"
    ```
    NOTE: the above is shown for reference. Invoke from shell; confirm exit 0.

    **Step 5 — Verify the phase event landed**:
    - `grep -c '"event":"phase.completed"' .planning/STATE.md` returns 5 (one each for Phases 1, 2, 3, 4, and now 5).
    - `tail -1 .planning/STATE.md | jq '.event'` returns `"phase.completed"`.
    - `tail -1 .planning/STATE.md | jq '.refs | length'` returns 3.

    **Step 6 — Re-update the 05-SUMMARY.md frontmatter with the actual cutover timestamp**:
    - Read the cutover event row's `ts` field: `_cutover_ts=$(grep '"event":"cutover.completed"' .planning/STATE.md | tail -1 | jq -r '.ts')`.
    - Update `cutover_event_ts: "<placeholder>"` in `05-SUMMARY.md`'s frontmatter to `cutover_event_ts: "$_cutover_ts"`.
    - `git add .planning/phases/05-dogfood-e2e/05-SUMMARY.md`.

    **Step 7 — Stage the STATE.md changes**:
    - `git add .planning/STATE.md`.

    **Step 8 — Run the full bats suite one final time before committing** (Pitfall 7 guard):
    - `bats core/tests/ tests/e2e/` MUST exit 0 with 81/0 reported. If non-zero, STOP and surface the failure to the operator — the cutover commit MUST land green.

    **Step 9 — Pre-commit human verification checkpoint** (declared via the next task, see below).

    **Step 10 — Final commit** (after operator approves checkpoint):
    - `git commit -m "chore(05-04): cutover off GSD — ROADMAP migrated, cutover.completed + phase.completed events appended"`. The commit message body explains the migration + events bundle per D-08; mentions Plan 05-04 closing Phase 5.
    - `git log -1 --stat` shows: `.planning/ROADMAP.md` modified (~10-25 lines), `.planning/STATE.md` modified (+2 rows), `.planning/phases/05-dogfood-e2e/05-SUMMARY.md` added (~80-160 lines). EXACTLY one commit. No co-author. No prior phase artifacts modified.

    **Step 11 — Post-commit verification**:
    - `git log -p -1 .planning/ROADMAP.md` shows the migration diff as the last change to the ROADMAP — confirms SC#5 "final commit of this phase" literal.
    - `git log -p -1 .planning/STATE.md` shows the cutover.completed + phase.completed rows in the SAME commit as the ROADMAP migration — confirms D-08 bundling.
    - `bats core/tests/nfr_audits.bats -f 'NFR-005'` AND `bats core/tests/nfr_audits.bats -f 'NFR-006'` both exit 0 — confirms NFR-005 (English-only) and NFR-006 (license/SPDX) audits remain green post-migration.

    Implements: D-07, D-08, Discretion #7, Discretion #10, SC#5. Cites: per D-07, D-08, Discretion #7, Discretion #10.
  </action>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -c '"event":"cutover.completed"' .planning/STATE.md | grep -qE '^[1-9][0-9]*$' && grep -c '"event":"phase.completed"' .planning/STATE.md | grep -qE '^5$' && tail -1 .planning/STATE.md | jq -e '.event == "phase.completed"' && grep -B 0 '"event":"cutover.completed"' .planning/STATE.md | tail -1 | jq -e '.refs | length == 3' && bats core/tests/ tests/e2e/ && bats core/tests/nfr_audits.bats -f 'NFR-005' && bats core/tests/nfr_audits.bats -f 'NFR-006'</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c '"event":"cutover.completed"' .planning/STATE.md` returns exactly 1.
    - `grep -c '"event":"phase.completed"' .planning/STATE.md` returns exactly 5.
    - The cutover.completed row has `refs` containing exactly: `.planning/ROADMAP.md`, `.planning/STATE.md`, `bootstrap.harness.chosen@2026-05-29T18:30:00Z`.
    - The Phase 5 phase.completed row has `refs` containing exactly: `.planning/phases/05-dogfood-e2e/05-SUMMARY.md`, `tests/e2e/full_loop.bats`, `docs/adr/0004-surface-3-propagation.md`.
    - `bats core/tests/ tests/e2e/` exits 0 with 81/0 reported.
    - `bats core/tests/nfr_audits.bats -f 'NFR-005'` exits 0.
    - `bats core/tests/nfr_audits.bats -f 'NFR-006'` exits 0.
    - `git log -1 --stat | head -20` shows exactly three files in the final commit: `.planning/ROADMAP.md`, `.planning/STATE.md`, `.planning/phases/05-dogfood-e2e/05-SUMMARY.md`.
    - The final commit message starts with `chore(05-04): cutover off GSD`.
    - `git log -p -1 .planning/STATE.md | grep -c '"event":"cutover.completed"\|"event":"phase.completed"'` returns 2 — both events in the SAME commit (D-08 bundling enforced).
    - No commit between the cutover commit and the prior plan commits introduces changes to `.planning/ROADMAP.md` or `.planning/STATE.md` — verified via `git log --oneline -5 -- .planning/ROADMAP.md .planning/STATE.md` showing only the cutover commit and any prior plan-SUMMARY commits.
    - The full `<automated>` verify command exits 0.
  </acceptance_criteria>

  <done>
    `.planning/STATE.md` contains exactly one `cutover.completed` row (with the three D-08 refs including the bookend back-ref) and exactly five `phase.completed` rows (the new Phase 5 row at the tail). The final commit bundles the ROADMAP migration + 05-SUMMARY.md + both STATE.md event rows in a single git commit. Full bats suite green at 81/0. NFR-005 and NFR-006 audits green post-migration. SC#5 closed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint 05-04-CP: Human verify ROADMAP migration narrative quality + cutover commit shape</name>

  <what-built>
    The four files modified in Plan 05-04 — `.planning/ROADMAP.md` (minimalist GSD→native migration per D-07), `.planning/phases/05-dogfood-e2e/05-SUMMARY.md` (Phase 5 close summary), and two new STATE.md event rows (`cutover.completed` + `phase.completed`) — are staged and ready to commit. The bats suite is green at 81/0. The verification gates have all run automatically. This checkpoint exists because per `.planning/phases/05-dogfood-e2e/05-VALIDATION.md` §"Manual-Only Verifications", three behaviors require operator judgment: (1) the ROADMAP narrative diff is interpretive — only the Format-note callout should be stripped, no other content drift; (2) the cutover.completed event payload semantics — the refs list `[".planning/ROADMAP.md", ".planning/STATE.md", "bootstrap.harness.chosen@2026-05-29T18:30:00Z"]` is the symbolic bookend, operator confirms this matches D-08 intent; (3) ADR 0004's prose completeness — Context/Decision/Consequences/Alternatives/Open Questions sections are author-judgment (this was done in Plan 05-02, but the operator may want a final read before merging the cutover commit).
  </what-built>

  <how-to-verify>
    1. Review the staged ROADMAP migration diff:
       - Run `git diff --cached .planning/ROADMAP.md | less`.
       - Confirm: the `> **Format note (temporary):** ...` blockquote at line 3 is deleted.
       - Confirm: the Phase 5 §Plans block now lists 4 entries with `[x]` checkboxes.
       - Confirm: the Phase 5 Progress-table row reads `4/4 | Complete | 2026-05-30`.
       - Confirm: NO changes to Phase 1, 2, 3, or 4 sections (D-08 historical preservation).
       - Confirm: NO changes to the `# Roadmap: Chantier` H1 title, `## Overview`, or any other section structure.
       - Confirm: no French text introduced.

    2. Review the staged 05-SUMMARY.md:
       - Run `cat .planning/phases/05-dogfood-e2e/05-SUMMARY.md | less`.
       - Confirm: frontmatter has all required keys (phase, status, completed, plans, requirements_completed, bats_suite_before/after, shipped_artifacts, satisfies_project_criterion, cutover_event_ts).
       - Confirm: the `## Plans Executed`, `## Verification Results`, `## Resolved Discretion Items`, `## Handoff Notes for v0.2.0`, and `## Self-Check: PASSED` sections each contain meaningful content (not stubs).
       - Confirm: the Discretion Items table has 11 rows.
       - Confirm: the Handoff Notes section captures the v0.2.0 backlog accurately (F2, F4, second adapter, ADR 0003/0004 ratification, workflow skill authoring, extract-skills-from-phase, STATE.md compaction).

    3. Review the staged STATE.md additions:
       - Run `git diff --cached .planning/STATE.md`.
       - Confirm: exactly two new JSONL rows added, no existing rows modified.
       - The cutover.completed row: confirm `"event":"cutover.completed"`, `"refs"` contains the three expected values, summary text matches D-08.
       - The phase.completed row: confirm `"event":"phase.completed"`, `"refs"` contains the three expected values, summary captures Phase 5 deliverables.

    4. Inspect ADR 0004 (final read before cutover commit):
       - Run `cat docs/adr/0004-surface-3-propagation.md | less`.
       - Confirm: status `Proposed`; all eight sections present; Ratification path lists observable conditions; References cite ADR 0001 + Phase 4 plan 03 SUMMARY + the implementation locus.

    5. Confirm bats suite is green:
       - `bats core/tests/ tests/e2e/` — must report 81/0.

    6. Confirm shellcheck clean:
       - `shellcheck --shell=sh adapters/claude-code/run-task.sh` — must exit 0.

    7. If all checks pass: approve the commit. The executor then runs `git commit -m "chore(05-04): cutover off GSD — ROADMAP migrated, cutover.completed + phase.completed events appended"` to complete the cutover.

    8. If any check fails: describe the issue and the executor unwinds with `git reset HEAD .planning/ROADMAP.md .planning/STATE.md .planning/phases/05-dogfood-e2e/05-SUMMARY.md` and addresses the gap.
  </how-to-verify>

  <resume-signal>Type "approved" to proceed with the cutover commit, or describe the issue to address.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ROADMAP edit → narrative drift | Manual edit; risk: accidental Phase 1-4 history modification or non-minimalist diff |
| `chantier state append` → STATE.md | The only sanctioned writer (NFR-003); risk: drift between intent and actual JSONL row shape |
| Cutover commit boundary → git log | Bundling means one commit shows the symbolic moment; risk: split commits fracture the historical record |
| Discretion #7 back-reference timestamp | The string `bootstrap.harness.chosen@2026-05-29T18:30:00Z` is opaque to the binary (just a `--ref` value); risk: typo creates an unmatchable reference |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-HISTORY-DRIFT | Tampering | ROADMAP migration touches Phase 1-4 lines | mitigate | Acceptance criterion verifies via `git diff` that Phase 1-4 sections are unchanged; the checkpoint:human-verify step shows the diff to the operator before commit; D-08 explicit "Phase 4 plans and prior STATE.md history are NOT rewritten" governance. |
| T-NFR-003 | Tampering | STATE.md direct edit (out-of-band writer) | mitigate | Plan 05-02's NFR-003 @test (static guard) catches `>STATE.md` redirects in source; this plan invokes `chantier state append` (the sanctioned writer per ADR 0002); no direct `echo >>` or `>` redirection. The mkdir-mutex runtime guard handles concurrent-write protection. |
| T-NFR-005 | Compliance (English-only) | ROADMAP edit + SUMMARY authoring | mitigate | Acceptance criterion `grep -c '[À-ÿ]'` returns 0 for both ROADMAP.md and 05-SUMMARY.md; Plan 05-02's NFR-005 @test confirms; the `chantier state append` summary strings are English-authored per CONTEXT.md verbatim text. |
| T-NFR-006 | Governance | License/SPDX integrity post-migration | mitigate | Plan 05-02's NFR-006 @test re-runs in the `<verify>` block of Task 05-04-03; LICENSE + LICENSE-CREDITS + SPDX headers unchanged by Plan 05-04 (only ROADMAP/STATE/SUMMARY touched). |
| T-05-04-PITFALL-7 | Compliance (Bundling) | Cutover events in wrong commit | mitigate | Task 05-04-03 action sequence explicitly stages all four files BEFORE the single commit; acceptance criterion verifies via `git log -1 --stat` that exactly three files (ROADMAP.md + STATE.md + 05-SUMMARY.md) are in the final commit; `git log -p -1 .planning/STATE.md` shows both events in the same commit. |
| T-05-04-EVENT-REGEX | Compliance (ADR 0002) | cutover.completed event-name regex | accept | Both `cutover.completed` and `phase.completed` match ADR 0002's `^[a-z][a-z0-9]*(\.[a-z][a-z0-9]*)+$` regex; `phase.completed` is already in production (4 prior rows); `cutover.completed` is the first instance of its kind but the regex is enforced by the binary's `state_append` validation. |
| T-05-04-DISCRETION-7-TYPO | Tampering | Back-reference timestamp typo | mitigate | Acceptance criterion `tail -1 .planning/STATE.md | jq '.refs[2]'` returns the exact string `bootstrap.harness.chosen@2026-05-29T18:30:00Z` post-append; the checkpoint:human-verify step shows the diff before commit. |
| T-05-04-CHECKPOINT | Operational | Human-verify gate | accept | The checkpoint:human-verify task is the deliberate review point per VALIDATION.md "Manual-Only Verifications"; the gate is `blocking`; operator approval is required before the final commit. This is the only non-autonomous task in Phase 5. |
| T-05-04-SC | Tampering | npm/pip/cargo installs | accept | N/A — this plan installs no packages. All edits are in-tree Markdown + JSONL via the existing binary. |
</threat_model>

<verification>
- `.planning/ROADMAP.md` Format-note callout stripped; Phase 5 entry shows `[x]` with four plans listed; Progress row reads `4/4 | Complete | 2026-05-30`; Phase 1-4 sections unchanged
- `.planning/phases/05-dogfood-e2e/05-SUMMARY.md` exists with all required sections, ≥80 lines, English-only
- `grep -c '"event":"cutover.completed"' .planning/STATE.md` returns 1
- `grep -c '"event":"phase.completed"' .planning/STATE.md` returns 5
- Both new STATE.md rows landed in the SAME git commit as the ROADMAP migration (Pitfall 7 enforcement)
- `bats core/tests/ tests/e2e/` reports 81/0 post-cutover-commit
- `bats core/tests/nfr_audits.bats -f 'NFR-005'` and `bats core/tests/nfr_audits.bats -f 'NFR-006'` both exit 0 post-migration
- `shellcheck --shell=sh adapters/claude-code/run-task.sh` clean
- Final commit message: `chore(05-04): cutover off GSD — ROADMAP migrated, cutover.completed + phase.completed events appended`
- `git log -1 --stat` shows exactly three files in the final commit
</verification>

<success_criteria>
- D-07 implemented: ROADMAP migration is minimalist (only the Format-note callout stripped; narrative structure preserved).
- D-08 implemented: cutover.completed event in the final commit bundle, refs include the bookend back-ref per Discretion #7.
- Discretion #7 implemented: refs payload includes `bootstrap.harness.chosen@2026-05-29T18:30:00Z`.
- Discretion #10 implemented: phase.completed appended via direct `chantier state append` (symmetric with prior four phases).
- ROADMAP SC#5 closed: ROADMAP migrated to ADR 0001 native format in the final commit of Phase 5.
- v0.1.0 §Acceptance "v0.1.0 ships when" satisfied (all four bullets — FR-001..FR-010, NFR-001..NFR-006, integration test in `tests/e2e/`, ADR record contains at least one ADR resolving an ADR 0001 OQ).
- PROJECT.md v0.1.0 SC#5 closed: Chantier's own development is managed by Chantier (the dogfood loop ran end-to-end; GSD is no longer invoked).
- Phase 5 is complete. v0.1.0 is feature-complete (operator action — PROJECT.md status flip — is the only remaining housekeeping, called out in Handoff Notes).
</success_criteria>

<output>
Create `.planning/phases/05-dogfood-e2e/05-04-SUMMARY.md` when done. Include: (1) `git log -1 --stat` excerpt confirming three-file bundle; (2) `tail -2 .planning/STATE.md` showing the two new event rows with their full JSONL content; (3) `head -5 .planning/ROADMAP.md` showing the Format-note callout is stripped; (4) `bats core/tests/ tests/e2e/` final result 81/0; (5) the D-NN coverage matrix (D-01..D-10 → plan that closed each); (6) Discretion-item coverage matrix (11 items → resolution location); (7) the cutover.completed event row's `ts` field as the symbolic moment-of-cutover timestamp; (8) the v0.1.0 §Acceptance four-bullet closure proof.
</output>
</content>

---
phase: 05-dogfood-e2e
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/adr/0004-surface-3-propagation.md
  - core/tests/nfr_audits.bats
autonomous: true
requirements: [NFR-001, NFR-002, NFR-003, NFR-004, NFR-005, NFR-006]
tags: [adr, nfr-audits, bats, static-audit, surface-3, codification]
must_haves:
  truths:
    - "Per D-09: `docs/adr/0004-surface-3-propagation.md` exists at the project root, ships in `Proposed` status, mirrors the section structure of `docs/adr/0003-workflow-skill-design-principles.md` (Status header, Provenance, Context, Decision, Consequences, Alternatives considered, Open questions, Ratification path, References), and codifies the Phase 4 plan 03 discovery: the adapter SHALL `cp` plain files from `$DOSSIER/` to `$TASK_DIR/`, EXCLUDING `inputs.yml`, `env.sh`, `subagent.transcript.log`, and subdirectories (`reads/`, `upstream/`, `skill/`)."
    - "Per D-09 + RESEARCH Pitfall 6: ADR 0004 includes a `## Ratification path` section listing the observable conditions for moving from Proposed to Accepted (second harness adapter ships; cross-harness e2e proves the contract; maintainer reviews and updates) — modeled on ADR 0003 lines 196-206."
    - "Per D-05: `core/tests/nfr_audits.bats` exists as a single consolidated file with exactly six `@test` blocks, one per NFR. Path is `core/tests/` (NOT `tests/e2e/`) — the audits are tree-level static invariants, separate from the workflow proof in Plan 05-03."
    - "Per D-06 NFR-001: the @test for NFR-001 duplicates the deny-list grep pattern from `core/tests/adapter_isolation.bats:42-122` inline (NOT sourced) per RESEARCH Open Question 4 recommendation. The `_full` literal is byte-identical to `core/bin/chantier:687`, `core/bin/chantier:912`, and `core/tests/adapter_isolation.bats:46`. The path-only carve-out for `adapters/claude-code/` mirrors Phase 4 D-10."
    - "Per D-06 NFR-002 + Discretion #5: the @test for NFR-002 runs `shellcheck --shell=sh` per-file in a `for` loop (NOT `find ... | xargs shellcheck`) for better bats failure legibility. Scope: every `.sh` file under `core/bin/`, `core/tests/`, `adapters/*/`, `skills/*/`, plus the literal `core/bin/chantier` binary (extensionless). Additionally greps for forbidden bash-only constructs `[[ `, `<<<`, `mapfile`, `declare -a`, `local -a`. Skips with explanation if `command -v shellcheck` is absent."
    - "Per D-06 NFR-003: the @test for NFR-003 greps for `>[[:space:]]*[^&].*STATE\\.md` (single-redirect to STATE.md, excluding `>>` append and `2>&1` style) outside `core/bin/chantier` (where `state_append()` is the only sanctioned writer). Scope: `core/`, `skills/`, `adapters/`, `tests/`. The runtime mkdir-mutex from Phase 2 D-09 is the runtime guard; this audit is the static guard."
    - "Per D-06 NFR-004: the @test for NFR-004 greps for network primitives `curl `, `wget `, `http://`, `https://`, `nc -`, `telnet ` in executable code only (`*.sh` and the `chantier` binary). Documentation `.md` files are exempt by file extension. Comment lines starting with `#` containing URLs are stripped before the deny-list pattern is applied. The three legitimate `https://issue/237` references in `skills/subagent-driven-development/{SKILL.md,PRESSURE.md}` (issue citations) are in `.md` files and exempt by extension."
    - "Per D-06 NFR-005 + Discretion #6 + Pitfall 5: the @test for NFR-005 uses the French-stop-words density approach (RECOMMENDED). It greps each in-scope file for occurrences of `\\bavec\\b|\\bdonc\\b|\\bainsi\\b|\\bpour\\b|\\bdans\\b|\\bc.est\\b|\\bn.est\\b|\\bcette\\b|\\bnous\\b` and flags any file with ≥5 hits. Scan scope: `README.md`, `LICENSE`, `LICENSE-CREDITS`, `CONTRIBUTING.md`, `docs/adr/`, `docs/vision.md`, `docs/research/`, `core/`, `skills/`, `adapters/`, `tests/`. Exempted by scope (NOT walked): `.planning/`, `docs/strategy/`. This approach catches sentence-level French leakage without false-positive-flagging isolated loanwords (naïve, café, résumé)."
    - "Per D-06 NFR-006: the @test for NFR-006 asserts: (a) `head -1 LICENSE` equals `MIT License`; (b) `LICENSE-CREDITS` file exists; (c) every `.sh` file under `core/bin/`, `core/tests/`, `adapters/`, `skills/`, `tests/` contains `SPDX-License-Identifier: MIT` (per Phase 4 D-NFR-006 convention); (d) `LICENSE` contains the literal `Chantier Contributors`; (e) `LICENSE` does NOT match the pattern `\\(c\\)[[:space:]]+[A-Z][a-z]+[[:space:]]+[A-Z][a-z]+` (no per-person attribution; collective copyright)."
    - "Per Pattern A + Pattern B: `setup()` block loads `test_helper/bats-support/load` and `test_helper/bats-assert/load`, then `cd \"$BATS_TEST_DIRNAME/../..\"` lands at repo root. Every line in the bats file containing a harness identifier (`claude-code`, `mcp__`, `claude_ai_`, `@codebase`, `cursor`, `codex-cli`, `copilot-cli`, `gemini-cli`, `opencode`) carries a trailing `# HARNESS_DENY_LIST_CHECK` comment; the audit's `grep -v 'HARNESS_DENY_LIST_CHECK'` step strips them before applying the deny-list pattern."
    - "Per SC#4: running `bats core/tests/nfr_audits.bats` against the current Chantier tree exits 0 — all six NFRs hold pre-existing the patch (verified by host probes in RESEARCH §Standard Stack). The audit gate is preventative for future regressions."
    - "After this plan lands, bats suite goes from 74/0 (post Plan 05-01) to 80/0 (74 + 6 new @tests in `nfr_audits.bats`). The ADR file ships under `docs/adr/` and validates by inspection against the ADR 0003 template; no schema check."
  artifacts:
    - path: "docs/adr/0004-surface-3-propagation.md"
      provides: "ADR 0004 Proposed-status codification of Surface 3 propagation contract (D-09)"
      min_lines: 110
      contains: "Status: Proposed"
    - path: "core/tests/nfr_audits.bats"
      provides: "Consolidated 6-@test NFR audit harness (D-05, D-06)"
      min_lines: 220
      contains: "nfr_audits: NFR-001"
  key_links:
    - from: "core/tests/nfr_audits.bats (NFR-001 @test)"
      to: "core/bin/chantier:687,912 + core/tests/adapter_isolation.bats:46"
      via: "byte-identical _full deny-list literal"
      pattern: "mcp__\\|claude_ai_\\|@codebase\\|claude-code\\|cursor\\|codex-cli\\|copilot-cli\\|gemini-cli\\|opencode"
    - from: "core/tests/nfr_audits.bats (NFR-006 @test)"
      to: "LICENSE, LICENSE-CREDITS, every *.sh file"
      via: "head -1 + file-exists + SPDX grep + per-person regex"
      pattern: "SPDX-License-Identifier: MIT"
    - from: "docs/adr/0004-surface-3-propagation.md"
      to: "adapters/claude-code/run-task.sh:202-219"
      via: "References section citing the implementation locus"
      pattern: "adapters/claude-code/run-task\\.sh"
    - from: "docs/adr/0004-surface-3-propagation.md"
      to: "ADR 0001 §Surface 3 + ADR 0003 template"
      via: "Provenance and section-structure inheritance"
      pattern: "ADR 0001|ADR 0003"
---

<objective>
Ship two independent artifacts in one plan: (1) ADR 0004 in `Proposed` status at `docs/adr/0004-surface-3-propagation.md`, codifying the Surface 3 propagation contract that Phase 4 plan 03 discovered (the adapter `cp`s plain files from `$DOSSIER/` to `$TASK_DIR/`, excluding adapter-owned artifacts and subdirectories); and (2) `core/tests/nfr_audits.bats` — a single consolidated bats file with six `@test` blocks, one per NFR-001..NFR-006, providing the SC#4-explicit independent verification gate.

Purpose: Implements D-05 (consolidated audit file), D-06 (per-NFR audit shapes), D-09 (ADR 0004 codification reflex — author Proposed, defer ratification until a second adapter exists). Locks the v0.1.0 acceptance gate "NFR-001 through NFR-006 are independently verified" (ROADMAP SC#4) in test code. The two artifacts share no source overlap — they ship in parallel within this plan and the file-set has zero intersection with Plan 05-01's `adapters/claude-code/run-task.sh` + `core/tests/adapter_upstream_e2e.bats` (verified A5 in RESEARCH).

Output: One new Markdown ADR (~110-160 lines, mirrors ADR 0003 template); one new bats file (~220-280 lines, six independent @tests). After this plan: `bats core/tests/` exits 0 with 80/0 reported; ADR 0004 is the fourth ADR in the project; `docs/adr/0004-surface-3-propagation.md` parses as standard Markdown with the canonical Provenance/Context/Decision/Consequences/Alternatives/Open-questions/Ratification-path/References sections. Independently shippable: produces a green test today AND verifiably documents a contract that has been live since Phase 4 plan 03 (i.e., the audit goes from "vacuously green" to "actively-enforcing six-NFR green" the moment this plan lands).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-dogfood-e2e/05-CONTEXT.md
@.planning/phases/05-dogfood-e2e/05-RESEARCH.md
@.planning/phases/05-dogfood-e2e/05-PATTERNS.md
@.planning/phases/05-dogfood-e2e/05-VALIDATION.md
@docs/adr/0001-state-skill-contract.md
@docs/adr/0002-runtime-binary-and-state-format.md
@docs/adr/0003-workflow-skill-design-principles.md
@core/tests/adapter_isolation.bats
@core/tests/skill_uniformity.bats
@core/bin/chantier
@adapters/claude-code/run-task.sh
@LICENSE
@LICENSE-CREDITS
</context>

<tasks>

<task type="auto">
  <name>Task 05-02-01: Author docs/adr/0004-surface-3-propagation.md in Proposed status</name>

  <read_first>
    - docs/adr/0003-workflow-skill-design-principles.md (FULL FILE, 216 lines — PRIMARY template. ADR 0004 copies section-for-section structure: the YAML-like dash-bullet header block (Status, Date proposed, Date accepted, Deciders, Supersedes, Superseded by); the blockquote synopsis (3-paragraph framing); the `---` separator; the seven canonical sections in order: `## Provenance`, `## Context`, `## Decision`, `## Consequences` (with `### Positive` and `### Negative` subsections; optional `### Mitigations`), `## Alternatives considered` (with `### Alternative A — name`, `### Alternative B — name`, `### Alternative C — name` subsections), `## Open questions (deferred)`, `## Ratification path` (lines 196-206 are the load-bearing shape), `## References`. Copy ALL section names, bullet shapes, and the Ratification-path block structure verbatim — only the prose content changes.)
    - docs/adr/0001-state-skill-contract.md LINES 82-180 (Surface 2 and Surface 3 specifications — ADR 0004's Context section cites this as the parent contract. Specifically lines 142-180 describe Surface 3's `output.md` + `output.json` requirement without specifying the mechanism; ADR 0004 fills that gap.)
    - docs/adr/0002-runtime-binary-and-state-format.md (read for ADR-template fluency — the third ADR in the series sets the precedent for status/version/references conventions)
    - adapters/claude-code/run-task.sh LINES 195-225 (the Surface 3 propagation block in the production adapter — the implementation ADR 0004 codifies. The exact line range may shift slightly depending on the Phase 4 plan 03 fix's final line numbers; locate the section by grepping for `# Section 5` or the `cp` loop that copies dossier files to TASK_DIR while excluding `inputs.yml`, `env.sh`, and `subagent.transcript.log`. Reference these line numbers in ADR 0004's References section verbatim — they are the load-bearing implementation locus.)
    - .planning/phases/04-claude-code-adapter/04-SUMMARY.md (read §"Surface 3 propagation discovery" or §"Handoff Notes" — the historical narrative that ADR 0004's Provenance section captures. F1 finding = ADR 0004 codification trigger.)
    - .planning/phases/04-claude-code-adapter/04-03-SUMMARY.md (the plan-03 SUMMARY where the propagation fix landed — Provenance cites this commit/SUMMARY)
    - .planning/phases/05-dogfood-e2e/05-RESEARCH.md LINES 741-844 (Code Example 1 — full ADR 0004 skeleton with section-by-section guidance. Use this as the prose-content backbone; the planner's authorship per Discretion #4 is the exact wording, NOT the section structure.)
    - .planning/phases/05-dogfood-e2e/05-PATTERNS.md LINES 425-481 (template-mirror map for `0004-surface-3-propagation.md`; the Ratification-path block shape at lines 462-477 is verbatim-instructive)
    - .planning/phases/05-dogfood-e2e/05-CONTEXT.md LINES 48 (D-09 verbatim — the locked decision this ADR enforces)
    - .planning/REQUIREMENTS.md §Acceptance "v0.1.0 ships when" (the line "ADR record contains ADR 0001 (state/skill contract), ADR 0002 (STATE.md format + binary spec), and at least one ADR resolving one of the four deferred questions from ADR 0001" — ADR 0004 is one of the candidates satisfying that clause)
  </read_first>

  <action>
    Create `docs/adr/0004-surface-3-propagation.md`. Mirror `docs/adr/0003-workflow-skill-design-principles.md` section-for-section. Use the RESEARCH §Code Example 1 skeleton (lines 741-844) as the prose backbone. Do NOT include fenced code blocks except where ADR 0003 includes them (typically in `## Decision` and `## Alternatives considered` for short illustrative snippets ≤10 lines).

    **Header block** (copy ADR 0003 lines 1-9, change values only):
    - `# ADR 0004 — Surface 3 propagation`
    - `- **Status:** Proposed`
    - `- **Date proposed:** 2026-05-30`
    - `- **Date accepted:** —`
    - `- **Deciders:** Chantier founding contributors`
    - `- **Supersedes:** —`
    - `- **Superseded by:** —`

    **Synopsis blockquote** (3 paragraphs, ~10-15 lines total). Frame the gap (ADR 0001 §Surface 3 specifies the destination paths for `output.md` + `output.json` in `state_writes`, but not the mechanism by which they arrive there); the discovery moment (Phase 4 plan 03 found that the skill writes to `$PWD` = the dossier root per Phase 4 D-06, while `chantier validate-task` expects them at `.planning/phases/<phase>/tasks/<task>/`); the resolution (the adapter performs a propagation step — `cp` of plain files from dossier to TASK_DIR, excluding adapter-owned artifacts). Close with "Status is **Proposed**; ratification requires a second harness adapter to validate the contract cross-harness."

    **`---` horizontal rule separator** (ADR 0003 line 11 precedent).

    **`## Provenance`** (~8-12 lines). Cite the Phase 4 plan 03 SUMMARY (`.planning/phases/04-claude-code-adapter/04-03-SUMMARY.md`) and the implementation locus (`adapters/claude-code/run-task.sh` lines ~195-225 — confirm exact line range by reading the file at task execution time). Describe the discovery in 2-3 sentences: the e2e test caught a real gap in the smoke-test where `chantier validate-task` exited non-zero because `output.md` did not exist at the gate-2-expected path; the ~12-line `cp` fix at the adapter's Section 5 made the dossier→TASK_DIR round-trip work.

    **`## Context`** (~15-20 lines). Three forces, one bullet per force, modeled on ADR 0003's Context section:
    1. ADR 0001 Surface 3 specifies the *destination* (paths in `state_writes` resolving to `.planning/phases/<phase>/tasks/<task>/output.{md,json}`) but not the *mechanism* (who copies, when, with what exclusions).
    2. Phase 4 D-06 made the dossier worktree-local (`$WORKTREE/.chantier/dossiers/<task>/`), and Phase 3 D-04 made the skill responsible for emitting `output.md` + `output.json` to its `$PWD`. The skill's `$PWD` is the dossier (per the adapter's `cd "$DOSSIER"` in the dispatch heredoc), NOT the TASK_DIR.
    3. The adapter is the only component aware of both directories and the only component that runs between the subagent exit and the `chantier validate-task` invocation. So the adapter is the logical owner of the propagation step.

    **`## Decision`** (~15-25 lines). State the contract precisely:
    - "The adapter SHALL, after the subagent's `claude -p` exits 0 (or the `CHANTIER_CLAUDE_BIN` stub exits 0) AND BEFORE invoking `chantier validate-task`, copy every plain file from `$DOSSIER/` to `$TASK_DIR/`, EXCLUDING `inputs.yml`, `env.sh`, `subagent.transcript.log`, and ALL subdirectories (`reads/`, `upstream/`, `skill/`)."
    - Use `cp` (NOT `mv`) to honor Phase 4 D-08 dossier preservation — the dossier remains inspectable for forensics even after propagation completes.
    - On `claude -p` failure: the adapter SHALL move `output.md` and `output.json` (if they exist) to `.planning/phases/<phase>/tasks/<task>/attempts/<NN>/` per Phase 4 D-04 exit-matrix wiring; propagation to TASK_DIR is gated on green dispatch.
    - The exclusion list is a static contract; the adapter implementation lists each excluded filename literally.

    **`## Consequences`**:
    - `### Positive` (3-5 bullets): skill body does not need to know about `$CHANTIER_TASK_DIR`; dossier preservation (forensics) and TASK_DIR canonicalization coexist; Surface 3 validation gates (2 and 3 in `chantier validate-task`) operate on stable canonical paths regardless of harness; the contract is observable by file inspection (no introspection of the adapter required).
    - `### Negative` (2-3 bullets): the exclusion list is a static contract — adapter-owned artifacts added in the future must be explicitly added to the list, creating drift risk; per-file `cp` is non-transactional (a killed adapter mid-propagation leaves TASK_DIR partial); the propagation step adds I/O on every dispatch, even when validate-task is going to fail (acceptable cost for the forensic value).
    - `### Mitigations` (optional, 2-3 bullets if included): code-review checklist for new adapters includes the exclusion list; future v0.2 may add transactional rename if a real failure mode surfaces; the static NFR-003 audit (this plan's NFR-003 @test) catches any out-of-band STATE.md writes.

    **`## Alternatives considered`** — three subsections (`### Alternative A`, `### Alternative B`, `### Alternative C`), each ~6-10 lines, with explicit Rejected/Accepted disposition and rationale per ADR 0003 precedent:
    - **Alternative A — Skill writes directly to TASK_DIR** (rejected): would require every skill body to know `$CHANTIER_TASK_DIR` env var; couples skill bodies to adapter scaffold; violates Phase 3 D-04 self-emission and Phase 3 portability claim.
    - **Alternative B — Symlink dossier root to TASK_DIR before dispatch** (rejected): couples two namespaces; failing skill leaves partial state in TASK_DIR; ADR 0001 Surface 3 wants the validation gates run against final state, not in-flight state.
    - **Alternative C — Skill emits a manifest, adapter consumes it** (rejected): adds a new file format and a new contract; the current `cp dossier/* → TASK_DIR/` with exclusion list achieves the same outcome with no new contract surface.

    **`## Open questions (deferred)`** — two-three questions per RESEARCH lines 812-822:
    1. Subdirectory propagation — current contract copies only plain files in `$DOSSIER/` root; if a future skill writes artifacts into subdirectories (`attempts/`, `logs/`), the propagation does not include them. Phase 5 dogfood does not exercise this. Revisit if a v0.2 skill needs subdirectory artifacts.
    2. Atomic propagation — current `cp` is per-file; a killed adapter mid-propagation leaves TASK_DIR partial. Phase 4 has not surfaced this in production; v0.2+ may add a transactional rename pattern (cp to `$TASK_DIR/.staging/`, then rename) if a failure mode surfaces.
    3. (Optional third) — Exclusion-list canonicalization: should the list live in a single canonical place (e.g., `core/schemas/adapter-exclusions.json`) read by every adapter, or remain inlined in each `run-task.sh`? Trade-off: DRY vs. POSIX-shell-portable JSON parsing. Defer until second adapter ships.

    **`## Ratification path`** (verbatim shape from ADR 0003 lines 196-206, adapt the conditions). The block:
    ```
    This ADR remains in **Proposed** status until:

    1. A second harness adapter (e.g., `adapters/cursor/`) ships and exercises the Surface 3 propagation contract.
    2. A bats test under `tests/e2e/` proves the contract works on both adapters (`output.md` + `output.json` land in TASK_DIR; excluded artifacts remain only in the dossier).
    3. A maintainer reviews this ADR with cross-harness evidence and either updates the contract or moves status to **Accepted**.

    Until ratification, this ADR is advisory. The first cross-harness propagation PR should reference this ADR explicitly.
    ```
    NOTE: the above is shown for reference; write it as plain Markdown inside the ADR, not as a fenced code block (ADRs use Markdown narrative for prose).

    **`## References`** (5-8 bullets, modeled on ADR 0003 lines 210-216):
    - `docs/adr/0001-state-skill-contract.md` §Surface 3 — establishes that `output.md` + `output.json` are mandatory at canonical paths.
    - `docs/adr/0001-state-skill-contract.md` §Surface 2 — establishes the dossier model that `cp` propagates FROM.
    - `docs/adr/0002-runtime-binary-and-state-format.md` — the exit-code matrix the adapter honors when propagation succeeds (exit 0) or the wrapped subagent fails (exit 1).
    - `.planning/phases/04-claude-code-adapter/04-03-SUMMARY.md` — the discovery moment and the implementation patch.
    - `adapters/claude-code/run-task.sh` lines [verify range at write time, expected ~195-225] — the implementation that this ADR codifies. Cite the exact line range or anchor by grep-able section comment.
    - `.planning/phases/05-dogfood-e2e/05-CONTEXT.md` §D-09 — the locked decision authorizing this ADR's authorship in Phase 5.

    **English-only** (NFR-005): no accented characters in the ADR body. Verify via `grep -c '[À-ÿ]' docs/adr/0004-surface-3-propagation.md` returns 0.

    **No fenced code blocks inside `<action>` here**: the above is directive prose. The executor writes the ADR as plain Markdown with the section structure, prose content, and one or two short fenced code blocks ONLY where the implementation reference (exclusion-list literal, propagation pseudo-code) warrants it — mirroring ADR 0003's selective use of fenced blocks.

    Implements: D-09. Cites: per D-09 and ADR 0001 §Surface 3.
  </action>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && test -f docs/adr/0004-surface-3-propagation.md && head -10 docs/adr/0004-surface-3-propagation.md | grep -qE '^- \*\*Status:\*\* Proposed' && grep -q '^## Provenance' docs/adr/0004-surface-3-propagation.md && grep -q '^## Context' docs/adr/0004-surface-3-propagation.md && grep -q '^## Decision' docs/adr/0004-surface-3-propagation.md && grep -q '^## Consequences' docs/adr/0004-surface-3-propagation.md && grep -q '^## Alternatives considered' docs/adr/0004-surface-3-propagation.md && grep -q '^## Open questions' docs/adr/0004-surface-3-propagation.md && grep -q '^## Ratification path' docs/adr/0004-surface-3-propagation.md && grep -q '^## References' docs/adr/0004-surface-3-propagation.md && [ "$(grep -c '[À-ÿ]' docs/adr/0004-surface-3-propagation.md)" -eq 0 ] && [ "$(wc -l < docs/adr/0004-surface-3-propagation.md)" -ge 110 ]</automated>
  </verify>

  <acceptance_criteria>
    - File `docs/adr/0004-surface-3-propagation.md` exists at the project root.
    - `head -10 docs/adr/0004-surface-3-propagation.md` shows the title `# ADR 0004 — Surface 3 propagation` followed by the dash-bullet header block with `Status: Proposed`.
    - The file contains all eight canonical sections in order: `## Provenance`, `## Context`, `## Decision`, `## Consequences`, `## Alternatives considered`, `## Open questions`, `## Ratification path`, `## References` — verifiable via `grep -c '^## ' docs/adr/0004-surface-3-propagation.md` returning ≥ 8.
    - The `## Consequences` section contains both `### Positive` and `### Negative` subsections — `grep -c '^### Positive\|^### Negative' docs/adr/0004-surface-3-propagation.md` returns 2 (or 3 if `### Mitigations` is also included).
    - The `## Alternatives considered` section contains at least three subsections — `grep -c '^### Alternative' docs/adr/0004-surface-3-propagation.md` returns ≥ 3.
    - File length ≥ 110 lines (substantial codification, not a stub) and ≤ 250 lines (focused, not bloated).
    - `grep -c '[À-ÿ]' docs/adr/0004-surface-3-propagation.md` returns 0 — no accented characters; NFR-005 English-only respected.
    - `grep -c 'ADR 0001' docs/adr/0004-surface-3-propagation.md` returns ≥ 2 (Surface 2 and Surface 3 both cited in References).
    - `grep -c 'adapters/claude-code/run-task.sh' docs/adr/0004-surface-3-propagation.md` returns ≥ 1 (the implementation locus is cited).
    - `grep -c '04-03-SUMMARY' docs/adr/0004-surface-3-propagation.md` returns ≥ 1 (the discovery moment is cited).
    - The `## Ratification path` section contains numbered conditions (1., 2., 3.) — verifiable via `awk '/^## Ratification path/,/^## /' docs/adr/0004-surface-3-propagation.md | grep -cE '^[0-9]+\.'` returns ≥ 3.
    - No fenced code blocks containing more than 10 lines of code (ADR is prose-first; selective short illustrative blocks OK).
    - The full `<automated>` verify command exits 0.
  </acceptance_criteria>

  <done>
    `docs/adr/0004-surface-3-propagation.md` ships in Proposed status with all eight canonical sections, a Ratification path listing observable conditions, and References to ADR 0001 §Surface 2/3 + Phase 4 plan 03 SUMMARY + the implementation locus in `adapters/claude-code/run-task.sh`. English-only. Length 110-250 lines. Verify command exits 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 05-02-02: Author core/tests/nfr_audits.bats with six independent @test blocks (NFR-001..NFR-006)</name>

  <read_first>
    - core/tests/adapter_isolation.bats (FULL FILE, 124 lines — PRIMARY analog for NFR-001 audit shape per 05-PATTERNS.md §"core/tests/nfr_audits.bats". The NFR-001 @test in `nfr_audits.bats` duplicates this file's logic inline. Read: the `setup()` block, the byte-identical `_full` and `_narrow` deny-list literals at line 46, the `case "$_file" in` arm dispatch at lines 57-111, the HARNESS_DENY_LIST_CHECK marker convention, the POSIX find walk at lines 113-115, the heredoc-fed while-read pattern, the violation-accumulation pattern, the error-print idiom.)
    - core/tests/skill_uniformity.bats (FULL FILE, 82 lines — SECONDARY analog for the multi-@test layout per 05-PATTERNS.md. Read: the `setup()` block with simple `cd "$BATS_TEST_DIRNAME/../.."` to repo root (no TMPHOME needed for static audits), the three independent @test blocks with no shared mutable state, the `fail` helper usage from bats-assert, the file-walk patterns over `skills/` directory tree.)
    - core/bin/chantier LINES 685-702 (gate 4 deny-list literal at line 687 — the NFR-001 @test's `_full` MUST be byte-identical to this line; the marker convention is established here)
    - core/bin/chantier LINES 905-918 (--self-test self-scan; line 912 is the second byte-identical occurrence of `_full`; line 913's `grep -v 'HARNESS_DENY_LIST_CHECK'` is the marker-filtering idiom the NFR-001 @test reuses)
    - core/bin/chantier (search for `state_append()` function definition — typically around lines 159-209 — to understand which function the NFR-003 @test exempts. The audit greps for `STATE.md` writes OUTSIDE this function in OTHER files; the `core/bin/chantier` file itself is exempted via case-arm because state_append IS the sanctioned writer.)
    - LICENSE (FULL FILE — the NFR-006 @test asserts `head -1` equals `MIT License`; reads for the `Chantier Contributors` substring; greps for the no-per-person pattern. Confirm the file's current first line and copyright block shape.)
    - LICENSE-CREDITS (read to confirm existence — the NFR-006 @test uses `[ -f LICENSE-CREDITS ]`. Empty-or-not-empty is irrelevant; presence is what NFR-006 asserts.)
    - adapters/claude-code/run-task.sh LINES 1-3 (SPDX header convention — the NFR-006 @test greps every `.sh` file for `SPDX-License-Identifier: MIT` per Phase 4 D-NFR-006 convention)
    - skills/test-driven-development/run.sh LINES 1-3 (additional SPDX-header example; confirm the precise string format the @test must match)
    - skills/subagent-driven-development/SKILL.md AND skills/subagent-driven-development/PRESSURE.md (these files contain legitimate `https://` URLs citing obra/superpowers#237 — the NFR-004 @test must NOT flag these because they live in `.md` files which the @test exempts by extension)
    - .planning/phases/05-dogfood-e2e/05-RESEARCH.md LINES 509-545 (Pattern 2 narrative — the NFR-004 @test skeleton with deny-list + comment-line exemption + HARNESS_DENY_LIST_CHECK filter. Use this as the load-bearing shape for the NFR-004 @test.)
    - .planning/phases/05-dogfood-e2e/05-PATTERNS.md LINES 198-346 (full pattern map for `core/tests/nfr_audits.bats` with skeleton code for NFR-001, NFR-002, NFR-003, NFR-005, NFR-006; NFR-004 deferred to RESEARCH Pattern 2)
    - .planning/phases/05-dogfood-e2e/05-CONTEXT.md LINES 33-39 (D-05 + D-06 verbatim — the locked per-NFR audit shapes)
    - .planning/phases/05-dogfood-e2e/05-RESEARCH.md LINES 683-695 (Pitfall 5 — NFR-005 false-positive avoidance; the French-stop-words approach is the recommended choice over `[À-ÿ]` broad regex)
    - .planning/phases/05-dogfood-e2e/05-RESEARCH.md LINES 695-707 (Pitfall 6 — ADR-status conventions; NOT directly applicable to this task, read for context only)
    - .planning/REQUIREMENTS.md §Non-functional requirements (NFR-001..NFR-006 verbatim definitions — each @test's `@test "..."` name and inline comment cites the NFR ID and definition)
  </read_first>

  <behavior>
    - **NFR-001 @test (green today, preventative):** Running the @test against the current Chantier tree exits 0. The four shipped SKILL.md files (`skills/{using-git-worktrees,test-driven-development,requesting-code-review,subagent-driven-development}/SKILL.md`) each declare `- claude-code` under `harness_adapters:`; the audit filters that exact YAML line via `grep -vE '^[[:space:]]*-[[:space:]]*claude-code$'` before applying `_full`, so these files pass. Files under `adapters/claude-code/` apply `_narrow` (path-only carve-out per Phase 4 D-10). Files under `core/tests/test_helper/` are skipped via the `test_helper/` filter in the find pipeline. The bats file's own deny-list literals carry `HARNESS_DENY_LIST_CHECK` markers and are stripped before grep.
    - **NFR-002 @test (green today, conditional on shellcheck):** Running the @test against the current tree exits 0 when `shellcheck` is installed (verified by `command -v shellcheck`). The @test runs `shellcheck --shell=sh` per-file over every `.sh` file in `core/bin/`, `core/tests/`, `adapters/`, `skills/`, plus the literal `core/bin/chantier` binary. The current state of all production files is shellcheck-clean (Phase 2..4 ratified). The @test also greps for `[[ `, `<<<`, `mapfile`, `declare -a`, `local -a` and finds none in the production tree. If `shellcheck` is absent on the host, the @test SKIPs (matches existing bats pattern in `core/tests/self_test.bats`) instead of failing.
    - **NFR-003 @test (green today, preventative):** Running the @test against the current tree exits 0. The grep pattern `>[[:space:]]*[^&].*STATE\.md` (single redirect, not `>>` append nor `2>&1`) finds zero violations outside `core/bin/chantier`. Note: `core/bin/chantier` itself contains `>` writes to STATE.md inside `state_append()` — the @test exempts this file via case-arm. All bats tests that write STATE.md inside `$BATS_TEST_TMPDIR` invoke `chantier state append` via subprocess (Phase 4 idiom); none use direct `>` redirect.
    - **NFR-004 @test (green today, comment-aware):** Running the @test against the current tree exits 0. The grep pattern catches `curl `, `wget `, `http://`, `https://`, `nc -`, `telnet ` in executable code (`*.sh` + the `chantier` binary). The pre-filter strips comment lines (`^[[:space:]]*#.*http`) and HARNESS_DENY_LIST_CHECK lines. Documentation `.md` files are exempt by file-extension scope (the find walk includes only `*.sh` and the bare `chantier` filename). The three legitimate `https://github.com/.../issues/237` references in `skills/subagent-driven-development/{SKILL.md,PRESSURE.md}` are `.md` files and NOT in scope.
    - **NFR-005 @test (green today, density-based):** Running the @test against the current tree exits 0. The French-stop-words density check (`\bavec\b|\bdonc\b|...`) finds zero files with ≥5 hits across the in-scope tree (`README.md`, `LICENSE`, `LICENSE-CREDITS`, `CONTRIBUTING.md`, `docs/adr/`, `docs/vision.md`, `docs/research/`, `core/`, `skills/`, `adapters/`, `tests/`). Out-of-scope (NOT walked): `.planning/` (French session prose lives here legitimately) and `docs/strategy/` (sketches may quote conversation). The threshold ≥5 hits avoids flagging isolated loanwords like `naïve`, `café`, `résumé`.
    - **NFR-006 @test (green today, multi-assertion):** Running the @test against the current tree exits 0. `head -1 LICENSE` returns `MIT License`. `LICENSE-CREDITS` exists. Every `.sh` file under `core/bin/`, `core/tests/`, `adapters/`, `skills/`, `tests/` (zero files in `tests/` at this point — empty subdirectory expected) contains `SPDX-License-Identifier: MIT`. `LICENSE` contains the substring `Chantier Contributors`. No per-person `(c) FirstName LastName` pattern in `LICENSE`.
    - **Suite total:** After this file lands and Plan 05-01 already landed, `bats core/tests/` reports 80/0 (74 prior including Plan 05-01's regression + 6 new from this file).
    - **Independence:** Each @test is self-contained — no shared mutable state, no test-ordering dependencies. Running `bats core/tests/nfr_audits.bats -f 'NFR-003'` exercises only the NFR-003 @test and exits 0 in isolation.
    - **Falsifiability (manual probe, NOT committed):** Writing `printf 'cursor\n' >> core/bin/chantier; bats core/tests/nfr_audits.bats -f 'NFR-001'; echo exit=$?; git checkout core/bin/chantier` demonstrates the NFR-001 @test goes red on injected harness identifier and recovers on revert.
  </behavior>

  <action>
    Create `core/tests/nfr_audits.bats`. Structure: shell-mode bats file with one `setup()` block and six `@test` blocks. The file is one continuous source — no helper functions defined outside `setup()` (each @test is self-contained).

    **File-prelude comment** (8-12 lines): file path, purpose (six-@test consolidated NFR audit per `.planning/phases/05-dogfood-e2e/05-CONTEXT.md` D-05 and D-06), source (ADR 0001 + REQUIREMENTS.md NFR-001..NFR-006), the SC#4 → @test mapping (one @test per NFR), the relationship to `adapter_isolation.bats` (NFR-001 duplicates inline per RESEARCH Open Question 4 recommendation). No SPDX header (bats files exempt per Phase 4 D-NFR-006 convention, project-level LICENSE governs).

    **`setup()` block** (Pattern A simple form):
    ```
    setup() {
        load 'test_helper/bats-support/load'
        load 'test_helper/bats-assert/load'
        cd "$BATS_TEST_DIRNAME/../.."
    }
    ```

    **@test 1 — NFR-001 (no harness identifiers outside `adapters/claude-code/`)** (~30 lines):
    - Name: `@test "nfr_audits: NFR-001 — no harness identifiers in skill bodies or core (cross-tree deny-list)" { # HARNESS_DENY_LIST_CHECK`.
    - Declare `_full='mcp__|claude_ai_|@codebase|claude-code|cursor|codex-cli|copilot-cli|gemini-cli|opencode'` with trailing `# HARNESS_DENY_LIST_CHECK`. The literal MUST be byte-identical to `core/bin/chantier:687`.
    - Declare `_narrow='@codebase|cursor|codex-cli|copilot-cli|gemini-cli|opencode'` with trailing `# HARNESS_DENY_LIST_CHECK`.
    - `_violations=""`; use the case-arm dispatch from 05-PATTERNS.md lines 217-249 over a heredoc-fed `find core skills adapters -type f 2>/dev/null | grep -v 'HARNESS_DENY_LIST_CHECK\|test_helper/' | sort` pipeline.
    - Case arms in order: (a) `core/bin/chantier|core/schemas/skill.json|core/tests/adapter_isolation.bats|core/tests/nfr_audits.bats) continue ;;` — exempt the sanctioned-deny-list-carrier files; (b) `skills/*/SKILL.md)` arm applies `_full` after filtering the `^[[:space:]]*-[[:space:]]*claude-code$` line; (c) `adapters/claude-code/*)` arm applies `_narrow` (D-10 carve-out, trailing HARNESS_DENY_LIST_CHECK); (d) default `*)` arm applies `_full`.
    - On violations: `[ -z "$_violations" ] || { printf 'nfr_audits: NFR-001 violations:\n%b' "$_violations" >&2; false; }`.

    **@test 2 — NFR-002 (POSIX sh + jq only)** (~25 lines):
    - Name: `@test "nfr_audits: NFR-002 — POSIX sh + jq only (shellcheck + bash-ism grep)"`.
    - `command -v shellcheck >/dev/null 2>&1 || skip "shellcheck not installed"`.
    - Enumerate `.sh` files plus the bare `chantier` binary: `_sh_files=$(find core/bin core/tests adapters skills tests -type f \( -name '*.sh' -o -name 'chantier' \) 2>/dev/null | sort)`.
    - `_violations=""; for _f in $_sh_files; do shellcheck --shell=sh "$_f" >/dev/null 2>&1 || _violations="${_violations}${_f}\n"; done`.
    - `[ -z "$_violations" ] || { printf 'nfr_audits: NFR-002 shellcheck violations:\n%b' "$_violations" >&2; false; }`.
    - Bash-isms grep: `_bash_pat='\[\[ |<<<|mapfile|declare -a|local -a'`; for each `_f` in `$_sh_files`, if `grep -E "$_bash_pat" "$_f" >/dev/null 2>&1` then `printf 'nfr_audits: NFR-002 bash-only construct in %s\n' "$_f" >&2; false`.

    **@test 3 — NFR-003 (STATE.md append-only)** (~20 lines):
    - Name: `@test "nfr_audits: NFR-003 — STATE.md append-only (no single-> redirect outside state_append)"`.
    - Pattern: `_pat='>[[:space:]]*[^&].*STATE\.md'` (single redirect, not `>>` append nor `2>&1`).
    - Walk `find core skills adapters tests -type f \( -name '*.sh' -o -name '*.bats' \) 2>/dev/null | sort`.
    - Case-arm: `core/bin/chantier) continue ;;` — exempt the sanctioned `state_append()` writer.
    - Default: `if grep -qE "$_pat" "$_file"; then _violations="${_violations}${_file}\n"; fi`.
    - On violations: `[ -z "$_violations" ] || { printf 'nfr_audits: NFR-003 violations:\n%b' "$_violations" >&2; false; }`.

    **@test 4 — NFR-004 (no network primitives in executable code)** (~30 lines, mirror RESEARCH lines 516-544):
    - Name: `@test "nfr_audits: NFR-004 — no network primitives in executable code" { # HARNESS_DENY_LIST_CHECK`.
    - Pattern: `_net_pat='curl[[:space:]]|wget[[:space:]]|http[s]?://|nc[[:space:]]-|telnet[[:space:]]'`.
    - Walk `find core/bin adapters skills -type f \( -name '*.sh' -o -name 'chantier' \) 2>/dev/null | sort`.
    - For each file: filter via `grep -v 'HARNESS_DENY_LIST_CHECK' | grep -vE '^[[:space:]]*#.*http'`, then `grep -qE "$_net_pat"`; if match, append to `_violations`.
    - On violations: `[ -z "$_violations" ] || { printf 'nfr_audits: NFR-004 violations:\n%b' "$_violations" >&2; false; }`.

    **@test 5 — NFR-005 (English-only; French stop-words density)** (~25 lines):
    - Name: `@test "nfr_audits: NFR-005 — English-only public artifacts (French stop-word density)"`.
    - Stop-words pattern: `_stop_words='\bavec\b|\bdonc\b|\bainsi\b|\bpour\b|\bdans\b|\bc.est\b|\bn.est\b|\bcette\b|\bnous\b'` (word-boundary `\b` requires `grep -E` POSIX-extended-regex semantics; the `.` in `c.est` and `n.est` accepts both straight apostrophe and curly-quote per the discussion-log idiom).
    - Scan scope: `_scan_paths='README.md LICENSE LICENSE-CREDITS CONTRIBUTING.md'`, `_scan_dirs='docs/adr docs/vision.md docs/research core skills adapters tests'`.
    - Exempt (NOT walked): `.planning/` (legitimate French in session prose) and `docs/strategy/` (sketches may quote conversation).
    - Walk via `find $_scan_paths $_scan_dirs -type f 2>/dev/null | sort`.
    - Per file: `_hits=$(grep -cE "$_stop_words" "$_file" 2>/dev/null || true); [ "$_hits" -ge 5 ] && _violations="${_violations}${_file}: $_hits French stop-words\n"`.
    - On violations: `[ -z "$_violations" ] || { printf 'nfr_audits: NFR-005 violations:\n%b' "$_violations" >&2; false; }`.

    **@test 6 — NFR-006 (MIT + collective copyright + SPDX headers)** (~25 lines):
    - Name: `@test "nfr_audits: NFR-006 — MIT license, collective copyright, SPDX headers"`.
    - Assertions in order:
      1. `head -1 LICENSE | grep -qx 'MIT License' || { printf 'nfr_audits: NFR-006 LICENSE first line is not exactly \"MIT License\"\n' >&2; false; }`.
      2. `[ -f LICENSE-CREDITS ] || { printf 'nfr_audits: NFR-006 LICENSE-CREDITS missing\n' >&2; false; }`.
      3. `grep -q 'Chantier Contributors' LICENSE || { printf 'nfr_audits: NFR-006 LICENSE missing collective \"Chantier Contributors\"\n' >&2; false; }`.
      4. SPDX header on every `.sh`: enumerate `find core/bin core/tests adapters skills tests -type f -name '*.sh' 2>/dev/null | sort`; for each, `grep -q 'SPDX-License-Identifier: MIT' "$_f" || _missing="${_missing}${_f}\n"`; `[ -z "$_missing" ] || { printf 'nfr_audits: NFR-006 missing SPDX header:\n%b' "$_missing" >&2; false; }`.
      5. No per-person copyright: `! grep -qE '\(c\)[[:space:]]+[A-Z][a-z]+[[:space:]]+[A-Z][a-z]+' LICENSE || { printf 'nfr_audits: NFR-006 LICENSE contains per-person attribution (collective copyright required)\n' >&2; false; }`.

    **HARNESS_DENY_LIST_CHECK hygiene** (Pattern B): every line in `nfr_audits.bats` containing a harness identifier (every `_full` literal, every `_narrow` literal, every case-arm pattern `adapters/claude-code/*)`, every `@test` name mentioning identifiers) carries a trailing `# HARNESS_DENY_LIST_CHECK` comment. The case-arm `core/tests/nfr_audits.bats) continue ;;` is the self-exemption (double-belt with marker filter).

    **No fenced code blocks inside the file** — bats files are continuous shell source.

    Implements: D-05 (single consolidated file), D-06 (per-NFR audit shapes). Cites: per D-05, D-06, and RESEARCH Open Question 4 (NFR-001 duplicate inline).
  </action>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && bats core/tests/nfr_audits.bats && bats core/tests/</automated>
  </verify>

  <acceptance_criteria>
    - File `core/tests/nfr_audits.bats` exists at the repo root.
    - `bats core/tests/nfr_audits.bats` exits 0 against the current tree — all six NFRs hold.
    - `bats core/tests/nfr_audits.bats` reports exactly 6 tests via `bats --tap core/tests/nfr_audits.bats | grep -c '^ok'` returning 6.
    - Each @test can be exercised in isolation: `bats core/tests/nfr_audits.bats -f 'NFR-001'` exits 0, `... -f 'NFR-002'` exits 0, ... `... -f 'NFR-006'` exits 0.
    - `bats core/tests/` reports 80/0 (74 prior including Plan 05-01's regression + 6 from this file).
    - `grep -c 'HARNESS_DENY_LIST_CHECK' core/tests/nfr_audits.bats` returns ≥ 6 (every deny-list literal, every `adapters/claude-code/*)` case arm, every grep invocation against `$_full` or `$_narrow`).
    - `grep -v '^#' core/tests/nfr_audits.bats | grep -v 'HARNESS_DENY_LIST_CHECK' | grep -cE 'mcp__|claude_ai_|@codebase|claude-code|cursor|codex-cli|copilot-cli|gemini-cli|opencode'` returns 0 — the marker convention fully exempts the audit from self-triggering.
    - The `_full` regex byte-identical proof: `grep -oE 'mcp__\\|[^"]*opencode' core/tests/nfr_audits.bats | head -1` matches the same extraction from `core/bin/chantier:687` (compare via `diff` or `cmp`).
    - `bats core/tests/adapter_isolation.bats` still exits 0 with the new file present — the Phase 4 NFR-001 audit's `grep -v 'HARNESS_DENY_LIST_CHECK\|test_helper/'` step strips the new bats file's marker-tagged lines, and the case-arm exemption (`core/tests/nfr_audits.bats) continue ;;`) is the double-belt.
    - `shellcheck --shell=sh core/tests/nfr_audits.bats` — NOT applicable (bats files are not pure shell); skip this check.
    - The full `<automated>` verify command exits 0.
  </acceptance_criteria>

  <done>
    `bats core/tests/nfr_audits.bats` exits 0 with 6/6 reported; the full bats suite is 80/0; every NFR-001..NFR-006 has a dedicated @test that can be exercised in isolation; the NFR-001 @test's `_full` literal is byte-identical to `core/bin/chantier:687`; the Phase 4 adapter isolation audit remains green with the new file present.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source tree → static audit | The six @test blocks read files from disk only; never execute them; no untrusted input crosses |
| ADR document → human reader | ADR 0004 is advisory documentation; status `Proposed` is observable; no runtime authority |
| nfr_audits.bats deny-list literals → audit self-source | The bats file contains every harness-identifier substring; marker convention (HARNESS_DENY_LIST_CHECK) prevents self-triggering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-NFR-001 | Information Disclosure | Harness identifier leak audit (NFR-001 @test) | mitigate | Byte-identical `_full` literal to `core/bin/chantier:687`; case-arm dispatch with path-only carve-out for `adapters/claude-code/` (D-10) and `skills/*/SKILL.md` frontmatter line filter; HARNESS_DENY_LIST_CHECK self-exemption marker on every deny-list literal line; double-belt case-arm exemption for the bats file itself; `test_helper/` filter excludes vendored submodules. |
| T-NFR-002 | Compliance (NFR-002) | POSIX-shell purity audit | mitigate | `shellcheck --shell=sh` per-file loop catches non-POSIX constructs; bash-ism grep (`[[ |<<<|mapfile|declare -a|local -a`) catches grammar drift; `command -v shellcheck` precondition with `skip` (not `false`) on missing tool — matches existing bats pattern in `self_test.bats`. |
| T-NFR-003 | Tampering | STATE.md append-only audit (static guard) | mitigate | Static grep for `>[[:space:]]*[^&].*STATE\.md` outside `core/bin/chantier`; complements the runtime mkdir-mutex in `state_append()` (ADR 0002 D-09); the static guard catches source-level violations BEFORE runtime; the runtime guard catches concurrent-write violations AT runtime. |
| T-NFR-004 | Information Disclosure | Network exfiltration audit | mitigate | Network primitive deny-list (`curl |wget |http[s]?://|nc -|telnet`) in executable code only; `.md` doc files exempt by file-extension scope (legitimate URLs in citations); comment-line stripping prevents false positives on `# https://...` URL references; HARNESS_DENY_LIST_CHECK filter removes the audit's own deny-list lines. The audit catches `curl` in a skill's `run.sh`, NOT the `claude -p` binary's own network behavior (which is the skill's opt-in per NFR-004). |
| T-NFR-005 | Compliance (operational hygiene) | French-leakage audit | mitigate | Density-based French-stop-words grep (≥5 hits per file) avoids false positives on isolated loanwords (`naïve`, `café`, `résumé`) per RESEARCH Pitfall 5; scope-based exemption for `.planning/` and `docs/strategy/` (NOT walked); word-boundary `\b` constraints; `.` in `c.est`/`n.est` accepts both straight and curly apostrophe. |
| T-NFR-006 | Governance (collective copyright) | License + copyright audit | mitigate | Five independent assertions: `head -1 LICENSE` equals `MIT License`; `LICENSE-CREDITS` file exists; every `.sh` carries SPDX header (Phase 4 D-NFR-006 convention); `Chantier Contributors` substring in LICENSE; no per-person `(c) FirstName LastName` regex match in LICENSE. The audit catches drive-by personal-credit attempts and SPDX header drift. |
| T-05-02-ADR-DRIFT | Tampering | ADR 0004 vs. implementation | accept | ADR 0004 is Proposed status; ratification deferred per D-09 until cross-harness evidence exists. The implementation at `adapters/claude-code/run-task.sh:202-219` is the source of truth; if drift occurs before ratification, ADR 0004 is updated (Proposed status license to revise without supersession). |
| T-05-02-SC | Tampering | npm/pip/cargo installs | accept | N/A — this plan installs no packages. ADR 0004 is Markdown text; `nfr_audits.bats` is bash text; both ship from the in-tree edit only. |
</threat_model>

<verification>
- `bats core/tests/nfr_audits.bats` exits 0 with 6/6 reported
- `bats core/tests/` reports 80/0 (74 prior + 6 new)
- `bats core/tests/adapter_isolation.bats` exits 0 (Phase 4 audit preserved)
- `docs/adr/0004-surface-3-propagation.md` exists, status Proposed, ≥110 lines, all eight canonical sections present
- `grep -c '[À-ÿ]' docs/adr/0004-surface-3-propagation.md` returns 0 (NFR-005)
- `grep -c '[À-ÿ]' core/tests/nfr_audits.bats` returns 0 (NFR-005)
- `bats core/tests/nfr_audits.bats -f 'NFR-001'` exits 0 in isolation (independence proof; repeat for NFR-002 through NFR-006)
- NFR-001 deny-list literal byte-identical to `core/bin/chantier:687`
- `grep -c 'HARNESS_DENY_LIST_CHECK' core/tests/nfr_audits.bats` returns ≥ 6
</verification>

<success_criteria>
- D-05 implemented: single consolidated file `core/tests/nfr_audits.bats` with six @test blocks.
- D-06 implemented: each @test matches the per-NFR audit shape locked in CONTEXT.md.
- D-09 implemented: ADR 0004 ships in Proposed status with the canonical eight-section structure, Ratification path listing observable conditions, References to ADR 0001 §Surface 2/3 + Phase 4 plan 03 SUMMARY + the implementation locus.
- SC#4 (ROADMAP Phase 5) moves from claim to enforcement — NFR-001..NFR-006 are independently verified by six @test blocks.
- REQUIREMENTS.md §Acceptance "ADR record contains ... at least one ADR resolving one of the four deferred questions from ADR 0001" is satisfied by ADR 0004 (resolves the Surface 3 propagation mechanism that ADR 0001 left implicit).
- Bats suite goes 74/0 → 80/0 (exactly six new @tests, one new file).
</success_criteria>

<output>
Create `.planning/phases/05-dogfood-e2e/05-02-SUMMARY.md` when done. Include: (1) bats output excerpt showing 6/6 from `nfr_audits.bats` and the 80/0 full-suite total; (2) byte-identical proof that NFR-001's `_full` matches `core/bin/chantier:687` (`diff <(sed -n '687p' ...) <(grep -oE 'mcp__\\|...opencode' ...)` or similar); (3) ADR 0004 section-count proof (`grep -c '^## '` returns 8); (4) the per-NFR `bats -f 'NFR-NNN'` independence proof matrix; (5) NFR-005 English-only proofs for both new files (`grep -c '[À-ÿ]'` returns 0); (6) a one-line citation of the Phase 4 SUMMARY F1 finding line that ADR 0004 closes.
</output>
</content>

---
phase: 05-dogfood-e2e
plan: 03
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - tests/e2e/full_loop.bats
autonomous: true
requirements: [NFR-001, NFR-002, NFR-004]
tags: [e2e, dogfood, bats, integration-test, chantier-new, multi-task, full-loop]
must_haves:
  truths:
    - "Per D-03: `tests/e2e/full_loop.bats` exists at the new top-level `tests/e2e/` directory (NOT under `core/tests/`). It is the only file in `tests/e2e/` at v0.1.0 close. The directory is greenfield — created by this plan."
    - "Per D-03: the test loads `'../../core/tests/test_helper/bats-support/load'` and `'../../core/tests/test_helper/bats-assert/load'` (path-relative to `tests/e2e/`) — same submodule helpers as the existing bats suite; no new test-framework dependency."
    - "Per D-03 + SC#1: the test invokes the REAL `core/bin/chantier new <name>` binary to scaffold a fresh project under `$BATS_TEST_TMPDIR/home/chantier-e2e-dogfood/` (Discretion #9 — descriptive kebab-case project name) — this is the load-bearing claim that the full new-project → plan → execute → verify loop works end-to-end via Chantier-built tooling only."
    - "Per D-03: the test writes a synthetic two-task PLAN.md mirroring the F3 dogfood shape (t1: `depends_on: []`; t2: `depends_on: [t1]`), copies the live `test-driven-development` skill body into the synthetic project's `skills/test-driven-development/`, dispatches `$ADAPTER t1` then `$ADAPTER t2` sequentially (Discretion #3 operator-orders-dispatch), and asserts `chantier validate-task t1` AND `chantier validate-task t2` both exit 0."
    - "Per D-04: the test sets `CHANTIER_CLAUDE_BIN=\"$BATS_TEST_TMPDIR/stub/claude\"` BY DEFAULT (the same Phase 4 D-15 inline stub used in `core/tests/adapter_claude_code_e2e.bats:83-101` and in Plan 05-01's `core/tests/adapter_upstream_e2e.bats`). If the operator sets `CHANTIER_E2E_REAL_CLAUDE=1`, the test SKIPS setting `CHANTIER_CLAUDE_BIN` so the adapter falls back to the real `claude` binary on PATH. CI never sets this flag — NFR-004 default offline holds."
    - "Per D-04 + Discretion #8: the CHANTIER_CLAUDE_BIN stub is DUPLICATED INLINE (not extracted to a shared helper at `core/tests/test_helper/stubs/claude.sh`) — RESEARCH Discretion #8 recommendation is to duplicate inline because the e2e variant has the same shape as the Phase 4 variant and no e2e-specific behavior (e.g., dispatch-sequence logging) is needed."
    - "Per SC#2: after the full loop, the synthetic project's `$WORKTREE/.planning/STATE.md` contains exactly six dispatch events: `task.started × 2`, `skill.completed × 2`, `task.completed × 2` (verified via `grep -c` per Pattern E). NO event is `task.failed` (both dispatches green). The test ASSERTS these counts via `[ \"$_started\" -eq 2 ]` etc."
    - "Per SC#3 + NFR-004: with `CHANTIER_E2E_REAL_CLAUDE` unset (the CI default), the test exercises NO network — the dispatch goes through the inline stub which runs the skill body locally inside the dossier via `cd \"$DOSSIER\" && . ./env.sh && sh ./skill/run.sh`. The test contains a self-assertion that `CHANTIER_CLAUDE_BIN` is non-empty and executable (when the opt-in is unset), defending against Pitfall 8 (dev's local `claude` binary on PATH silently making the test pass locally but fail in CI)."
    - "Per D-01 + D-02 (F3 fix proof at full-loop scale): after t2's dispatch completes, the test asserts `[ -e \".chantier/dossiers/t2/upstream/t1/output.json\" ]` — this is the F3-fix-uses-itself signal. If the F3 fix from Plan 05-01 is missing or reverts, this assertion fails. The dogfood-feature claim (D-01 + D-02) is observable end-to-end through Chantier's own tooling."
    - "After this plan lands, bats suite goes from 80/0 (post Plan 05-02) to 81/0 — exactly one new bats file (`tests/e2e/full_loop.bats`) contributing exactly one new `@test`."
    - "Per ROADMAP SC#1: the integration test runs the full new-project → plan → execute → verify loop using only Chantier-built tooling (`chantier new` + `adapters/claude-code/run-task.sh` + `chantier validate-task` + `chantier state append` via the skill body) — zero `/gsd-*` invocations during execution."
  artifacts:
    - path: "tests/e2e/full_loop.bats"
      provides: "Phase 5 dogfood E2E integration test — full new-project → plan → execute → verify loop with CHANTIER_E2E_REAL_CLAUDE opt-in gate (D-03, D-04, SC#1, SC#2, SC#3)"
      min_lines: 200
      contains: "CHANTIER_E2E_REAL_CLAUDE"
  key_links:
    - from: "tests/e2e/full_loop.bats"
      to: "core/bin/chantier (new + validate-task)"
      via: "run $CHANTIER <subcommand> inside the synthetic project tmpdir"
      pattern: "run .CHANTIER. (new|validate-task)"
    - from: "tests/e2e/full_loop.bats"
      to: "adapters/claude-code/run-task.sh (with Plan 05-01 F3 fix)"
      via: "run $ADAPTER <task-id> sequentially t1 then t2"
      pattern: "run .ADAPTER. .t[12]."
    - from: "tests/e2e/full_loop.bats"
      to: "$BATS_TEST_TMPDIR/stub/claude (CHANTIER_CLAUDE_BIN default)"
      via: "inline duplicate of Phase 4 D-15 stub (Discretion #8)"
      pattern: "CHANTIER_CLAUDE_BIN="
    - from: "tests/e2e/full_loop.bats"
      to: "$WORKTREE/.chantier/dossiers/t2/upstream/t1/output.json"
      via: "filesystem -e existence check after t2 dispatch (F3 fix proof)"
      pattern: "upstream/t1/output\\.json"
    - from: "tests/e2e/full_loop.bats"
      to: "skills/test-driven-development/run.sh"
      via: "cp into synthetic project's skills/test-driven-development/; dispatched by stub"
      pattern: "test-driven-development"
---

<objective>
Ship `tests/e2e/full_loop.bats` — the Phase 5 dogfood integration test that runs the FULL Chantier loop using only Chantier-built tooling: invokes `chantier new` to scaffold a fresh project under `$BATS_TEST_TMPDIR`, writes a synthetic two-task PLAN.md mirroring the F3 dogfood shape, dispatches both tasks sequentially via `adapters/claude-code/run-task.sh` (with the F3 fix from Plan 05-01 staging `upstream/t1/output.json` into t2's dossier), runs `chantier validate-task` on both, and asserts the populated `.planning/STATE.md` contains exactly the expected six dispatch events with no contract violations. Default offline via inline `CHANTIER_CLAUDE_BIN` stub (D-04 + D-15); opt-in real-claude via `CHANTIER_E2E_REAL_CLAUDE=1` env hook for local dev validation (D-04; CI never sets this flag).

Purpose: Implements D-03 (single-bats top-level integration test at new `tests/e2e/`), D-04 (default-stub + opt-in real-claude env gate), and ROADMAP SC#1, SC#2, SC#3 simultaneously. The test is the Chantier-on-Chantier proof: the framework that planned-and-executed Phases 2..5 verifies itself end-to-end through its own commands, with zero `/gsd-*` invocations during the test run. The integration test is also v0.1.0's load-bearing acceptance criterion per `.planning/REQUIREMENTS.md` §Acceptance ("a non-trivial end-to-end demo exists as an integration test in `tests/e2e/`").

Output: One new bats file at `tests/e2e/full_loop.bats` (~200-280 lines, one comprehensive @test). One new top-level directory `tests/e2e/` (created implicitly by the file write). After this plan: `bats core/tests/ tests/e2e/` exits 0 with 81/0 reported (80 prior including Plan 05-02's NFR audits + 1 from this file); the SC#1–SC#4 criteria for ROADMAP Phase 5 move from claim to enforcement; SC#5 remains pending (handled by Plan 05-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-dogfood-e2e/05-CONTEXT.md
@.planning/phases/05-dogfood-e2e/05-RESEARCH.md
@.planning/phases/05-dogfood-e2e/05-PATTERNS.md
@.planning/phases/05-dogfood-e2e/05-VALIDATION.md
@.planning/phases/05-dogfood-e2e/05-01-SUMMARY.md
@.planning/phases/05-dogfood-e2e/05-02-SUMMARY.md
@docs/adr/0001-state-skill-contract.md
@docs/adr/0002-runtime-binary-and-state-format.md
@adapters/claude-code/run-task.sh
@core/bin/chantier
@core/tests/adapter_claude_code_e2e.bats
@core/tests/adapter_upstream_e2e.bats
@core/tests/skill_test_driven_development_e2e.bats
@skills/test-driven-development/SKILL.md
@skills/test-driven-development/run.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 05-03-01: Author tests/e2e/full_loop.bats — full new-project → plan → execute → verify loop integration test</name>

  <read_first>
    - core/tests/adapter_claude_code_e2e.bats (FULL FILE, 293 lines — PRIMARY analog per 05-PATTERNS.md §"tests/e2e/full_loop.bats (NEW)". The new file is the structural mirror at full-loop scale. Copy verbatim or adapt: file-purpose shell-prelude comment; `setup()` block loaders (lines 29-31) WITH path-adjustment for `tests/e2e/` relative to `core/tests/test_helper/` (the new file uses `load '../../core/tests/test_helper/bats-support/load'` instead of `load 'test_helper/bats-support/load'`); REPO_ROOT canonicalization (`REPO_ROOT=$(cd \"$BATS_TEST_DIRNAME/../..\" && pwd -P)` lands at repo root from `tests/e2e/`); CHANTIER and ADAPTER exports; PATH-prepend of `$REPO_ROOT/core/bin`; TMPHOME setup with `$BATS_TEST_TMPDIR/home`; the CHANTIER_CLAUDE_BIN stub at lines 83-101 (verbatim duplicate per Discretion #8); the `make_plan` helper structure at lines 116-170 (adapt to write TWO task blocks per Plan 05-01's pattern); the jq-and-grep assertion patterns at lines 274-289 (multiply by 2 for two-task chain); the error-surface block at lines 213-219 (copy verbatim).)
    - core/tests/adapter_upstream_e2e.bats (the file shipped by Plan 05-01 — SECONDARY analog. Read for the two-task PLAN.md heredoc shape, the F3-specific assertion pattern at the end, and the HARNESS_DENY_LIST_CHECK marker convention applied to a two-task test. The new file at `tests/e2e/full_loop.bats` shares the two-task PLAN structure but ADDS the `chantier new` scaffold step at the front — meaning the new file is `adapter_upstream_e2e.bats` PLUS `chantier new` prepended.)
    - core/tests/skill_test_driven_development_e2e.bats (FULL FILE, ~170 lines — tertiary analog. Read for the skill-body-copy pattern at lines 178-183 — the new file copies `skills/test-driven-development/{SKILL.md,PRESSURE.md,run.sh}` into the synthetic project's `skills/test-driven-development/` after `chantier new` completes.)
    - core/bin/chantier (search for `new_project` function, expected around lines 717-847 — read to understand exactly what `chantier new <name>` scaffolds: an empty `.planning/` directory with stub `PROJECT.md`, `REQUIREMENTS.md`, `ROADMAP.md`, `STATE.md`, `config.json`. The test SHOULD assert that `chantier new` exited 0 and that `$TMPHOME/chantier-e2e-dogfood/.planning/` exists with the expected stubs.)
    - core/bin/chantier (search for `validate_task` function, around lines 448-714 — read to confirm the test's `chantier validate-task` invocations will exercise all five ADR 0001 gates against the synthetic project's structure.)
    - adapters/claude-code/run-task.sh (FULL FILE with the Plan 05-01 F3 fix applied — read to confirm the adapter expects: `$WORKTREE` to be a git work tree (via `git rev-parse --show-toplevel`), `$WORKTREE/.planning/phases/<phase>/PLAN.md` to contain the task block, `$WORKTREE/skills/<skill-id>/SKILL.md` to exist. The test's setup must satisfy all three: `git init -q + git commit -q` inside the synthetic project; `.planning/phases/dogfood-phase/PLAN.md` written before dispatch; `skills/test-driven-development/` populated before dispatch.)
    - .planning/phases/05-dogfood-e2e/05-PATTERNS.md LINES 350-421 (full pattern map for `tests/e2e/full_loop.bats`: path-relative loaders, REPO_ROOT canonicalization, CHANTIER_CLAUDE_BIN stub, opt-in real-claude env gate, chantier new invocation, two-task PLAN.md authoring, sequential dispatch + assertion, skill body copy)
    - .planning/phases/05-dogfood-e2e/05-RESEARCH.md LINES 343-505 (full Pattern 1 e2e skeleton — lines 348-391 are the setup() block; lines 393-505 are the @test body. This is the load-bearing reference.)
    - .planning/phases/05-dogfood-e2e/05-CONTEXT.md LINES 27-30 (D-03 + D-04 verbatim)
    - .planning/phases/05-dogfood-e2e/05-RESEARCH.md LINES 622-733 (Pitfalls 1-8 — load-bearing for the test's defensive shell quoting (Pitfall 1: path with space), BSD/GNU portability (Pitfall 2), the CHANTIER_E2E_REAL_CLAUDE opt-in handling (Pitfall 8 — "Bats e2e passes locally because claude IS on PATH"), and the cutover.completed event ordering (Pitfall 7 — handled by Plan 05-04, NOT this test))
    - skills/test-driven-development/SKILL.md (the skill body the synthetic project executes — confirms `harness_adapters:` contains `- claude-code` so the synthetic project's SKILL.md validates against `chantier validate-task` gate 4 deny-list)
    - skills/test-driven-development/run.sh LINES 80-228 (the deterministic red-phase behavior under `test_command: "false"` — emits `output.md` + `output.json` with `red_exit_code=1`, ISO-8601 `red_step_timestamp`, ≥4 `invariants_applied`; appends `skill.completed` to STATE.md via `chantier state append`)
    - core/tests/fixtures/skills/test-driven-development/dossier/inputs.yml (4-line deterministic fixture — embedded into both t1's and t2's `inputs:` blocks in the synthetic PLAN.md heredoc)
    - .planning/REQUIREMENTS.md §Acceptance "v0.1.0 ships when" (the line "A non-trivial end-to-end demo exists as an integration test in tests/e2e/" — this is the load-bearing acceptance criterion this plan implements)
  </read_first>

  <behavior>
    - **End-to-end happy path (D-03 + SC#1 + SC#2):** Running `bats tests/e2e/full_loop.bats` exits 0 against the post-Plan-05-01-and-05-02 tree. The test invokes `chantier new chantier-e2e-dogfood` (Discretion #9 project name), cd's into the new project, `git init` + `git commit` to make it a worktree (the adapter's lax interpretation accepts main checkout per Phase 4 D-05 + Open Question A5), copies the `test-driven-development` skill body, writes a two-task PLAN.md, dispatches both tasks via the F3-fixed adapter, runs `chantier validate-task` on both — and all assertions pass.
    - **Three-event-per-task signal (SC#2):** After both dispatches, the synthetic project's `.planning/STATE.md` contains: 2 `task.started` events, 2 `skill.completed` events, 2 `task.completed` events. `grep -c` returning 2 for each event name. Six events total from the dispatch (NOT counting the per-row STATE.md frontmatter line and NOT counting any prior bootstrap events — the test's `chantier new` produced a fresh empty STATE.md).
    - **F3 fix verification at full-loop scale (D-01 + D-02 propagated to e2e):** The assertion `[ -e ".chantier/dossiers/t2/upstream/t1/output.json" ]` succeeds — proves Plan 05-01's F3 fix works through the FULL loop (not just the in-tree regression). If a future regression breaks the F3 fix, this test goes red.
    - **CHANTIER_E2E_REAL_CLAUDE default-off (D-04 + NFR-004 + SC#3):** With the env var unset (the CI default), the test sets `CHANTIER_CLAUDE_BIN` to the inline stub and the adapter dispatches through it. No network is exercised. Pitfall 8 mitigation: the test ALSO asserts `[ -n "$CHANTIER_CLAUDE_BIN" ] && [ -x "$CHANTIER_CLAUDE_BIN" ]` after the setup() block runs (guards against the silent fall-through-to-real-claude failure mode).
    - **CHANTIER_E2E_REAL_CLAUDE opt-in path (D-04, NOT exercised in CI):** With `CHANTIER_E2E_REAL_CLAUDE=1` exported, the test's setup() does NOT set `CHANTIER_CLAUDE_BIN`, so the adapter falls back to the real `claude` binary on PATH. CI never sets this flag. The test's setup() contains a conditional documented with a comment citing D-04 and Pitfall 8.
    - **No regression of Phase 4 e2e or Plan 05-01 regression:** `bats core/tests/adapter_claude_code_e2e.bats` and `bats core/tests/adapter_upstream_e2e.bats` both remain green after this file lands. The new file does not modify any other test.
    - **Bats suite total:** `bats core/tests/ tests/e2e/` reports 81/0 (80 prior + 1 from this file).
    - **NFR-001 audit compatibility:** The new file contains `claude-code` references with trailing `# HARNESS_DENY_LIST_CHECK` markers; the new file path `tests/e2e/full_loop.bats` is NOT under `adapters/claude-code/` (so no path-only carve-out applies), so the marker filter is the only mechanism preventing self-trigger. `bats core/tests/adapter_isolation.bats` continues green; `bats core/tests/nfr_audits.bats -f 'NFR-001'` continues green.
    - **NFR-005 English-only:** The new bats file body contains no French; `grep -c '[À-ÿ]' tests/e2e/full_loop.bats` returns 0.
    - **NFR-002 POSIX-portable:** Bats files are not pure shell; shellcheck does not apply. The shell commands inside the @test body and the stub heredoc use POSIX-only constructs (no `[[ ]]`, no `<<<`, no arrays).
  </behavior>

  <action>
    Create `tests/e2e/full_loop.bats`. The directory `tests/e2e/` does not yet exist — the write operation implicitly creates it. NO `tests/.gitignore` or `tests/e2e/.gitignore` is needed (we want git to track the file).

    **File-prelude block comment** (10-15 lines): file path, purpose (Phase 5 dogfood E2E per `.planning/phases/05-dogfood-e2e/05-CONTEXT.md` D-03 and D-04; closes ROADMAP SC#1–SC#3; v0.1.0 acceptance per `.planning/REQUIREMENTS.md` §Acceptance), source (mirror of `core/tests/adapter_claude_code_e2e.bats` at full-loop scale per 05-PATTERNS.md), the CHANTIER_E2E_REAL_CLAUDE opt-in contract (default-off / CI-strict / dev-opt-in per D-04 + Pitfall 8). Add a brief note that this file is the only file under `tests/e2e/` at v0.1.0; future top-level test categories (`tests/integration/`, `tests/manual/`) slot here per RESEARCH §"Recommended Project Structure".

    **`setup()` block** (mirror analog adapter_claude_code_e2e.bats:29-71 with the path-adjustment from 05-PATTERNS.md §"Path-relative loader"):
    - `load '../../core/tests/test_helper/bats-support/load'` — path-relative from `tests/e2e/` to `core/tests/test_helper/`.
    - `load '../../core/tests/test_helper/bats-assert/load'`.
    - `export REPO_ROOT`; `REPO_ROOT=$(cd "$BATS_TEST_DIRNAME/../.." && pwd -P)` — from `tests/e2e/`, `../..` lands at repo root.
    - `export CHANTIER="$REPO_ROOT/core/bin/chantier"`.
    - `export ADAPTER="$REPO_ROOT/adapters/claude-code/run-task.sh"  # HARNESS_DENY_LIST_CHECK`.
    - `export PATH="$REPO_ROOT/core/bin:$PATH"` — for subagent's `chantier state append` from inside the skill's `run.sh`.
    - `mkdir -p "$BATS_TEST_TMPDIR/home"`; `cd "$BATS_TEST_TMPDIR/home"`; `export TMPHOME`; `TMPHOME=$(pwd -P)` — canonicalize against macOS `/var → /private/var`.

    **CHANTIER_CLAUDE_BIN inline stub** (verbatim from `core/tests/adapter_claude_code_e2e.bats:83-101` per Discretion #8 inline-duplicate):
    - `mkdir -p "$BATS_TEST_TMPDIR/stub"`.
    - Write `$BATS_TEST_TMPDIR/stub/claude` via `<<'STUB_EOF'` single-quoted heredoc (Pattern H). Body: same as Plan 05-01 — `#!/bin/sh; set -eu; PROMPT=""; while [ $# -gt 0 ]; do case "$1" in -p|--print) shift; PROMPT="$1" ;; *) ;; esac; shift 2>/dev/null || true; done; DOSSIER=$(printf '%s\n' "$PROMPT" | grep -oE '/[^ "]+/\.chantier/dossiers/[^ "]+' | head -n 1); [ -n "$DOSSIER" ] || { printf 'stub: no dossier in prompt\n' >&2; exit 1; }; printf 'subagent (stub): cd %s\n' "$DOSSIER"; cd "$DOSSIER" && . ./env.sh && sh ./skill/run.sh; exit $?`.
    - `chmod +x "$BATS_TEST_TMPDIR/stub/claude"`.

    **D-04 opt-in conditional** (the load-bearing new behavior over Plan 05-01):
    - Add a guarded export with explanatory comment:
      `# D-04 opt-in: CHANTIER_E2E_REAL_CLAUDE=1 → unset CHANTIER_CLAUDE_BIN so the adapter`
      `# falls back to the real claude binary on PATH. CI never sets this flag — NFR-004`
      `# default-offline holds. Pitfall 8: explicitly set CHANTIER_CLAUDE_BIN unless opt-in.`
      `if [ "${CHANTIER_E2E_REAL_CLAUDE:-}" != "1" ]; then`
      `    export CHANTIER_CLAUDE_BIN="$BATS_TEST_TMPDIR/stub/claude"`
      `fi`

    **Single @test block** (the one comprehensive integration assertion):
    - Name: `@test "tests/e2e/full_loop: chantier new + 2-task chain + adapter dispatch + validate-task green (SC#1, SC#2, SC#3, D-01, D-02, D-03, D-04)"`.
    - Body sequence:

      **Step 1 — Defensive Pitfall 8 self-assertion** (unless opt-in):
      - `if [ "${CHANTIER_E2E_REAL_CLAUDE:-}" != "1" ]; then [ -n "$CHANTIER_CLAUDE_BIN" ]; [ -x "$CHANTIER_CLAUDE_BIN" ]; fi`.

      **Step 2 — `chantier new` (SC#1):**
      - `cd "$TMPHOME"`.
      - `run "$CHANTIER" new chantier-e2e-dogfood`.
      - `[ "$status" -eq 0 ]`.
      - `cd "$TMPHOME/chantier-e2e-dogfood"`.
      - Assert the scaffold landed: `[ -f .planning/PROJECT.md ]`, `[ -f .planning/REQUIREMENTS.md ]`, `[ -f .planning/ROADMAP.md ]`, `[ -f .planning/STATE.md ]`, `[ -f .planning/config.json ]`.

      **Step 3 — Make the project a git work tree** (the adapter requires this):
      - `git init -q`.
      - `git config user.email "test@chantier"`.
      - `git config user.name "test"`.
      - `git add -A`.
      - `git commit -q -m "scaffold"`.

      **Step 4 — Copy the live test-driven-development skill into the synthetic project:**
      - `mkdir -p skills/test-driven-development  # HARNESS_DENY_LIST_CHECK is NOT needed here; "test-driven-development" is a skill name, not a harness identifier`.
      - `cp "$REPO_ROOT/skills/test-driven-development/SKILL.md"    skills/test-driven-development/`.
      - `cp "$REPO_ROOT/skills/test-driven-development/PRESSURE.md" skills/test-driven-development/`.
      - `cp "$REPO_ROOT/skills/test-driven-development/run.sh"      skills/test-driven-development/`.
      - `chmod +x skills/test-driven-development/run.sh`.
      - `git add -A && git commit -q -m "scaffold-skill"`.

      **Step 5 — Write the synthetic two-task PLAN.md** (same shape as Plan 05-01's PLAN, regenerated under the new project's `.planning/phases/dogfood-phase/`):
      - `mkdir -p .planning/phases/dogfood-phase`.
      - `cat > .planning/phases/dogfood-phase/PLAN.md <<'PLAN_EOF'` followed by the two task blocks (t1 with `depends_on: []`; t2 with `depends_on:` then `  - t1`), each task carrying the four-scalar TDD red-phase inputs (`target_file: "src/dummy.sh"`, `test_framework: "bats"`, `phase: "red"`, `test_command: "false"`), `state_writes` pointing at `.planning/phases/dogfood-phase/tasks/<task>/`, and the two TDD acceptance bullets. Use single-quoted `PLAN_EOF` per Pattern H.
      - `git add -A && git commit -q -m "scaffold-plan"`.

      **Step 6 — Sequential dispatch (Discretion #3 operator-orders, mirrors Plan 05-01):**
      - `run "$ADAPTER" t1` — wrap in error-surface pattern (if `$status -ne 0`, printf adapter exit + output + STATE.md to stderr; assert `[ "$status" -eq 0 ]`).
      - `[ -f ".planning/phases/dogfood-phase/tasks/t1/output.json" ]` — Surface 3 propagation (Phase 4 plan 03 fix) produced the canonical output.
      - `run "$ADAPTER" t2` — same error-surface wrapper. The F3 fix from Plan 05-01 stages `upstream/t1/output.json` into t2's dossier before the subagent runs.
      - `[ -f ".planning/phases/dogfood-phase/tasks/t2/output.json" ]`.

      **Step 7 — validate-task green on both (SC#2):**
      - `run "$CHANTIER" validate-task t1; [ "$status" -eq 0 ]`.
      - `run "$CHANTIER" validate-task t2; [ "$status" -eq 0 ]`.

      **Step 8 — STATE.md event-count assertions (SC#2):**
      - `_started=$(grep -cE '"event":"task\.started"' .planning/STATE.md); [ "$_started" -eq 2 ]`.
      - `_skill=$(grep -cE '"event":"skill\.completed"' .planning/STATE.md); [ "$_skill" -eq 2 ]`.
      - `_completed=$(grep -cE '"event":"task\.completed"' .planning/STATE.md); [ "$_completed" -eq 2 ]`.
      - `_failed=$(grep -cE '"event":"task\.failed"' .planning/STATE.md); [ "$_failed" -eq 0 ]` — no failures.

      **Step 9 — F3 fix proof at full-loop scale (D-01 + D-02 propagated):**
      - Comment: `# F3 fix proof through the full loop: t2's dossier MUST contain upstream/t1/output.json.`.
      - `[ -e ".chantier/dossiers/t2/upstream/t1/output.json" ]`.

      **Step 10 — NFR-004 self-defense (SC#3 + Pitfall 8 final assertion):**
      - `if [ "${CHANTIER_E2E_REAL_CLAUDE:-}" != "1" ]; then [ -n "$CHANTIER_CLAUDE_BIN" ]; [ -x "$CHANTIER_CLAUDE_BIN" ]; fi` — confirm the stub was actually used, not bypassed.

    **HARNESS_DENY_LIST_CHECK hygiene** (Pattern B): every line containing a harness identifier (`claude-code`, `mcp__`, `claude_ai_`, `@codebase`, `cursor`, etc.) carries a trailing `# HARNESS_DENY_LIST_CHECK` comment. The `test-driven-development` skill name is NOT a harness identifier — no marker needed. The bats file's own deny-list-grep-able literals are confined to the `$ADAPTER` export line (which carries the marker) and explanatory comments (which also carry the marker).

    **Quoting hygiene** (Pitfall 1): double-quote `$REPO_ROOT`, `$TMPHOME`, `$BATS_TEST_TMPDIR`, `$CHANTIER`, `$ADAPTER`, and all `cd` arguments to defend against the path-with-space in the local dev environment.

    **No fenced code blocks inside the bats file** — bats files are continuous shell source.

    Implements: D-03, D-04, SC#1, SC#2, SC#3. Closes ROADMAP Phase 5 SC#1–SC#4 (SC#4 was closed by Plan 05-02's NFR audits; this file is the integration counterpart). Cites: per D-03, D-04.
  </action>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && unset CHANTIER_E2E_REAL_CLAUDE && bats tests/e2e/full_loop.bats && bats core/tests/ tests/e2e/</automated>
  </verify>

  <acceptance_criteria>
    - Directory `tests/e2e/` exists at the repo root.
    - File `tests/e2e/full_loop.bats` exists.
    - `bats tests/e2e/full_loop.bats` exits 0 with `CHANTIER_E2E_REAL_CLAUDE` unset (the CI default) — the inline stub dispatches both tasks; both `validate-task` invocations exit 0; the F3-specific assertion succeeds.
    - `bats core/tests/ tests/e2e/` reports 81/0 (80 prior including Plan 05-02's 6 NFR audits + 1 from this file).
    - `bats core/tests/adapter_claude_code_e2e.bats` exits 0 (Phase 4 single-task e2e unaffected).
    - `bats core/tests/adapter_upstream_e2e.bats` exits 0 (Plan 05-01 in-tree regression unaffected).
    - `bats core/tests/nfr_audits.bats` exits 0 with 6/6 (Plan 05-02 NFR audits unaffected; specifically NFR-001's deny-list does not flag the new bats file because every harness identifier carries `HARNESS_DENY_LIST_CHECK` per Pattern B).
    - `bats core/tests/adapter_isolation.bats` exits 0 (Phase 4 NFR-001 audit unaffected).
    - `grep -c 'HARNESS_DENY_LIST_CHECK' tests/e2e/full_loop.bats` returns ≥ 2 — the marker is present on every line mentioning a harness identifier.
    - `grep -v '^#' tests/e2e/full_loop.bats | grep -v 'HARNESS_DENY_LIST_CHECK' | grep -cE 'mcp__|claude_ai_|@codebase|claude-code|cursor|codex-cli|copilot-cli|gemini-cli|opencode'` returns 0 — the marker convention fully exempts the test file from self-triggering NFR-001.
    - `grep -c '[À-ÿ]' tests/e2e/full_loop.bats` returns 0 — no accented characters; NFR-005 English-only respected.
    - The CHANTIER_CLAUDE_BIN stub heredoc is byte-identical to the analogs in `core/tests/adapter_claude_code_e2e.bats` and `core/tests/adapter_upstream_e2e.bats` (Plan 05-01) — verify via three-way `diff` of the heredoc bodies returns no differences.
    - `grep -c 'CHANTIER_E2E_REAL_CLAUDE' tests/e2e/full_loop.bats` returns ≥ 2 — the opt-in env gate is referenced in the setup() conditional AND in the body's Pitfall 8 self-assertion.
    - `grep -c 'chantier-e2e-dogfood' tests/e2e/full_loop.bats` returns ≥ 1 — Discretion #9 project name is used.
    - Manual opt-in probe (NOT committed, NOT in automated verify): `CHANTIER_E2E_REAL_CLAUDE=1 bats tests/e2e/full_loop.bats` exits 0 IF the host has a real `claude` binary on PATH; otherwise exits non-zero with the adapter's `claude binary not found` error. This is the dev-facing flag; CI never sets it.
    - The full `<automated>` verify command exits 0.
  </acceptance_criteria>

  <done>
    `bats tests/e2e/full_loop.bats` exits 0 with `CHANTIER_E2E_REAL_CLAUDE` unset; `bats core/tests/ tests/e2e/` reports 81/0; the integration test exercises `chantier new` + 2-task PLAN.md + sequential adapter dispatch + `chantier validate-task` + STATE.md event-count assertions + F3-fix-proof-at-full-loop-scale; HARNESS_DENY_LIST_CHECK markers prevent NFR-001 self-trigger; NFR-005 English-only is respected; the CHANTIER_E2E_REAL_CLAUDE opt-in is wired but inert in CI.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Synthetic project scaffold → adapter dispatch | `chantier new` produces a fresh project; the adapter consumes its PLAN.md and skill body; risk: scaffold gaps surface as adapter exit errors |
| `CHANTIER_CLAUDE_BIN` env var → dispatch target | Default points to inline stub; opt-in unsets to fall through to real `claude` on PATH; risk: silent fallthrough makes CI false-pass |
| Local dev `claude` binary → CI dispatch | Pitfall 8: dev has `claude` installed; test passes locally with real network; CI lacks `claude` so test exits 3 |
| Two-task chain → operator dispatch order | Discretion #3: operator dispatches t1 then t2; out-of-order surfaces as adapter exit 2 (F3 fix's failure mode); not exercised by this test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-NFR-004 | Information Disclosure | Network access during e2e | mitigate | CHANTIER_CLAUDE_BIN default-set inline stub (Phase 4 D-15 verbatim per Discretion #8); the stub does NO network (it cd's to the dossier and execs the skill's local run.sh); the conditional opt-in via CHANTIER_E2E_REAL_CLAUDE is the explicit exception the operator must declare. CI invocation `bats tests/e2e/` exercises only the default-stub path. The Plan 05-02 NFR-004 audit's static grep catches any source-level network primitive leakage. |
| T-05-03-PITFALL-8 | Compliance (NFR-004) | Stub bypass via local claude on PATH | mitigate | Defensive self-assertion in @test body: `[ -n "$CHANTIER_CLAUDE_BIN" ] && [ -x "$CHANTIER_CLAUDE_BIN" ]` when opt-in is unset. If the dev forgets to set CHANTIER_CLAUDE_BIN (or a refactor breaks the setup() default), the assertion fails before the real claude binary is invoked, catching the misconfiguration immediately. |
| T-05-03-SCAFFOLD-GAP | Tampering | chantier new scaffold incompleteness | accept | The test asserts presence of all five scaffolded files (`PROJECT.md`, `REQUIREMENTS.md`, `ROADMAP.md`, `STATE.md`, `config.json`); if a future `chantier new` patch breaks the scaffold, the test fails BEFORE the adapter dispatch, giving a clear localized error. Phase 2 plan 05 ratified the scaffold; risk is low. |
| T-05-03-NFR-001 | Compliance (NFR-001) | Harness identifier leak into new test file | mitigate | HARNESS_DENY_LIST_CHECK marker on every line mentioning harness identifiers per Pattern B. The new file at `tests/e2e/full_loop.bats` is NOT under `adapters/claude-code/` (no path-only carve-out applies), so the marker is the sole mechanism. Plan 05-02's NFR-001 @test verifies this works (the new file goes through the default `*)` case-arm applying `_full`; HARNESS_DENY_LIST_CHECK lines are stripped first). |
| T-05-03-PATH-SPACE | Compliance (Pitfall 1) | Path-with-space in local dev env | mitigate | Every `$REPO_ROOT`, `$TMPHOME`, `$BATS_TEST_TMPDIR`, `$CHANTIER`, `$ADAPTER` reference is double-quoted; `pwd -P` canonicalizes against macOS `/var → /private/var` symlink (Pattern A). Phase 4's adapter_claude_code_e2e.bats runs on the same host without truncation. |
| T-05-03-BSD-GNU | Compliance (NFR-002 + Pitfall 2) | BSD vs GNU divergence in CI vs dev | mitigate | All find/grep/awk constructs are POSIX-portable (no `-regextype`, no `-print0`, no `grep -P`, no BSD-only `sed -i ''`); idioms mirror Phase 4's adapter_claude_code_e2e.bats which is known-clean on macOS BSD and Linux GNU. |
| T-05-03-SC | Tampering | npm/pip/cargo installs | accept | N/A — this plan installs no packages. Bats + jq + git + shellcheck already host-verified per Phase 4. |
</threat_model>

<verification>
- `bats tests/e2e/full_loop.bats` exits 0 with `CHANTIER_E2E_REAL_CLAUDE` unset
- `bats core/tests/ tests/e2e/` reports 81/0
- Phase 4 e2e (`adapter_claude_code_e2e.bats`) and Plan 05-01 regression (`adapter_upstream_e2e.bats`) and Plan 05-02 NFR audits (`nfr_audits.bats`, `adapter_isolation.bats`) all remain green
- HARNESS_DENY_LIST_CHECK markers prevent NFR-001 self-trigger (`grep -c HARNESS_DENY_LIST_CHECK tests/e2e/full_loop.bats >= 2`; effective deny-list residue is 0)
- NFR-005 English-only: `grep -c '[À-ÿ]' tests/e2e/full_loop.bats` returns 0
- CHANTIER_CLAUDE_BIN stub heredoc byte-identical across all three bats files using it (Phase 4 e2e, Plan 05-01 regression, this file)
- ROADMAP SC#1, SC#2, SC#3 move from claim to enforcement (test source-of-truth for each)
- v0.1.0 acceptance criterion ("non-trivial e2e demo in tests/e2e/") satisfied
</verification>

<success_criteria>
- D-03 implemented: single-bats file at the new top-level `tests/e2e/` directory, loads same submodule helpers, scaffolds via `chantier new`, dispatches multi-task chain through `adapters/claude-code/run-task.sh`, validates via `chantier validate-task`.
- D-04 implemented: default-stub via inline CHANTIER_CLAUDE_BIN; opt-in real-claude via `CHANTIER_E2E_REAL_CLAUDE=1` env gate; CI never sets the flag; Pitfall 8 mitigation via self-assertion.
- ROADMAP SC#1 satisfied: full new-project → plan → execute → verify loop in `tests/e2e/`.
- ROADMAP SC#2 satisfied: populated STATE.md with no `chantier validate-task` contract violations.
- ROADMAP SC#3 satisfied: passes in CI without network access (default-stub path; opt-in path is dev-only).
- v0.1.0 §Acceptance "non-trivial end-to-end demo as integration test in `tests/e2e/`" satisfied.
- Bats suite goes 80/0 → 81/0 (exactly one new test, one new file, one new directory).
</success_criteria>

<output>
Create `.planning/phases/05-dogfood-e2e/05-03-SUMMARY.md` when done. Include: (1) `bats tests/e2e/full_loop.bats` output excerpt (1/1 green) and the full-suite 81/0 confirmation; (2) the CHANTIER_CLAUDE_BIN stub heredoc byte-identical proof (three-way diff across the three bats files); (3) the HARNESS_DENY_LIST_CHECK marker count and effective deny-list residue count (≥2 and 0 respectively); (4) the NFR-005 English-only confirmation; (5) a manual probe note for the CHANTIER_E2E_REAL_CLAUDE opt-in path with the result if a real claude binary is available on the dev host (NOT a CI gate); (6) the SC#1/SC#2/SC#3 → assertion mapping table; (7) a one-line citation of the v0.1.0 acceptance criterion this plan closes.
</output>
</content>
